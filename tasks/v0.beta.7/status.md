# Task status — v0.beta.7 (generated)

> Generated by `scripts/status.sh v0.beta.7` on branch `feat` at commit `1668066`.
> Derived view — specs stay authored. Re-run the script and paste the table,
> keeping the Notes. (The Branch column derives `task/<slug>` from the slug, so a
> `merged` there can point at a **stale/preserved** branch — see Notes for which
> rows are genuinely done.)

| Spec | Header status | Branch `task/<slug>` | Worktree `.tasks/<slug>` | TODO.md |
|---|---|---|---|---|
| [annotation-processors](specs/annotation-processors.md) | pending | merged | present | 12/16 done |
| [erika](specs/erika.md) | pending | merged | present | 12/13 done |
| [jhonstart](specs/jhonstart.md) | pending | merged | present | 0/10 done |
| [rakun](specs/rakun.md) | pending | open (feb96f0) | present | 0/15 done |
| [stdlib-backends-parity](specs/stdlib-backends-parity.md) | pending | open (734a468) | present | 5/10 done |

## Notes

- **Worktrees (2026-06-09), branched off `feat`:** `.tasks/annotation-processors`
  (`task/annotation-processors`), `.tasks/stdlib-backends-parity`
  (`task/stdlib-backends-parity`), `.tasks/rakun` (`task/rakun-port`),
  `.tasks/erika` (`task/erika-port`), `.tasks/jhonstart` (`task/jhonstart-port`).
  Each `TODO.md` is seeded from its spec.

- **annotation-processors — merged into `feat`.** The generic `from "<lib>"`
  loader (P0, `compiler-cli/src/cli/libs.zig`) + the `@Decl` reflection model and
  decorator bodies (P1–P3) landed. 12/16: the P3 *wiring contribution* remainder
  (a decorator body returning generated decls into the DI graph / router) is the
  open piece — its consumer (rakun DI/router) is lib-side.

- **erika — DONE, merged into `feat`** (`task/erika-port`, in `1668066`). The
  LINQ lib moved out of `std` into `libs/erika/` (reached via `from "erika"`),
  `selectMany` + multi-field projection landed (G3/G2), 24 in-file tests pass.
  The Branch `merged` is genuine here. 12/13: the one open item is the
  `erika "…"`-after-import gap — the generic loader binds a lib's **namespace** but
  not bare values / same-named template fns into value scope (not erika-specific;
  fixing it is generic loader/import-resolver work in core, out of erika's scope).

- **jhonstart — still pending.** The Branch `merged` is the **stale** v0.beta.6
  `task/jhonstart` (long since merged), **not** the live `task/jhonstart-port`
  (`0/10`, not started). Unblocked now that `annotation-processors` merged; port
  the UI surface into `libs/jhonstart/*.bp` (pure `.bp` client, no core code).

- **rakun branch naming.** `.tasks/rakun` runs on **`task/rakun-port`** (fresh off
  `feat`), **not** `task/rakun` (`feb96f0`, the preserved core-coupled F2/F3
  reference, left untouched). `scripts/status.sh` derives the branch from the slug,
  so the auto row points at the *reference*; the live work is on `task/rakun-port`.
  Unblocked (annotation-processors merged) — port behaviour into `libs/rakun/*.bp`.

- **stdlib-backends-parity** — open on `task/stdlib-backends-parity` (`734a468`),
  5/10; parallel-safe with the lib ports.

- **Acceptance gate for the set:** `grep -riE "rakun|jhonstart|erika"
  modules/compiler-core/src` returns nothing (std exempt), shipped as a test by
  `annotation-processors` P0 and extended to `erika` (erika left `std`).
