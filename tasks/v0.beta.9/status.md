# Task status — v0.beta.9 (Wave 1 of 3)

> Generated by `scripts/status.sh v0.beta.9` on branch `feat` at commit `038ac51`.
> Wave 1 — the two keystones + two independents.

| Spec | Header status | Branch `task/<slug>` | Worktree `.tasks/<slug>` | TODO.md |
|---|---|---|---|---|
| [expr-custom](specs/expr-custom.md) | pending — keystone | — (not started) | — | — |
| [module-system](specs/module-system.md) | pending — keystone | — (not started) | — | — |
| [lib-test-runner](specs/lib-test-runner.md) | pending | — (new module) | — | — |
| [mutual-recursion](specs/mutual-recursion.md) | pending — F0 already resolved, regression-only | open (5d70b4d) | — | — |

## Notes

- **Wave = parallel.** Four areas: comptime/template/builtins (`expr-custom`),
  parser/resolver/codegen + `libs/std` pilot (`module-system`), a new
  `modules/lib-test-runner/`, and an inference regression (`mutual-recursion`).
- **Two keystones land here.** `expr-custom` unblocks `sublanguage-lsp` (Wave 2) +
  `erika-query-ast`/`jhonstart-html-ast` (Wave 3); `module-system` unblocks
  `libs-module-migration` (Wave 2). Different core regions → late merge resolves overlap.
- **mutual-recursion** — forward refs already type-check on all four backends + run on
  commonJS (verified 2026-06-10); regression test + non-JS run confirmation only.
- **Codegen-emitter order:** `module-system`'s F4 is the first of four emitter specs;
  `stdlib`/`cross-module`/`effect` (all Wave 2) follow by merge-order.
