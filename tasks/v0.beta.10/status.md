# Task status — v0.beta.10 (Wave 2 of 3)

> Generated by `scripts/status.sh v0.beta.10` on branch `feat` at commit `038ac51`.
> Wave 2 — the biggest wave: LSP reader + core/codegen strands + lib migration + vscode.

| Spec | Header status | Branch `task/<slug>` | Worktree `.tasks/<slug>` | TODO.md |
|---|---|---|---|---|
| [sublanguage-lsp](specs/sublanguage-lsp.md) | pending — needs `expr-custom` (Wave 1) | — | — | — |
| [stdlib-backends-parity](specs/stdlib-backends-parity.md) | pending | `task/stdlib-backends-beam` | present | 2/11 done |
| [cross-module-codegen](specs/cross-module-codegen.md) | pending | open (a9e2ad2, unmerged) | — | — |
| [effect-annotations](specs/effect-annotations.md) | pending | — (not started) | — | — |
| [libs-module-migration](specs/libs-module-migration.md) | pending — needs `module-system` (Wave 1) | — | — | — |
| [sublanguage-vscode](specs/sublanguage-vscode.md) | pending — needs `sublanguage-lsp` (this wave) | — | — | — |

## Notes

- **Mostly disjoint:** language-server (`sublanguage-lsp`), codegen emitters
  (`stdlib`/`cross-module`/`effect-annotations` core regions), the six lib packages
  (`libs-module-migration`), the VS Code extension (`sublanguage-vscode`).
- **Two coordination notes (not blockers):**
  1. `sublanguage-vscode` depends on `sublanguage-lsp` at **runtime** — same wave;
     vscode's grammar/theme work runs in parallel and **verifies last** against the
     running LSP.
  2. **Three codegen-emitter specs here** (`stdlib`, `cross-module`, `effect-annotations`)
     → **sequence by merge-order** (different emitter regions: stdlib dispatch /
     cross-package linking / function-keyword lowering).
- **libs-module-migration** re-roots the lib **structure** (`root.bp`/`mod`) here, before
  the lib-**content** rewrites in Wave 3 (erika/html/rakun keep one file each, so the
  tree stays valid).
- **effect-annotations** — `#[@<effect>]` replaces `*fn`; **annotation is
  implementation-only**; short-vs-long type naming still an open decision.
- **stdlib** live on `task/stdlib-backends-beam` (2/11). **cross-module** = v0.beta.6
  orphan `a9e2ad2` (review/rebase).
