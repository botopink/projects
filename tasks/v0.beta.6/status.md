# Task status — v0.beta.6 (generated)

> Generated by `scripts/status.sh v0.beta.6` on branch `feat`
> at commit `7cb441c`. Derived view — specs stay authored. Re-run the script and
> paste the table below, keeping the Notes section.

| Spec | Header status | Branch `task/<slug>` | Worktree `.tasks/<slug>` | TODO.md |
|---|---|---|---|---|
| [annotation-processors](specs/annotation-processors.md) | pending | — | — | — |
| [cross-module-codegen](specs/cross-module-codegen.md) | done — erlang/beam/wasm cross-module parity merged into `feat` (99fb9e9; wasm single-module limit recorded) | open (a9e2ad2) | — | — |
| [erika](specs/erika.md) | done (see "Realized scope" below) | merged | — | — |
| [implement-completeness](specs/implement-completeness.md) | done — G5–G7 merged into `feat` (be13669 / ece318b) | — | — | — |
| [jhonstart-language-gaps](specs/jhonstart-language-gaps.md) | done — 4 generic language gaps merged into `feat` (94c3bfa via ed2931e) | merged | — | — |
| [mutual-recursion](specs/mutual-recursion.md) | done — forward refs between top-level fns merged into `feat` (180ef93) | open (5d70b4d) | — | — |
| [rakun](specs/rakun.md) | pending | open (feb96f0) | — | — |
| [stdlib-backends-and-tooling](specs/stdlib-backends-and-tooling.md) | partial — Part C (editor tooling) + Part A2 (`s.contains`→`includes`) merged into `feat` (via 0d2a6a6); Part A1/A3 + Part B (backend-parity F1–F6) pending → carried into a new task | merged | — | — |

## Notes

- **Consolidated (2026-06-08).** The set's `.tasks/<slug>` worktrees were removed
  after the work was merged into `feat`; the `task/<slug>` branches are preserved.
  The header `**Status**` column is the source of truth for done-ness (D4): where
  the **Branch** column shows `open (<sha>)` for a *done* spec, that `<sha>` is the
  TODO-seed tip while the actual work landed on `feat` via other commits
  (cross-module-codegen 99fb9e9, mutual-recursion 180ef93). `merged` means the
  `task/<slug>` tip is an ancestor of `feat`.

- **Direction shift → the lib-agnostic core.** See [`README.md`](README.md) §Theme
  and [`plan.md`](plan.md). The generic work (cross-module-codegen, the four
  jhonstart **language** gaps, implement-completeness, mutual-recursion, erika +
  the data-driven std registry, and stdlib-backends-and-tooling's done parts)
  landed in `feat`. The lib-coupled work was re-aimed onto
  [`annotation-processors`](specs/annotation-processors.md) — the keystone spec
  whose P0 removes the rakun foundation from the core. It carries **no branch
  yet**: a new set executes it.

- **rakun** is **specs only** here. The interim F2/F3 reference implementation
  (comptime component scan + DI-graph cycle detection + annotation validation,
  still rakun-coupled in the core) stays on `task/rakun` (`feb96f0`), **not merged**,
  so `feat` gains no new lib-specific core code. rakun is re-specified as a client
  of `annotation-processors`.

- **Residual coupling debt (for the new set).** Already on `feat` from v0.beta.5
  and to be removed by the new tasks: (1) the rakun foundation
  (`registerRakunLib`/`markRakunImports`/`rakunExports`/`rakunTypeDecls`/
  `expandRakunImports`/`rakun_pkg_modules`/`isRakunPkgPath` + the `rakun.d.bp`/
  `http.bp` embeds) — `annotation-processors` P0; (2)
  `modules/compiler-core/src/comptime/tests/jhonstart.zig` — a jhonstart-named
  compiler test (layering violation per `jhonstart.md`), to be folded into the
  generic `effects.zig`/`templates.zig` + context-inference scenarios.

- **stdlib-backends-and-tooling** is partial: Part C (editor tooling) and Part A2
  (`s.contains`→native `includes`) merged; Part A1/A3 (mirror JS method lowering
  in erlang/beam/wasm; `default fn` body inference; generic-extends-generic) and
  Part B (backend-parity F1–F6) are open → a new task.
