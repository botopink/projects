# Task status — v0.beta.8 (generated)

> Generated by `scripts/status.sh v0.beta.8` on branch `feat` at commit `159f986`.
> Derived view — specs stay authored. Re-run the script and paste the table,
> keeping the Notes. **The Branch column derives `task/<slug>` from the slug and is
> unreliable here:** three slugs collide with preserved/merged v0.beta.7 branches, so
> the real v0.beta.8 work runs on the renamed branches in the Notes; and a fresh
> branch sitting at `feat`'s tip reads as `merged`. Trust the **Worktree** + **TODO.md**
> columns and the Notes.

| Spec | Header status | Branch `task/<slug>` | Worktree `.tasks/<slug>` | TODO.md |
|---|---|---|---|---|
| [erika](specs/erika.md) | pending | merged | present | 1/7 done |
| [generic-loader-binding](specs/generic-loader-binding.md) | pending | merged | present | 0/9 done |
| [jhonstart-html](specs/jhonstart-html.md) | pending | merged | present | 0/9 done |
| [onze](specs/onze.md) | pending | merged | present | 0/8 done |
| [rakun](specs/rakun.md) | pending | open (feb96f0) | present | 0/15 done |
| [stdlib-backends-parity](specs/stdlib-backends-parity.md) | pending | merged | present | 0/12 done |

## Notes

- **Worktrees created 2026-06-10, all branched off `feat` (`159f986`).** Branch names
  (3 suffixed to avoid colliding with preserved v0.beta.7 branches):

  | Slug | Worktree | Branch | Wave |
  |---|---|---|---|
  | generic-loader-binding | `.tasks/generic-loader-binding` | `task/generic-loader-binding` | 1 (keystone) |
  | rakun | `.tasks/rakun` | `task/rakun-di` | 1 |
  | onze | `.tasks/onze` | `task/onze` | 1 |
  | stdlib-backends-parity | `.tasks/stdlib-backends-parity` | `task/stdlib-backends-beam` | 1 |
  | jhonstart-html | `.tasks/jhonstart-html` | `task/jhonstart-html` | 2 (gated) |
  | erika | `.tasks/erika` | `task/erika-dsl` | 2 (gated) |

- **The derived Branch column is wrong here.** `erika`/`stdlib-backends-parity` show
  `merged` = the v0.beta.7 branches `task/erika`/`task/stdlib-backends-parity` (already
  in `feat`), NOT the live `task/erika-dsl`/`task/stdlib-backends-beam`. `rakun` shows
  `open (feb96f0)` = the preserved core-coupled reference, NOT `task/rakun-di`. The
  three free slugs read `merged` only because the fresh branch sits at `feat`'s tip.

- **Waves (parallelism).** Wave 1 starts now (no deps): `generic-loader-binding`,
  `rakun`, `onze`, `stdlib-backends-parity`. Wave 2 (`jhonstart-html`, `erika`) is
  gated on `generic-loader-binding` binding the bare template fn — `git merge feat`
  into those worktrees once the keystone lands. DAG edge:
  `generic-loader-binding → {jhonstart-html, erika}`.

- **File overlap to watch:** `generic-loader-binding` and `stdlib-backends-parity`
  both touch `modules/compiler-core/src/comptime/infer.zig` (different regions — not a
  dependency; the later merge resolves it).

- **erika 1/7** — F0 (runnable `examples/erika-linq`, 4/4 green) already landed in
  `feat`; F1 (`erika "…"` cross-module) + F2 (docs) remain.

- **Acceptance gate (carried from v0.beta.7):** `grep -riE "rakun|jhonstart|erika|onze"
  modules/compiler-core/src` returns nothing (std exempt) — re-confirm at each merge.
