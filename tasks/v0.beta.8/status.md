# Task status — v0.beta.8 (generated)

> Generated by `scripts/status.sh v0.beta.8` on branch `feat` at commit `aeb01f4`.
> Derived view — specs stay authored. Re-run the script and paste the table,
> keeping the Notes. (The Branch column derives `task/<slug>` from the slug, so a
> `merged`/`open` there can point at a **stale/preserved** branch from an earlier
> set — see Notes for what is genuinely in progress.)

| Spec | Header status | Branch `task/<slug>` | Worktree `.tasks/<slug>` | TODO.md |
|---|---|---|---|---|
| [generic-loader-binding](specs/generic-loader-binding.md) | pending | — | — | — |
| [jhonstart-html](specs/jhonstart-html.md) | pending | — | — | — |
| [erika](specs/erika.md) | pending | merged | — | — |
| [onze](specs/onze.md) | pending | — | — | — |
| [rakun](specs/rakun.md) | pending | open (feb96f0) | — | — |
| [stdlib-backends-parity](specs/stdlib-backends-parity.md) | pending | merged | — | — |

## Notes

- **Set just opened (2026-06-10) — no worktrees yet.** All four specs are
  `pending`; none has a `.tasks/<slug>/` worktree or `TODO.md`. Start execution per
  `tasks/AGENTS.md` workflow phase 3: `git worktree add .tasks/<slug> -b
  task/<slug> feat`, seed `TODO.md` from the spec's steps, then re-run the script.

- **Stale branch derivations (slug → `task/<slug>`):**
  - **rakun** `open (feb96f0)` is the **preserved core-coupled F2/F3 reference**
    (`task/rakun`), left untouched. The live v0.beta.7 work ran on
    `task/rakun-port` (F3 placement, merged into `feat`); this set's F2/F4/F5 wiring
    starts fresh off `feat`.
  - **stdlib-backends-parity** `merged` points at the v0.beta.7
    `task/stdlib-backends-parity` (A1 erlang + A3 + literal-receiver parser, merged
    in `aeb01f4`). The **remaining** work (beam/wasm + Part B) is what this set's
    spec covers — start a fresh worktree.
  - **erika** `merged` points at the v0.beta.7 `task/erika-port` (the lib, merged in
    `1668066`). The **remaining** work (a runnable example + the cross-module
    `erika "…"` form) is what this set's spec covers — start a fresh worktree.
  - **generic-loader-binding** / **jhonstart-html** / **onze** have no branch yet
    (new slugs).

- **DAG:** `generic-loader-binding → {jhonstart-html, erika}` (bare template-fn
  binding — `html` / `erika "…"`). `rakun`, `onze`, and `stdlib-backends-parity` are
  independent — all other strands parallel.

- **Acceptance gate (carried from v0.beta.7):** `grep -riE "rakun|jhonstart|erika"
  modules/compiler-core/src` returns nothing (std exempt), shipped as a test —
  re-confirm green at each merge into `feat`.
