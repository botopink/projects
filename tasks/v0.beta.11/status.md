# Task status — v0.beta.11 (Wave 3 of 3)

> Generated by `scripts/status.sh v0.beta.11` on branch `feat` at commit `038ac51`.
> Final wave — three disjoint lib-content rewrites.

| Spec | Header status | Branch `task/<slug>` | Worktree `.tasks/<slug>` | TODO.md |
|---|---|---|---|---|
| [erika-query-ast](specs/erika-query-ast.md) | pending — needs `expr-custom` (Wave 1) | — | — | — |
| [jhonstart-html-ast](specs/jhonstart-html-ast.md) | pending — needs `expr-custom` (Wave 1) | — | — | — |
| [rakun](specs/rakun.md) | pending | `task/rakun-di` | present | 9/14 done |

## Notes

- **Wave = parallel.** Three different lib packages, no shared files: `libs/erika`
  (`erika-query-ast`), `libs/jhonstart` (`jhonstart-html-ast`), `libs/rakun`+`libs/server`
  (`rakun`).
- **Content rewrites on top of the migrated tree.** These land after
  `libs-module-migration` (Wave 2) re-rooted the libs. Each producer keeps a single file
  (`erika.bp`/`html.bp` — lexer/AST/parser inside it) and rakun edits its existing
  modules, so the Wave-2 structure is untouched; this wave rewrites only the bodies.
- **erika-query-ast** / **jhonstart-html-ast** — the two sub-language producers; need
  `expr-custom` (Wave 1) for the `@ExprCustom` dual lowering. Disjoint libs → parallel.
- **rakun** — live on `task/rakun-di` (worktree, 9/14, **not** `task/rakun`=feb96f0 the
  stale reference); F2-scopes + F5 real server remain.
- **End state:** with this wave the whole backlog is landed.
