# Status — 1.0.10-beta

**Updated:** · **Progress:** ~23 % (29 of 127 work items landed)

Count: 32 compiler carry-over items (`00`, C-01…C-32) · `01-std` (6 steps + 3 carried std fronts + the bundled `routing`, `actions` and `validation` libraries) ·
`02-packaging` · 51 rakun · 9 jhonstart · 22 emilia · 9 onze fronts. The percentage weighs items
equally; it is a ratio, not a measurement. The decisions are in
[`decisions-taken.md`](./decisions-taken.md); the questions still open for the maintainer are in
[`decisions-pending.md`](./decisions-pending.md).

One line per front, with its current state; the detail and the open boxes are in each front's
README.

## Done
- [x] Milestone set-up — 1.0.5-beta closed ([`../1.0.5-beta/closure.md`](../1.0.5-beta/closure.md)); 1.0.6 … 1.0.9-beta absorbed and deleted (decision 68, proof in [`unification.md`](./unification.md)); the top-level documents and the six track directories (`00`–`06`) specified, each front carried by name under its track
- [x] `00` C-01 · `13-module-identity` — halves 1–3 and decision 109: one module per `type`, every erlang/BEAM atom `<package>@<path>@@<Decl>`, the identity in the value (a record is `{TypeAtom, …}`, variant tags qualified, wasm boxes a value with its declaration), the name-keyed tables answer the module the import names, host-backed record reads by declared offset, the sidecar refusal returned to its callers. Open: a comptime atom names its template but not its file (`01-checker`'s). A behavior consumed by three modules keeps three copies — superseded by decision 23
- [x] `00` C-02 — an index is a method call: `xs[k]` is `xs.at(k)` and `xs[a..b]` is `xs.slice(a, b)`, on four backends; a user type answering `at` is indexable
- [x] `00 · 01-checker` (C-08, C-09) — `if (x != null)` narrows (one entry per tested name, guard clauses included); a leading-dot enum path resolves by the expected type; step 8 rows R2 (a type's closure comes with it), R4 (a behavior-typed field accepts its implementer), R5 (a pattern in binding position; `refutable-val-pattern`), R6, R8 (a type position takes a type), R9 and handover 15 (a call whose callee is an expression); steps 6 (generics carry their arguments, `Self<…>` required), 7, 11 (`Dict` implements `Display`), 12 (one variant table) and 13 (a local ends with its body) closed; decisions 8 §1.3 (explicit type arguments at a use), 110 for types, 136 (a primitive receiver answers only its declared methods) and 2 (a body may not fall off its end); labelled arguments reorder in a complete call; an unknown annotation family is refused; the comptime atom names its file. Open: step 9's last boxes (every error snapshot's box names its file, `unify.zig` locations, the `throw` location) and step 14 (decision 112, DSL hygiene)
- [x] `00 · 01-checker` — decision 110: an imported type's `as` alias in expression position (`D.empty()`, `S.Dark`, `S.Custom(9)`) reaches the backends as the declared type; `modules/import_alias_on_type` runs on all four targets
- [x] `00 · 02-erlang` / `00 · 03-beam` (C-03, C-06, C-07, C-24) — the §7 formatter and decision 8 at run time on erlang; C-06's and C-07's beam halves; a primitive's host spelling reaches its method (0203-a); BR5 — beam compiles `@External.Erlang` templates at build time, a refused template keeps the run-time path (0203-b); absent prints `null`; `x is Enum.Variant`; a labelled enum-variant argument by its slot; `return` inside a loop; a top-level `fn` as a value; a cross-module `fn`-typed field applied; non-ASCII literals as UTF-8; `@todo` / `@panic` raise their own tuples. `--target all` 810 / 20 / 0, `--target beam` 218 / 3 / 0. Open: the erlang and beam rows under Pending
- [x] `00 · 04-js` / `00 · 05-wasm` (C-06, C-09, C-18 halves) — JS-4 (`val Circle(r) = s;` a plain destructure) and its wasm twin; `adder(3)(4)`; decision 47 on the printer (0405-b); a negative index counts from the end on every backend (decision 139); labelled construction by label; the TypeScript typedefs pass `tsc --noEmit --strict`; a behavior literal's `self`; `std/process` imported beside the test runner; on wasm: `Display`, a primitive boxed into `unknown` (D1–D4), `?.` guards, a slice's `null` end, enum and generator methods, `String.at` and the located host refusal, a trap on a name two linked modules declare, self tail calls, the `primitives.bp` audit, per-module mangling of the link. Open: the rows under Pending
- [x] `00 · 09-ecosystem-residuals` — erika reformatted to C-12's rules (token-identical, idempotent) and its repro directory retired. Open: step 4 re-run on decision 109's atoms; steps 2–3's other rows are tracks B–E's
- [x] `00 · 10-cli-residuals` — a sidecar is shipped by the resolved dependency; one that cannot be shipped is a located error on the `dependencies` entry, returned to the callers. Open: the tail rows under Pending
- [x] `00 · 11-tooling` (C-19) — closed: declaration-name builders, signature help over a record constructor, behavior methods with their return types; `botopink test` refuses a type-unit module `erlc` rejects; the process rows (the gate lock and the green-tree record, `pre-merge-commit`, per-run library output, the stale-binary refusal, the run total, the erlang build compiled by the OTP compiler, known-red lines pinned to a library commit, the quoted member list checked against the tool)
- [x] `00 · 12-language-tests` (C-16) — the residual cells, the runner's own tally, C-06's wasm half accepted by running, every beam cell run and listed with its owner
- [x] `00 · 14-comptime-on-beam` · `00 · 18-comptime-runtimes` (C-20, C-26) — a comptime body reaches the node as BEAM bytes (`comptime/runtime/beam/`, `persistent_beam.zig`; nothing compiles Erlang); commonJS / wasm builds evaluate comptime on the wat runtime (decision 84); the doubled `snapshots/codegen/{beam,wat}/` tree under the runtime-parity audit; `zig build compiler-web`. Open: the CI matrix run
- [x] `00 · 15-language-surface` — closed: every decided-against form refused by name with its cell; a negative decorator argument; a one-line lambda or loop body needs no `;`. C-13's parser patch is 16-formatter's
- [x] `00 · 16-formatter` (C-11, C-12, C-13, G7) — `format --check` walks the whole project; the width rules (16-a, 16-b; a trailing comma keeps a list open, decision 133); the `;` after a braced block optional, not printed, and migrated out of the compiler's trees. Open: refusing that `;` after the libraries and `tests/language` migrate (decision 132); 09's reformat
- [x] `00 · 17-beam-memory` (C-05, C-10) — the `#[@BeamMemory]` carrier, rule and validation; a module `var` lives where the annotation says on erlang and beam through `std/beam`'s primitives; the refusals and cells; the docs text and the rakun migration plan; the layer-2 shapes re-run under `erl`. Open: no `Dict` satisfies the `Ets` seed rule yet; an unknown annotation family checks clean (`01-checker`'s, decision 15)
- [x] `00 · 19-use-activation` (C-27) — closed; the `use` statement and its destructuring under front 24's grant; front 67's `optimistic-like-example.bp` compiles with the tuple form
- [x] `00 · 20-builtins-surface` (C-28) — closed
- [x] `00 · 21-effect-chain` (C-29) — closed; its outcomes hold in front 24's surface; `run/generator_break_value.bp` green on the four targets (wasm's `emitGenEnd`)
- [x] `00 · 22-loops` (C-30) — closed; `for` / `while` / `loop`, `iter` / `stream` prefixes, ranges, the library sweep
- [x] `00 · 23-std-purity` (C-31) — steps 1–6: the import tree (one `ImportPath` per leaf, `as` on four backends, located refusals), the embedded std following `mod`, the pure root with `io/` and `testing/`, `collections` with type-scoped constructors; decision 110 on the use side — `as` on a type leaf (hover `D = Dict`), `import {io}` a namespace of its submodules, `collections.Dict.empty()` through the namespace on four targets (`comptime/std_namespace.zig`, std-c). Open: the gate's rows
- [x] `00 · 24-effects-by-return` (C-32) — the return is the annotation (decisions 118–128): `@Task` / `@Component<C, T>` / `@Iterator` / `@Stream`, `async { }`, `iter` / `stream` loops, `YieldStep<T>`, the three-source hint, type aliases; libraries swept, every `test-libs` row at its pre-sweep count. Open: the README's unticked boxes
- [x] `01-std` — steps 1–4 (`@src()` and the fallible test body, `testing.asserts`, `testing.snapshots`, `testing.mocks`; each box re-run — open: the `.snap.new` hooks of rakun, jhonstart, emilia and onze, the rakun example's `from "onze"`); `01-std-lib-enablement` steps 11–13 (the JSON writers, `json.Json`, `json.decode`, its numerals converted in botopink — decision 142); front 03's content hash (in `hash`); `02-std-async-primitives` (`std/async`, in 24-g's shape); the bundled `routing`, `actions` and `validation` libraries (01std-a, c…e). Open: step 5 (the onze takeover), step 6's copied std fronts, the rows under Pending
- [x] `02-packaging` — step 1 (the shared manifest model, object-form `dependencies`, workspaces); step 2 for rakun, jhonstart, emilia and erika; front 95's cuts (`jhonstart-html`, `-markup`, `-link`, `-test`, `rakun-app`, `erika-test`, `emilia-test`) and the onze takeover prepared for the maintainer (95 step 2). Open: the rows under Pending, 95-a…e
- [x] `03-rakun` — the audit (329 boxes held), front 04's erlang-only close (every manifest `["erlang"]`, the node halves deleted, the BEAM listener over HTTP and TLS), and fronts 07 · 10 · 21 · 23 · 63 · 72 closed; 05 · 06 · 08 · 11 · 13 · 14 · 16 · 17 · 18 · 19 · 22 · 25 · 62 · 65 · 74 · 82 landed with a tail (18: the Redis arm runs only with `RAKUN_TEST_REDIS_URL`; 19: the broker double and `bootAndExit` wait on fronts 15/16, `FakeRequest` hands handlers `toRequest()` until an implementer converts to its behavior; 82: the dev-profile cache default is front 80's). rakun builds no HTML: `rakun-app` is the route table and the page seam (`ChunkWriter` / `PageRenderer`). Every choice to confirm is `decisions-pending.md` 03r-a…e; every compiler defect met is a `language-gaps.md` row
- [x] `04-jhonstart` — fronts 94, 26, 27, 28, 29, 30, 31, 32 and 67 landed; members `jhonstart-link`, `-html`, `-markup`, `-test` (the helpers of `modules.md` § 5, 21 tests), `-forms`, `-emilia`, `-dom-test` (front 30's browser half on a minimal document, commonJS, 30-g); the five example projects of `modules.md` § 8 (32 snapshots). What onze owed landed: the island starter table `globals.starters` (`registerStarter`, per-route `registerRouteStarters` — 29-a), `app(…, lang:)` (30-f), the render over a `ChunkWriter`-shaped record (contract 5d); front 48's rendered cells in the bridge. Open: front 17's log line (31-b), 26's shared late-signal handler; choices 26-a, 27-a, 29-a, 30-b…g, 31-a, 31-b; four compiler repros (Pending). The globals are one `pub val globals` (decision 140). The query hole is closed (26-b): `PageContext` has no `query`, `searchParams()` / `request()` mark the render and the payload's `d` is that mark (`jhonstart` 199 → 204 on both rows); decisions 113's and 117's jhonstart halves verified landed; no `// LANGUAGE GAP:` marker is left in jhonstart's code, and `link.bp`'s note on imported defaults (closed by C-04) is corrected
- [x] `05-emilia` — all 22 fronts (33–48, 54–59) landed, **734 / 734** on both rows, fifteen local examples green on both rows (`emilia-card` with jhonstart by workspace path), `emilia-test` 1/1. Measured against Tailwind 4.3.2's compiled output: translate and skew (05emilia-i), and now scale (`--tw-scale-{x,y,z}` percentages, composing), fractions (`calc(N / D * 100%)` through one `fraction`), `open:` / `group-open:` with `[open]`, v4.1's `rtl:` / `ltr:`, `marker:` / `selection:` as lists of one-`&` variants (05emilia-j), `spacingNegHalf` and `Neg.Half.0` (05emilia-k), every unconfirmed column confirmed and its family moved to upstream's form (05emilia-l: opacity percentages, `calc(Ndeg * -1)`, `rawShadow`, `rawTransitionProperty`), front 54's refusal asserted by its message, the dummy-record `??` reads turned into null guards; front 40's divide calls `siblingSelector()` with the byte-identity test, and front 46's `AccentAuto` (decision 81) — both had landed. 2 boxes open, each owned by another front (56's `hashHex` → std `contentHash`, onze 68's shared-literal assertion). Choices 05emilia-a…l in `decisions-pending.md`.
- [x] `libs-external-methods` — a host-bound `declare fn` written inside a `type` body is a method of it on every backend (commonJS class member + `__bp_adopt` for host-built records, erlang exported from the type's module, beam through a hidden `declare fn`, wasm refused at the call, `.d.ts` declared): . std's owner-bound host functions moved into their types — `io.net` (`Listener`, `Socket`, `TlsListener`, `TlsSocket`) and `regex` (`Regex.matches`); owner-less ones stay free. Choices lem-a…f in `decisions-pending.md`.
- [x] decision 140 — a module-level `pub val` of any type is read from an importing module on all four backends, and an imported module's body runs before the importer's, dependencies first (`tests/language/modules/pub_val_across_modules`); jhonstart's globals are `pub val globals`
- [x] decision 141 — the BEAM lowering takes `receive`, `!`, `catch E`, `try … of`, `try … after` and integer-field binary patterns; `'__bp_erl_eval'/2` is gone, a refused template is a located build error; 177 / 177 shipped templates lower (169 before)
- [x] decision 142 — `json.decode` converts a numeral to its correctly rounded `f64` in botopink (no host `strtod` cell); bit-identical to the host on the boundary values and 3 500 randomized numerals, commonJS and erlang
- [x] onze F1 — a sibling's `pub val` read by a test: the erlang test runner loads the sibling (`modules/pub_val_in_a_test`)
- [x] onze F2 — an imported record's closure reaches the types its module imports from the package's other modules (`modules/import_type_closure_across_modules`)
- [x] onze F4 / F9 — `xs.at(i).unwrapOr(d)` lowers in a method body and inside parentheses (`run/unwrap_or_positions.bp`)
- [x] onze F5 — `"src": "."` is honoured by `build`, `check` and `test` (`modules/src_at_package_root`)
- [x] onze F6 — a one-argument `slice` as an `if` branch's value gets its default on erlang (`run/string_slice_in_if_branch.bp`)
- [x] onze F7 — integer `/` truncates toward zero on every backend (`run/integer_division_truncates.bp`); the rule awaits the maintainer's confirmation (docs.md was silent)
- [x] onze F8 — a lexer error in an imported module is the reported, located error (`modules/lexer_error_in_imported_module`)
- [x] onze F10 — an imported module's `'_botopink_init'/0` runs, by decision 140 (`modules/pub_val_across_modules`)
- [x] erlang float literal — every number token is a valid Erlang number (`5e-324` → `5.0e-324`, `0xFF` → `16#FF`), and an exponent literal is an `f64` (`run/float_literal_spellings.bp`, `run/number_literal_erlang_spellings.bp`)
- [x] `00 · 11-tooling` — `test-libs` is deterministic on the rakun cells: `botopink test` gives each run its own scratch directory (`BOTOPINK_TEST_TMPDIR`), and rakun's fixture projects and scratch files live there instead of under the member both of its cells share
- [x] language gaps, rakun rows — 19 of 22 closed (behavior dispatch by the receiver's type, nested `return`, `@Result` methods, nested test files, `length`, imported fn fields, imported behaviors and aliases, unimported types, method arity, `@Result` patterns beside an enum `Error`, decorator siblings, emitted proxies, namespace imports, implementer → behavior; four no longer reproduced); open: decorator imports, and `try` / captured `var` writes in a lambda (decisions-pending lg-a, lg-b)
- [x] `00 · 01-checker` (C-04 across a module boundary) — a closed default (a literal, `true` / `false`, a sign, an array or tuple of those) travels with an imported function, so a short call to a sibling's or a dependency's function is filled; a default naming a private binding stays the arity error (`modules/default_argument_across_modules`, `modules/default_argument_open_across_modules`). jhonstart's and emilia's `attrs:`-everywhere workarounds and their gap rows are gone

- [x] `01-std` — `querystring` is a codec: percent-decoding, a field split on its first `=`, `parseForm` reading `+` as a space, `stringify` escaping, and the 03r-e refusals as `Error`s (std-a); identical on commonJS, erlang and beam
- [x] `01-std` — `fs.exists` is "a path that exists, of any kind" on both rows (`/dev/null` included; a symlink followed, std-b)
- [x] `01-std` — `asserts.throwsWith` matches a needle with a `"` or a `\` on erlang: the cell reads a `@panic` / `@todo`'s own text, not its `~p` rendering
- [x] `00 · 01-checker` (`front/checker-rows`) — step 9's last boxes (every error box names its file; no unlocated unification error; a `throw` located at itself) and step 14, decision 112's DSL hygiene (the library's names resolve in the library, `e.lookup` answers the declaration's identity, go-to-definition follows); a decorator carries a function its module imports from the project or a package, and two functions of one name reaching it are refused. Open: a `from "std"` function in a decorator body (`language-gaps.md`)
- [x] `00 · 24` / commonJS — an `if` block that `await`s without returning, in a `@Task` body, is an `async` function called in place (`run/task_await_in_if_block`); a test module that does not finish is named (`<module> EXITED WITH STATUS n`) and, under `--json`, is one failure (`module_crashed`)
- [x] `00 · 02-erlang` — a bare `return;` in an `if` block of a `@Task<void>` body leaves the function: no longer reproduced on erlang (`tests/language/run.sh --target erlang --only run/task_void_return_in_if_block.bp`); wasm's own half fixed (`run/task_void_return_in_if_block`)
- [x] `00 · 10-cli-residuals` — a dependency's modules compile in import order, not `files` order (`modules/dependency_files_order`); an `@External.Erlang` module neither shipped nor in the Erlang code path is a located build error, on `build` and `test` alike (`run/external_erlang_host_module_missing`); `botopink build --target erlang` ships `.erl` sidecars (`modules/erlang_host_sidecar_shipped`); a sidecar named like a module of the build is shipped (`modules/erlang_sidecar_named_like_a_module`)
- [x] `00 · 02-erlang` / `00 · 11-tooling` / `00 · 01-checker` — the silent skip of a sibling `erlc` refuses no longer reproduces (the runner's loader refuses the run, naming the module); `print` / `println` are unbound and the refusal names `@print` (`reject/bare_print_call`)
- [x] `00 · 05-wasm` — per-module mangling of a function two linked modules declare (`modules/linked_fn_name_collision`, `modules/namespace_import_module`); `f64` float literals and decimal radix literals (`run/number_literal_erlang_spellings` on all four); boxed `f64` record fields and float method signatures (`run/float_record_field`); a `?V` over a type parameter boxed (C-18's wasm half: `run/index_dict`, `run/index_at_optional`, `run/index_user_type`, `run/explicit_type_arguments`); a lambda in a function-typed field typed by the field (`modules/imported_fn_field_call`); an array out of a `@Result` payload (`run/try_start_positions`). Open: type-parameter `==` (`modules/method_on_unimported_type`), `val assert` over a record (`run/val_assert_record_pattern`)
- [x] `00 · 03-beam` (C-03) — every `pub` host `declare fn` of std has a wrapper on beam, a template one compiled at build time: `fs.exists`, `os.eol` and six more answer from another module (`run/std_template_host_fns_across_modules`)
- [x] `00 · 03-beam` / `00 · 05-wasm` — `String.split("")` cuts into codepoints on every target (beam ran `string:split/3`, wasm cut bytes); `run/string_split_empty_separator`
- [x] `00 · 01-checker` — a member of a `@Result` that is not one of its methods is `result-member-not-a-method`, located, naming `try` / `unwrapOr` (`reject/result_field_read`, `reject/result_unknown_method`)
- [x] `01-std` — the FAIL line names `src/main.bp:<line>` on commonJS and erlang; `testing.asserts` builds on wasm (a call of a host-backed assertion is refused at the call); the pre-commit gate refuses a staged `*.snap.new` and `botopink test` lists the candidates
## In analysis
- [x] `00 · 25-gate-perf` (C-33) — steps 1–4 (the bounded worker pool for `botopink-lib-test`, `tests/language` and `check-docs`; the compiler-core suite in shards; the gate's stages side by side; a content-keyed, relocated `.beam` cache for the erlang cells — warm gate 227 → 132 s, `test-libs` 168 → 70 s, same verdicts and output). Decision 143: `test-libs` and the gate run in place in a worktree. Open: the per-cell dependency compile (a `compiler-core` front)

## Pending
- [x] `00 · 01-checker` — a free function whose first parameter is named `self` lost it (erlang `twice/1`, commonJS `NaN`); closed: `self` outside a `type`/`behavior` body is `self-param-outside-type`, at the name (`reject/self_param_free_fn`); std's four slice helpers renamed their parameter
- [x] `04-jhonstart` findings, minimised: an `Array<B>` of two implementers types (`run/behavior_array_of_implementers`); commonJS's global `slice` patch reads a missing start as 0 (`run/host_array_slice_without_start`); a project `pub fn attempt` beside `import {match.attempt} from "routing"` no longer reorders the modules (`modules/import_from_package_beside_same_name`), and a `//` comment quoting `from "<pkg>"` no longer loads the package; a type reached only through an imported record's field and a parameter named like an imported hook do not reproduce (`botopink run --target commonJS|erlang` over each shape, closed by 01 R2 and step 13)
- [ ] `03-rakun` front 62's std substitutes — rakun's `splitQuery` / `encodeQuery` stay; std's `querystring` is now the codec they stood in for (§ Done), so the two can move onto it, with `decodeComponent`'s keep-as-written reading against `querystring`'s refusal (`decisions-pending.md` std-a). The other loader-defect workarounds are deleted
- [ ] `03-rakun` front 23 — **`PageRenderer` answers `@Task<void>`** (`rakun-app/src/ssr.bp:60`) where contract 5d types it `@Task<@Result<void, string>>`: the render's `Error(message)` has nowhere to go, so rakun cannot answer it as a raise (500 before the first chunk, close after). jhonstart's side of 5d is asserted with a `ChunkWriter`-shaped record (`streaming_test.bp` "chunk writer: …")
- [ ] `06-onze` front 68 — adopt what jhonstart and emilia now provide: emilia's `styleRule(tokens, th)` for the `styleMap` and the runtime `s` check, jhonstart's `registerStarter` / `registerRouteStarters` over `globals.starters` in place of the entry's own `__jhIslandStarters` cell (which `hydrate()` no longer reads), and `app(…, lang:)` for front 50's `--lang`
- [ ] `03-rakun` / `06-onze` — the "declared parameter defaults are never applied" rows and `// LANGUAGE GAP:` markers in their specs predate C-04 across a module boundary: a closed default now travels; each row is to be re-measured by its track
- [ ] `03-rakun` front 74 (the tail) — its hand-rolled POSIX-only `absolutePath` treats `C:\\certs\\server.pem` as relative, so on the blocking `windows-2022` commonJS row the "absolute path searched" refusal names the wrong path; replace it with std's `path` and assert idempotence (`absolutePath(resolved) == resolved`), without importing `io.process` in the test file
- [x] `00 · 11-tooling` — the cwd-anchored build roots of the test harness helpers escaped `scripts/check-test-scratch.sh`; closed: the script scans every line of a file under a `tests/` directory as well as every `test` block, and the codegen and comptime harnesses' build roots and the LSP harness's eval roots are `test_scratch` paths (`buildRootPathFromSrc(io, loc)`)
- [x] `05-emilia` front 45 — measured against Tailwind 4.3.2's compiled output, the code was wrong and now follows it: translate and skew write their `--tw-*` variable and compose, `transform-cpu` / `-gpu` are v4's chain rows; the spec, `reference-coverage.md`, `tailwind-mapping.md` and `test-snap.md` say so (`emilia.bp` front-45 tests, `emilia-transforms` 18/18)
- [x] `05-emilia` front 54 / front 56 — the `--tw-*` identity defaults are upstream's `@property` registrations, carried as `Block`s by the translate and skew sheets, with `renderDocument`'s `@layer properties` `@supports` fallback (05emilia-i; `output.bp` "renderDocument — `@property` blocks bring upstream's `properties` layer")
- [x] `00 · 11-tooling` — `known-red-libs.txt` pinned a compiler-repo claim about a library-repo state; closed: a line is `<lib> <target> <commit> <owner> <reason…>`, and `test-libs.sh` fails it as stale — red or green — when the library's checkout has moved from `<commit>` or it names none (`test_tooling.sh` pins both)
- [ ] `03-rakun` front 05 — **owes the boot call that makes front 14 do anything.** Front 14 landed every piece and exercised the call site (`test/config_test.bp` binds a `#[configurationProperties("app.billing")]` + `#[validated]` record, validates it and asserts the key-named refusal text), but `config.bp`'s comment names `rkConfigValidate`, which does not exist, and `modules/rakun/**` is not front 14's. **The debt is one addition to front 05's boot sequence, after binding and before the first component**: `validate<TypeName>(bound)` then `refuseInvalidConfig(typeName, prefix, report)`. Until it lands, a project can declare `#[validated]` on a configuration record and boot with an invalid one
- [ ] `03-rakun` — **`#[validated]` now exists in two packages and one of them does nothing.** Front 05 declares a placement-only marker in `modules/rakun/src/config.bp:1795` ("until 14 lands this is placement only"); front 14 landed the real one in `rakun-validation`. An application must import it from `rakun-validation` and **must not import both names into one module**. The failure shape is the expensive part and is now documented in four places an author passes through: importing front 05's leaves `validate<TypeName>` undefined and the red lands at the **call site as an unbound variable**, nowhere near the annotation. Recommendation: delete front 05's, now that the condition its own comment set has been met — but it is front 05's file and front 14 was right not to touch it
- [x] `00 · 01-checker` — **an integer literal does not widen to `i64` in arithmetic, and that diagnostic has no location.** Closed. Measured by rakun front 14: `pub fn shrink(x: i64) -> i64 { return x - 1000; }` reds `type mismatch: expected i64, got i32` on both rows with **no line and no column — only the file**. The contrast is what makes it actionable: a *comparison* widens (`x > 0` is fine), an `i32` literal in a **parameter** position reds *with* a location, and `val n: i64 = 5;` is fine. There is no `i64` literal spelling at all, so the front's tests build one from the clock. Two defects in one repro — the missing widening and the missing location — and the second is the one that costs minutes per encounter
- [ ] `03-rakun` front 05 — **an `Array<string>` configuration field binds as a scalar.** front 05's `config.bp` comment asserts that `hosts: string[]` reflects as `typeName == ""` "the same as `Array<string>` would"; front 14 measured that `Array<string>` renders **`"Array"`**, and front 05's `#[configurationProperties]` emitter has no `"Array"` arm — so such a field falls through to `rkValueOr`. Correctly not front 14's file. This is the same `@Decl.Field.typeName` gap jhonstart front 29 hit from the other side, where `string[]`, a function type and a tuple type all render the empty string
- [x] rakun / jhonstart — every record without a field list is `type X()` / `type X() { … }` (decision 138), and jhonstart binds an awaited hook before comparing it (decision 137)
- [x] `00 · 01-checker` — a fully qualified `Shape.Circle(r: 3)` typed as `Hole` when two enums declare `Circle` (the flat bare-name table); closed by 01 step 12
- [x] `00 · 01-checker` — a local `val` leaked into the module scope of every later top-level declaration (`repository/jhonstart/repro/local-binding-leaks-to-later-decls/`); closed by 01 step 13
- [x] `00 · 01-checker` / comptime — `@Decl`'s type names erased the element type and were empty for `T[]`, function and tuple types; closed: every type name is the type as the source spells it (`run/decl_type_name_spelled`). jhonstart compares the spelled `@Component<ElementBase, Element>`; `validation` and `testing.mocks` read the spelling
- [ ] `03-rakun` — two tests assert the refusal of a parameter "whose type reflection loses", which `@Decl`'s spelled type names no longer make: `rakun-data` `test/sql_build_test.bp:137` over `src/sql/transactional.bp:63`, `rakun-security` `test/build_test.bp:99` over `src/method_security.bp:129` (and `rakun-hateoas` `src/hal.bp:105` names `typeName == ""`); banked in `known-red-libs.txt`, the commonJS rows now build (`restricted-targets.txt` `build` → `1`)
- [ ] `03-rakun` front 04 — **graceful shutdown's hard half is the listening socket in `rakun_runtime.erl`**, which is front 04's, not front 07's and not front 76's. Front 07 established the distinction rather than reaching into `modules/rakun/`: front 76's `readinessDrained()` is the soft half its own spec says to land without, and "close the listening socket, keep connection processes alive" cannot be written from `rakun-web` at all. Routed here
- [x] `00 · 11-tooling` — `rakun-web commonJS`'s reason text; closed: the rakun ledger lines were rewritten (`build`, "rakun is erlang-only (decision 113)"), measured by `zig build test-libs`
- [x] `00 · 01-checker` — a fully-labelled self tail call hung, and labelled arguments were applied by position on every backend (`diff(b: 1, a: 10)`, `slice(end: 3, start: 1)`); closed in the checker, which reorders a complete labelled call
- [x] `00 · 11-tooling` — the fixed-path test race (`fix/test-path-race`): `compiler-cli`, `language-server`, `lib-test-runner` and `bpmp` wrote fixed paths under the shared checkout and failed under concurrent processes; fixed with per-run roots, a fixed-path site that does not compile and `scripts/check-test-scratch.sh` as a dependency of `zig build test`; `runtime.zig`'s cache write and the shared `persistent_erl` log fell with it.
- [ ] `00 · 08-hygiene` — `docs.md` § *Decided, not yet implemented* re-derived by running every row against the compiler of decisions 136–143: every row holds as written (the `;` after a block statement is still optional, an imported function's default still unfilled, a behavior-typed call on wasm still traps, the eleven deliberately-absent forms each refused with their code); `test-docs` 91 / 75 / 8 / 0; and every relative link of the documents and `specs/1.0.10-beta/` resolves. Open, all in other fronts' files (C-23): 14 `primitives.d.bp` comments, the `@external(<target>, …)` comments, four `codegen/tests/**` comments, the transport-error diagnostic's test, and ten `.zig` files `zig fmt --check` reds on an untouched base (`effect_generator.zig`, named here first, passes now) — sites by owner in `08-hygiene` § Open
- [x] `00 · 01-checker` — **a fifth symptom of the flat bare-name table, and the worst-worded one yet.** Closed (01 step 12). Measured by emilia front 41 : `val h: Token = .Color.Hex("#abc")` reports `'Hex' is not declared in any behavior implemented for 'Ns'` — the leading-dot resolver walked `.Color` into front 54's **`Ns`** enum, which has a `Color` member, instead of into the annotated type `Token` that the author wrote one token earlier. The diagnostic names a type the author never mentioned, in a file where `Ns` is incidental. Add it to `01-checker` step 12's table; it is the same root as `.Red` colliding, the section leaf with no shorthand, and the leaf shadowed by a top-level variant
- [x] `00 · 01-checker` / `00 · 15-language-surface` — a diagnostic naming the empty string (`unbound variable ''`); does not reproduce: `botopink run --target commonJS` over `[.Raw("x")]` under `Tok[]` and `.Effect.Shadow.Raw("x")` under `Tok` constructs both (01 step 12); the stale `language-gaps.md` row is gone
- [ ] `00 · 05-wasm` / `00 · 01-checker` — **two host-wrapper rules meet on wasm** (decisions-pending ck-host): `run/external_wrapper_keeps_refusal` is listed on wasm
- [x] `00 · 11-tooling` — concurrent gates starved each other; closed: one gate per machine — `gate.sh` takes a lock and a second one waits, naming the holder's pid, checkout and start time; a `--staged` run whose working tree, sibling libraries and toolchain were already gated green runs stage 1 only (`scripts/AGENTS.md` § Gate lock, § Green-tree record)
- [ ] `03-rakun` — `modules/rakun/src/config.bp` and `src/file_router.bp` both declare `pub fn sub(string, i32, i32)` and `pub fn charOf(string, i32)`, which the cross-module duplicate refusal now rejects in `test/config_test.bp`; two one-line fixes in rakun, banked in `known-red-libs.txt`
- [ ] `00 · 01-std` — **refusing duplicate `pub` *declarations* was measured and rejected**: the refusal belongs to the **consumer's** unqualified use, keyed by the importing module, not to the declaration. `arch` is folded into `io.os` (`io.process.arch` was the same host call); `empty` and `fromList` left with decision 111's type-scoped constructors. Eleven names remain. Ten are different functions under one name, consumed qualified: `parse` (`json`, `querystring`, `url`), `stringify` (`json`, `querystring`), `matches` (`hash`, `regex`, `testing.asserts`), `normalize` (`path`, `unicode`), `eq` (`collections`, `testing.mocks`), and `erlang`'s BIF bindings `abs`, `round`, `trunc` (beside `math`), `exit` (beside `io.process`), `float` (beside `io.random`). `errorText` (`async`, `testing.asserts`) is one function twice, and stays twice: rakun reads `asserts.errorText`, and a std module cannot import a sibling
- [x] `00 · 15-language-surface` / `00 · 01-checker` — `from` stays reserved everywhere; a keyword where a field, a parameter or a binding is declared is `reserved-word-as-name`, naming the word at it (`reject/reserved_word_field_name`, `reject/reserved_word_param_name`)
- [ ] `00 · 05-wasm` — a method called through a behavior-typed value traps on wasm (`unreachable`), in one module or across two. The checker half — a conforming value from another module refused — does not reproduce: `botopink run` over a sibling `mod` and a `path` package, `useIt(Bob(…))` and `make() -> Greeter`, prints on commonJS, erlang and beam
- [ ] `01-std` step 6 · `01-std-lib-enablement` — steps 1–13 landed (§ Done); step 14 holds for jhonstart and emilia (no JSON copy left, both green on both rows) and is open for rakun's three sites: `rakun-data/src/sql/health.bp:37` `jsonText`, `rakun/src/autoconfig_registry.bp:150-195`'s scanner, `rakun-actuator/src/health.bp:47` `rkActuatorJsonObject`. Found on the way: `import {io.process}` shadows Node's global `process` in the commonJS test runner (a codegen defect); a non-ASCII string literal reaches erlang as latin1 and a codepoint above U+00FF is `illegal character`
- [ ] `01-std` step 6 — the three copied std fronts: `02-std-async-primitives` and `03-std-content-hash` landed, `01-std-lib-enablement` as the row above; what the step still owes is in [`01-std/README.md`](./01-std/README.md) § Step 6
- [x] `00 · 01-checker` / `00 · 04-js` — a single-target `#[@External]` declaration refuses at every statically present call on the other target; answered by the strictest reading: a wrapper whose body is only a foreign call inherits no restriction (`run/external_wrapper_keeps_refusal`, `docs.md` § host bindings) — a member needing it names its `targets` or gives the declaration the other binding
- [ ] `01-std` step 5 — `onze13` → `onze` name takeover (decision 79). Step 4 (`testing.mocks`) is done; this waits on the maintainer tagging and archiving the old mocking repository, which front 49 (and through it track E, front 68 and jhonstart 27/29's holes) waits on
- [ ] `02-packaging` step 2 (the `-test` members) — jhonstart, emilia, rakun and erika have theirs (empty `pub` surface, one inline test); onze's comes with the takeover (decision 79 — the maintainer's tag, archive and re-point; front 95 step 2 has the commands)
- [x] `02-packaging` — no example depends on another library by `{ git }` or an old-layout path: `emilia-card` → jhonstart is `{ "path": "../../../jhonstart/modules/jhonstart" }` (jhonstart's examples depend on their own workspace only)
- [ ] `00 · 01-checker` / `01-std` — a std module's decorator cannot be reached from a consumer: `#[mocks.mock]` **parses** (the annotation name lands as `"mocks.mock"`) but a `from "std"` import registers nothing in `env.decorators` (`comptime/infer.zig` `markStdImports` marks the module and its types only), so the marker silently does nothing and the consumer gets `unbound variable`; and the bare fallback is not expressible either — `import {mock, when, eq} from "std"` is refused with `unknown "std" module in import`, because the last segment is read as a module name. Closing it needs **two** changes (register a std module's `@Decl`-shaped `pub fn`s under `<mod>.<fn>`, and self-qualification inside a std module), and the lookup fix alone would be worse than the refusal — the marker would fire and emit code that does not compile
- [ ] `00 · 04-js` / `00 · 02-erlang` (**erlang and beam halves landed**) / `00 · 05-wasm` — **`x is <Enum>.<Variant>` is wrong on every target measured, each differently, and only one of the three is loud.** string), Num(n: i32) }` with `val x: Token = Token.Text(v: "hi")`: **Re-confirmed after the cross-module fix landed**, unchanged on all three — that fix moved the *cross-module* name tables and left the in-module ones alone, which is exactly what its own report said it was leaving to `01-checker`.
  - **commonJS** throws `TypeError: Function has non-object prototype 'undefined' in instanceof check` — `isTest` lowers the dotted path to `instanceof Token.Text`, and `Token.Text` is the enum's static *factory function*, whose `.prototype` is `undefined`;
  - **erlang** answers **`false` for `x is Token.Text` and `false` for `x is Token.Num`** at exit 0. The first is a **silent wrong answer**, which is worse than the commonJS throw and was not recorded before;
  - **wasm** answers `true` for `Token.Text` and then **traps** on `Token.Num`.
  The bare forms are fine on all three (`x is Token` and `o is Other` for a record answer `true` on commonJS, erlang and wasm) — a top-level enum is exported and required, so there is one copy. `buildPatternCheck`'s `.variant` arm still emits `instanceof <bare variant name>`, which names no class any module emits — latent, because the checker refuses `val assert Shape.Circle(r) = s` today. `charCodeAt` has no wasm or beam lowering: `@print("A".charCodeAt(0))` answers `65` on commonJS and erlang and traps on wasm (`unreachable`). **No owner** — the spread makes it one front, not three.
- [x] `00 · 11-tooling` — two gates over one library checkout wrote the same output; closed: `botopink test` writes `test-out/<target>/<id>/` and `botopink-lib-test`'s compile-only cell `lib-test-build/<target>/<id>/`, removed when the cell ends; a worktree's gate runs in place (decision 143)
- [ ] `00 · 02-erlang` — **the mirror of the commonJS collision, pre-existing.** `patternNodeExtra` asks `enum_variants` first *unconditionally*, so a `case` over a union of **records** whose arm names a record that some enum also declares as a variant matches the variant atom and dies: `{{case_clause,{main__t__block,3}}, …, main, shapeName, 1}`. commonJS no longer shares it after; no cell covers it
- [x] `00 · 11-tooling` — the two process hazards; closed: `scripts/git-hooks/pre-merge-commit` runs the gate on a merge that commits by itself, and `test-libs` writes only per-run directories (the row above)
- [ ] `05-emilia` — families no open row named, measured against Tailwind 4.3.2 while confirming the others and left as they are (05emilia-l): the transition presets write `var(--ease-out)` / `150ms` where upstream writes `var(--tw-ease, var(--default-transition-timing-function))` / `var(--tw-duration, …)` and `--tw-ease`; `backdrop-opacity-*` is `opacity(0.5)` where upstream is `opacity(50%)`; `border-spacing-*` writes the property where upstream writes `--tw-border-spacing-{x,y}`; the backdrop filters omit `-webkit-backdrop-filter`; `divide-*` omits upstream's `border-*-style:var(--tw-border-style)`
- [ ] `00 · 01-checker` — `x != null && x.f` does not narrow `x` in the right operand of `&&` (`` `f` is read off an optional `?T` — write `?.f` ``), though `&&` narrows its then-branch; found in emilia's `coalesceStep`, written as a nested `if`
- [ ] `03-rakun` — `??`-with-a-dummy-record workarounds that `if (x == null)` narrowing makes unnecessary (emilia's are gone): `modules/rakun-actuator/src/endpoint_host.bp:136` and `:152` (`EndpointResponse(…)`), `modules/rakun-security/test/basic_test.bp:137` and `:174` (`UserDetails(…)`), `modules/rakun/test/config_test.bp:548` (`Doc(…)`); and front 22's carried row 7, whose note that an optional must be read through `if (matchPath(…)) { m -> … }` is no longer the only spelling
- [x] `00 · 11-tooling` — a quiet auto-merge dropped a member from a quoted workspace refusal; closed: `botopink-lib-test` checks every quote of the refusal's member list in a workspace root's `*.md` against the list the tool prints, and a mismatch fails `test-libs` (`test_tooling.sh`)
- [ ] `00 · 01-checker` — a section leaf has no shorthand against a section type: with `fn breakToCss(b: Token.Layout.Break)`, `breakToCss(.Zeta)` reds `unbound variable 'Zeta'`, and with a top-level variant of the same name beside it the shorthand resolves through the flat bare-name table and reds `type mismatch: … got function`; a section leaf must be written from the enum root
- [x] `00 · 11-tooling` — library threads could measure a stale compiler; closed: `build.zig` embeds the content hash of the sources it builds from (`modules/source-stamp`), and `botopink test` and `botopink-lib-test` refuse to start when their checkout's sources have changed since
- [ ] `00 · 02-erlang` — **`Array.lastIndexOf` does not lower on erlang**: `function lastIndexOf/2 undefined`, a hard error invisible until the second target runs. Found by emilia front 40, which drew the standing conclusion with it: run `--target erlang` **before every commit**, not once at the end of a front. still `out/erl/main.erl: function lastIndexOf/2 undefined`, and still invisible until the second target runs
- [x] `00 · 11-tooling` — `botopink test`'s last line was a module's count; closed: text mode ends with `total: <P> passed, <F> failed in <N> module(s)`, a module whose runner printed no summary fails the run, and `botopink-lib-test` reads a cell's count from the run total (`test_tooling.sh` `multi`)
- [ ] `03-rakun` front 23 / `06-onze` 49 — **the dynamic mark has two writers.** jhonstart's half is closed (26-b): `PageContext` has no `query`, `searchParams()` and `request()` mark, and the payload's `d` is jhonstart's own mark. rakun front 23 still says `isDynamic()` is what onze reads to fill `d`, and rakun's `markDynamic("searchParams")` fires on any read of the `Request`'s query — which onze makes on every request when it builds `PageInput.query` / `RequestData.query`, so rakun would mark every page dynamic. rakun's cache decision is to read jhonstart's `d` (onze hands it back through the `Response`) or onze is to build the input without the marking read
- [ ] `00 · 02-erlang` / `00 · 01-checker` — shapes front 23 met writing a renderer, still open on erlang and in the checker: a function-valued record field called in place (`v.tagOf(e)`) lowers to a method call on erlang (`function tagOf/2 undefined`); a local or parameter may shadow an imported module-level `pub fn`; `xs.at(i).unwrapOr(…)` reads the element unwrapped inside a function generic in its element type; `Array<fn() -> @Task<El>>` beside another parameter generic in `El` is refused `generic-arg-skip-forbidden`
- [x] `00 · 11-tooling` — `botopink build --target erlang` never invoked the OTP compiler; closed: the build compiles every emitted `.erl` in memory in one `erl` and fails naming each refusal, or when `erl` cannot run (`cli_contract.sh`). Its first finding: bare `print`/`println` have no erlang lowering, the module is refused (`@print` is the portable spelling)
- [x] `02-packaging` — `emilia-card`'s `targets` is `["commonJS", "erlang"]` (4/4 on both rows)
- [x] `00 · 11-tooling` — `compiler-cli`'s shared-cwd flakes (`LibFileNotFound`, `LibNotFound`, `cli.config.workspaceRefusal`); closed: every fixed test path is a `test_scratch` path — four concurrent copies of the `compiler-cli` test binary, three rounds, and two concurrent `zig build test` runs, three rounds, all green
- [ ] `00` — three more writers to a shared path under a checkout they do not own, named while fixing the first: `compileCell`'s `lib-test-build/<target>` is per target and never per run; **it *is* read back** — `modules/compiler-cli/tests/test_tooling.sh:107` checks the directory exists — but only as an existence check, so a concurrent overwrite is still invisible. ("Nothing reads it back" was my wording and it was wrong; the correction is the path-race front's); `runtime.zig`'s content-keyed `CACHE_ROOT` is written with a plain truncate-and-write where its neighbour `template_eval.writeModule` stages and renames *precisely* against this, so a torn tail reads as a short entry; and `.botopinkbuild/tmp/persistent_erl` is a fixed shared server directory
- [x] `00 · 11-tooling` — the `loadone-missing` flake; closed with the row above (`loadone` is a `test_scratch` path)
- [ ] `00 · 01-checker` — **`runtime_builtin_names` short-circuits to `void` before the declaration is read**, throwing away three declared answers exactly the way the index expression's was: `@todo()` and `@panic()` are declared `-> noreturn` in `libs/std/src/builtins_fns.d.bp` and type as `void`, so `val x: i32 = @todo();` reds `expected i32, got void` — which is the canonical stub form — and `@module()` is declared `-> module` and types `void` too. `x is T` is fine (`bool`), and `@print`/`@trap`/`@emit` are `void` by design. Found by C-02, which fixed the same shape one level over, re-measured: `return @todo();` in a `-> i32` function still reds at the call
- [ ] `00 · 01-checker` — an enum section head silently captures a top-level variant of the same name (`Layout.Break { After, … }` beside `After(inner: Token[])` broke `Token.After(inner)`'s payload at run time in an unrelated module); a name that cannot be constructed must not capture one that can — a type error, not a run-time `undefined`
- [x] `00 · 11-tooling` / `00 · 01-checker` — an import had to name the types its imported type mentions; closed: no longer reproduces — a consumer of `emilia` importing `{Options}` alone reads `o.theme.entries` and `o.base` on commonJS and erlang, and `modules/import_type_closure` and `modules/import_type_closure_across_modules` are green
- [ ] `00 · 01-checker` — **a full-arity labelled call is still zipped positionally.** C-04 made a parameter claimable by **label**, and the enum-path fix generalised what that costs: *any* position that reads something by argument index is wrong once a label can claim a parameter. The enum-path fix inverted C-04's own plan rather than building a second mapping (two mappings can disagree, and the expectation would then name the wrong enum), and made the expectation read **nothing** where the parameter is not knowable rather than read the wrong thing — but the pre-existing `for (typedArgs, f.params)` zip for a full-arity labelled call is untouched, and `planDefaultFill` deliberately does not plan for it. So `P(y: 2, x: 1)` with every argument written still binds by position in the checker, which is the same shape as the commonJS row already recorded above
- [ ] `00 · 02-erlang` / C-02 — **`xs[0]` types `void`.** `val ts: Array<i32> = [1,2]; val x: i32 = ts[0];` reds `expected i32, got void`, for user enums and primitives alike — found while measuring the enum-path fix, which had to write `ts.length()` in its array reproduction to work around it. Decision 63 makes `xs[0]` *be* `xs.at(0)`, so the index expression should have the method's answer and does not. still reds, so C-02's index rewrite did not reach the *typing* of the result
- [ ] `00 · 01-checker` (the tail of the enum-path fix) — the single-segment dot shorthand resolves through the flat bare-name table, so with two enums declaring `Red`, `val c: Cool = .Red;` is a `ReferenceError` on commonJS and, on erlang, binds the other enum's `Red` to a `Cool`-typed name (`{case_clause, …}` later); ES4's `enum_with_head` message picks its wording by map order
- [ ] `00` — compiler shapes rakun front 05 met writing a configuration reader (each in rakun's `AGENTS.md`): `Array.pop` on erlang reads without removing; `try` unwraps only in a `val` binding on erlang (`return try f()` and `g(try f())` double-wrap); a `loop` body is recursion on commonJS (1 000 iterations overflow); an ALL-CAPS module-level `val` becomes an erlang variable; a `var` in a nested block of a loop body is unbound on erlang; a `val` bound in a loop lambda loses its string type; `x.field.length()` is emitted against JS's `length` property; a `//` comment in a braced block swallows the `}` on commonJS; an integer literal never widens to `i64`; an `if` expression cannot sit right of `+`; `from` is a keyword; `@Decl` renders an empty `typeName` for a generic field; a member's `files` order is a dependency order
- [ ] `00 · 01-checker` / the compile driver — an imported fn's default cannot be filled (`import {greet} from "helper"; greet("w")` → `'greet' expects 2 argument(s), got 1` on every target): the cross-module export registry carries no plain `FnDecl`. Also unreached by C-04: a `..` spread call, a pipeline RHS, an interface associated fn, a `"std"`-qualified call, a function value's arity, and a free `fn` declaring only trailing defaults while a record may declare a leading one
- [ ] `00 · 01-checker` — cross-module behavior identity: a `declare fn` returning an imported `Greeter` does not unify with a parameter typed `Greeter` from the same import (`type mismatch: expected behavior Greeter {…}, got Greeter`)
- [ ] `00 · 02-erlang` — worktree `.tasks/erlang-module-load` (`fix/erlang-module-load`): two emitter gaps rakun front 04 measured. A module-level `val` with a side effect never runs (`topValForms` emits a 0-arity function called on each *read*, and `'_botopink_main'/0` is not emitted in test mode at all), so a library whose component model is module-load self-registration registers nothing; and a method on a host-supplied `behavior` receiver lowers to a bare local call (`req.param("name")` → `function param/2 undefined`). Until they close, no track B front can claim its target is testable
- [ ] `00 · 02-erlang` (C-03) — `std-unsupported-on-target` names the module's **first** declaration (`std/erlang.abs`) instead of the function actually called; a wording defect with a cell already in the suite. **Confirmed with a distinguishing call**: `erlang.element(1, "x")` on commonJS reds `std-unsupported-on-target: std/erlang.abs has no ``@external`` for target 'node'`. `abs` is `erlang.bp`'s first `pub declare fn` and `element` is its fifth — the reader is sent to a function they did not call, and the location is the module rather than the call
- [ ] Decision 113 in landed code (jhonstart's half landed: every marker `data-jh-*`, `linkMount` / `formMount` ordinary imports, the router matching through `routing`) — rakun 23's render leaves `ssr.bp` / `ssr.mjs` once jhonstart front 30 lands; rakun 04 closes with the core on `["erlang"]` and `runtime.mjs` deleted; rakun 24's `actionForm` becomes `actionIdOf`. Decision 114 in landed code: rakun 22's page registry drops `LayoutProps` / `rkAppRegisterPage` over `Element` for an opaque `PageRenderer`; rakun 23's `setPageRender` / `RenderedPage` become `page(pattern, render)` over `ChunkWriter`; rakun 24 reads the wire names from `rakun.actions.field` / `rakun.actions.header`; jhonstart 28 drops `rakun_request_context` and `fillRequest` for the `RequestData` onze hands the render
- [ ] Decision 115 in landed code — the matcher at rakun `modules/rakun/src/file_router.bp:49-479` moves to the bundled library `libs/routing` (`01-std/04-routing-lib`) and rakun 22 imports it (its Step 7); jhonstart 26's router imports `parseTable` / `matchPath` from `routing` and takes no `match`; `file_router.bp:795` reads `rakun.appDir` instead of `onze.appDir`; rakun 24's body limit is `rakun.actions.bodyLimit`; jhonstart 31 gains `redirect(url)` and 30 the late-signal markup (`data-jh-g`, `globals.signal`); onze 49 writes the `rakun.*` keys and 53's pages import `notFound` / `redirect` / `cookies` from jhonstart
- [ ] Decision 116 in landed code — rakun front 63's vocabulary and `n` codec move to `routing`'s `navigation` with `nav:` reasons (no `jhonstart:` spelling left); rakun-web's `middleware.bp:60-110` / `filter.bp:545-565` matchers call `routing`'s `pattern` (front 07); rakun 24 and jhonstart 67 import `libs/actions` and the golden `state` literal leaves both test files; `modules/rakun-validation` (landed, 54/0) moves to `libs/validation` and rakun 14 Step 7 deletes the member, rakun's workspace root becoming `["erlang"]`; the JSON copies (`ssr.bp:641-676`, `report.bp:80`, `rakun-web/src/error.bp:131`, `config.bp:394-433`) go to std `json`; rakun's `request_context.bp:507-610` codec and jhonstart's `decodePairs` / `encodePairs` (`router.bp:123-162`) go to std `encoding`, with jhonstart 26's `a b` cell; emilia's `hashHex` (`emilia.bp:80`) goes to std `contentHash`; onze 69 configures rakun-web 82's static server
- [ ] Decision 117 in landed code (jhonstart's half landed: `streaming.bp`'s render writes to `Response` and answers `notFound` / `redirect` with 404 / 307 itself, `globals.signal` is `__bp2`, `redirectAllowed`, the three markers refuse a non-component, `clientApp`; open there: front 26 step 6's shared late-signal handler) — rakun 23's `ChunkWriter` gains `setStatus` / `setHeader` and a page renderer's signal becomes a 500, rakun 63 drops page signals and `boundaryFor`, rakun 24's action `redirect` goes into the envelope's `n`; onze 49 adapts the response with no signal `case` and writes `rakun.actions.bodyLimit` from `OnzeConfig.actionsBodyLimit`; onze 53's pages, layouts and templates are components; `01-std-lib-enablement` gains the JSON writers (front 07 folded in) and `Json` / `json.decode`, which rakun 05's config reader, `actions` and jhonstart's payload reader use; `routing`, `actions` and `validation` carry no sidecar; `rakun-test` is `["erlang"]`

## Open
- [ ] `00` C-04, C-07…C-11, C-13…C-18, C-20 (absorbed by C-26), C-21…C-25 — no worktree, no owner
- [ ] `03-rakun` — 51 fronts, **697 of 1977 boxes** on `front/03-rakun` (329 at the audit). Closed: 07, 23, 63, 72. Landed with a tail: 04 (56/61), 05 (59/61), 06 (44/53), 08 (51/53), 11 (50/63), 13 (27/28), 14 (33/34), 17 (35/37), 22 (33/34), 25 (23/27), 62 (43/45), 65 (40/42), 74 (29/34). In flight: 10, 16. Next unblocked: 18 (session), then 12 (cache) → 24 · 60 · 64; 15 · 19 · 21 · 66 · 80 · 82. Counts (erlang, the only row): `rakun` 350/0, `rakun-web` 183/0, `rakun-app` 72/0, `rakun-actuator-api` 10/0, `rakun-actuator` 38/0, `rakun-data` 77/0, `rakun-client` 70/0, `rakun-logging` 54/0, `rakun-test` 1/0
- [ ] `06-onze` — 9 fronts; **audited 2026-09-26** on `front/06-onze` against the prepared orchestrator workspace (`front/95-onze-orchestrator` `91ea040`, cut locally in the worktree's onze clone — the decision-79 takeover itself is the maintainer's): of 352 boxes none held — every member was a skeleton `root.bp` with one inline test, and the only files a box names that existed were `AGENTS.md`, `README.md` and the CI workflow. Work proceeds in the READMEs' dependency order (49 first). **49** (onze `90d5b87`): 30 of 36 boxes — the core (`config`, `types`, `integration`: `bootSite` registers the `jhonstart-emilia` bridge, installs the bundle hooks and the wire names, `rakunEntries` the five `rakun.*` keys, `responseOver` / `chainFor` / `pageInput`), `onze-test`'s core helpers and `fixtureTree`, `docs.md`; onze 21/21 and onze-test 7/7 on commonJS and erlang, every member green under `botopink-lib-test` from a standalone copy. Open: `Onze.run` and the per-page renderer (rakun 23 step 1 / 04 / 82 do not exist yet), choices 49-a…d, compiler findings F1–F4 in the README. **53** steps 1–2 (onze `fbe105b`, `aadf0a0`): 9 of 41 — the store, the seed posts, the app tree (layouts, `/`, `/blog`, `/blog/[slug]`, `/about`), `/` and `/blog` rendered in process through onze's boot on both rows (10 tests), the whole tree built by `onze build`; the app sits under `src/` (finding F5: `"src": "."` cannot reach a nested module); steps 2–7 wait on front 50's staging and on rakun. **68** (onze `5e68a7e`/`6ba1b25`/`c90199d`): 57 of 63 — `onze-bundler`'s manifest (one parser, both rows), scanner, client graph, refusals, chunking, generated entry (built and run under node in a scratch package), `<Script>`, dev rebuild and the tags as `RenderHooks`, 37 tests on commonJS and erlang; open: `styleRule` (emilia 56), jhonstart's starter table name, and the three boxes that need `onze build` / `Onze.run`. **69** (onze `2c48df7`/`3f42122`): 22 of 24 — CSS modules, the stylesheet and its `Y` record, `pageRenderHooks`, the two static roots (`AssetRoot` until rakun-web 82), the preprocessor hook, the no-`flush()` gate; 12 tests on both rows. **50** (onze `b8130bf`/`a697968`/`f1cfdf8`/`13f896a`): 32 of 45 — resolve, scan, the check and the staged tree (checked by a real `botopink check`), create (the scaffold passes `botopink check`; `examples/scaffold`), info, `onze build` end to end (both halves compiled, the bundle linked by file and booting under node, a stable build id), the dispatch; 23 tests on commonJS; open: `dev`, `start` (rakun's boot), `prerender/`. **71** (onze `0db4091`): 33 of 41 — build id, `.rel` / `sys.config` / `vm.args` / `bin/onze`, a real `systools` run, the Dockerfile, packaging with the secret scan, the ordered shutdown, static export; 9 tests on both rows. **52** (onze `2382a03`): 23 of 25 — self-hosted Google faces over a fetch seam, the adjusted fallback, local fonts, `fontHead`, the metrics sidecars; 7 tests on both rows; open: the metrics generator (52-a), front 70's citation. **51** (onze `55946aa`): 31 of 34 — `Image`, the allowlist matrix, `srcset`, the encoder port under `timeout`, pass-through, the handler outcome; 7 tests on both rows. **70** (onze `a1ed330`): 39 of 43 — the card style subset, sidecar metrics, layout, SVG, the rasterizer port (a real PNG via `rsvg-convert`), the card key; 10 tests on erlang. **All green against compiler `71694839`** (01-checker merged): onze 21, onze-test 7, onze-bundler 39, onze-assets 26, onze-release 9 and the blog 7 on both rows, onze-cli 23 on commonJS, onze-og 10 on erlang; `zig build test-libs` from a standalone copy: 77 passed, 2 failed (the scaffold's missing `files`, fixed in onze `1495b78`), and the two restricted onze cells (`onze-cli`·erlang, `onze-og`·commonJS) need ledger lines in `scripts/restricted-targets.txt`

## Deferred out of this milestone
- See [`deferred.md`](./deferred.md) — GraalVM/AOT/CRaC/JNDI/Servlet/agents, Vercel hosting, the bundler plugin APIs, the CSS features that read user files, the old `onze` mocking runtime; plus the per-track reference-coverage holes listed in each `unification.md`.
