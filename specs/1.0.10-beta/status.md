# Status — 1.0.10-beta

**Updated:** 2026-09-26 · **Progress:** ~22 % (28 of 127 work items landed)

Count: 32 compiler carry-over items (`00`, C-01…C-32) · `01-std` (6 steps + 3 carried std fronts + the bundled `routing`, `actions` and `validation` libraries) ·
`02-packaging` · 51 rakun · 9 jhonstart · 22 emilia · 9 onze fronts. The percentage weighs items
equally; it is a ratio, not a measurement. Open questions for the maintainer: none — 91, 92,
93, 97 and 99 are answered by decisions 102–108, which open fronts `00 · 21-effect-chain`,
`22-loops` and `23-std-purity`, and 94, 100 and 101 by decision 113 (the libraries split by
concern), whose eight open seams decision 114 answers, and 114's five open points decision 115;
decision 116 settles nine more pieces two libraries both run, and decision 117 the nine points
113–116 left (page signals jhonstart's end to end, components, `json.decode`, `.bp`-only bundled
libraries); decisions 118–127 register the maintainer's effect revision (the return type is the
annotation, `@Task<T>`, only `@Result` fails, `@Iterator<T>` / `@Stream<T>`, `async { }`, `iter` /
`stream` loops, no compatibility mode), which opens `00 · 24-effects-by-return`, and 128 merges
`@Use<C, T>` / `@Component<T>` into `@Component<C, T>`. Decisions 68–90, 95–128 are in
[`decisions-taken.md`](./decisions-taken.md).

## Done
- [x] `02-packaging · 95` — the relocation-only cuts and the missing `-test` members, on `front/95-packaging` (worktree `.tasks/95-packaging`, not merged): jhonstart `8cec7e1` (front 27's `link.bp` / `reconcile.bp` and their suites → `modules/jhonstart-link/`; core 120 → 85, member 35, both rows), rakun `2a01ea5` (fronts 22 and 23 — `file_router`, `ssr`, their host halves, four suites, four fixture trees — → `modules/rakun-app/`; core 369 → 310 / 0 commonJS and 367 → 308 / 2 erlang, member 59 / 0 both rows; `examples/rakun-ssr` byte-identical; `rakun-test` gets its inline test), erika `1922dce` (`modules/erika-test/`, 1/1), onze `92a7b3a` (the retirement banner). The onze takeover (decision 79) is **prepared**: the orchestrator's workspace is the orphan branch `front/95-onze-orchestrator` (`91ea040`, seven members, each 1/1 on both rows on a copy of the tree), and front 95 step 2 lists the maintainer's commands and the two ledger lines. `zig build test-libs` over the branches: 66 passed, 0 failed, 19 restricted (pinned), exit 0 (58 before). Five choices to confirm: `decisions-pending.md` 95-a…e. **Ownership** lines of rakun 22–25, 60, 61, 63, 66 and jhonstart 27 and their `fronts.md` rows name the member paths
- [x] `00 · 16-formatter` — the open boxes re-verified (all 35 ticked with evidence) and C-12 / G7 / C-13 implemented: compiler `0f0be511` (step 1 re-measured → G8, a comment before an enum/section `}` deleted, and D2, the handler-less `val assert` printed with a `catch` that broke 15 rakun packages; C-12's comment column; G7), `7146d402` (C-12: binary runs, brace-less `if`, argument lists, array/tuple/behavior literals measure width, enclosing first — `decisions-pending.md` 16-a/b/c), `44ec5e3a` (the bundled libraries reformatted), `a688bfb5` / `6c33c6f4` / `7af79f44` (C-13: the `;` after a braced `if`/loop/`case` optional, not printed, and migrated out of the compiler's trees — `libs/std`, `examples/`, the bundled libraries, `docs.md`; re-run `16-formatter/c13-migrate.py` after 23's tree move). On `front/16-formatter`, not pushed, not merged. `zig build test` 2 512/2 512, `test-libs` 58, `test-docs` 68/0, `tests/language` 799/28/0. Left: refusing the `;` after 12 (275 sites) and 09 (rakun 454, jhonstart 40, erika 28, onze 1) migrate (16-d); 09's reformat at C-12's rules
- [x] `01-std/04-routing-lib` · `05-actions-lib` · `06-validation-lib` — the three bundled libraries, on `front/01-std-libs` (worktree `.tasks/01-std-libs`, not merged): compiler `f2f5819a` (routing + the registry), `ef3156b0` (actions), `edac72bf` (validation); rakun `1636a99` (the consumer switch). `libs/routing` (segment, table, match — the file-router matcher moved with its 26 tests — route_kinds, slot_states, url_rules, navigation with `nav:`, pattern) 66 / 0, `libs/actions` (state, envelope, rpc, refresh; JSON through `json.decode`) 19 / 0, `libs/validation` (rakun-validation's seven modules; `MessageSource` injected; host tables as inline templates) 54 / 0 — each on erlang and commonJS, pure `.bp`, in `TREES`. **Bundled**: `build.zig`'s `bundled_packages` (`std`, `routing`, `actions`, `validation`) generates `comptime.bundled_packages`; the CLI and the LSP load an imported bundled package's embedded modules as `<pkg>/<stem>`, so the codegens are untouched (`routing@match`, `./routing/match.js`) and compiler-core names no package but std; a bundled name in `dependencies` is refused. rakun: `file_router.bp` imports the matcher, rakun-web's `:param` grammars are `pattern`'s, `modules/rakun-validation` is deleted (`boot.bp` → `config_check.bp`, `Rakun.run` installs rakun's message source; `config.bp`'s placement-only `#[validated]` removed — the decorator registry is keyed by name). rakun 388 → 369 / 0 commonJS, 386 → 367 / 2 erlang (26 moved, 7 arrived); rakun-web 104 / 0; test-libs 58 / 0; `zig build test` green; language tests 799 / 28 expected / 0. Five implementation choices recorded as `decisions-pending.md` 01std-a…e. Open: the cold-cache gate boxes, the compiler-suite bundled test (01std-a), one rakun-web literal (01std-c), and the consumer fronts that are not written (rakun 24/60/61/63/65, jhonstart 26/27/30/31/67, onze 68)
- [x] `01-std-lib-enablement` step 13 (and step 12's last box) — `json.Json` and `json.decode`, compiler `ec1fbf38`: a botopink RFC 8259 reader, member order kept, duplicates / trailing text / non-RFC numbers / overflow / unpaired surrogates / raw control characters refused, every error at a UTF-8 byte offset; 13 tests, std 417 / 0 on both rows. Measured on the way and fixed there: behavior methods (`String.chars`, `Bool.negate`) are prototype patches on commonJS that a consumer of std does not install — `decode` failed from a consumer (`s.chars is not a function`) until it used its own cells. Open in the front: Step 10's `io:` import and tree line (wait on `23-std-purity`'s move) and Step 14's grep (the consumer fronts')
- [x] `01-std-lib-enablement` step 11 — `json.quote`/`unquote`/`array`/`object`: compiler `52448056`, 7 tests green on both rows; the erlang `quote` walks bytes (OTP's `json:encode` writes uppercase `\u001F`), and non-ASCII test text is built by a private cell because the erlang backend truncates `\u{…}` above U+007F (handed to `02-erlang`)
- [x] `00 · 03-beam` — C-06's and C-07's beam halves, and the beam rows no step named: compiler `75dc950b` (`A...B` is two `is_ge`), `60a236c5` (tuple / `..` / labels / literal payloads / primitive type names / `true`·`false` tested element by element), `129093a5` (a variant's labelled argument by its declared slot), `8a329437` (a lambda's `if`/`case`/`try` tail is its value), `4ccae51d` (synth helpers per module — `UnknownFunction`), `1380a66e` (a read or call the emit cannot place asks the value — `modules/{field,method,type}_name_collision`, 05-wasm step 9's beam row), `833b9fd4` (the module body once, before `main`), `57014c3d` (`is` / `==` by value per §4.1 / §2.3); merged with `feat` at `809a4955`. `--target beam`: 118 passed / 9 expected / 1 failed (was 79 / 19 / 6) — the one is `run/effect_method.bp`, a `while` that `yield`s in a method (22-loops); four beam lines deleted; 39 beam snapshots re-record `.S` only and one RUN LOG moves (`rows[0].length` → `2`, run by hand); `beam_export_audit` 464/464. BR5 stays out (decision 62)
- [x] `00 · 05-wasm` step 9, first row — self-recursion in tail position: compiler `1914ea21` on `front/05-wasm` (not pushed, not merged). `return f(args)` inside `fn f` evaluates every argument, re-binds the parameters in reverse and branches to a `(loop $__tail …)` the body is wrapped in only when such a call exists, so no other function's WAT moved and no snapshot was re-recorded. `count(1000000, 0)` answers on wasm where it trapped `call stack exhausted` at 100 000; `run/tail_self_call.bp` is green on commonJS, erlang, wasm and beam. The `?T` carrier half of the old `fix/wasm-optional` line landed with that branch; its tail-call half is this.
- [x] `00 · gate-perf` step 1 — `botopink-lib-test` runs cells on a bounded worker pool (one per CPU, bounded by `MemAvailable / 768 MiB`, each cell admitted only while `procs_running` ≤ CPUs) and emits them in discovery order from the captured output: compiler `c16691d7` (merged over `feat` `0beaa1f9` as `aaf09e2b`, on `front/gate-perf`, not pushed). Equivalence: fixture libraries with a red test, a type error, a test-less compile error, a dead erlang sidecar and a green one print byte-identical JSONL, text and `test-libs.sh` verdicts (same exit 1 / 0) against the pre-change binaries and against `--jobs 1`; the full library matrix's JSONL is identical to `feat`'s modulo `duration_ms`. Warm gate, `zig build test-libs`: 1038 s → 230 s (whole gate 1151 s → 337 s), other gates running alongside in both; CPU-seconds unchanged — the win is wall clock. Next: the erlang cells' own cost (`.erl` compiled once per run, not per test module; the sidecar shipper's per-qualifier root rescan) and `test-language`'s pool
- [x] `00 · 17-beam-memory` steps 4–5 (C-10) — compiler `5fe15171` (erlang) · `bbcdff8e` (beam) on `front/17-beam-memory-2` (not pushed, not merged), gate green on both. A module `var` lives where `#[@BeamMemory]` says on both BEAM targets: the reader and every write lower onto `std/beam`'s ten primitives as calls into `std@beam` (decision 43's layer 2 — no `.zig` names ETS), which a BEAM build of a module with a `var` now pulls in; storage is `'<module atom>@@<var>'`. `ProcessDict` is per process (five spawned requests read `3` each, `main` reads `0`); `Ets` reads `15` after five processes × three increments through decision 39's registered owner (it wins `etsNew(named_table)`, seeds, registers, parks in `timer:sleep(infinity)`, so registered means seeded); `PersistentTerm` is put from `-on_load` (a new `erl_ast.Form.on_load`; `{attributes, [{on_load, …}]}` on beam) — under `botopink test` the escript runner calls the hook itself. The checker refuses on every target a `PersistentTerm` write, an `Ets` write that recomputes the value (a `Dict` exempt, decision 42), an `Ets` increment of a non-integer (decision 40) and a non-literal `Ets` seed; `keyed = true` has no row-per-key lowering and is refused on erlang/beam. Beam needed decision 64's wrapper: wired for the plain `module:symbol` form (`run/std_erlang_node` passes on beam; C-03's template bullet stays open). Cells: three `run/beam_memory_*` (`erlang beam`), four `reject/`; `run/module_var` passes on erlang and beam. Re-recorded, each read: `erlang/src_in_a_test` (the sibling loader runs load hooks) and six `beam/external_*` (the new wrapper function, labels shifted, no RUN LOG moved). **Finding:** no `Dict` satisfies the `Ets` seed rule (no `Dict` literal; `comptime dict.empty()` does not fold), so rakun's `Ets` registries cannot be declared yet; `docs-text.md` part 2 is now publishable by 08
- [x] `02-packaging · 95` — the `-test` members and the DSL cut: jhonstart `4c02054` (`modules/jhonstart-html/` holds `html.bp` + `html_test.bp` + `elements_test.bp`, `modules/jhonstart-test/` empty with one test; `examples/jhonstart-html` → `examples/jhonstart-markup`, the DSL member took the name; core 120/120, `-html` 5/5, `-test` 1/1, `-markup` 7/7 on both rows) and emilia `f6e740e` (`modules/emilia-test/`, 1/1 both rows), both on `front/95-package-restructure`, not merged. The `06-onze` **Owns** lines of 49–52 and 68 read the member paths. Not done, with the reason in the front's README: the std/asserts step (landed by `01-std` under `asserts-api.md`'s names), the onze takeover (decision 79, waits on the maintainer's tag-and-archive), `_archived/onze-mock` (refused by 79), and rakun's member changes (fronts 04 and 14, decisions 113/116)
- [x] `00 · 11-tooling` closed (C-19's tail): compiler `e4baa820` on `front/11-tooling` (not pushed, not merged). Steps 1–4 and the C-19 builders were already at HEAD (`f952bfc6`); what was owed landed — the `AGENTS.md` lines of `src/comptime/`, `language-server/src/` and `language-server/src/tests/`, five `front 11 carve-out` LSP tests that read the 1.0.3 surface for a `type`, an `enum` and a `behavior` through completion, inlay hints and signature help, and `engine.recordCtorSignature` — signature help over `Point(` was **null** (a constructor's binding is `named`, not `.func`) and now lists the declaration's fields; and `buildInterfaceDeclName`, which dropped every behavior method's return type and generics, prints them. Extension gate re-run against this compiler from a scratch copy (`npm test` 43, `compiler-check` passes); submodule left at `dd46d1f`, not initialised. 07 step-3 row and 10 step-4 row struck.
- [x] `00 · 19` step 3 (C-27) — destructuring from a `use`: compiler `f58fd392`, merged into `feat`. `val #(shown, push) = use optimistic(12, …)` binds each name to the element of the hook's `R` at its position — `push : fn(action: i32) -> i32`, so `push(shown)` types with nothing annotated — an `R` a generic hook left unresolved is committed to a tuple of the pattern's arity, and a pattern of another arity, or a hook whose `R` is no tuple, is `use-tuple-arity` at the binding (decision 67). One snapshot pair re-recorded on the four targets, because its old `state -> i32` shape is exactly what the rule now refuses; row 5 (a `use` inside a nested closure) has its cell; `reject/use_tuple_{arity,of_non_tuple}.bp`. The 67 example is not compiled — it imports front 67's `optimistic`, which jhonstart lacks, and is spelled for 21.
- [x] `00 · 14-comptime-on-beam` step 3 + `18-comptime-runtimes` step 1b (C-20, C-26's last step) — **a comptime body reaches the node as BEAM bytes; nothing compiles Erlang**: compiler `8af76f41` (the lowering, the listing, the load), `ca2fedea` (its speed), `e3890252` (`origin/feat` merged in), `248d0896` (`persistent_erl.zig` → `persistent_beam.zig`, cmds 1/2 and the `.erl` staging deleted) on `front/14-comptime-on-beam` (not pushed, not merged). The generated Erlang is read back and lowered to `beam_file` instructions (`comptime/runtime/beam/`, not a mode of `beam_asm.zig`: one decision of what a body means, `erlang.zig`'s), assembled in Zig, loaded by cmd 4, listed as `.S` (`codegen/beam/asm_text.zig`). erika-linq's module: `compile:file` 49.1 + load 1.4 ms → 2.9 ms lower-and-assemble (ReleaseSafe) + 0.6 ms load; build `--target erlang` 273 → 161 ms ReleaseSafe. 33 `COMPTIME ERLANG` → `COMPTIME BEAM ASSEMBLY` (a rename; every `COMPTIME REPLY` byte-identical). 0 refusals over the suite (17 template + 19 decorator bodies), `test-libs` (58 cells pass; 2 + 61 bodies) and `tests/language` (799/28/0 all, 214/7/0 beam); beam export audit 475/475 + the comptime tree's 5/5, runtime-parity 1 415 / 0, `test-web` green. C-01 was not needed (the comptime atom is decision 109's `bp@comptime__…`). Steps 0–2 hold as before (`bef762b`).
- [x] `01-std` step 6 · `02-std-async-primitives` — `src/async.bp` on `std/02-async-primitives` (`fe5a35ad`, merged over `feat` `4fe1747e`, gate green on every stage, **not yet in `feat`**): two surfaces over `@Future<T>`. The task surface (`allOf` / `settleOf` / `raceOf` / `timeout`) takes UNSTARTED `fn() -> @Future<T>` thunks and is measurably concurrent on both rows — three 60 ms tasks under `allOf` finish in under 120 ms on erlang (`spawn` per task, replies tagged by a per-call `make_ref()` and gathered by index) as on commonJS (`Promise.allSettled` over the called thunks). The future surface (`all` / `allSettled` / `race`) is `Promise.all` parity for the client half and is honest on erlang rather than concurrent — `race` answers element zero there, and the test asserts BOTH behaviours so a lazier erlang `@Future` reds a cell instead of silently dating the docblock. Two things the spec did not ask for and decision 67 did: `allOf` / `all` settle everything and name **every** failed task (`async.allOf: 3 of 4 tasks failed: [0] one; [2] three; [3] four`), not the first as `Promise.all` does; and `errorText` reads the `Error` side of a settled element, which `@Result` has no builtin for. `timeout` is `raceOf` over the task and a `delay`-then-`failed`, no third host cell, and it keeps a task's own failure message apart from `"timeout"`. 25 inline tests, `25 passed, 0 failed` on commonJS and erlang; a scratch consumer package's `import {async} from "std"` resolves on both. One deviation, measured before step 1 and written into the README: `root.bp` gets `pub mod async;` in this front's commit, because a module `root.bp` does not name is not embedded and its tests never run — front 01 inherits the line. One parser defect found on the way and banked in `libs/std/AGENTS.md` rather than fixed here: `botopink format` closes a wrapped `declare fn` parameter list with a trailing comma, and a trailing comma after a parameter whose type ends in a fn type is a parse error, so the file carries a do-not-format banner
- [x] `00 · 09-ecosystem-residuals` — erika, the one tree the front keeps in this milestone: erika `5400245` on `front/09-ecosystem-residuals` (not pushed, not merged), against compiler `f58fd392`, which it does not touch. **The reformat after C-12's method-chain rule**: `modules/erika/src/erika.bp` 57+/32−, ten chain hunks, token-identical, idempotent, the cell 31/31 on both targets before and after, and `examples/erika-linq`'s emitted output `diff -r` byte-identical on commonJS and erlang — the four measurements the first pass set as the bar, met again. **One file left red on purpose**: the formatter moves a trailing comment on an array-literal element (`main.bp:111-113`) to the line below it, where it is false of the next element; the 1.0.4-beta row this README said did not reproduce at `c2dd780` does reproduce at `f58fd392` in that one shape, and it is registered in 16-formatter's handover table rather than dodged by rewriting the comments. Step 3's §5.1 for erika is **nothing** (zero `case` statements — the "all five" was emilia 31 / onze 6 / jhonstart 1 / erika 0). Step 4 **measured at the atom shape C-01 halves 2–3 left**: `erika@erika__t__query.erl` and siblings, 31/0 + 9/0 on erlang, `run --target erlang` printing the commonJS run's six lines; decision 109's `@@` is not in `erlang.zig` yet, so the re-run after it stays open. Step 5's erika `AGENTS.md` re-derived by running: three "parser quirks" (`if (a && b)`, `(expr).method()`, a `//` in a closure body) **all parse and run** at `f58fd392` — which also makes C-08's "`if (a && b)` does not parse in condition position" false at this compiler — and the dead `tasks/v0.beta.7` spec link replaced. Two things worth keeping: `zig build test-libs` **refuses from a `.tasks/*` worktree** (the runner walks up to the main checkout's `repository/` and finds the library twice — decision 67, not a red; the same cell runs with `botopink-lib-test --lib-root <worktree>/repository` from outside the checkout), and the README's `libs.zig:564` / `test_cmd.zig:189` / `build.zig:215` line refs had drifted to `:803` / `:253` / `:290`. Steps 2 and 3's other rows, and the other four `AGENTS.md`, are tracks B–E's under `fronts.md`
- [x] `00 · 01` step 8 R2 (C-09) — importing a type brings its type closure: compiler `29b5a725` on `front/01-checker`, merged into `feat`. `import { User, makeUser } from "users"` with `User(role: Role)` prints `admin` on commonJS and erlang without naming `Role`; the closure is registered as types only, so `Role(name: …)` still needs `Role` in the clause. New cell `tests/language/modules/import_type_closure`. No snapshot moved.
- [x] `00 · 01` step 8 R6 (C-09) — a method on an associated fn's result: compiler `d9302cbc` on `front/01-checker`, merged into `feat`. `Array.range(0, 3).map({ x -> x + 2 })` types by `array<i32>` and prints `[2, 3, 4]` on erlang with no `'__bp_prim_map'` (the emitted call is `lists:map(…, array_range(0, 3))`) and on commonJS. No snapshot moved.
- [x] `00 · 01` step 8 R9 (C-09) — the three N25 diagnostics: compiler `912467b8` on `front/01-checker` (merged into `feat`; `origin/feat` with front 22 merged in at `24f81c24`). `reject/{wrapper_without_annotation,val_assert_after_catch,two_effect_markers}.bp` are each refused for their own reason with the caret on the return type, the `catch` and the second annotation, and leave `expected-failures.txt`. One snapshot re-recorded and read: `comptime/errors/a_result_return_without_result` (new message; caret from `@todo()` to the return type).
- [x] `00 · 01` step 8 R8 (C-09) — a type position takes a type: compiler `3eab4dd6` on `front/01-checker`, merged into `feat`. `val n = 5; val x: n = 7;` is `'n' is a value, not a type`, located at the binding; `val T = i32;` and `val U = T;` still name types; imports, std's `Array` and behaviors keep resolving — the first cut refused those too and redded std and rakun in `test-libs`, which is how the accepted kinds were measured. No snapshot moved.
- [x] `00 · 01` step 8 R4 (C-09) — a behavior-typed field accepts its implementer: compiler `6260ab75` on `front/01-checker`, merged into `feat`. `Holder(h: H(id: 1))` and `use1(H(id: 2))` with `H implement Handler` check and run on commonJS and erlang, through `extends` as well; a record that does not implement the behavior still reds at the value. No snapshot moved.
- [x] `00 · 01` handover 15 (C-09) — a call whose callee is an expression: compiler `3aa693bc` on `front/01-checker` (merged into `feat`; `origin/feat` merged in at `66af80de`). `adder(3)(4)` types by the returned fn (arity and return checked); `.Circle(radius: 1)` — front 15's row 1 — is the expected enum's constructor, spliced in as `Shape.Circle(…)` through the index-rewrite channel so no backend changed, and a located refusal when nothing says which type (no more `unbound variable ''`). `test/curried_call.bp` now compiles; its lines moved to C-09's backend half, and its third test, which the dropped callee satisfied by accident, asserts `pick(false)(5)`. Decision 112 (DSL hygiene) is written in as 01 step 14 and its three cells as 12 step 4 item 5.
- [x] `00 · 01` step 8 R5 (C-09) — a pattern in binding position: compiler `f2a54c1` on `front/01-checker`, merged into `feat`. `val Circle(r) = s;` binds `r` typed through the `case` arm walk, as a `val`. The failure behaviour is decided by decision 67: the bare form checks only where the pattern cannot fail (a one-variant `type`, a record's constructor, a spread-only list) and every refutable one is `refutable-val-pattern` at the binding, naming `val assert` and `case` — so 04's JS-4 lowering is a plain destructure with no test (recorded in `04-js/pattern-binding.md`). No snapshot moved.
- [x] `00 · 01` step 10 close-out (C-08) — decision 11: compiler `26cf90c0` on `front/01-checker`, merged into `feat`. The three `DOCUMENTED SKIP`s that pinned the parse error of `<Pattern> as <name>` are deleted with their three `comptime/ast/` snapshots; the form is not part of the language, the row is recorded in `01-checker/residual-rows.md`, and step 10's four acceptance boxes are ticked (the `if (a && b)`, `_` binder, `assert e is P;` and decision 12 halves landed earlier on this front).
- [x] `00 · 12-language-tests` — C-16 verified, and six beam cells nobody ran: compiler `cf61cb82` on `front/12-language-tests`, merged into `feat`. Steps 4.2–4.4 **exist and are green** on all four targets (`modules/local_dependency`, `run/panic_aborts` + `run/todo_aborts`, `run/external_erlang_only`), the tally is the runner's, decisions 63–66 have their cell or sentence — the carry-over row's "unverified" is answered by running: `run.sh` 553 / 42 / 0 at `f58fd392`. `run.sh --target beam` was **79 / 19 / 6 failed**, all six unlisted and every one a cell another front added and measured on the three targets of `--target all` only — the four `modules/*_name_collision` cells (`ead0b645`'s own message named two of them), `run/labelled_arguments.bp` (`205` for `502`) and `run/effect_method.bp` (`ConditionLoopValueUnsupported`, unlocated). Listed against `03 step 3 (D6)`, `03 step 4` and four `03 (no step; …)` cells with the assembled answer quoted, handed to `03-beam` as a table; beam is 79 / 25 / 0 after. `AGENTS.md` recounted at `f58fd392` (165 cells, +31 since C-04 named one by one); a module-level `var` parses and checks now (17-beam-memory's, no cell). Not done: the 1.0.5 owner spellings are not repointed (delete-only file, twenty fronts in flight; the carried copies keep the step numbers, and `AGENTS.md` says so); `reject/loop_while.bp` contradicts decision 105 and is 22's to rewrite.
- [x] `00 · 19` step 3 (C-27) — destructuring from a `use`: compiler `f58fd392` on `front/19-use-activation` (not pushed, not merged). `val #(shown, push) = use optimistic(12, …)` binds each name to the element of the hook's `R` at its position — `push : fn(action: i32) -> i32`, so `push(shown)` types with nothing annotated — an `R` a generic hook left unresolved is committed to a tuple of the pattern's arity, and a pattern of another arity, or a hook whose `R` is no tuple, is `use-tuple-arity` at the binding (decision 67). One snapshot pair re-recorded on the four targets, because its old `state -> i32` shape is exactly what the rule now refuses; row 5 (a `use` inside a nested closure) has its cell; `reject/use_tuple_{arity,of_non_tuple}.bp`. The 67 example is not compiled — it imports front 67's `optimistic`, which jhonstart lacks, and is spelled for 21.
- [x] `00 · 15-language-surface` closed — the cells of steps 3 and 4b, written by the front itself (12 had closed): compiler `0b6fc392` on `front/15-language-surface`, on `feat` `82e32e36`. Eight `reject/` cells, one per decided-against form, each pinning its kind and location, and `run/decorator_negative_argument` + `run/loop_one_line_body` passing on all four targets — the second spelled `for (xs) { x -> … }` since decision 105 removed `loop (…)`. `run.sh` 658 / 29 / 0; `--target beam` 146 / 7 / 1, the one `run/effect_method.bp` (unlisted and red before, not this front's); `zig build test` green. No `expected-failures.txt` line. C-13 (`decision-29-parser-half.patch`) stays with 16-formatter. Not pushed, not merged
- [x] `00 · 15-language-surface` steps 4b and 5 — compiler `727813e5` on `front/15-language-surface`, pre-commit gate and `scripts/gate.sh --cold` green, **no snapshot re-recorded** (two added). `#[mark(-20)]` is one annotation argument and the decorator receives the number (commonJS and erlang); a one-line trailing-lambda or `loop (…) { x -> … }` body needs no `;` (`requiredExceptLast`, like a fn body). Step 5 handed over: eight `reject/` cells and two `run/` cells specified for 12 with measured `.expect` lines; the `docs.md` rows for 08 under D2; three checker rows for 01; `surface-gaps.md` re-measured with a *Now* column. Left to 12: writing the cells. Not pushed, not merged
- [x] `00 · 15-language-surface` step 3 — a decided-against form is refused by name: compiler `8d6fa0e7` on `front/15-language-surface`, **no snapshot re-recorded**, four parser snapshots added. Eight kinds (`ternary-absent`, `bitwise-operator-absent`, `char-literal-absent`, `nested-fn-decl`, `list-spread-not-last` — existed, never raised — `list-spread-dot-dot-dot`, `implement-clause-for`, `tuple-literal-label`), each raised once at the site every spelling reaches; `&`, `^` and `'a'` lex as tokens now instead of stopping the lexer with an unlocatable "unexpected character". `??` parses (decision 28), so the README's `nullishCoalescingAbsent` row was wrong and is corrected. Not pushed, not merged
- [x] `00 · 17-beam-memory` step 0 (C-05) — re-measured at the compiler's `4fe1747e`, nothing edited: the Problem program is refused at `check` **and** at `build` on every target (exit 1, nothing emitted); the `var` form prints `2` on node and wasmtime, does not compile on erlang (`Hits` unbound) and prints **`0` at exit 0 on beam** (the write is dropped — `%% assign to unknown variable` in the `.S`); questions 38–43 are taken, not opened; the migration count step 1's commit message did not carry is 447 assignments to a bare name over 212 `.bp` files, 447 to a `var`, 0 to a `val`; `std/beam` answers all ten primitives under `erl` on erlang and is `undef` on beam (C-03's beam half); rakun's five registry ranges hold at `10c63974` and now have an 850-line erlang twin (`rakun_runtime.erl`, a supervised table owner — decision 39 built by hand)
- [x] `00 · 17-beam-memory` steps 1–3 (C-05) — the carrier, the rule and the validation were already at `4fe1747e` (`8146d2b6`); what this pass added is the step-1 follow-up `37342d95`: an annotated `val` shorthand is refused **at the annotation** (it was located at the next token) and the `expectError` cases the acceptance named are in `parser/tests/surface.zig`; the README's boxes are ticked against what was run — two parser snapshot re-records (`labels` array), no codegen re-record, C-05's boxes ticked likewise. Gate green cold
- [x] `00 · 17-beam-memory` step 6 (docs text, to 08) — [`docs-text.md`](./00-compiler-carry-over/17-beam-memory/docs-text.md): part 1 is true at `4fe1747e` (the `val` rule, the module `var`, the validated `@BeamMemory` with its five refusals verbatim) and can go into `docs.md` now; part 2 is the three mode paragraphs — `Ets` is cache and counting memory, `keyed = false` stores a `Dict` whole and lost 6 of 40 000 concurrent writes, `PersistentTerm` is re-seeded on every reload — and waits for C-10, because a paragraph saying `hits += 1` is `ets:update_counter` while erlang emits an unbound `Hits` is the failure decision 41 was taken against. The warning box is struck: decision 42 (b), no warning by decision, not by the missing channel
- [x] `00 · 17-beam-memory` step 7 (the cells, C-05) — compiler `3cd77667`: nine `tests/language` cells written into the suite (12 was absorbed into C-16, so nobody else's file moves) and every one run first — `run/module_var` (`2` on commonJS and wasm; erlang `Hits` unbound and beam printing `0` at exit 0 are `expected-failures.txt` rows against C-10 quoting the emission), `test/beam_memory_noop` (the annotation is a no-op off the BEAM; each test writes and reads back), seven `reject/` cells for the diagnostics of steps 1 and 3. Gate green: 561 passed / 44 expected / 0 failed in the language suite. The per-mode BEAM cells and step 4's three refusals are specified in the README for C-10, named file by file
- [x] `00 · 17-beam-memory` step 8 (the payoff, specified for 09 / 03-rakun under decision 17) — [`rakun-migration.md`](./00-compiler-carry-over/17-beam-memory/rakun-migration.md), re-derived at rakun `10c63974` and emilia `9c19e22`: `runtime.mjs` still sheds 96 of 231 lines and `runtime.bp` 13 of 16 declarations (`rkDispatch`/`rkDispatchHttp`/`rkServe` stay), the mode per registry (`PersistentTerm` for `scanned`, `props`, `routes`; `Ets(keyed = true)` for `builds` and `singletons`), and the 850-line `rakun_runtime.erl` twin that did not exist when the README was measured sheds its five registry sections (`:171-297`) and the `rakun_registry` owner it built by hand. One caveat only `singletons` has: its erlang half is `ets:insert_new` and the design's `Dict` write is an overwriting `ets:insert` — three answers written, the third (an `insertNew` form lowered to `ets:insert_new`, decision 40's shape) recommended. emilia's cell moved to `emilia.bp:53-60` and is still `ProcessDict`'s validation case, blocked on the ordered dict, not on `erase`. Nothing here opens before C-10
- [x] `01-std` front 03 — content hash: compiler `d4dc2c93`…`7a49f6c8`. `libs/std/src/content_hash.bp` (flat, `pub mod content_hash;` in `root.bp`; front 23 folds it into `hash.bp`) carries `contentHash` (the `emilia.hashHex` djb2 templates verbatim) and `strongHash` (SHA-256 truncated to 32 hex), the framed `cacheKey`/`strongCacheKey`, the quoted `etag`/`weakEtag` and the 304 `matches`, and `fingerprint`, which keeps the extension last. 19 inline tests green on commonJS and erlang with every expected hex a literal; the front's example runs 6/6 from a consumer package on both. `fingerprint` measures the extension as the last `.`-piece rather than with `lastIndexOf`, which answers a byte offset on Erlang where `slice` counts characters
- [x] `00 · 23-std-purity` (C-31) steps 1–5 — the import tree, the embedded std following `mod`, the pure root: compiler `f9794f03` · `c2b911cb` · `0d831e7a`. `import {a: {b: {c}}, x.y.z as z2, e.t.r*}` flattens to one `ImportPath` per leaf and only the leaf is bound; `as` is honoured on all four backends (the `language-gaps.md` alias row closes); `import-name-collision`, `import-alias-on-type`, `import-alias-on-activation` and `import-group-modifier` are located refusals. `stdPkgFilesFromRoot` follows `mod X;` into `X.bp` or `X/mod.bp` (a throwaway `probe/` ran on four targets; the flat table is byte-identical). A module at `std/<name>` importing anything under `io` is `std-root-imports-io`. **Steps 3 and 5** (on `front/23-std-purity`, not merged): compiler `6a9d2f6e` (the tree) · `c4611dd9` (ledger back to its header) · `899a80bb` (headers, `clock.` test names); rakun `94d0d38`, jhonstart `29a6d70`, emilia `4cac151`, erika `6c20885` (onze imports nothing from std). Decision 106's tree: a pure root of fifteen modules (`collections` = `Dict`/`Set`/`Queue`/`Order` with decision 111's type-scoped constructors, `hash` = crypto's digests + hash + content_hash, `encoding` + base64), `io/` (fs, http, net, clock (was time), random (+ randomBytes), os, env, process), `testing/` (asserts, snapshots, mocks); `root.bp` seventeen lines, `io/mod.bp` eight, `testing/mod.bp` three; std stays out of the bundled registry (01std-a). std 417 / 0 on both rows as before; every library cell at its pre-move count (`test-libs` 58 passed / 0 failed, 19 restricted at their pins); `scripts/gate.sh --cold` green at `6a9d2f6e` (rakun in the ledger), `c4611dd9` and `899a80bb`; the same cold gate at `feat` `ffe2db69` ticks 04/05/06's cold-cache boxes. Two CLI defects the nested tree exposed, fixed (decisions-pending 23-c): `shipMjsSidecars` read `io/random` as dependency `io`, and `test_cmd` wrote type units where a folder module's runner never looked (`{error,undef}`). Open: step 6 — decision 110 (type alias, folder namespace) and `collections.Dict.empty()` through the namespace (23-a)
- [x] `00 · 22-loops` steps 2–4 — decision 105 on four backends: compiler `ac5f5703` (commonJS `75b2311c`, erlang `4f83aa6b`, beam `e7e2f36d`, wasm `d6c335be`); every loop a statement, `#[@generator] loop` a generator on all four (`dobros` prints `2 4 … 18 20`), `a...b` inclusive, the decision-52/55 lowerings deleted, no front-22 line in `expected-failures.txt`. Library sweep on `front/22-loops` (rakun `1236eaa`, jhonstart `4a8b41f`, erika `0f099f6`) merged into each library's `feat` (rakun `a8ba8bd`, with one `loop (` feat added after the sweep; jhonstart `8e8dbe2`; erika `8f53989`) and the compiler merged into `feat` as `52843fd5`, which deletes the 29 `22-loops/4` known reds and returns the 17 restricted pins to `0` — `test-libs` 48 passed, 0 known red.
- [x] `00-compiler-carry-over/21-effect-chain` step 5 — one context wrapper `@Component<C, T>` (decision 128): compiler `528753d6`, jhonstart `9339f3f`; merged into `feat` without the gate (the maintainer's call) — the gate is owed at the next compiler commit
- [x] `00-compiler-carry-over/21-effect-chain` step 3 — `getContex` → `getContext` (decision 108): compiler `366151ce`, the three snapshots moved by `git mv` (caret one column right); merged compiler feat `0e20844f` as `3bee550e`
- [x] `00-compiler-carry-over/21-effect-chain` step 4 (library half) — jhonstart `0c32c7b` on `front/21-effect-chain` (atop jhonstart feat `8e8dbe2`): `implement @Context<ElementBase>`, hooks `#[@use] … -> @Use<ElementBase, T>`, components `#[@use] … -> @Component<Element>`, `request()` a hook, `renderComponent`; counts unchanged (jhonstart 125/0 both rows, counter 4/0, todo 3/0, html 7/0; emilia-card 4/0). The compiler commit deleting the seven `known-red-libs.txt` lines and restoring the two `restricted-targets.txt` pins waits until the shared checkout's jhonstart carries `0c32c7b` — the gate reads libraries from the main checkout
- [x] `00-compiler-carry-over/21-effect-chain` step 2 — `#[@use]` answers `@Use<C, T>` / `@Component<T>`, `@Context<Base>` is the owner marker (decisions 102, 104): compiler `12d4dbb0`. Decisions 89/90 code deleted; one flag (`#[@use]`) gates `use` and `@getContex`; commonJS emits every `#[@use]` body as `async function` and `use f()` as `await f()`; a `use` in a nested closure is refused. jhonstart (and the examples compiling against it) sit in `known-red-libs.txt` for the step-4 window
- [x] `00-compiler-carry-over/21-effect-chain` step 1 — the three generators (decision 103): compiler `62f3cb62` (`#[@resultGenerator]` / `@ResultGenerator<T, E>`, `YieldStep<T, E = void>`, no `C`), `2bb1eb67` (`@Generator<T>`, no return or error channel, `try`/`throw` refused naming `@ResultGenerator`), `1b4ebf39` (merge of 22-loops: generator-scope `break v` is 22's lowering), `44755bfd` (`@FutureGenerator<T, E>`). erlang and wasm pin `run/generator_break_value.bp` / `run/generator_levels.bp` against the eager generator scope (C-30)
- [x] `05-emilia` front 45 — transforms: emilia `9c19e22`, 528 → **569** on both rows, fifteen example members. (Row above for the detail; landed in the first gate-free window of the afternoon, together with rakun 74.)
- [x] `03-rakun` front 74 — TLS and SSL bundles: rakun `317e0fe`, core 346 → **387** and `rakun-web` 83 → **104**, both on both rows; the two erlang reds still front 04's, and **no ledger count moves**. Every failure mode is a refusal that names something — half a keystore, a missing file naming the absolute path searched, a JKS naming the exact `keytool -importkeystore -deststoretype PKCS12` command **with no property that lifts it**, a key that does not belong to its chain checked at *startup* rather than at first handshake, and a listener naming an unconfigured bundle, which refuses rather than falling back to plaintext. Its **sixth plant redded nothing, and that is the finding**: it proved `sslReload`'s explicit save-and-restore was dead code, because nothing is written until everything is read and checked — the branch is deleted and the property is documented as ordering, with the plant recorded in `AGENTS.md` as the reason. Five things it could not reach, each naming the exact missing piece: steps 2/3 end-to-end want front 04's acceptor to take its transport from `rakun_ssl:transport/1`; the step 5 watcher wants front 16's scheduler (**and cannot be a sidecar `gen_server`, because the reload is botopink and erlang cannot call back into it**); step 6's registration wants front 11's behaviors; and PKCS#12 wants `public_key`'s path plus a refusal naming the OTP version
- [x] `05-emilia` front 45 — transforms: branch `front/45-transforms` at `9c19e22`, **528 → 569** on both rows and a fifteenth example member, **held out of `feat` only because two compiler gates are mid-`test-libs` against the shared checkout**. 96 leaves under one `Transform` section, and the head audit ran on the real compiler: a `Transform` **section** beside front 44's `Transition.Transform` **leaf** resolves and dispatches on both targets, and no top-level `Transform(…)` payload variant was declared — which is exactly the hazard front 44 handed forward. **No new compiler defect**, and it verified the pin was not stale by confirming both known gaps behaved as documented. Its class-fragment probe **fired on correct output** in the first draft (`var(--tw-scale-x)` contains `scale-x`) and the *control* caught it — the pattern fronts 38, 41 and 44 built now has a save to its name
- [x] `03-rakun` front 14 — validation: rakun `af933f7`. `modules/rakun-validation` goes from **no test blocks at all to 54 / 0 on both rows**; `rakun-web` stays 83/0 and the core 346/0 and 344/2. The failure mode it was told to design out is **in the tests, not only the prose**: a marker on a field whose type it cannot check is a located compile error, an unregistered `#[constraint("cpf")]` is a violation naming the registered list, `bindInt("age","12x")` answers `0` **and** records a `typeMismatch`, and `refuseInvalidConfig` is an `assert` with no flag that softens it. `report.bp` has **zero imports**, which is the client-bundle requirement written as a fact about the source rather than a promise. Four plants, the fourth redding only the erlang row, which is what proves the two host halves are independently under test — and the first is the better story: it redded fewer cells than it should have, so the front found the gap in its own parity inputs, replaced a redundant one with a 50-character name at the inclusive bound, and re-ran until the plant redded the digest too
- [x] `05-emilia` front 44 — transitions and animation: emilia `f3f997e`, 495 → **528** on both rows and a fourteenth example member. 36 leaves, and **no compiler defect met at all** — the only red in the front was its own placeholder. The judgement worth keeping is what it declined to write: the spec opens a gate for transcribing the four `@keyframes` bodies into the dispatcher, and front 54 already carries them in `keyframeEntries()` inside `defaultTheme()` with `output.bp` already hoisting them, so `animationSheet` reads the body out of the theme — one copy of that table in the library, with the consequence asserted rather than described (a theme carrying no body for a name emits the declaration and hoists no block, which is also what makes `.Animate.None` blockless without a special case). It is also the worst case for the class-fragment probe — `var(--animate-spin)` contains `animate-spin` — and handled it head-on, with controls asserting the probe does **not** fire on either legitimate string. First plant redded fifteen cells across two members
- [x] `03-rakun` front 07 — the filter chain: rakun `3243f4b`. `modules/rakun-web` goes from **no tests at all to 83 / 0 on both rows**; the core member is unchanged at 346/0 and 344/2. The ordered chain with the 11-row band as one assertable string, two entry points, `withHeader`/`withHeaders` with `Vary` unioned and `Set-Cookie` refused naming front 62's API, deny-all CORS that echoes an origin and never answers `*`, and RFC 9457 with digest-in-body / reason-in-log proved by a cell that extracts the digest from the body and finds the same string in the log. **It answered a question I asked instead of leaving it recorded as an unknown, then changed its own implementation on the answer**: a tuple-array parameter type, an array literal of pair literals and `.0`/`.1` reads all parse and run on both rows, so `withHeaders` takes `#(string, string)[]` — its earlier reason for the flat form had borrowed the decorator-argument rule, which applies to annotations, and `withHeaders` is never written inside one. The pair type makes "a name with no value" **unrepresentable** where the flat form could only refuse it at run time, so a `@panic` and its cell are gone. It also **declined** the cheaper name-relative ordering front 72 uses, and the distinction is worth keeping: a Kahn sort over named edges works for auto-configurations, which know each other by name and are few, while the chain's numeric band is a contract across ten fronts that never meet — front 10 lands at −300 without knowing which entries exist in a given build, and two entries with no declared edge would have an *undefined* order, which is exactly what its whole-sequence assertion exists to catch
- [x] `00 · 04-js` — the three commonJS divergences: compiler `5b5f4dbe`, **zero snapshots moved**. Labelled construction placed by the labels it names, for records and for enum variants; an effect annotation on a record method read off the annotation list (`ast.BehaviorMethod` has no `effect` field, which is exactly why `fnKeyword` never saw one); and a `#[@context]` body that awaits emitting `async function`, taken narrowly and on the cell's own instruction — reading the **built body** rather than the declared effect means the only outputs that can move are the ones node refused to load at all. The merge with the cross-module fix **improved** the result rather than surviving it: both sides had rewritten the same import arm, and feat's `picked` lookup carrying this branch's body means an imported record's slot names now come from the module the import *names* rather than from whichever the walk reached last. The front then closed the same axis on the commonJS side — a variant name two enums of one module declare claims nothing by label and falls back to positional
- [x] `04-jhonstart` front 29 — the client boundary: jhonstart `0af0b4d`, 103 → **125** on both rows, all 22 new assertions running on each. Decision 77 taken literally: `islandAttrOf(id)` is the **only** occurrence of `"data-onze-i"` in the tree and `islandId(ordinal)` the only spelling of an id, so front 23's `RenderHooks.islandAttr` and front 68's entry read one definition rather than two that must agree; `serverSlotAttr()` gets the same treatment. Four comptime refusals enforced and located. **The front says plainly that it does not land the guarantee its priority line claims**: three rules — that a `#[client]` component's props record carries `#[clientProps]`, that nothing reachable from a client component imports `serverOnly`/`request`, and that the props written into an island are the ones declared — are module-**graph** predicates and `@Decl` has no parameters, so *a secret read on the server still reaches the browser today if it is written into an island's props, and nothing notices*. That sentence is in the source header, `AGENTS.md`, `docs.md`, the CHANGELOG and the commit message. The whitelist was **tightened** rather than loosened when the spec proved unspellable
- [x] `05-emilia` front 41 — effects: emilia `3a3ca90`, 463 → **495** on both rows and a thirteenth example member. The defect it existed to fix was `box-shadow:sm` — the Tailwind class *suffix* emitted where a CSS value belongs. 102 leaves across `Effect`, `Blend` and `Mask`, none resolving a shadow (front 54's namespaces through `shadowVar`/`insetShadowVar`/`textShadowVar`, no new literal ladder). All fifteen new section heads audited against the 84 top-level payload variants, and the two that repeat existing top-level **section** names were run on both targets before being declared — `AGENTS.md` now records that a head beside another head is safe and the audit is against payload variants only. **The strongest plant set any front has run**: reverting one shadow value redded eleven cells across three fronts including a consumer package. Two things left unfixed and written down instead — there is no `Ns.TextShadow`, so `--text-shadow-*` entries land in the Text namespace and a `clearNamespace` would drop them with the font sizes (pinned by tests, with `--inset-shadow-*` as the control; front 54's to add); and front 40's `Ring`/`Effect.Shadow` composition claim is not true today, and making it true would emit a declaration no row of the reference carries
- [x] `00 · 02-erlang` / `00 · 13-module-identity` — **the name-keyed tables answer the question actually asked**: compiler `ead0b645`, gate green end to end (`test-libs` 38 passed / 1 known red / 19 restricted, `test-language` 534 / 40 expected / 0). Four tables, not one, and two before codegen — which is why commonJS and wasm answered the *other* module's value at exit 0 while only erlang failed loudly. `ExportInfo.arity`, a `CrossModule.owners` multimap and `pick(name, source, arity)`: the module the import **names** answers first, then the call's arity, then `.contested`. The reversed direction was the CLI's topological sort — a symbol→first-module map sent the edge to the wrong module, which then sorted after its own importer. Four cells, each proved able to fail by planting the pre-fix behaviour. **The rejected design is the part to remember**: refusing duplicate *declarations* is the obvious rule and is wrong, because `libs/std` already exports thirteen duplicated names, all consumed qualified — the fault is keyed by the consumer and owed by an unqualified use
- [x] `03-rakun` front 72 — auto-configuration: rakun `10b6898`, 302 → **346** on commonJS and 300 → **344** on erlang (the two reds still front 04's), 44 new cells. The pass **sorts before it evaluates**, and an ordering annotation expands to the named configuration *and the beans it owns* — without that, `#[conditionalOnMissingBean]` sees the owner applied and its bean still missing. Every failure mode names something: an unmatched configuration's bean factory raises with the failing condition, a pass that never ran is its own refusal, an exclusion naming nothing halts and lists the registered names, a cycle halts naming its members. `#[profile]` on a frozen stereotype could not gate `__rkMake_<Type>`, so the marker leaves the component **unbuilt** and emits a raiser carrying the diagnosis rather than a duplicate factory — recorded as a narrowing that ends when `decorators.bp` unfreezes. Both plants landed hard: deleting the sort redded **ten** cells including the three that exist to say the order is not the registration order
- [x] `05-emilia` front 38 — typography: emilia `b86cea6`, 419 → **463** on both rows and ten example members to twelve. 437 leaves (`Text`, `Font`, and a new top-level `List`), every new section head checked against the 84 top-level payload variants first because of the `Layout.Break`/`After` shadow rule. **The library's last literal `rem` ladder is gone** — `textSizeToCss` deleted, not widened — and that is why the blast radius was four times what the spec predicted: fronts 36, 37, 39 and 40 had all re-pointed their regression control at `.Text.Size.Lg` after front 40 took `.Border.Rounded.Lg` away, and **no token in emilia resolves a length any more**, so a token-shaped control for that probe no longer exists. All four walks are now hand-built declarations paired with the same fact asserted from the other side. No compiler defect met, and the front said so. Two unconfirmed reference rows marked **at the declaration** and each stating what is *not* provisional about it — the better pattern, and the answer to the standing complaint from front 40
- [x] `04-jhonstart` front 27 — Link and the reconciler: jhonstart `1707823`, 68 → **103** on both rows with all 35 new assertions running on each, because `link.bp` and `reconcile.bp` reach **no host cell of any target**. `LinkProps` + five `with*` refiners (chosen because an *imported* declared default is still not filled, so a six-parameter signature would make every consumer spell all six), a `#[@context] Link` emitting only the attributes that differ from their default and no pass-through `attrs` slot, § 8's four prefetch rows verbatim with an unknown kind answering `skip`, and `layoutKeys`/`sharedDepth`. The client half — delegated click, prefetch, link status, the reconciler driver — is **absent rather than stubbed**, each with the exact cell it needs named, and three acceptance rows are declared unassertable in the test-file headers rather than faked. `docs.md`'s header blurb, which had claimed a `.d.bp` remained since front 26 falsified it, was corrected in the same bump
- [x] `04-jhonstart` front 28 — server components: jhonstart `6d6c007`, 51 → **68** on both rows with all 17 new assertions **running** on erlang. `server.bp` replaces `server.d.bp`: `RequestData` with `param`/`queryParam`/`header`/`cookie` through front 26's `pairValue`, `fillRequest` as the single writer and the `pub` seam rakun front 62's dispatcher calls once per request, `renderServerComponent` over a lambda thunk, and both host halves shipped cell for cell (`server_runtime.mjs`, `sidecars/jhonstart_server.erl`). Decision 90 measured working end to end: a `#[@future] fn … -> @Future<Element>` activates `use pathname()` with no `#[@context]`, green on both rows. Three things the front **declined to invent**, each the right call: front 01's `escape` does not exist in std at `2e6bb4ac`, so the suite pins the *unescaped* answer and the day it lands the change shows as a red cell; front 02's `awaitAll`/`race`/`allSettled` are named and unshipped; and the rakun element adapter front 23 asked for was left unwritten because the two specs disagreed about who owns it — decision 113 deleted it
- [x] 1.0.5-beta closed — [`../1.0.5-beta/closure.md`](../1.0.5-beta/closure.md); fronts 06 and 10 landed in full, 15 with three hold-backs, the rest partial (row by row there)
- [x] 1.0.6 … 1.0.9-beta absorbed and deleted (decision 68) — every 1.0.9 front copied by name under its track; the merged drafts deleted; proof in [`unification.md`](./unification.md)
- [x] Top-level documents — `overview.md`, `fronts.md`, `contracts.md` (+ contract 7, the test/snapshot contract), `deferred.md`, `language-gaps.md` (+ the `00` owner column and the `@src()` row), `unification.md`, `decisions-taken.md` (68–70), `decisions-pending.md` (71–82), this file
- [x] `00-compiler-carry-over/README.md` — C-01…C-27 prioritised, with the 1.0.5 deep dives carried beside it (17 front directories)
- [x] `00 · 13-module-identity` — half 3's **erlang** record reads, closed on compiler `ef2604af`. Two defects, not one: a host-backed `declare fn` whose declared return is a record had its answer read positionally, and the field offset was guessed from the names one file knows, so two records sharing a field name made the read take the neighbour's — `1` where the answer is `2`, at exit 0, and past the tuple when the declarations sit further apart. Fixed at the host boundary with an idempotent adopt, so the three templates half 3 had already swept stay untouched, and by counting the offset over every `pub` record of the program. The argument that makes this the right place: the by-hand sweep is not merely incomplete but **uncompletable**, since a consumer library's sidecar is not this compiler's source. rakun 200/9 → **207/2**
- [x] `00` C-02 — an index **is** a method call: compiler `6c7a1fc6`. `xs[k]` is `xs.at(k)` and `xs[a..b]` is `xs.slice(a, b)`, rewritten in the checker under the index node's own loc so the method's lowering and C-04's default fill find it again — **no file under `codegen/` was touched**, which is the claim the row rests on. `xs[0]` typed `void` before and types `?i32` now; `d["a"]` went from `undefined` / a crash / a trap / `undefined` to **`1` on all four**; a user `Matrix` answering `at` is indexable with no compiler change, which is the whole point of the rule. The spec's "silence at exit 0" on beam was only half true and was re-measured rather than repeated. Two backends did need changes and were **not** reached into: each reproduces with the method spelled by hand and no index in the program, which is what proves the rewrite is not misplaced
- [x] `00 · 01-checker` — `if (x != null)` narrows: compiler `9fbd84a0`. The defect was not a missing `!= null` arm but the **shape of the store** — a single `guardArgName`/`guardNarrowedType` pair could only ever narrow the last name a condition tested, which is why `a != null && b != null` never worked; it is a list now, one entry per name with the type it takes on each side, plus the early-return guard clause that outlives its `if`. Fourteen shapes × four backends were measured before and after, and the four that still do not narrow are written as limits with reasons — notably `loop (x != null)`, refused on purpose because a condition loop reassigns the name it tests, so narrowing it would red programs that work today. 474 → **509/36/0**
- [x] `00` C-01 half 3 — the identity in the value: compiler `a8087490`, steps 14–19. A record is `{TypeAtom, F1, …}` on erlang and beam, a variant tag is qualified, wasm boxes a value with its declaration, and commonJS's decision 5 was verified rather than changed. The measurement the half exists for: `Person(name:"a",age:1) == Vec(name:"a",age:1)` answered **true** on erlang and beam and now answers **false** on all four — it had no cell that ran everywhere, because `botopink test` refuses beam and wasm, so `run/type_identity_equality.bp` is new. 13 expected-failure lines green **by running**; two re-classified rather than deleted (one to C-07, one to 05-wasm). 519 of 535 RUN LOGs byte-identical, the 16 that moved each explained — including one honest entry, `wasm/src_in_a_method` printing *the same wrong answer at a different address*. Five defects the runs found were fixed on the way, among them a section enum being given two atoms because its identity was taken from the writing module rather than its outer enum's owner, which took emilia from 0/10 and 2/10 to 10/10
- [x] `00` C-01 half 2 — policy 3 on both BEAM backends: compiler `cbd5f1ec`. One module per `type` (`<file>__t__<decl>`), the mangled `<Owner>_<method>` names **deleted, not bypassed**; 25 erlang + 26 beam cells re-recorded with all 51 RUN LOGs byte-identical, and the beam export audit assembling every unit with `erlc +from_asm`. A `type` with no bodied method emits no unit — which is why the ~67 cells the survey predicted were really 25. Three defects only a real run found, each fixed on the way (the empty-unit rule; `decls_start` shifting under the dropped `-export` placeholder, which emptied the comptime listing; `method_owners` needing the adopted behavior defaults). policy-3 §2.2 vs decision 23 resolved for 23
- [x] `00 · 13-module-identity` step 0 — the two doc calls closed: A-flat recorded (decision 6: `out/erl/`, `out/beam/`, commonJS and wasm unchanged) and the Elixir claim **dropped** rather than verified (`elixir` still not installed); no code moved
- [x] `00 · 13-module-identity` half 1 audited against HEAD and ticked (steps 1–6): compiler `bd0201c3` — the harness test step 3 owed (two aux modules rendering one atom, one cell per BEAM backend) **found the refusal reading a freed key**: `seen` kept the atom slice the loop freed per iteration, so the erlang check segfaulted and the beam check compared garbage; fixed with the test. Step 2 re-run on `examples/modules` (five `.erl`, atom == basename, `erlc` clean, commonJS still mirrored, `clean` whole); audit 453/453. One box stays open: a comptime atom names its template but not its file (`bp@comptime__tpl__…`), which needs `env.TemplateEvalCtx` in `src/comptime/{env.zig,comptime.zig}` — 01's, beyond the carve-out
- [x] `00 · 13-module-identity` half 2 audited and ticked (steps 7–13): compiler `def58473` corrects the stale `'Array_range'` comment the gate named (the assoc default fn is the bare `array_range`). **Two README boxes are answered by decision 23, not by code**: `interfaceAssocAtom` stays and a behavior consumed by three modules still has three copies, because `__b__` is reserved and emits nothing — the one-copy shape needs the behavior module the decision declined; recorded as such, not ticked
- [x] `00 · 13-module-identity` half 3 audited and ticked (steps 14–20, and C-01's acceptance with them): compiler `e2da8408` adds the one test step 14 still owed — two types of one module rendering one atom is a `duplicate_decl` fault naming both — and writing it corrected the example the check carried (`Foo_Bar`/`FooBar` do not collide; `Person`/`person` and `Foo_Bar`/`Foo__Bar` do). Step 20 recorded as decision 21's T2 with its price; step 19 taken the first way (`a71786b3`). Half 1's step 5 file-naming half is the only README box left open, and it is 01's
- [x] `00 · 13-module-identity` — the sidecar shipper's refusal is an error the callers answer: compiler `1f46874d`. `build.zig` and `test_cmd.zig` ran `shipMjsSidecars(…) catch {}`, which is why `refuseSidecar` had to `std.process.exit(1)` by hand; the two swallows are gone, the refusal returns `error.SidecarRefused` after the located diagnostic, and both commands answer exit 1 — same output, same exit, no process exit inside a library routine (CLI contract cell unchanged and green)
- [x] `00 · 13-module-identity` — decision 109, the module atom: compiler `ac90ebfd`. Every erlang/BEAM atom starts with its package (`myapp@main`, `std@math@@PI`) and a declaration's module is `<package>@<path>@@<Decl>` with its case kept (`myapp@main@@SourceLocation`), replacing A2's `__t__`/`__b__` qualifiers and option A's `RESERVED`/`bp@` prefix (every atom holds an `@`, so none is an OTP name). `manifest` refuses a `name` that does not start with a lowercase letter or is `bp`; an erlang/beam compilation without a `botopink.json` is refused (`no_package`), and the compiler's tests compile under the implicit manifest `test`. 663 erlang/beam snapshots re-recorded for the spelling alone — each equals its old text once the package is dropped, `@@` read as `__t__` and case folded; 695 RUN LOGs byte-identical but one compile-error log quoting the file name
- [x] `00 · 11-tooling` / C-01 — **`botopink test` no longer swallows an `erlc` failure of a type-unit module**: closed by compiler `b3f80e75` (the emitted runner's `__bp_load_siblings/0` sends a `compile:file/2` refusal to `'__bp_dead_module'`, which names the file, prints erlc's diagnostics on standard_error and `halt(1)`s before a test runs); a per-type unit is one of the `.erl` files beside the script, so it is covered by the same arm. Re-checked 2026-09-25 while auditing front 13: `std_package.zig` "the test runner loads a std sibling and refuses a dead one" pins the arm and asserts the old `_ -> ok` skip is gone
- [x] `01-std` steps 1–4 — `@src()` + the fallible test body (73/74), `std/asserts` (26 functions answering `@Result<void, string>`), `std/snapshots` (the `.snap` format and the `.new` protocol, no update flag — 72), and the retired `onze` as `std/mocks` with **no** `.mjs` sidecar: its six module globals became `globalThis.__bp_mocks`, nine tests green on commonJS and erlang. One silent break caught by `zig build` and not by the merge: the `@src()` plumb read `manifest.src` where `manifest` had become the imported module
- [x] `00 · 19` step 2 (C-27) — decisions 89 and 90 in the compiler: `contextInfoFromReturn` reads the owner through `@Future<T>` and `FnContext.annotated` is set by `#[@context]` **or** a wrapper effect whose unwrapped return owns a context, so a server component activates hooks with no second annotation. A third refusal appeared with it — a wrapper effect whose unwrapped return owns no context is `use-of-non-context-fn`
- [x] `00 · 10-cli-residuals` — a sidecar is shipped by the **resolved dependency**, not by a name asked of the roots (`libDirByName` deleted), and one that cannot be shipped is a located error with the caret on the `dependencies` entry, exit 1. Two builds that exited 0 and then died at run time now work. It exits by hand because `build.zig` and `test_cmd.zig` swallow the shipper's error — front 13's row below
- [x] `04-jhonstart` front 94 — the element surface: jhonstart `8e97603`. 42 `pub fn` (two builders, the two predicates front 23 imports, 32 non-void and 6 void constructors), 9/9 → **27/27** on commonJS and erlang, `element.bp`/`hooks.bp`/`html.bp` still frozen. It unblocks fronts 24, 26, 27, 29, 30, 31, 32, 53 and 67, which each carried a private copy of the constructors they needed
- [x] `02-packaging` step 2 (jhonstart) — jhonstart `e068998`: four members, no umbrella row, and the `.mjs` sidecar measured in four layouts, which is what re-aimed `00 · 10-cli-residuals` at the real defect
- [x] Decisions 95–98 — the effects are a chain (`@Context` ⊃ `@Future` ⊃ `@Result`, the annotation granting every capability at or below its level); one `ContextBase` per function and `Element` carrying its base; and one word for suspension, which renames the async-generator effect to `#[@futureGenerator]` → `@FutureGenerator`. Front 20 owns their implementation
- [x] `00 · 20-builtins-surface` (C-28) — decisions 95, 96 and 98 in the compiler, and eleven of the twelve findings closed: compiler `6cd50ff2`. The step-0 table (24 probes, `try`/`await`/`use`/`yield` × six bodies) **corrected two of the decision's own premises**: `try` had no gate at all — `inferJumpExpr` asked only that its operand be a `@Result` and never looked at the enclosing body, in any of the six or in a plain `fn` — so the generator's refusal had to be *written*, not preserved; and `yield` was accepted in `#[@result]` and `#[@context]` because the guard read `if (env.starFn) |ctx|` and skipped rather than failing closed, which also refused a legal loop-comprehension `yield` in a `#[@future]` body. The chain could not be spelled as `implement` clauses (a `behavior` carries `extends`, and `builtins.d.bp` is parsed by nothing), so it is `comptime/effect_chain.zig` asked by all four checks, with a drift test that reads the file in both directions. `?T.expect` is gone and refused — deleting the arm alone was measured to be *worse* than the alias, since an unknown method on a `?T` types permissively and fails at run time. Language suite 377 → **401/53/0**, beam **49/23/0**
- [x] `00 · 20-builtins-surface` step 4 — the residues (F9): compiler `c22ecf2d` + `a822ad85`. Measured first: `05ed1d40` had moved `inline` off `External.Node`, but the checker never read the declaration, so `#[@External.Node("…", inline = true)]` still checked and ran at exit 0 — the switch that looks like it bypasses the dispatch table and does not. `infer.zig`'s `external_variants` restates the five variants with their `declares_inline` bit (a drift test reads `builtins.d.bp` in both directions, the `effect_chain.zig` pattern), and `refuseUnreadInline` reds at the annotation for the three unread shapes — the flag on `Node` / `Wasm` / `Typescript`, the flag anywhere but last (both `hasExternalInline` read the last argument only), a non-bool value — on a `declare fn` and on a behavior's or type's methods alike; the flag on `Erlang` / `Beam` in both spellings still typechecks. Five error snapshots, one ok cell, the drift test, `reject/external_inline_unread`; `docs.md` and four `AGENTS.md`. `External` is not `Target` in one sentence. `zig build test` and `test-libs` green; **`botopink fmt --check libs/std/src/builtins.d.bp` still refuses the file at `fn await(self: Self)` (`:144`, `await` is a keyword), pre-existing since step 2 and front 21's `Future` to rename — the README's "formats" gate box stays open for that one reason**
- [x] `00 · 20-builtins-surface` gate, re-measured on the post-24 surface at compiler `0cd949a4`: cold `zig build test` (`.zig-cache`, `zig-out` and the runtime cache deleted) 23/23 steps, **2474/2474** tests, and the `effect_chain.zig` drift tests 6/6 (`-Dtest-filter="effect chain"`) — green, no change needed. The front stays open on the one box it cannot close itself: `builtins.d.bp` does not format (see Pending)
- [x] `00 · 20-builtins-surface` closed — the "formats" box: compiler `873217b1` (`front/20-parser-gaps`). The two parser rows: an unannotated `[pub] declare fn` reads its signature with `parseSignature` (generics, `comptime` and `_` parameters, a whole `-> TypeRef` return — `field<T, F>`, `getContext<T>(comptime _: type) -> Component<T, any>`), shared with behavior `fn` members; a behavior `val` member carries a `TypeRef` (`val fields: Field[];`). `_` is a bodyless declaration's placeholder and a function with a body refuses it (`discard-param-with-body`). `builtins.d.bp` formatted (layout only) and in `scripts/format-check.sh` with `builtins_fns.d.bp`. `scanDeclareFnExternal` stops on a prelude parse failure instead of `catch return` (decision 67), pinned by a codegen test; what the parseable file adds was measured — nothing observable on commonJS, three `'__bp_print'` templates on erlang that `isPrintBuiltin` pre-empts — and **no codegen snapshot moved** (six parser AST snapshots did, for the new shapes). `zig build test` 23/23 steps, **2484/2484**; language suite 799/28/0; `test-docs` 68 checked, 0 failed; drift (`-Dtest-filter="effect chain"`) green
- [x] `03-rakun` front 05 — configuration and profiles: rakun `b977463`. Eight sources merged into front 04's table with a "beats the row below" cell for every row and a full-stack cell setting one key in six sources at once; `#[value("server.port")]` reads whatever won, and `--rakun.profiles.active=prod` is proven by running. 31 → **87** on commonJS and **85/2** on erlang (the two reds are front 04's `request/6`). The front **shipped no sidecar**, against its own Owns list and DoD, and argued it: `botopink test` compiles every `test/*.bp` on both rows with no per-target gate, so a `declare fn` carrying only `#[@External.Erlang]` reds the commonJS row at the call site; `runtime.mjs` is frozen so there is no Node twin to pair it with; and `shipErlSidecars` would skip an unreferenced `rakun_config.erl` *silently*. The readers are botopink over std's `fs`/`env` instead — one implementation answering identically on both rows, which is the only claim worth making. Relaxed binding moved from the emitter to the reader for the same reason: a decorator body cannot call a sibling, so an emitter rule would bind one spelling where the reader binds all three
- [x] `05-emilia` fronts 34 and 36 — the modifier table and the layout vocabulary: emilia `e22cf80`. 83 modifier variants (82 `Variant`-returning fns, the three dark-mode strategies proven one cell each, `Open` put inside one `:is()` rather than relaxing front 56's one-`&` check) and the whole layout surface — display, `Position`, the nine-direction `Inset`, overflow/overscroll, visibility, z-index, float/clear, object-fit, aspect, columns and breaks. 233 → **313** inline tests on both targets, six example members. Front 36's proof that no literal length survives is a walk over its **776** leaves plus a control that must fail and a mutation test that did; its `Columns` emits `var(--container-*)` against the README's `rem` literal, because a literal would be a second spelling of one width and an override would move `max-width` while leaving `columns` behind
- [x] `05-emilia` front 35 — padding, margin, size and space: emilia `ea0811d`. Six of the seven literal `rem` ladders are **deleted**, not widened, and the one that emitted Tailwind class names under `margin-x:` — a property that is not CSS — now answers `margin-left:calc(var(--spacing) * 4);margin-right:…`. `Auto` became a value on the ladder, which gives `mx-auto` its two properties for free. Pinned by a walk over **1074** `Pad`/`Margin`/`Space` leaves asserting no `rem` and none of the old class-name fragments survive, plus 566 `Size` leaves. The named `MaxW` widths emit `var(--container-md)` rather than the spec's `28rem` literal, because a literal would have made the theme's own `--container-*` an override that silently does nothing — the same defect the ladders were. 202 → **233** inline tests on both targets, plus an `examples/emilia-spacing` member
- [x] `05-emilia` front 33 — the colour palette: emilia `a687850`. The 26×11 grid on `Color` and `Bg.Color`, `paletteVar(family, shade)` taking **strings rather than a token** so a border/ring/shadow/SVG front writes `"border-color:" + paletteVar(f, s)` and needs no table of its own, `paletteEntries()` with all 286 OKLCH values from upstream 4.3.2, and `Alpha(percent, inner)` implemented as a `Sheet` rewrite rather than the spec's string wrap — so `Hover([Alpha(…)])` and `Alpha([Hover(…)])` are the same rule instead of one of them losing its selector. `colorTokenToCss`'s `Red(_inner) -> "color:red"` and the dead `redPaletteHex` are gone, proven from a consumer package: `color:var(--color-red-600)` where it used to say `color:red`. 121 → **202** inline tests on both targets
- [x] `05-emilia` front 56 — the cascade and output model: emilia `27c3343`. `Rule`/`Block`/`Sheet`/`Variant` with a one-`&` nesting template, the layer order, `Options` (theme, base, prefix, important, layers), the drain codec, the conflict rule, and the six V1 modifiers as variants; `flushSheet` deleted rather than left unreachable. 60 → **121** inline tests on both targets, plus a new `examples/emilia-cascade` member. Fronts 33/34/35 now have the interface they were each about to invent, and the audit rows for `group-*`, `peer-*`, `rtl`, `in-*`, `*`, `**`, `space-*`, `divide-*`, `@supports`, `@container`, `@keyframes`, `@layer`, `important` and `prefix` have somewhere to go; `@property --tw-*` still does not, and whichever of 44/46 needs it adds a fifth layer name
- [x] `04-jhonstart` front 26 — the router: jhonstart `2bb6fd9`, 27 → **51** blocks on both rows with all 24 new ones **running** on erlang, and the consumer surface proved as a run from a scratch package outside the tree — a `#[@context]` component rendering through `use pathname()` / `use selectedLayoutSegment()` / `use params()`, green on both rows with both sidecars shipped through the dependency. It answered the `query` hole front 23 left rather than inheriting it: front 26 **needs** the guarantee, because a render that reads the query and is cached as static serves one reader's `?sort=desc` to everyone — silent, and invisible to any test that renders one request — and it named the three edits that would close it, all in front 22's file. It declared one deliberate deviation instead of hiding it: `root.bp` and `botopink.json` are front 94's, but without `pub mod router;` the file is not compiled at all and the alternative was delivering nothing; it appended exactly the two lines front 94's own listing reserves for front 26
- [x] `03-rakun` front 23 — the SSR pipeline: rakun `18a7a5e`, 267/0 → **302/0** on commonJS and 265/2 → **300/2** on erlang, the two being front 04's own. **This is the front the milestone was converging on**: with it, jhonstart's chain (26 → 27–32) is unblocked. All 35 new cells run on both rows and none was skipped — the payload round trip goes through `JSON.parse` *and* OTP's own `json:decode/1`, a `</script>` parameter is asserted to yield a document with no `<img`, and two 50 ms loaders are asserted to finish under 100 ms on the row that actually spawns. It owns `RenderHooks` (decision 77) and calls none of onze's functions; it found the record needed an eighth field and `Payload` two more, because each spec's key table had outrun its own record. One acceptance is honestly **not** a cell: `__onzeFill`'s DOM idempotence is browser code with no erlang twin, so the protocol is asserted and the DOM half named as front 68's
- [x] `03-rakun` front 06 — the context API: rakun `d72225c`. The container's doors — the bean registry, the parent/child `Context` chain, `#[provides]` with qualifiers and primary, the two lifecycle passes, events and `bootSequence()`, the three scopes, eager init and shutdown. 209/0 → **267/0** on commonJS and 200/9 → **265/2** on erlang, the two remaining being front 04's own `request/6`. Its sidecar decision was measured rather than cited: the registry stores a factory, a thunk, a closure and a generator — four funs — and the 1.0.6 sketch that resolved through `list_to_existing_atom` could not have run either, because an atom is not callable. **It also declined credit it could have taken**: an erlang red it had worked around turned out to be fixed by the compiler landing mid-run, and it said so instead of keeping the attribution. It argues one acceptance should be struck — "a component whose `#[value]` key is missing fails the boot" describes a failure that exists nowhere, since `rkProp` answers `""` and `rkPropInt` answers `0` by deliberate invariant in the frozen runtime
- [x] `03-rakun` front 62 — the request context: rakun `30bcbae`. The frame and its epoch, the five phases, headers, cookies and the `Set-Cookie` queue, draft mode, `connection()`, deferred `after()` work and the per-request memo table — 136 → **209** cells on commonJS, all 73 new ones running on the erlang row too. It took front 22's sidecar shape rather than front 05's, and applied the test rather than the precedent: what it stores is a queue of deferred **thunks** and a memo of arbitrary typed values, which no string table holds, so the host files are justified where front 05's would have been a second copy — with front 05's three measurements each *satisfied* rather than waived. The deferred-work cells are the ones worth reading: a child spawned with `spawn_monitor` reading the frozen frame, a raising child counted and logged with the request id, an overrunning child killed at 40 ms, and the frame erased while a sleeping child is still outstanding
- [x] `03-rakun` front 04 — the Erlang runtime: rakun `f5e0d1c`, track B's first landing. `rakun_runtime.erl` (36 KB, `erlc -Werror` clean) with all 16 `declare fn`s paired and the camelCase → snake_case mapping verified across ten emitted modules; 14 `.bp` assertions run byte-identically on commonJS and erlang, and `rkServe` — the one cell no `.bp` can call, since it binds a socket and blocks — is covered by a 45-assertion Erlang round-trip (supervision restart, a singleton race across two processes, a real socket on an ephemeral port, `max-connections`, `idle-timeout`, keep-alive header leakage, `eaddrinuse` naming the port). erlang went from **1 passing cell to 21**; the remaining 6 and one uncompilable file are the two compiler gaps above, not rakun's. `targets` stays `["commonJS"]` until they close — widening it now would only move a known red into `botopink-lib-test`
- [x] `05-emilia` front 54 — the theme: emilia `58061fe`, track D's first landing. `Theme` is a flat `ThemeEntry` list with nineteen validated namespace prefixes (`Ns`/`nsPrefix`, the only place a prefix is written), `spacing(n)` answers `calc(var(--spacing) * n)` so emilia never resolves a spacing value, the `@theme` block is always emitted, and `DarkMode` ships for front 34. `themeCss(defaultTheme())` is byte-identical to the 70 lines `05-emilia/test-snap.md` predicted; 17/17 → 6/6 + 37/37 + 17/17 plus a new `examples/emilia-theme` member at 6/6, on both targets
- [x] `02-packaging` step 2 (emilia, erika, and the compiler consequence) — emilia `d539978`, erika `eb24070`, both fast-forwarded into their `feat`: each umbrella is a workspace, the core is `modules/<lib>/` with an explicit `files` list, the example is a member with `{ "workspace": true }`, and the pre-commit hook runs every member. emilia 17/17 on commonJS and erlang, erika 31/31; no umbrella has a runner row. Neither library has its `<lib>-test` member yet — that waits on `01-std` steps 2–3
- [x] jhonstart green under decision 88 — `b89c787` on jhonstart's `feat`, the library track's first commit: `#[@context]` on every activating body, the hook nouns (`counter`, `router`, `toggle`), `src/client_runtime.{bp,mjs}` for `state`/`effect`/`memo`, the examples on a `path` dependency; 9/9 on commonJS and erlang, the three manifest-carrying examples build, test and run. Its two `known-red-libs.txt` rows went with the compiler bump (`fa30a214`) — a listed cell that passes fails the gate
- [x] `00` C-16 and C-06's wasm-half acceptance — `59ad600e` on the compiler's `feat`: the six `loop_*` RUN LOGs compile and run under `wasmtime` byte-identically and `run/case_range_value.bp` passes, so C-06's wasm half is accepted **by running**, with no defect in `wat.zig`; C-16 lands the residual cells (local dependency on the workspaces manifest, `@panic`/`@todo` with a non-zero exit through `<name>.exit`, a no-external-target cell, the index cells, the qualified std host call) and the runner prints the tally the header used to keep by hand. Recounted from the files: **363/53/0** on commonJS+erlang+wasm, **35/22/0** on beam, 107 cells, 67 expected-failure lines
- [x] Spec maintenance — decisions 75/76 and 83–86 folded into `02-packaging` § Mechanism and front 18 (`e326d9a4`); front 19 rewritten after 87–90 with the `#[@context]` sweep of `04-jhonstart` (`d723b7ea`, 39 annotations); decision 77's `Owns:` lines and the onze order (`96191e56`); `fronts.md` § Waves regenerated from the dependency lines — 11 levels, 95 fronts, 46 waves changed (`00942275`)
- [x] Decisions 90, 91, 92 — 90 taken on 2026-09-21 (a wrapper effect whose return unwraps to a context owner activates on its own; R5 unamended, 88 stands where there is no wrapper effect), which unblocked front 19 step 2; 91 and 92 raised by the sweep and open, both non-blocking
- [x] `02-packaging` step 2 (rakun) — landed on rakun's `feat` `e50a63b`: the umbrella is a workspace (`workspaces: ["modules/*", "examples/*"]`), the core moved to `modules/rakun/` (6 `files`, `runtime.mjs`, 5 tests — 17/17), the 13 scaffolds carry `files` + `{ "rakun": { "workspace": true } }` and their target, the example is member `rakun-example` (renamed from `rakun-app`, which front 22's submodule will take), the pre-commit hook runs every member; `botopink-lib-test` sees 15 rows, no new red
- [x] `00 · 18-comptime-runtimes` steps 0–1c (C-26, part) — landed on `feat` `85a8ab4e` (+ `361d255d`: release runners install OTP 28): `codegen/beam/beam_file.zig` writes a loadable `.beam` (FOR1 chunks, `LitT` stored zlib, `Line`), `opcodes.zig` generated from OTP 28's `genop.tab` (184, 118 emittable — the OTP-24-stable subset), three `erl`-validated round-trip tests; cmd 4 loads `.beam` bytes in-frame; the server and two preludes are compiled by `erlc` at `zig build` and embedded (`server_source.zig`, `render_resident.zig`) — `erlc` leaves the user's machine, OTP 28 floor refusal; erika-linq build 627 → 464 ms. Remaining C-26: the untyped BEAM lowering (`beam_asm.zig`, 1 500–2 500 LOC, after C-01), `persistent_wat.zig`, the selector (84), the `{beam,wat}` layout (85), the browser build's comptime half (after `persistent_wat.zig`)
- [x] `02-packaging` step 1 — landed on `feat` `81e10a18`: the shared manifest model `modules/manifest/` (workspace vs package, `workspaces` globs, members, `targets` inheritance), object-form `dependencies` only (`git`/`path`/`{ "workspace": true }`, one source, one pin), discovery via `manifest.scanRoots` in runner/loader/LSP/bpmp, `botopink test` per member with `✗ ships nothing` for a member without `files`, `docs/botopink-json.md`, 47 fixtures + tests; 20 located error messages
- [x] `00` C-19, and the 1.0.5 worktrees `beammem` (C-05 steps 1–3), `identity` (C-03 erlang half), `ecosystem` (C-02 `libs/std` half) — landed on the compiler's `feat` on 2026-09-20 with a cold gate green each (`2788be9f`, `a6b5b62a`, `1769456d`; two real `at`-rename regressions fixed on the way); worktrees removed
- [x] `00 · 19-use-activation` step 1 (C-27) — landed on `feat` `85f883bd`: `docs.md` documents `use`/`@Context`, the static-prefix guard holds at any nesting, the commonJS React rename is gone (`use f(x)` → `f(x)` everywhere), `#[@context]` is required to activate a hook (`use-without-context-effect`), 8 language cells (348/45/0). jhonstart is in `known-red-libs.txt` until its hooks gain `#[@context]`
- [x] `00` C-11 + C-12 acceptance — landed on `feat` `f5c58e34`: `format --check` walks the whole project (`.bp` + `.d.bp`, nested projects), `reject/<n>.bp` beside its `.expect` is structurally exempt, `scripts/format-check.sh` is gate stage 3 for the trees that are canonical (`examples/modules` only today — the reds are listed in its header with owner), 3 unit + 1 contract tests; C-12 measured: 0 hunks outside the chain rule across six trees, 29 chains opened (12 of the 44 predicted were not chains), `fits` predicate tests added
- [x] `00 · 19-use-activation/` (C-27) — specified: README (steps 0–5: the language reference, `use` in `#[@future]` bodies, tuple destructuring, the boundary directives, the lowering contract), `surface.md` (construct × backend table), `evidence.md`; questions 87–89 raised; `04-jhonstart/**` swept to the rule (`use router()`, `use pathname()`, `use actionState(…)` — zero `use use` left)
- [x] `00 · 18-comptime-runtimes/` (C-26) — specified: README (steps 0–5), `current-path.md`, `beam-file-format.md`, `wat-runtime.md`, `snapshot-layout.md`, `browser-build.md`, `evidence.md`; questions 83–86 raised
- [x] `00` C-19 — declaration-name builders: landed on the compiler's `feat` on 2026-09-20 (`wip(tooling)` + merge `fix/tooling-step5`, pushed); gate green at the tip
- [x] `00` C-06 (wasm half) and C-12 (formatter width) — landed on the compiler's `feat` on 2026-09-20 as-is (`wip(wasm)` + `fix/wasm-patterns`; `wip(formatter)` + `fix/fits`, pushed) — **landed, not verified**: their acceptance rows (zero bytes moved, the per-tree measurement, the four-backend loop cells) are still open under Pending
- [x] `01-std/` — `README.md`, `asserts-api.md` (26 functions + the old-onze migration table), `src-builtin.md` (`@src()`), `snapshots.md`, `onze-migration.md`, `modules.md`, `test-snap.md`, `unification.md`, the two plan examples under `examples/`, fronts 01–03 carried
- [x] `02-packaging/README.md` — the `modules/**` + `examples/**` rules, manifests, `test-libs` discovery, dependency direction; front 95 carried beside it
- [x] `03-rakun/` — 51 fronts carried with their 1.0.6/1.0.7 appendices (26 sections) and examples; `README.md` (levels computed from the dependency lines), `modules.md` (28 submodules + `starters/`, the 13 scaffolded dirs reconciled), `unification.md` (+ the Spring Boot sections still unowned), `test-snap.md` (~380 cases) and `test-snap-examples.md` (8 projects, ~76 cases)
- [x] `04-jhonstart/` — 9 fronts + 8 appendices + 19 carried examples; `README.md`, `modules.md` (5 submodules), `unification.md`, `test-snap.md` (~55 cases), `test-snap-examples.md` (5 projects, 32 cases)
- [x] `05-emilia/` — 22 fronts + 17 appendices + 15 carried examples; `README.md`, `modules.md` (2 submodules; `tokens.bp` stays one file, sub-dispatchers move per front), `reference-coverage.md` (267 Tailwind rows), `tailwind-mapping.md`, `unification.md`, `test-snap.md` (~130 cases), `test-snap-examples.md` (9 projects)
- [x] `06-onze/` — 9 fronts, `onze13` → `onze` normalised (34 identifiers); `README.md`, `modules.md` (7 submodules), `unification.md`, `test-snap.md` (55 cases), `test-snap-examples.md` (21 cases)
- [x] `00 · 04-js` — the two lambda lowerings that answered **wrongly** rather than crashing: compiler `b3665d32` (a lambda's expression body is its value, not a discarded statement) and `3e987e17` (a lambda argument is typed from the primitive receiver's own signature), both in `feat`. Re-measured 2026-09-21 against `2e6bb4ac`: `xs.map({ x -> if (x > 1) { x * 10 } else { x } })` and `items.filter({ e -> e.name.contains("x") })` answer `1 · 30 · 1` on commonJS **and** erlang — the point of the pair was that one target was silently wrong, so the evidence has to be the two agreeing, not one passing
- [x] `00 · 11-tooling` — **a brief of mine claimed a ledger line that does not exist**, and the front checked rather than complying: I told rakun front 14 its member was in `scripts/restricted-targets.txt`. It is in neither that file nor `known-red-libs.txt`, and `modules/rakun-validation/botopink.json` already declares `["commonJS", "erlang"]`, so both cells are ordinary asserts. Verified by me afterwards. Recorded because the failure mode it avoided is the expensive one: a thread that trusts a brief over the tree would have **added** a row to satisfy it, and an unnecessary line in that file is a hard assert against a cell that is fine
- [x] `03-rakun` — **the two duplicated helpers, and the reason the fix could not wait.** Landed on rakun's `feat` as `28bff3a`: `charOf` and `sub` live in `config.bp` and nowhere else; `file_router.bp` and `ssr.bp` import them from there. Their own comment blamed the rule that a decorator body cannot call a sibling function — real, and not covering these two, whose call sites in that file are all in ordinary functions while the decorator bodies further down already carry their own separate copies, which is what the rule actually forces. Behaviour-neutral and measured that way, with the *unfixed* tree under the same compiler as the control.
  **It was not a latent ambiguity.** Front 07's `rakun-web` imports `sub` from the package, so the moment the refusal landed, `filter.bp` and `middleware.bp` stopped compiling and **every compiler gate run against the shared checkout redded** on a member neither the compiler nor `rakun-web` had touched. Two fronts reported it independently, hours apart, and **neither invented a ledger row to get past it** — one of them said so in as many words: a known-red row written for another front is the escape hatch decision 67 forbids. The fix was the third option neither had asked for, and it removes the cause rather than qualifying two import lines.
  **The ordering held as predicted**: the dedupe makes the listed `rakun · commonJS` cell pass, and a listed cell that passes fails the gate, so the fix and the `known-red-libs.txt` deletion are adjacent commits. `known-red-libs.txt` is empty again, and `restricted-targets.txt`'s `rakun-web commonJS` line got the prose it had owed since front 07 landed — it said "no tests yet" while the cell runs 83
- [x] `00 · 01-std` / `00 · 02-erlang` — **five `libs/std` modules were dead on the erlang row, not one, and the silent skip is what hid them**: compiler `fecec6b4`, gate green on every stage (`test-libs` 46/0/0 known red, `test-language` 547 / 42 expected / 0). A consumer importing every std module and running the output through `erlc` refused **`std@path` (4 sites), `std@querystring`, `std@queue__t__queue`, `std@snapshots` (11) and `std@url`** — 19 sites — and after the fix, zero, with the guard re-planted bringing back exactly the same 19 in the same five. **Three defects, not one**, and two of my three claims were wrong: the skip is not in `modules/lib-test-runner/src` (that module only spawns `botopink test` and reads exit codes) but in the **emitted escript's** `'__bp_load_siblings'/0`; and there is a third defect that kills the repro even once the lowering is fixed — the loader was emitted only when `imported_fns`/`imported_types`/a type module were present, and `from "std"` fills none of those, so `std@querystring:parse/1` was a remote call into a module the escript **never loaded**. Proven by planting that route alone. The loader's `_ -> ok` is now a named refusal that formats `compile:file/2`'s own diagnostic onto `standard_error` (so `--json`'s stdout stays pure JSONL) and halts **before any test runs**; no flag demotes it. On the final merge it **re-planted the predicate term's removal rather than trusting a byte-identical auto-merge**, confirming it is still load-bearing after the import walk was rewritten under it — and re-derived the `.targets` cell count and the measurement line instead of re-pasting them, repointing the SHA to the tree the number actually came from **The blast radius was wider than five, and nobody had counted it**: the five are the *lowering* defect's, but the **loader** defect made **every** `from "std"` module unreachable from a test file on erlang. rakun's `AGENTS.md:1371–1375` records that as a permanent rule and names `crypto`, `base64`, `time` and `unicode` — none of which is in the five. Measured by me 2026-09-21 on `4fe1747e`: one test file importing `querystring`, `time` and `unicode` directly answers **1 passed / 0 failed on commonJS and on erlang**. So the third defect's fix retired a whole class of library workaround that its own front never knew it was writing.
- [x] `05-emilia`-style cleanup for **erika** — erika `bcdd05d`. `repro/erlang-imported-method-name/` is deleted and `repro/` went with it: the README's only section was that case, and a preamble with no sections is not a file. `AGENTS.md` and `CHANGELOG.md` close the gap in **erika's own** convention (a bold marker after an em dash, resolution stated inline with the landing version) and keep the durable half — two records of one imported module both declaring `toArray` — because that shape outlives the defect. Re-measured with the library untouched: `examples/erika-linq --target erlang` 1 passed / 8 failed → **9 / 0**. The restriction line still reads `["commonJS"]` while the ledger measures it at 0; lifting it is that member's front's call
- [x] `05-emilia` front 40 — borders, outlines, rings and divided lists: emilia `6200a9c`, 375 → **419** on both targets. The front whose *stub* caused two incidents now declares the full 26×11 grid on **five** colour sub-sections (the README says four), and its `Divide` selector is byte-compared to front 35's `siblingSelector()` both inside the library and from a consumer package. The walk is the strongest yet and it is strongest because the front **tested its own test**: the literal probe alone stayed green when it planted `border-color:teal` — this front's own historical defect — since `teal` is neither a hex, an `oklch(` nor a `rem`, so it added a colour-reference walk over all 1440 cells and a 288-way distinctness walk, then re-planted three different defects and watched each redden in the right place. One of those also reddened front 35's `Space` tests, which is the single-spelling contract working in both directions
- [x] `05-emilia` front 39 — backgrounds and gradients: emilia `41de483`. 340 → **375** on both targets and an eighth example member; 29 keyword leaves plus three 291-leaf gradient stop families, every colour through front 33's `paletteVar` so this front holds no colour table. Its walk covers **910** leaves with controls that must fail *and* a planted defect that reddened it, and it took the gradient composition apart rather than transcribing it: copying upstream verbatim would be a bug here because emilia emits no `@property` block, so the stop list would be invalid at computed-value time — and the README's own inferred shape was broken too, since a bare `var(--tw-gradient-to)` poisons the whole list and `From` alone would render nothing. The resolution keeps the README's list shape with upstream's registered default written as a `var(…, transparent)` fallback, and states the price it pays (a `Stop` with no `From` declares nothing)
- [x] `05-emilia` front 37 — flex, grid and `Gap`: emilia `9ae18b4`. 313 → **340** on both targets, a seventh example member, and the **last literal `rem` ladder in the library is gone** — `flexGapScale` deleted, `.Flex.Gap.N` routed through `spacing(n)` and compared to `.Gap.All.N` value-to-value rather than to an expected string, with `spacing.bp`'s docblock corrected to say six were front 35's and the seventh this front's. The walk covers 356 leaves and carries **two** controls: a leaf asserted to *carry* a `rem`, and a planted `0.25rem` that reddened it (250/2) and was removed. Its head audit caught something the spec did not anticipate: **`Self` is a language keyword**, so `.Flex.Self` and `.Flex.Place.Self` do not parse — the family was flattened the way front 36 flattened `Break`, with no emitted byte changed. The spec's own `flex-example.bp` uses both spellings, so its "Language gaps: None" was false
- [x] `05-emilia` — `examples/emilia-card`'s two class-body assertions are **ordinary goldens again**. They had deliberately pinned the pre-fix output of the commonJS `instanceof` defect so the fix would show as a change; the fix landed (a uniquely-named variant compares the `tag` string too), the goldens were updated, and the `known-red-libs.txt` line is gone — that file now holds nothing but its header. Verified 2026-09-21 by reading both the ledger and the example: the comment above the two assertions still explains the whole defect and then says the pin is lifted, which is the right way to retire one. A listed cell that passes fails the gate, so the empty file is also the proof that no cell anywhere is still red-by-permission **Superseded the same day**: the ledger is no longer empty — the cross-module export fix found a real duplicate in rakun and banked a line against `03-rakun`. The emilia half of this row stands; the "no cell anywhere is red by permission" reading of it lasted about four hours
- [x] `00 · 01-checker` — a leading-dot enum path resolved by **hash order** rather than by the expected type: the resolver iterated `env.typeDefs`, which holds the synthesised section enums beside the real ones, and returned the first enum whose section tree carried the path. `Token` carried `Color.Red.500` and so did `__Token__Border`, so the winner flipped when the typedef set grew and six emilia cells were unreachable in every spelling. Closed on compiler `f01c508a`: the expected type decides, the fully qualified spelling resolves, and an ambiguous path is refused naming both candidates — sorted, so the diagnostic does not reintroduce the non-determinism. emilia asserted the six shades at `1cd39b2`
- [x] `00 · 18-comptime-runtimes` steps 2–5 (C-26, part; 1b landed since by front 14, row above) — landed on `front/18-comptime-runtimes` (compiler `ed32ae82` + `9515a2d4` + `80a19bf7` + `1b1b34de` + `96948082` + `5cd2de13` + `baef853b` + `b5cec2a5`, origin/feat `0e20844f` merged in as `0beaa1f9`, fast-forwarded into `feat` with the cold gate green, runtime parity 1407 pairs): `zig build compiler-web` builds compiler-core for wasm32-wasi (ReleaseSmall 2.64 MB, 813 KB gzip), `glue.js` (WASI shim + Worker protocol), `index.html`, `zig build test-web` + the CI step; `comptime/runtime/runtime.zig` folds `persistent_erl` and the RUN LOG executors out on wasm and refuses a decorator or template there with a located diagnostic; `comptime_bench.sh --project` builds a workspace member again (erika-linq); step 0's counts and cold suite time (2 m 42 s, `evidence.md` E-15); step 2's binary emitter (`80a19bf7`: every wasm RUN LOG now runs the binary, the page runs the wasm target); **step 2, the wat runtime** (`1b1b34de`: the generated Erlang lowered to wasm and run in-process on wasm3 — 0 disagreements with the BEAM over every codegen fixture and 242 library evaluations, 0 refusals; replies read in sorted key order); **step 3, decision 84** (`96948082`: commonJS/wasm builds evaluate comptime on wat — no process spawned, strace). **step 5's comptime half** (`5cd2de13`: the page runs the bodies on its own engine; its COMPTIME REPLY equals the native one); origin/feat `52843fd5` (front 22) merged in (`8562e934`). **step 4, the doubled tree** (`baef853b` + `b5cec2a5`: `snapshots/codegen/{beam,wat}/`, 1 407 pairs audited equal by `snap_audit.sh --mode=runtime-parity`, a gate stage). Since, on `front/18-comptime-runtimes` (compiler `2371d128` + `07913054`, atop `feat` `0cd949a4`, not merged): decision 84 is chosen inside `comptime.compile`, so `botopink check` of a commonJS/wasm project no longer spawns `erl`; the wasm3 `@cImport` builds for macOS (release.yml's five targets all cross-build locally), test.yml's windows row installs OTP 28; bench re-run (E-16). Open: 1b — **moved to front 14** (its step 3; not blocked on C-01, the comptime atom is already decision 109's), then this front's `persistent_beam.zig` rename and the boxes that read 14's bytes; the CI matrix run (after the push)
- [x] `00 · 02-erlang` step 1 — §7 formatter on erlang: F1–F4's cells (`run/tuple_print`, `run/print_formatter`, `run/display_print`) green at compiler `643bff80` (landed by C-01 half 3); F5 closed by deleting the two stale "commonJS writes `1`" notes in `src/codegen/AGENTS.md` — `@print(1.0)` is `1.0` on commonJS and erlang. Docs only, no snapshot moved
- [x] `00 · 02-erlang` step 2 (C-07 erlang half) — decision 8 at run time on erlang: compiler `6f7ef37d`. `x is i32` and an `i32` arm test by value (`is_number(X) andalso X == trunc(X)` + range; `f64` is any number) and the narrowed value is converted — `N = trunc(I32)` on an arm binder, `X@n = trunc(X)` opening `if (x is i32)`'s then-arm; `==`/`!=` with an `unknown` operand are `==`/`/=`, typed operands keep `=:=`; `unknown` stores nothing. `test/case_unknown.bp` 6/6 on erlang, its line gone; one run-log fixture pins D1–D3; no snapshot moved

## In analysis
- [ ] `00 · 25-gate-perf` (C-33) — steps 1–3 of 4 landed on the branch · worktree `.tasks/25-gate-perf` (`front/25-gate-perf`, not pushed) · warm gate **144.3 s → 89.4 s (−38 %)**, cold **264.8 s → 116.0 s (−56 %)**, measured under other agents' load (heavier after than before); compiler `112d248a` (`tests/language/run.sh` and `scripts/check-docs.sh` on the shared bounded pool `scripts/lib/pool.sh`), `49cc56aa` (the compiler-core suite as 8 shards, `modules/test-shard/runner.zig`: `zig build test` 25.9 → 6.8 s warm, 138.0 → 33.7 s cold) and `9724b417` (stages 4b–10 of `scripts/gate.sh` side by side, reported in stage order); output and verdicts equal to the serial runs, red cells included; remaining: step 4, `test-libs`' erlang cells (257 of 356 serial seconds; a content-keyed `.beam` cache for `precompileErlang` is the measured next step)
- [ ] `00 · 05-wasm` — worktree `.tasks/wasm-refusals` (`fix/wasm-refusals`): `String.at` lowered rather than refused (the pieces were already in `wat.zig`), and a host-backed `declare fn` with no wasm host refused at compile time in the same words the other three backends use — no suite cell carries a `.targets` exception any more. Merging onto C-01 half 2

## Pending
- [ ] `00 · 21-effect-chain` (C-29) — ready to start · waiting on a worktree only: decisions 102/103/104/108 — `@Context<Base>` as the owner marker, `#[@use]` with `@Use<C, T>` / `@Component<T>` as the only grant of `use` (89/90 revoked), `@Generator<T>` · `@ResultGenerator<T, E>` · `@FutureGenerator<T, E>` over one `YieldStep`, `getContext`, and the jhonstart sweep (36 annotations, 24 wrappers) through `known-red-libs.txt`; ≈ 400 source lines, ≈ 250 snapshots, one `EffectKind` value per commit
- [ ] `00 · 23-std-purity` (C-31) — steps 1–5 landed (§ Done); step 6 open: decision 110 on the use side (`as` on a type leaf binds a checker-local name, `import {io} from "std"` is a namespace of its submodules) and decision 111's constructor through the namespace (`collections.Dict.empty()` is `unbound variable 'collections'` today; the leaf form `import {collections.Dict}` works on four targets) — `decisions-pending.md` 23-a
- [ ] `00 · 24-effects-by-return` (C-32) — **ready to land** on `front/24-integration` (compiler `f7398c40`, not pushed, not in `feat`): E1–E8 and the § Cells suite integrated with the codemod (E6, migration-only mode 24-d), `YieldStep<T>` / `.next()` on four backends, the three-source E3.9 hint, type aliases and the LSP; libraries swept (jhonstart `0206b91`, emilia `273b08d`, onze `b1e690d`, rakun `7ca7f2c`, erika unchanged) and every test-libs row at its pre-sweep count; `scripts/gate.sh --cold` green from a standalone copy, `--target beam` 204 / 7 / 0, vscode-extension 49/49. Open in the README (3 boxes): six guide.md fences that do not type as written (guide slips, and three checker gaps — `catch null` into `?U`, `null` narrowing after a `noreturn` call, a component called inside a component) plus the unrefused `#[layout] -> Element`; the E8 grep's wording (records left in fronts 19–22); `AGENTS.md` per commit. Choices to confirm: `decisions-pending.md` 24-a … 24-g
- [ ] `03-rakun` front 62's std substitutes — **the deletion was owed, the *other* deletion was not, and the proof it was not is that nothing redded.** Branch `fix/std-substitutes` at `646d71e`, gate green, **held out of `feat` only because a compiler gate is mid-`test-libs` against the shared checkout**. The five-module table, measured directly by a test file with an arithmetic control cell in each: `querystring`, `time`, `unicode`, `crypto`, `base64` all **2/2 on both rows**, with the probe proved falsifiable first. So the loader-defect bullets went: `nowMs()`/`sinceUnder()` deleted, the general rule at `AGENTS.md:1371–1375` deleted, and two leaning clauses trimmed. **But `splitQuery`/`encodeQuery` stay, and `AGENTS.md`'s "two bodies to delete when std is fixed" was wrong on its own terms**: std's `querystring` was never equivalent to them, it is *weaker* — `parse("a=%20b")` answers the literal `%20b` (no percent-decoding, where `route.query` is a decoded dict), `parse("a=b=c")` answers `b` (it splits on **every** `=`, where the form grammar cuts at the first), and `stringify([#("a b", "c;d")])` puts a space and a `;` straight into a URL. std's own header says the call site should pre-escape. **The alarming half**: the front swapped both bodies for the std calls anyway and ran the full suite — **387/0 and 385/2, every cell green**. Three measurable disagreements and rakun's suite could not tell. It added one cell that pins them and verified it reds under the swap
- [ ] `01-std` — **`std/querystring` is a splitter, not a codec, and nothing says so where a caller looks.** Measured by the rakun cleanup: `parse` does not percent-decode and splits on every `=`; `stringify` escapes nothing. The module's own header does say "the call site should pre-escape", which is why this is a surface question rather than a bug — but a module named `querystring` whose `parse` returns `%20b` is one a consumer will get wrong once each, and front 62 got it right only by having written its own first. Either the names should say what they do, or `parse`/`stringify` should do what their names say; `01-std` owns which
- [ ] `03-rakun` front 74 (the tail) — **a POSIX-only `absolutePath` that would fail a blocking CI row, found by the front's own follow-up before it reached anyone.** Worktree `.tasks/rakun-abspath` (`fix/ssl-absolute-path`). The chain is worth recording whole: `std/path` was dead on the erlang row when front 74 was written, so it hand-rolled `absolutePath` out of `indexOf("/") == 0` and string concatenation; the compiler fixed `std/path` an hour later; I told the front its comment was stale; and chasing the comment it found the *code* the comment was keeping alive. `.github/workflows/test.yml:50` is `{ runner: windows-2022, target: commonJS, allow_fail: false }` with `continue-on-error: ${{ matrix.allow_fail }}` at `:52` — verified by me — and on that row `absolutePath("C:\\certs\\server.pem")` treats a Windows absolute path as relative, so the "absolute path searched" refusal **names the wrong path**, which is the one thing that refusal exists to get right; and the cell asserting `resolved.indexOf("/") == 0` fails outright. `path.isAbsolute` knows about a drive letter and a leading slash does not. The replacement assertion is idempotence — `absolutePath(resolved) == resolved` — which holds on both shapes and, deliberately, does **not** import `process`, because a test file that does loses every cell on commonJS
- [ ] `00 · 04-js` / `00 · 11-tooling` — **a test file that imports `std`'s `process` loses every cell in it on commonJS, and the loss is invisible in the counts.** Found by rakun front 74; re-measured by me 2026-09-21 on `4fe1747e` with a control. The emitted `const process` **shadows node's global**, so `__bp_run_tests`'s `const filter = process.argv[2]` throws `TypeError: Cannot read properties of undefined (reading '2')` before any test runs. The file prints **no `N passed, M failed` line at all** — not a failure, not a skip; the cells simply are not in the count. The same file with the import removed prints `1 passed, 0 failed`, and erlang passes either way. One mitigation already exists and is worth stating precisely: the run **exits 1**, so a gate catches it — it is silent to the *count*, not to the exit code. That is exactly the distinction that lets it survive in a suite somebody reads by eye
- [ ] `01-std` — **`fs.exists` disagrees about a character device**: `fs.exists("/dev/null")` is `true` on commonJS (`existsSync`) and `false` on erlang (`filelib:is_file/1`). Re-measured on `4fe1747e`. A std divergence, not any library's, and the kind that only shows up in code that touches real paths — which is why a TLS front found it
- [ ] `00 · 11-tooling` — **the fixed-path guard does not fully hold the property it was asked for, and the proof is that a new site went through it within the hour.** Found by the path-race front while reading its own merge diff. `modules/compiler-core/src/codegen/tests/helpers.zig` has three cwd-anchored build roots (`:97` `buildRootPathFromSrc`, `:621` `erlang_test_mode_contains`, `:767` `test_mode_run_log`) — verified by me on `fecec6b4` — and `scripts/check-test-scratch.sh` passes over all three because they sit in **helper functions, not `test` blocks**, and the scanner only tracks literals between `test "` and the next column-0 `}`. Two are pre-existing on untouched `2e6bb4ac`; **the third is new**, added by the `libs/std` front straight through the gap. The front **measured before concluding** and it does not bite today: compiler-core at four concurrent processes is **1917/1917 green on all four**, because those roots hold emitted artifacts, nothing deletes the tree on entry, the bytes are deterministic per slug, and the tests that actually execute go through `makeScratchDir`'s per-run root. So it is a latent instance of the same shape as `lib-test-build/<target>`, on the same evidence. It was **deliberately not fixed on that merge**, with the reason recorded rather than the omission: `buildRootPathFromSrc` is comptime-only (a `SourceLocation` → comptime string) while `test_scratch.path` needs `io` at runtime, so closing it is a signature change in a file another front landed in an hour ago — not work to do inside a merge whose job is to be boring
- [ ] `05-emilia` front 45's spec — **three acceptance claims are falsified, two of them by upstream itself.** The front checked rather than transcribing, and the spec had *told* it to check on the first: (a) `§ 16.6`'s property column is wrong — upstream prints `transform: skewX(<n>deg)` and carries no `--tw-skew-*` row, so the twelve skew leaves emit upstream's property and `skew-x:` is asserted **absent**, with the consequence recorded that the two skew axes therefore do **not** compose; (b) Step 3's translate shape appears in *neither* source — the reference file and upstream both print one `translate:` declaration, and upstream additionally carries the five `translate-y-*` rows the reference omits, so the y mirror is **confirmed rather than interpolated**; (c) Step 6's claim that `§ 16.7`'s four rows "now resolve" is false — they read six `--tw-*` variables that no token in emilia sets. They are transcribed **verbatim** (byte-equality is the gate) and **marked inert** in five places, and the front deliberately gave them no fallbacks, because `.Shorthand.Cpu` would then emit an identity transform that silently overwrites the `skewX` beside it
- [ ] `05-emilia` front 54 / front 56 — **`--tw-*` identity defaults cannot be theme entries**, which falsifies front 45's Step 3 as written: `extendTheme` `@panic`s on a name in no known namespace and `--tw-` is in none of `Ns`'s nineteen prefixes. Front 39's `cssVarOr` is the house answer and was **called, not re-spelled** — every translate row carries `var(--tw-translate-y, 0)`, without which a lone `translate-x-4` is invalid at computed-value time and moves nothing. The consequence is asserted in the direction that is true: `[.TranslateX.Half, .TranslateY.Half]` does **not** compose, two declarations of one property, last wins. Making it compose needs `@property` emission, which is a new output kind and front 56's — recorded as the condition under which the "inert" half of that test should be deleted
- [ ] `00 · 11-tooling` — **`known-red-libs.txt` pins a compiler-repo claim about a library-repo state, and the two repositories are never committed together.** Found by the `libs/std` front on 2026-09-21 while blocked on exactly the adjacency I had been protecting: the rakun dedupe landed in `repository/rakun` at 10:51:33, the compiler-side deletion of the line it retires gated for another half hour, and in between **every** `test-libs` run reported `known reds that pass (delete them from scripts/known-red-libs.txt): rakun·commonJS` and exited 1. No ordering discipline closes that window, because the fix and the deletion live in different repositories and there is no commit that contains both. The earlier instances of the shared-checkout row flapped a *cell*; this one **desynchronises a ledger**, which is worse: the gate is correct, loud, and unactionable by the person who hits it. Two candidate remedies, neither obviously right: pin the line to a submodule SHA so it retires itself when the library moves past it, or move the ledger into the library repo where the state it describes actually lives. The second is a bigger change and probably the honest one **One correction, from the front that found it**: its blocked first run and its ledger-desync second run are *the same event seen at two stages of one fix* — the red before the rakun commit and the green after it, neither of them adjacent to the compiler-side deletion. Recorded as one instance, not two.
- [ ] `03-rakun` front 05 — **owes the boot call that makes front 14 do anything.** Front 14 landed every piece and exercised the call site (`test/config_test.bp` binds a `#[configurationProperties("app.billing")]` + `#[validated]` record, validates it and asserts the key-named refusal text), but `config.bp`'s comment names `rkConfigValidate`, which does not exist, and `modules/rakun/**` is not front 14's. **The debt is one addition to front 05's boot sequence, after binding and before the first component**: `validate<TypeName>(bound)` then `refuseInvalidConfig(typeName, prefix, report)`. Until it lands, a project can declare `#[validated]` on a configuration record and boot with an invalid one
- [ ] `03-rakun` — **`#[validated]` now exists in two packages and one of them does nothing.** Front 05 declares a placement-only marker in `modules/rakun/src/config.bp:1795` ("until 14 lands this is placement only"); front 14 landed the real one in `rakun-validation`. An application must import it from `rakun-validation` and **must not import both names into one module**. The failure shape is the expensive part and is now documented in four places an author passes through: importing front 05's leaves `validate<TypeName>` undefined and the red lands at the **call site as an unbound variable**, nowhere near the annotation. Recommendation: delete front 05's, now that the condition its own comment set has been met — but it is front 05's file and front 14 was right not to touch it
- [ ] `00 · 01-checker` — **an integer literal does not widen to `i64` in arithmetic, and that diagnostic has no location.** Measured by rakun front 14: `pub fn shrink(x: i64) -> i64 { return x - 1000; }` reds `type mismatch: expected i64, got i32` on both rows with **no line and no column — only the file**. The contrast is what makes it actionable: a *comparison* widens (`x > 0` is fine), an `i32` literal in a **parameter** position reds *with* a location, and `val n: i64 = 5;` is fine. There is no `i64` literal spelling at all, so the front's tests build one from the clock. Two defects in one repro — the missing widening and the missing location — and the second is the one that costs minutes per encounter
- [ ] `03-rakun` front 05 — **an `Array<string>` configuration field binds as a scalar.** front 05's `config.bp` comment asserts that `hosts: string[]` reflects as `typeName == ""` "the same as `Array<string>` would"; front 14 measured that `Array<string>` renders **`"Array"`**, and front 05's `#[configurationProperties]` emitter has no `"Array"` arm — so such a field falls through to `rkValueOr`. Correctly not front 14's file. This is the same `@Decl.Field.typeName` gap jhonstart front 29 hit from the other side, where `string[]`, a function type and a tuple type all render the empty string
- [ ] `00 · 02-erlang` — **a record method's owner resolves on erlang only when the type is imported into the *calling* module**, even though the value arrives from a function that did import it. Front 14's repro: `make()` returns a `Box` from another module, and `make().doubled()` gives commonJS `1 passed` and erlang `repro_test.erl:6:12: function doubled/1 undefined` → **no summary line for that module at all**, exit 127. Adding `import {Box} from "shape";` to the test makes it green. The gate does catch this (non-zero exit) but prints no count — the same silent-skip family `.tasks/std-slice-shim` is fixing, and it should be re-measured against this repro once that lands
- [ ] `00 · 15-language-surface` — `pub type X()` with an **empty** field list is refused (`type-empty-field-list`) while `pub type X { …methods… }` with no field list at all is fine. Minor, measured by rakun front 14, listed so it is not rediscovered
- [ ] `00 · 01-checker` — **a *fully qualified* `Shape.Circle(r: 3)` types as `Hole` when both enums declare `Circle`.** Measured by me 2026-09-21 on `ead0b645` with a control: `type mismatch: expected Shape, got Hole`; rename `Hole.Circle` to `Hole.Round` and the identical program answers `9`. **This is the worst member of the flat-bare-name-table family**, because qualification is the documented way to disambiguate and it is ignored — every other symptom in that family has "write the full path" as its workaround, and this one says the workaround does not work. Filed as row 3c of `01-checker` step 12 and made the step's gate. (The commonJS front reported a neighbouring shape — the same two enums making a `case` over a `Shape` red as "`case` on 'Hole' is not exhaustive" — which did **not** reproduce in my arrangement; the `case` alone compiles. Recorded as theirs, unconfirmed in this shape, rather than merged into mine)
- [ ] `00 · 01-checker` — **a local `val` leaks into the module scope of every top-level declaration after it.** Found by jhonstart front 29, verified by me 2026-09-21 on `ead0b645` in two shapes. Bare: `fn holder() { val v = "inner"; return v; }` followed by `fn later() { return v; }` **compiles**, then fails at run time on commonJS (`ReferenceError: v is not defined`) and at `erlc` on erlang (`variable 'V' is unbound`) — the checker should have refused it and instead handed both backends a program naming nothing. Shadowing, which is the form that actually bites: `val p = Thing(n: 3)` inside one function makes `p("x")` in the **next** function type as `Thing` — `type mismatch: expected string, got Thing` — and in a `#[@context]` body front 29 got the same error **with no line and no column**. `p`, `a`, `li`, `text`, `form`, `link`, `title` and `body` are all exported tag constructors in that package, so any file there is one declaration order away from it. Repro committed at `repository/jhonstart/repro/local-binding-leaks-to-later-decls/`, twelve lines and jhonstart-free
- [ ] `00 · 01-checker` / comptime — **`@Decl.Field.typeName` erases the element type and is empty for `T[]`, function and tuple types.** Measured by front 29 over one record with a field of each shape: `Array<string>` and `Array<#(string,string)>` both answer `"Array"`, while `string[]`, `fn(x: i32) -> i32` and `#(string, string)` all answer the **empty string**. The consequence is a real narrowing of a shipped surface: front 29's spec asked for six whitelisted prop types and only four are expressible, because `"Array"` cannot be admitted without admitting `Array<Element>` and `Array<fn…>` through the one check that exists to refuse them. Under decision 67 the refusal won. This widens the front's own language-gap row from "no parameters on `Decl`" to "**no element type on `Field`**"
- [x] `00 · 15-language-surface` — **a negative integer literal cannot be written as a decorator argument**, and the diagnostic blames the wrong token. Verified by me 2026-09-21 on `ead0b645` with a control: `#[mark(20)]` compiles and runs; `#[mark(-20)]` reds `this token cannot appear here · unexpected ``20``` with the caret on the **digits**, not the minus — so the parser consumed `-` as something else and then reported the number. Found by rakun front 07, for which it is not cosmetic: **every order in its spec's band below zero became unwritable**, and the front shipped `#[order("-100")]` parsed through front 05's `toI32`, saying plainly that the deviation is forced rather than chosen. When the minus parses, the parameter goes back to `n: i32` and the `toI32(` wrapper is deleted; nothing else moves — **landed** in `727813e5` (15 step 4b): `#[mark(-20)]` is one argument and the decorator receives `-20`
- [x] `00 · 15-language-surface` — **a lambda body inside `loop` needs a semicolon the block shape says it should not.** `loop (xs) { x -> @print(x) };` reds `this token cannot appear here`; `loop (xs) { x -> @print(x); };` compiles. Verified on `ead0b645`. It cost rakun front 07 three compiles, and it sits next to the standing `docs.md` row that a block-shaped statement should end itself — **landed** in `727813e5` (15 step 4b): the loop body and the trailing lambda take `requiredExceptLast`
- [ ] `00 · 01-std` — **`asserts.throwsWith` cannot match a needle containing `"` on erlang.** Measured by rakun front 07 over one `@panic` whose text contains `withHeader("Set-Cookie"`: the needle with a bare quote passes on commonJS and fails on erlang; the needle with the quote **escaped** does the reverse; a quote-free needle passes on both. On erlang the harness reads the `~p` *rendering* of the raised binary, where `"` is escaped — the same family as front 62's em-dash note. The front made every refusal text in its member quote-free, which is a workaround in library source and should not have to be
- [ ] `03-rakun` front 04 — **graceful shutdown's hard half is the listening socket in `rakun_runtime.erl`**, which is front 04's, not front 07's and not front 76's. Front 07 established the distinction rather than reaching into `modules/rakun/`: front 76's `readinessDrained()` is the soft half its own spec says to land without, and "close the listening socket, keep connection processes alive" cannot be written from `rakun-web` at all. Routed here
- [ ] `00 · 11-tooling` — `scripts/restricted-targets.txt`'s `rakun-web commonJS 0` line keeps its pinned **0** — correct, since only the failed count is pinned and all 83 new tests pass — but its reason text now says "no tests yet", which is false. A prose-only edit, to be made in the same commit as any other ledger change rather than on its own gate
- [ ] `00 · 01-checker` — **a fully-labelled self tail call hangs.** `sum(acc: acc + n, n: n - 1)` becomes `sum(acc + n, n - 1)`, and `TailRewrite.step` then assigns argument *i* to parameter *i*, so the loop never terminates. The sharpest instance yet of the standing generalisation that *any* position reading something by argument index is wrong once a label can claim a parameter. Two more from the same sweep: `diff(b: 1, a: 10)` answers `-9` on all three backends where `docs.md` demands `9`, and `"abcdef".slice(end: 3, start: 1)` gives an empty line on commonJS, `function_clause` on erlang and an out-of-bounds trap on wasm. The checker types by position too (`P(y: "a", x: 1)` reds `expected i32, got string`), so a backend that re-ordered alone would type against one parameter and pass another — which is why the fix is the checker's and not four backends'
- [ ] `00 · 02-erlang` — a labelled **enum-variant** argument is placed nowhere on erlang (beam's half landed with `00 · 03-beam`) (`erlang.zig` places a labelled *record* argument by field index and a variant's not at all), so `Shape.Rect(height: 2, width: 5)` is `205` there. Both carry an `expected-failures.txt` line naming the cause rather than the symptom, each re-measured with the feature removed
- [ ] `00 · 05-wasm` — with every `#[@<effect>]` and `yield` stripped, **a plain record method looping over its own `Array<i32>` field still answers `0`**, and a plain method declared in an enum's body still traps `unreachable`. Nothing to do with effects; found while giving `run/effect_method.bp` its expected-failures line, and the line names this rather than the effect
- [ ] `00 · 11-tooling` — **the fixed-path race is fixed, gated green three merges deep, and queued behind one other branch** (worktree `.tasks/test-path-race`, `fix/test-path-race` at `e6576282`; held one more cycle so the extra merge falls on the branch whose merges are conflict-free rather than on the one reconciling `codegen/erlang.zig` by hand). Final numbers, re-measured on the merged tree rather than carried: `compiler-cli` was red in **5 of 5 trials at N=2 and 5 of 5 at N=4** on `2e6bb4ac`, with up to five failures per process; it is now green at N=1, 2, 4 **and 8**, and `language-server` green at N=4. `zig build test` 22/22 steps, **2286/2286**. Three of the brief's details were mine and wrong — `compiler-cli` is 89 tests not 30, `new.zig` and `cli/build.zig` hold no test scratch paths at all, and the defect was never compiler-cli's alone (`language-server`, `lib-test-runner` and `bpmp` red identically at four processes), so the scope I wrote would have left `zig build test` red at N=2. The guard is two-sided with no flag: a new fixed-path site **does not compile**, and `scripts/check-test-scratch.sh` — a dependency of `zig build test` — refuses a `.botopinkbuild`-prefixed literal inside a `test` block with no skip list. Two of the three other named writers fell with it: `runtime.zig` truncated-then-wrote a cache whose reader accepts anything beginning `OK:`, so **a reader arriving mid-write got a truncated tail returned as the program's own output, silently**; and `.botopinkbuild/tmp/persistent_erl` handed two `erl` children one log, so the transport diagnostic pointed at a mixture of both. **It also caught a prediction of mine**: I told it to expect `test-libs: 41 passed`; it measured **46**, checked why before reporting (five cells the dedupe unblocked, 0 failed, 0 known red, restricted 18 → 19 with no pinned number moving) rather than quoting my number back
- [ ] `00 · 08-hygiene` — `modules/compiler-core/src/comptime/tests/effect_generator.zig` fails `zig fmt --check` on an **untouched** base, found by the path-race front while gating. Pre-existing and unrelated to it; listed so that the next front to trip over it does not spend the same minutes deciding whether it caused it
- [ ] `00 · 01-checker` — **a fifth symptom of the flat bare-name table, and the worst-worded one yet.** Measured by emilia front 41 against `2e6bb4ac`: `val h: Token = .Color.Hex("#abc")` reports `'Hex' is not declared in any behavior implemented for 'Ns'` — the leading-dot resolver walked `.Color` into front 54's **`Ns`** enum, which has a `Color` member, instead of into the annotated type `Token` that the author wrote one token earlier. The diagnostic names a type the author never mentioned, in a file where `Ns` is incidental. Add it to `01-checker` step 12's table; it is the same root as `.Red` colliding, the section leaf with no shorthand, and the leaf shadowed by a top-level variant
- [ ] `00 · 01-checker` / `00 · 15-language-surface` — **a diagnostic that names the empty string.** Two separate forms produce `unbound variable ''` — a leading-dot path to a nested payload leaf on a bare enum, and a leading-dot path plus a payload call inside a typed array literal (`[.EffectShadowRaw("…")]`, reported at the open paren). Whatever the right answer for those forms is, a message quoting an empty name cannot be it, and both were re-measured on `2e6bb4ac` by emilia front 41 — `language-gaps.md` rows 51 and 52 carry the **stale** diagnostics for both, which is how a reader would fail to recognise the form they are looking at
- [ ] `00 · 04-js` — **commonJS cannot run a leading-dot variant at all**, with one enum in the file and no collision anywhere: `holeOf(.Circle)` emits `holeOf(Circle)` and throws `ReferenceError`. Found by the cross-module front, which is why its ambiguous cell is erlang-only. Sits directly beside `x is Enum.Variant` and the section-leaf shorthand — the same leading-dot form, failing at a different stage
- [ ] `00 · 05-wasm` — **wasm links every module into one flat namespace**, so two `parse` definitions reachable from one program collide there whatever any index says. The cross-module fix works around it by leaving the second module imported by nobody in its cell; closing it is wasm's own linking model, not a table
- [ ] `00 · 11-tooling` — **I over-parallelised, and the gate is the serialisation point nobody costed.** 2026-09-21: eight worktrees open at once on a 16-core machine drove load to 42–75 with 107 compiler processes, five `scripts/gate.sh` runs concurrent, and the leading pre-commit gate advancing at roughly **1.5 library cells an hour out of ~54** — six hours in and still inside emilia. No work was lost and nothing was killed; the four newer gate trees were `renice`d to 19 so the oldest finishes first, which costs nothing and is reversible. The durable lesson is a number: `gate.sh` runs the **whole** library suite, so N compiler worktrees do not cost N× — they cost far more than N×, and beyond roughly two concurrent gates the machine spends its time context-switching. Two remedies are worth having, neither of them "open fewer worktrees": a **gate lock** so a second `gate.sh` waits instead of competing, and a pre-commit that runs only what the commit's diff can affect. Recorded as the front's row because it is a tooling defect, not a scheduling mistake I can promise not to repeat
- [ ] `03-rakun` — **two `pub fn` declared twice in the same package, byte-identical, and which copy bound was walk order.** `modules/rakun/src/config.bp` and `src/file_router.bp` both declare `pub fn sub(string, i32, i32)` and `pub fn charOf(string, i32)`; `test/config_test.bp` imports with the bare `import { … };` form. Found by the cross-module fix, whose refusal fires on it: measured with the refusal reverted, **302 passed / 0 failed**; with it, `1 module(s) failed to compile: config_test`. It is two one-line fixes in rakun and it is rakun's to make — banked in `known-red-libs.txt` against `03-rakun` so the compiler fix is not held hostage to a sibling checkout. **This also corrects a claim I made earlier today**: the red-by-permission ledger is no longer empty, and the reason it gained a line is the healthiest one available — a new rule found a real duplicate that had been silently working
- [ ] `00 · 01-std` — **refusing duplicate `pub` *declarations* was measured and rejected**, which is worth recording because it is the obvious design and it is wrong: `libs/std` already exports **thirteen** duplicated names — `parse` in `json`, `querystring` and `url`, plus `abs`, `round`, `trunc`, `exit`, `float`, `arch`, `empty`, `eq`, `matches`, `normalize`, `stringify`, `fromList` — every one consumed qualified and none ambiguous in practice. The refusal therefore belongs to the **consumer's** unqualified use, keyed by the importing module, not to the declaration
- [ ] `00 · 15-language-surface` / `00 · 01-checker` — **`from` is a reserved word everywhere, and the field diagnostic actively misleads.** Measured 2026-09-21 against `2e6bb4ac`: `pub type Edge(from: string, to: string)` reds `error[field-needs-name]: a field with no name · write ``name: Type`` here` — pointing at `from`, which *is* a name — and `fn openerAfter(s: string, from: i32)` reds `this token cannot appear here · unexpected ``from```. Found by rakun front 72 writing a dependency graph, where `from`/`to` is the obvious pair; it renamed to `earlier`/`later` rather than bend a rule. Two separable questions: whether an import keyword needs to be reserved in *field and parameter* position at all, and — regardless of that answer — a diagnostic that tells the author to write the name they already wrote is wrong either way
- [ ] `00 · 02-erlang` / `00 · 11-tooling` — **the silent-skip half now has two independent measurements.** rakun front 72 hit it from a different direction than the `std/querystring` front did: `println(line)` (and `print`) has no erlang lowering, `erlc` refuses the emitted module, and the runner's `__bp_load_siblings/0` (`codegen/erlang.zig`) ends its case in `_ -> ok` — so the compile failure is skipped **silently**, the whole module is absent, and *every* function in it answers `{error,undef}`, pinned to whichever caller ran first. The front reported it as "the only real debugging detour of the front", which is the cost measured in hours rather than in cells. Worktree `.tasks/std-slice-shim` owns the runner half; this is the corroboration, from a module that has nothing to do with `libs/std`
- [ ] `00 · 01-checker` / `00 · 08-hygiene` — **a behavior-typed parameter refuses a conforming value, and the boundary is not the variable.** Measured 2026-09-21 against `2e6bb4ac`, three ways: `pub behavior Greeter { fn greet(self: Self) -> string; }` with `pub type Bob(n: string) implement Greeter { … }` and `fn useIt(g: Greeter)` reds `type mismatch: expected Greeter, got Bob` **in a single module**, and `expected behavior Greeter { fn greet(self: Self); }, got Bob` from a sibling `mod` *and* from a package `path` dependency — the same refusal on commonJS and erlang in all three. So the existing cross-module-behavior row is not about crossing modules: the form fails at zero distance too. **And that may be correct.** `docs.md` shows `implement Display` only for `@print` dispatch and **never shows a behavior used as a parameter type**, so it is not documented whether a behavior-typed parameter is a supported form at all, or whether the intended spelling is a generic bound. That question has to be answered before the refusal can be called a defect — which makes the first move a documentation one, not a compiler one. Recorded with the false start attached: the first attempt used `implement Bob as Greeter`, which no `.bp` in the tree spells, and the resulting parse error was reported only on the *package* path, not the sibling one — so a dependency's parse error can be masked by an import failure, which is a finding of its own **Second, independent measurement 2026-09-21, from rakun front 14**, which hit it writing a real SPI: same refusal, plus a variant mine did not cover — `fn f() -> Greeter { return En(…); }` reports `expected behavior Constraint {` with **the behavior body printed and truncated**. The front shipped `registerConstraint(name, code, check)` instead of the spec's `(name, c: Constraint)` and kept the `behavior` declared so application types still `implement` it and the shape stays compiler-checked. Two fronts meeting the same wall from different directions moves this from "possibly an undocumented non-form" to "a surface the specs assume exists" — the documentation question stands, but it now has a cost attached
- [ ] `01-std` step 6 · `01-std-lib-enablement` — **Steps 1–13 landed** (13 on `front/01-std-libs`, see § Done); earlier **in flight** in worktree `.tasks/01-std-lib-enablement` (`front/01-std-lib-enablement`), one gate-green commit per step, files landing flat at `libs/std/src/<name>.bp` (front 23 moves them). Landed: step 1 `escape.bp` (`3e8fe19d`, 9 tests), step 2 `path.withoutExtension`/`isInside` + `fs.walk`/`glob` (`c1b96644`, 8 tests), step 3 `encoding.bp` (`649bb8f7`, 10 tests), step 4 `hash.bp` (`26380757`, 5 tests), step 5 `time.bp` calendar half (`e5ef1566`, 8 tests; rakun commonJS on the known-red ledger — its tests bare-import rakun's own `Duration`), step 6 `random.secureToken`/`uuidV4` (`4566d775`, 3 tests; after merging compiler `feat` with front 22's loops), step 7 `regex` captures/compile (`811aa3a3`, 6 tests), step 8 `process.run`/`runShell` (`3b7cbee8`, 4 tests; `onSignal` left out — closure-to-host gap), step 9 `net.bp` (`f8ddf3c5`, 6 tests: erlang self-connect, timeout, refusal, TLS against a throwaway CA; commonJS asserts all sixteen refusals), step 10 (`4d2d1796`: a scratch consumer importing `{escape, encoding, hash, net, time, random, regex, path, fs}` from "std" is green on both targets after `encoding` stopped using a one-argument `slice`, which has no erlang shim when std is a dependency; `import {process}` shadows Node's global `process` in the commonJS test runner — a codegen defect. The `io: {net, clock}` import and the `io/net.bp` tree line closed with front 23's move); both rows green. Measured on the way: an integer literal is `i32` and never widens (`Duration(millis: n * 1000)` with `n: i64` is refused — the builders take `i32` and cross through a private identity cell), `any` admits no literal (a `Regex`/`Socket` a test builds by hand takes its handle from a cell answering `null`), a non-ASCII string literal reaches erlang as latin1 (`<<"\x{e9}">>`) and a codepoint above U+00FF is `illegal character` — three rows for `language-gaps.md`
- [ ] `01-std` step 6 — **the three copied std fronts, and they are what track C is waiting on.** Ordered 02 · 03 · 01 by the step's own text. **`02-std-async-primitives` is written and gated** on `std/02-async-primitives` (`fe5a35ad`, row in § Done) and waits only on its merge into `feat`; it carries its own `pub mod async;` line. Still to open: `03-std-content-hash`, then `01-std-lib-enablement`, which carries `escape` **and** the `root.bp` commit for the rest. `escape` is the one with the clearest debt: `libs/std/src/` carries twenty-four modules and `escape.bp` is not among them on any branch, while `01-std/modules.md`, the thirty-three-module `root.bp` in `01-std/README.md` and `test-snap.md:194` all name it down to the snapshot filenames. jhonstart 30 wants `async`, 31 wants `content_hash`, 32 wants `escape` — the whole of track C's remainder is these three. Front 28 already banked the cost of the `escape` gap the right way: it pins the **unescaped** answer, so the day `escape` lands the change shows as a red cell rather than a silent pass
- [ ] `00 · 02-erlang` — **a bare function name used as a value is an unbound erlang variable, and it takes the whole module with it.** Measured by jhonstart front 28 and re-measured by me 2026-09-21 against `2e6bb4ac`: `fn one() -> i32 { return 1; }` with `fn apply(f: fn() -> i32) -> i32 { return f(); }` called as `apply(one)` prints `1` on commonJS and fails `out/erl/main.erl:12:25: variable 'One' is unbound` on erlang — a **compile** failure, so the lambda form `apply({ -> one() })` in the same module never runs either and the member leaves the erlang row entirely. Nothing to do with `@Future`; the lambda spelling is the only one that lowers, and front 28 used it everywhere rather than working around it in library source. This is the same family as the `fn`-typed record field called across a module boundary, and probably the same fix
- [ ] `00 · 01-checker` / `00 · 04-js` — **a single-target `#[@External]` refuses at the *call*, in both directions, and a declaration alone is free.** Measured 2026-09-21 against `2e6bb4ac`, all four cases: an `#[@External.Erlang(…)]` cell wrapped in `pub fn method() { return hostMethod(); }` reds the **commonJS** compile — `` `hostMethod` has no `#[@External.<Target>(…)]` for the node backend ``, pointing at the wrapper's body — and the mirror is exact: an `#[@External.Node(…)]` cell wrapped the same way reds the **erlang** compile at its call site. But the same declaration **never called** compiles clean on the foreign target. So the rule is not "a foreign declaration poisons the member"; it is "a statically present call does", even a call no program on that target can reach. Refusing is right under decision 67. The cost is that one accessor nobody on the other row calls takes a whole member off that row: fronts 26, 27 and 28 each had to ship dual-target cells, and front 27's `linkStatus()` cannot ship at all because its wrapper would take all 103 assertions off erlang. The question worth answering is whether a wrapper whose body is only a foreign call can inherit that cell's target restriction instead of failing the member — that is a design question, not a patch
- [ ] `01-std` step 5 — `onze13` → `onze` name takeover (decision 79). **Step 4 is done** (`std/mocks` landed with the old library's six module globals as `globalThis.__bp_mocks`, nine tests green on both targets), so this step now waits on exactly one thing: the maintainer tagging and archiving the old repo. `repository/onze` today still holds the mocking library — `botopink.json` says "Mockito-style mocking + verification for botopink unit tests" — and front 49 **owns that repo's `botopink.json`, `root.bp` and `src/**`**, so no thread can start it without destroying a library the maintainer has not retired. **This one maintainer action is the widest block in the milestone**: track E's nine fronts all wait on 49, front 68 waits on 49, and front 68 is what jhonstart fronts 27 and 29 wait on — front 27 landed this hour with five named holes that are all front 68's, and rakun fronts 22–25 are building for a consumer that does not exist yet. Nothing else in track E is blocked by anything technical
- [ ] `02-packaging` step 2 (the `-test` members) — jhonstart, emilia, rakun and erika have theirs (empty `pub` surface, one inline test); onze's comes with the takeover (decision 79 — the maintainer's tag, archive and re-point; front 95 step 2 has the commands)
- [ ] `02-packaging` — an example's dependency on **another** library is still `{ git }` or a path into that library's old layout: emilia's `emilia-card` → jhonstart must become `{ "path": "../../../jhonstart/modules/jhonstart" }` once `.tasks/jhonstart-workspace` lands
- [ ] `00 · 10-cli-residuals` (the tail) — `shipErlSidecars` still `orelse continue`s an `.erl` a library names but does not carry (telling a missing sidecar from an OTP call such as `lists:map` needs a rule nobody owns), and two `catch continue`s in `libs.zig` (the re-read of emitted JS at `:752`, the `.erl` read at `:857`) are silent in the same way
- [ ] `00 · 01-checker` / `01-std` — a std module's decorator cannot be reached from a consumer: `#[mocks.mock]` **parses** (the annotation name lands as `"mocks.mock"`) but a `from "std"` import registers nothing in `env.decorators` (`comptime/infer.zig` `markStdImports` marks the module and its types only), so the marker silently does nothing and the consumer gets `unbound variable`; and the bare fallback is not expressible either — `import {mock, when, eq} from "std"` is refused with `unknown "std" module in import`, because the last segment is read as a module name. Closing it needs **two** changes (register a std module's `@Decl`-shaped `pub fn`s under `<mod>.<fn>`, and self-qualification inside a std module), and the lookup fix alone would be worse than the refusal — the marker would fire and emit code that does not compile
- [ ] `00 · 04-js` (a third pair, found by the erlang module-load front) — commonJS does not resolve `\"` inside an `@External.Node` template (the backslash reaches the emitted JS verbatim and the module dies with `SyntaxError`; erlang resolves it), and commonJS **drops the receiver on a `@Behavior(…)` literal's method** — it builds `greet: (self, who) => …` and calls it `g.greet(who)`, so `self` binds the first argument; erlang's two halves agree with each other
- [ ] `00 · 05-wasm` — wasm drops a `_`-named top-level statement (its named `val` is already once-at-load); beam's half landed with `00 · 03-beam`
- [ ] `00 · 04-js` / `00 · 02-erlang` / `00 · 05-wasm` — **`x is <Enum>.<Variant>` is wrong on every target measured, each differently, and only one of the three is loud.** Re-measured 2026-09-21 against `2e6bb4ac` on `pub type Token { Text(v: string), Num(n: i32) }` with `val x: Token = Token.Text(v: "hi")`: **Re-confirmed on `ead0b645` after the cross-module fix landed**, unchanged on all three — that fix moved the *cross-module* name tables and left the in-module ones alone, which is exactly what its own report said it was leaving to `01-checker`.
  - **commonJS** throws `TypeError: Function has non-object prototype 'undefined' in instanceof check` — `isTest` lowers the dotted path to `instanceof Token.Text`, and `Token.Text` is the enum's static *factory function*, whose `.prototype` is `undefined`;
  - **erlang** answers **`false` for `x is Token.Text` and `false` for `x is Token.Num`** at exit 0. The first is a **silent wrong answer**, which is worse than the commonJS throw and was not recorded before;
  - **wasm** answers `true` for `Token.Text` and then **traps** on `Token.Num`.
  The bare forms are fine on all three (`x is Token` and `o is Other` for a record answer `true` on commonJS, erlang and wasm) — a top-level enum is exported and required, so there is one copy. `buildPatternCheck`'s `.variant` arm still emits `instanceof <bare variant name>`, which names no class any module emits — latent, because the checker refuses `val assert Shape.Circle(r) = s` today. And `charCodeAt` has no wasm or beam lowering at all: `@print("A".charCodeAt(0))` traps on wasm with `unreachable`. **No owner** — and the three-target spread means it is one front, not three **Re-measured 2026-09-21 on `ead0b645`**: `@print("A".charCodeAt(0))` answers `65` on commonJS **and erlang** and traps on wasm — so the erlang half of that sentence is no longer true and only wasm (and beam, unmeasured here) is missing it.
- [ ] `00 · 02-erlang` — **a `return` inside a `loop` body does not leave the function.** `fn firstUnder(n, limit) { loop (0..3) { k -> if (n + k > limit) { return firstUnder(n - 1, limit); }; }; return n; }` called as `firstUnder(12, 10)` answers `8` on commonJS and wasm and **`12` on erlang**, exit 0 everywhere. A silent wrong answer, found by the commonJS tail-call front, which could not write it as a language cell for that reason. Re-measured 2026-09-21 against `2e6bb4ac`: **still live**, `8` on commonJS and `12` on erlang, exit 0 on both
- [ ] `00 · 11-tooling` — the shared-checkout race now has **three independent measurements** and a diagnosis precise enough to act on: `botopink-lib-test` runs `botopink test` with `cwd` set to each library's own directory **inside the shared `repository/<lib>` checkout**, so two worktrees gating at once write the same `out/`, and a half-written erlang module set is exactly the `{error,undef}` storm that then looks like a library red owned by nobody. C-02 ruled itself out three ways (emilia contains no index expression at all; a throwaway worktree at plain `origin/feat` passed; and its own tree passed whenever no other gate ran) and saw `emilia·erlang` fail twice and pass four times on the identical tree. **A fourth, and it is a different order of magnitude: on 2026-09-21 a *library front's merge broke an unrelated compiler front's gate*.** I fast-forwarded rakun's `feat` to `af933f77` at 11:09:59 while the `libs/std` front's `test-libs` stage — started 11:01:33 — was reading `repository/rakun`. It came back `rakun-web·erlang` failed and `rakun-web·commonJS` moved `0 → build`, with `Cannot find module './filter.js'`: the checkout changed under the scan. The front **stopped before committing** and eliminated itself three ways — it replaced `codegen/erlang.zig` with feat's own copy and saw the same failure, it grepped its new refusal string and found **zero** occurrences in the whole gate log so its code never ran, and it noted that commonJS was affected at all, which its erlang-only change cannot touch. I then measured `rakun-web` on both rows at the stable tip: **18 + 8 passed, 0 failed on each**. So there was nothing to fix and no ledger line to move, and the cost was one wasted gate cycle plus the front's time proving a negative. Until now this row held three sightings of one library cell flapping; this is the same mechanism with a blast radius that reaches across repositories, and it is the argument for the remedy being a **lock or a private checkout per run**, not a retry
- [ ] `00 · 02-erlang` — **the mirror of the commonJS collision, pre-existing.** `patternNodeExtra` asks `enum_variants` first *unconditionally*, so a `case` over a union of **records** whose arm names a record that some enum also declares as a variant matches the variant atom and dies: `{{case_clause,{main__t__block,3}}, …, main, shapeName, 1}`. commonJS no longer shares it after `7cae3071`; no cell covers it
- [ ] `00 · 05-wasm` — **the section-variant half of the same family.** `findVariant` takes a bare name from "every enum, first match wins", so a section whose name also names a record (`Size { Small, Large }` beside `type Size(px: i32)`) makes `.Layout.Size.Large` answer its parent's *first* arm — `display:block` where commonJS and erlang answer `font-size:large`. Deliberately kept out of the new cell so it stays green on four targets; the cell header says why
- [ ] `00 · 11-tooling` — **two process hazards the merges exposed, both of which present as someone else's red.** First: `git` runs `pre-merge-commit`, not `pre-commit`, on a merge, so **an auto-merge is never gated** unless the author amends it — several threads discovered this only because they amended for an unrelated reason. Second: `zig build test-libs` writes into the **shared** `repository/<lib>/` checkouts, so two branches gating at once clobber each other's `.botopinkbuild/test-out/` and the loser reports `MODULE_NOT_FOUND` on a library cell owned by nobody; measured tonight as `rakun·commonJS` missing three emitted test modules, green immediately on a re-run. Until both are closed, the rule is: run the gate explicitly after a merge, and re-run a lone `test-libs` red before believing it
- [ ] `00 · 05-wasm` (D1) — **a narrowed optional name is never unboxed.** Found by the narrowing front and, crucially, measured on **both** compilers so the blame is unambiguous: `val k: string = first.key;` inside `if (first != null)` printed `abc` on commonJS and erlang and **`272`** on wasm at exit 0 *before* narrowing landed. What the narrowing front changed is only the failure's volume — the same lines used to TRAP (`unreachable`, exit 134) because a field read off a fresh type variable lowered to `unreachable`, so a loud failure became a quiet wrong answer with the same missing unbox underneath. `if (es.at(0)) { f -> f.key.length() }` answers `3` on wasm, because that form does unbox
- [ ] `05-emilia` / `03-rakun` — workarounds that can go now that `if (x != null)` narrows: emilia's `??`-with-a-dummy-record at `modules/emilia/src/output.bp:569`, `src/emilia.bp:5102` (`firstRule`'s fallback `Rule(…)` exists only to feed `.at(0) ?? fallback`), `src/theme.bp:705` and `src/emilia.bp:7473`; and rakun front 22's carried row 7, whose note that an optional must be read through `if (matchPath(…)) { m -> … }` is no longer the only spelling
- [ ] `05-emilia` fronts 37/47 — four leaf sets declared **by interpolation and recorded as unconfirmed**: `grid-cols-7…11`, `col-span-3…12`, `col-start-2…13` and `order-3…12`. The spec asks for them to be confirmed against upstream before merge and the front had no upstream to read from; it said so in `AGENTS.md` and the CHANGELOG rather than presenting a guess as a transcription
- [ ] `00 · 11-tooling` — **a quiet auto-merge is more dangerous than a loud conflict.** emilia front 39's merge had three conflicts, all easy, and the damage was in a hunk git resolved silently: the workspace refusal message in `AGENTS.md` lists every member, and git took one side's copy whole, dropping the member the other side had added. The front noticed and restored it **from the compiler's own output** rather than retyping it. Any document that enumerates something the tool can also print should be generated or checked against the tool, not merged as prose
- [ ] `00 · 01-checker` — **a section leaf has no shorthand against a section type.** `val x: Token.<Section> = .Leaf;` resolves for **no leaf at all** — measured with controls by emilia front 39: `.Block` reds `expected __Token__Layout, got function`, `.Grid` and `.Hidden` red `unbound variable`. A record sharing the leaf's name only changes the error **text**, because its constructor is what is in scope — and that differing message is exactly what made a since-disproved "record shadows leaf" diagnosis look corroborated from a second direction. A section leaf must currently be written from the enum root. **Re-measured 2026-09-21 against `2e6bb4ac`, isolated, and the two messages are one defect**: with `Token { Layout { Break { Zeta, Omega } } }` and `fn breakToCss(b: Token.Layout.Break)`, the call `breakToCss(.Zeta)` reds `unbound variable 'Zeta'` on commonJS and erlang alike — there is no collision anywhere in that program, so the shorthand simply does not exist for a section leaf. Add a **top-level payload variant of the same name** (`After(inner: i32)` beside `Layout { Break { After, Before } }`) and the same call reds `type mismatch: expected __Token__Layout__Break, got function` instead: the shorthand resolved through the flat bare-name table, found the top-level variant's *factory function*, and reported a type problem for what is a resolution problem. That second message is the misleading one, and it is the same flat table under the enum-path family and the two-enums-declaring-`Red` collision — one root, four symptoms Re-confirmed on `ead0b645`: `unbound variable 'Zeta'`, unchanged.
- [ ] `00 · 11-tooling` — **the shared compiler binary is a stale-baseline generator, and it has now cost work twice.** Every library thread measures against `repository/botopink-lang/zig-out/bin/botopink`, rebuilt by hand. First occurrence: emilia front 39 measured a defect that was already fixed, worked around it in library source and wrote an `AGENTS.md` rule from the workaround — all of it reverted. Second: an erika cleanup measured 1/8 where the fix says 9/0, **stopped before committing** as its brief required, and diagnosed the cause by byte-scanning the binary (`__bp_method` absent, `__bp_field` present) rather than reporting "the numbers are wrong". The second is what the first would have looked like with a verification gate in front of it. The remedy stands: a library run should refuse to start when the binary is older than the pinned submodule — the measurement each thread takes is only as good as the binary under it, and neither thread could have known. **Interim remedy, applied 2026-09-21**: the binary at `2e6bb4ac` was copied once to a pinned path and every library thread opened since is told to use that path *and no other*, with the standing instruction to stop and report rather than work around anything suggesting it is out of date. That is the shape the real gate should take — a pin plus a refusal, not a reminder. **Note from the same day**: `feat` moved to `ead0b645` while three library fronts were mid-flight against the pin at `2e6bb4ac`. The pin was deliberately **not** moved — a baseline that changes under a running measurement is the whole defect this remedy exists for — so those three fronts' numbers are true of `2e6bb4ac` and have to be re-measured on the new tip before anyone quotes them against it
- [ ] `00 · 02-erlang` — **`Array.lastIndexOf` does not lower on erlang**: `function lastIndexOf/2 undefined`, a hard error invisible until the second target runs. Found by emilia front 40, which drew the standing conclusion with it: run `--target erlang` **before every commit**, not once at the end of a front. Re-measured 2026-09-21 against `2e6bb4ac`: still `out/erl/main.erl: function lastIndexOf/2 undefined`, and still invisible until the second target runs
- [ ] `05-emilia` — a spec-writing rule the fronts keep paying for: **an emission column written from memory should be marked provisional inline**, not only listed in a *Reference gaps* table at the bottom. Front 40 found two rows wrong that way — the ring shadow string was v3's `calc(…)` where v4 writes `0 0 0 Npx` with a 1px default, and the colour sub-sections are five, not four (`Ring.Offset.Color` is omitted) — and in both cases the front's own Reference-gaps table is what caught them, which reads as a to-do rather than as a warning that the rows above it may be false
- [ ] `00 · 11-tooling` — `botopink test` prints one summary **per module**, so the last line is the last module's count and not the run's total. Two fronts nearly reported a module count as a suite baseline tonight, and one said so explicitly. A run total, or a label on each summary, would remove a whole class of mis-reported numbers
- [ ] `00 · 09-ecosystem-residuals` / no scheduled owner — **`erika-linq` has 8 red erlang tests**, the largest thing any restriction was hiding, and erika has no track in this milestone: `09-ecosystem-residuals` keeps `repository/erika/**` as residue, so the ledger line names it, but nobody is actually scheduled to fix it
- [ ] `03-rakun` front 22 / `04-jhonstart` — **a dynamic-marking rule that cannot hold as written.** Front 23's spec says the marking "is done by the accessor, not by a developer remembering to declare it"; `searchParams(route)` marks, but `route.query` is a **public field** of front 22's `PageContext` and a direct read cannot be intercepted — so the rule holds for everyone who uses the accessor and for nobody who does not, which is the shape the sentence exists to rule out. Front 23 measured it, declined to close it by reaching into front 22's file, and recorded it as a hole for front 26. Closing it means dropping or privatising `query` on `PageContext`
- [ ] `00 · 02-erlang` / `00 · 04-js` — six shapes front 23 measured while writing a real renderer: a **function-valued record field must be read into a local before it is called** (`v.tagOf(e)` lowers to a method call — `function tagOf/2 undefined` on erlang, silent on commonJS); a local `val` **or a parameter** may shadow a module-level `pub fn` of the same name *for an importer*; `xs.at(i).unwrapOr(…)` reads the element back **unwrapped** inside a function generic in its element type; **`await` inside an `if`/`else` of a `#[@future]` body is emitted in a non-async arrow** on commonJS, so node refuses the whole file and kills every cell in it rather than one; a parameter typed `Array<fn() -> @Future<El>>` beside a second parameter generic in the same `El` is refused `generic-arg-skip-forbidden` while each half compiles alone; and **`std/querystring` does not compile on the erlang row** (`function slice/3 undefined` at `stripPrefix`), so every cell reaching it answers `{error, undef}`
- [ ] `00 · 02-erlang` — **a `fn`-typed field called across a module boundary lowers to an undefined local.** `c.set(5)` on a record whose `set` is a `fn(next: T)` field lowers correctly **inside the declaring module** (`(element(3, C))(5)`) and to a bare undefined call from any consumer — which every consumer of a hook library is by construction; commonJS is green on both sides. The site: `collectTypeShapes` records function-typed fields for the module's **own** `type` decls, while `collectImportedTypes`' `.record` arm copies `info.fields` — **names without types** — so the fn-typed arm never fires for an imported record and the call falls through to a plain local. The field *read* resolves, so the shape crosses and the field types do not. Self-contained repro with no jhonstart, no generics and no `@Context` in it: `repository/jhonstart/repro/erlang-imported-fn-field/`
- [ ] `00 · 01-checker` — **the checker accepts a bare `print(x)` that three of four backends cannot lower.** It type-checks everywhere (a `registerBuiltins` binding plus `pub declare fn print` in `builtins.d.bp`) and only commonJS lowers it: erlang gives an undefined local, beam `{unresolved_call, print, 1}`, wasm an `unreachable` trap. The language reference only ever writes `@print`. Either lower it everywhere or refuse it with a "did you mean `@print`?" — and note it is not one library's slip: `repository/onze/examples/onze/src/main.bp:77` writes it too and fails the same way, with no ledger line of its own Re-measured 2026-09-21 against `2e6bb4ac`: `print("hi")` prints `hi` on commonJS and dies `function print/1 undefined` on erlang.
- [ ] `00 · 11-tooling` — **`botopink build --target erlang` succeeding proves nothing about erlang**: a build transpiles and never invokes `erlc`. Measured while diagnosing the above — a build that "passed" sat beside a `botopink run --target erlang` that failed identically to the test row. Any evidence of the form "it builds on that target" should be read as "it emitted text"
- [ ] `02-packaging` — **`emilia-card`'s erlang restriction has outlived its reason**: the cell is fully green. The ledger now says so in writing, and the member's `targets` is its own front's to widen
- [ ] `00 · 11-tooling` — worktree `.tasks/test-path-race` (`fix/test-path-race`) — **`zig build test` has the same root, a different patient, and the cause everyone reported is wrong.** No test in that binary removes `modules/compiler-cli/.botopinkbuild/`; the only whole-tree remover is `botopink clean`, which no test dispatches against the real cwd (its unconditional `Removed …` print is what made it look otherwise). Measured: the binary alone is **30/30 green**, two copies in one cwd are **13/15 red** — `cli.libs.loadOne … LibFileNotFound`, `LibNotFound`, `cli.config.workspaceRefusal`. Identical root to the `test-out` race: a fixed path under a shared checkout, scoped per *test name* but not per *run*, wiped by each test's opening `deleteTree`, and triggered by a second concurrent **process** rather than a sibling test. The fix is a per-process root segment across **73** comptime `ws ++ "…"` sites in `libs.zig`, `config.zig`, `format_cmd.zig`, `resolver.zig`, `new.zig` and `build.zig` — its own front
- [ ] `00` — three more writers to a shared path under a checkout they do not own, named while fixing the first: `compileCell`'s `lib-test-build/<target>` is per target and never per run; **it *is* read back** — `modules/compiler-cli/tests/test_tooling.sh:107` checks the directory exists — but only as an existence check, so a concurrent overwrite is still invisible. ("Nothing reads it back" was my wording and it was wrong; the correction is the path-race front's); `runtime.zig`'s content-keyed `CACHE_ROOT` is written with a plain truncate-and-write where its neighbour `template_eval.writeModule` stages and renames *precisely* against this, so a torn tail reads as a short entry; and `.botopinkbuild/tmp/persistent_erl` is a fixed shared server directory
- [ ] `00 · 11-tooling` — a pre-existing flake, reproduced on an untouched tree 2 runs in 6: `zig build test` sometimes fails in `compiler-cli` because the `loadone-missing` test writes under `modules/compiler-cli/.botopinkbuild/` while another test in the same binary removes that directory
- [ ] `00 · 01-checker` — **`runtime_builtin_names` short-circuits to `void` before the declaration is read**, throwing away three declared answers exactly the way the index expression's was: `@todo()` and `@panic()` are declared `-> noreturn` in `libs/std/src/builtins_fns.d.bp` and type as `void`, so `val x: i32 = @todo();` reds `expected i32, got void` — which is the canonical stub form — and `@module()` is declared `-> module` and types `void` too. `x is T` is fine (`bool`), and `@print`/`@trap`/`@emit` are `void` by design. Found by C-02, which fixed the same shape one level over, re-measured 2026-09-21 against `2e6bb4ac`: `return @todo();` in a `-> i32` function still reds at the call
- [ ] `00 · 01-checker` — **an enum section head silently captures a top-level variant of the same name.** emilia front 36 declared `Layout.Break { After, Before, Inside }` beside front 34's top-level `After(inner: Token[])` / `Before(inner: Token[])`. Nothing redded; instead **that variant's payload projection broke**, so `Token.Before(inner)` built a record with no `inner` field and three of front 34's tests died with `Cannot read properties of undefined (reading 'fold')` **inside front 34's block**, pointing at nothing in the front that caused it. The front confirmed the cause by renaming the two heads, watching all three clear, then restoring and fixing it properly (`BreakAfter`/`BreakBefore`/`BreakInside`, flat, which is the 1.0.8 draft's own spelling); it also audited all 33 heads it adds against the 84 top-level variants and found these two the only collisions. **Leaf** names are safe — it is the head that shadows. The rule to implement: a name that cannot be constructed must not capture a constructor that can, and this must be a type error rather than a run-time `undefined` in an unrelated module
- [ ] `00 · 11-tooling` / `00 · 01-checker` — **an import must name the types its imported type transitively mentions, and the error points at a comment.** Found by emilia front 36 writing a consumer: importing `Theme` also requires importing `DarkMode`, and importing `Options` also requires `Rule`/`Block`/`Sheet` — names the consumer never writes. The diagnostic's location is a **comment line** (`src/main.bp:59:15`), so only the message text is usable, which is the part that makes it a tooling row as well as a checker one
- [ ] `05-emilia` fronts 36/47 — two upstream spellings flagged and deliberately not changed, each because changing one would split a convention in two: `Inset.*.Frac.Half` emits `top:50%` where upstream v4 emits `top: calc(1/2 * 100%)` (emilia's existing `Size.*.Frac` already emits bare percentages, so a lone divergence here would make the library inconsistent with itself), and front 34's three reference rows above. Confirm against upstream before the milestone closes and change them together or not at all
- [ ] `05-emilia` `reference-coverage.md` — **three modifier rows look a version behind upstream**, found by front 34 and flagged rather than invented (it transcribed the local reference and pinned the current spelling with a test, so a correction shows as a change): `open:` omits the legacy `[open]` attribute, which is what actually matches `<details open>` on engines without `:open` — upstream v4 emits `&:is([open], :popover-open, :open)`; `marker:`/`selection:` are `& ::marker`, which reaches only descendants, where upstream emits `& *::marker, &::marker` and includes the element itself; and `rtl:` is the pre-v4 `[dir="rtl"] &` where v4.1 emits `&:where(:dir(rtl), [dir="rtl"], [dir="rtl"] *)`. Note the wall: two of the three upstream forms carry more than one `&`, which front 56's `checkVariantSelector` refuses with no opt-out — so the fix is `:is(…)`, as front 34 did for `Open`, not a relaxed template
- [ ] `05-emilia` front 54 — **`spacingHalf` cannot spell `-0.5`.** It takes an `i32`, and an `i32` has no negative zero, so `spacingHalf(-0) == spacingHalf(0)`: `Neg.Half` is `{1,2,3}` and `-mt-0.5` has no token. Front 35 declined to write `calc(var(--spacing) * -0.5)` itself because that would take the single-author rule for the spacing scale away from 54 — 54 owes a signed half step
- [ ] `05-emilia` front 40 — must call front 35's `pub fn siblingSelector()` rather than spell `& > :not(:last-child)` again; the byte-identity test the spec asks for cannot exist until `Divide` does
- [ ] `00 · 01-checker` — **a full-arity labelled call is still zipped positionally.** C-04 made a parameter claimable by **label**, and the enum-path fix generalised what that costs: *any* position that reads something by argument index is wrong once a label can claim a parameter. The enum-path fix inverted C-04's own plan rather than building a second mapping (two mappings can disagree, and the expectation would then name the wrong enum), and made the expectation read **nothing** where the parameter is not knowable rather than read the wrong thing — but the pre-existing `for (typedArgs, f.params)` zip for a full-arity labelled call is untouched, and `planDefaultFill` deliberately does not plan for it. So `P(y: 2, x: 1)` with every argument written still binds by position in the checker, which is the same shape as the commonJS row already recorded above
- [ ] `00 · 02-erlang` / C-02 — **`xs[0]` types `void`.** `val ts: Array<i32> = [1,2]; val x: i32 = ts[0];` reds `expected i32, got void`, for user enums and primitives alike — found while measuring the enum-path fix, which had to write `ts.length()` in its array reproduction to work around it. Decision 63 makes `xs[0]` *be* `xs.at(0)`, so the index expression should have the method's answer and does not. Re-measured 2026-09-21 against `2e6bb4ac`: still reds, so C-02's index rewrite did not reach the *typing* of the result
- [ ] `00 · 01-checker` (the tail of the enum-path fix) — the **single-segment** dot shorthand has the same defect one level up: `.Red` resolves through `env.lookup(name)`, the flat table every enum's variants are bound into by bare name, so two enums each declaring a `Red` variant collapse onto one binding and the expected type is never consulted (`type Token { Red, Blue }` + `type Border { Red, Green }` + `val a: Token = .Red;` reds `expected Token, got Border`). And ES4's `enum_with_head` message blames the first enum in map order when nothing carries the path — it picks only the wording, never a type, so it is cosmetic but still non-deterministic. **Re-measured 2026-09-21 against `2e6bb4ac`, and the erlang half is the dangerous one**: with `pub type Warm { Red, Orange }` beside `pub type Cool { Red, Blue }`, `val c: Cool = .Red;` gives commonJS a loud `ReferenceError: Red is not defined`, but erlang **binds `Warm`'s Red to a `Cool`-typed name** and only dies later, at the `case` over `Cool` that has no arm for it — `{case_clause, main__t__warm__v__red}`. The value was wrong long before anything complained, and in a program whose `case` happened to have a catch-all arm nothing would complain at all
- [ ] `00` — **thirteen compiler shapes rakun front 05 met while writing a real configuration reader**, each recorded in that library's `AGENTS.md` and none worked around silently. Wrong answers first: `Array.pop` lowers to `lists:last/1` on erlang, which **reads without removing**; `try` unwraps only in a `val` binding there, so `return try f()` and `g(try f())` double-wrap; a `loop` body is recursion on commonJS, so 1 000 iterations overflow the stack (and `std`'s `random.intInRange` walks one frame per unit of range); an ALL-CAPS module-level `val` becomes an unbound erlang *variable*; a `var` declared in a nested block of a loop body comes out unbound on erlang; a `val` bound inside a `loop` lambda loses its string type, so `.length()` is emitted as a call; `x.field.length()` is emitted against JS's `length` property; and a `//` comment inside a braced block swallows the closing brace on commonJS. Then the surface gaps: an integer literal is `i32` with no widening and no cast, so an `i64` field cannot be given a value at all; an `if` expression cannot sit right of `+`; `${…}` interpolates inside string literals and `from` is a keyword; `@Decl` renders an empty `typeName` for any generic field, which is the only signal a list has; and the `files` order in a member's manifest is a **dependency** order that only a consumer's build reveals. `Array.pop` re-measured 2026-09-21 against `2e6bb4ac`: `@print(xs.pop()); @print(xs.length())` on `[1,2,3]` answers `3 · 2` on commonJS and `3 · 3` on erlang — the value is right and the receiver is untouched, which is why it reads as a library bug wherever it lands
- [ ] `00 · 01-checker` / the compile driver — **an imported fn's default cannot be filled.** C-04 fills a trailing default in the checker, but `env.fnParams` is this module's: the cross-module export registry carries template fns and decorators and no plain `FnDecl`, so `import {greet} from "helper"; greet("w")` still reds. Measured against jhonstart: `element.bp`'s 11 `attrs: []` paddings are same-module and fill; `elements.bp`'s **36** and `hooks.bp`'s **3** cross a module boundary and do not. Closing it is a `fnDeclRegistry` threaded through `analyzeModule`/`analyzeSource`/`analyzeMerged`/`resolveImports`/`registerExports` — a compile-driver change. The other six sites C-04 could not reach: a `..` spread call, a pipeline RHS, an interface associated fn (`Pair.of` — the site has only a `T.func`), a `"std"`-qualified call, a function **value**'s arity (`unify.zig` still compares `params.len` exactly), and the parser asymmetry that a free `fn` may declare only trailing defaults while a record may declare a leading one. Re-measured 2026-09-21 against `2e6bb4ac`, with the sibling declared (`mod helper;`) so the import actually resolves: still `error: 'greet' expects 2 argument(s), got 1` at the call site, on commonJS **and** erlang — it is a checker answer, so every target fails identically and `docs.md`'s C-04 paragraph is right to limit the claim to a declaration in the calling module
- [ ] `00 · 05-wasm` / `00 · 03-beam` — `String.slice`/`Array.slice` with a **written-out** open end (`s.slice(2, null)`, no default involved) traps on wasm and answers `0`/`badarith` on beam; the arity error was hiding it until C-04 removed it. Re-measured 2026-09-21 against `2e6bb4ac`: `"abcdef".slice(2, null)` answers `cdef` on commonJS and erlang and **traps on wasm** — `wasm trap: out of bounds memory access`, backtrace `__str_slice` ← `main`. The beam half was not re-measured: `botopink run --target beam` writes `out/beam/main.S` and does not execute, so a beam answer needs `erlc +from_asm` and is not something a one-line run proves
- [ ] `00 · 01-checker` — cross-module behavior identity: a `declare fn` returning an imported `Greeter` does not unify with a parameter typed `Greeter` from the same import (`type mismatch: expected behavior Greeter {…}, got Greeter`)
- [ ] `00 · 04-js` (a second round, all three measured by emilia front 56 while writing real code) — **(a)** a `case` arm over a **uniquely-named** variant lowers to `instanceof`, and `instanceof` does not cross a package boundary: a consumer package re-emits its own copy of the enum classes, so a value built there is never `instanceof` the class the library matches against and the whole `case` answers `undefined`, silently. An arm whose variant name repeats lowers to `_s.tag === "…"` and works. Two emilia tokens have been missing from `examples/emilia-card`'s output since before front 56 for this reason, and it is invisible inside emilia's own suite, where there is one copy of the classes. **(b)** the commonJS `String` prelude's `charCodeAt` patch is self-recursive (`this.valueOf().charCodeAt(index)` inside the replacement), and the prelude is installed by any module using a `String` member that needs a patch — one `s.slice(…)` made a hash function blow the stack program-wide. **(c)** `Array.reverse()` mutates its receiver on commonJS and not on erlang, so any fold over a reversed list is a silent target divergence. **(c) did not reproduce on 2026-09-21 against `2e6bb4ac`**, in either shape tried: `val ys = xs.reverse();` then reading `xs.at(0)` answers `1` on both targets, and `xs.reverse().at(0)` followed by a `loop (xs)` answers `3 · 1 · 2 · 3` on both. Either it was closed by something that landed since, or the divergence needs a shape neither of those is — the row stays only until whoever measured it supplies that shape, because a claim nobody can reproduce is worse than no row
- [ ] `00 · 02-erlang` — worktree `.tasks/erlang-module-load` (`fix/erlang-module-load`): two emitter gaps rakun front 04 measured. A module-level `val` with a side effect never runs (`topValForms` emits a 0-arity function called on each *read*, and `'_botopink_main'/0` is not emitted in test mode at all), so a library whose component model is module-load self-registration registers nothing; and a method on a host-supplied `behavior` receiver lowers to a bare local call (`req.param("name")` → `function param/2 undefined`). Until they close, no track B front can claim its target is testable
- [ ] `00 · 10-cli-residuals` (the tail, extended) — `shipErlSidecars` is called only from `test_cmd.zig`, so `botopink build --target erlang` copies **no** `.erl` at all: rakun's example build exits 0, emits ten modules with every `rakun_runtime:` qualifier, and ships neither the sidecar nor a loader
- [ ] `05-emilia` front 54's step-3 acceptance asks a test to assert the refused entry's **absence**, which is accept-and-ignore: a build that aborts is not a value a test can read. Its own Test plan gets this right two pages later, and the acceptance list is what must change
- [ ] `00 · 05-wasm` — a `case` used as a **value** with string arms types its binding as `i32`: `val a = case x { 5 { "five" } _ { "other" } }` prints `256`, a pointer. Found by C-08, which is unaffected by it because decision 54's optional form is rewritten to an `if` in `transform.zig` before `wat.zig` sees it. **Re-measured 2026-09-21 against `ead0b645` and it is syntax-dependent, which nobody had noticed**: the brace-arm spelling `case x { 5 { "five" } _ { "other" } }` still prints `256` on wasm, while the arrow-arm spelling `case x { 5 -> "five"; _ -> "other"; }` prints `five` there — same program, same values, two different paths through the wasm backend and only one of them broken. commonJS and erlang answer `five` for both. That narrows the fix to one arm-lowering path and gives every library front a workaround it can use today without leaving the language
- [ ] `00 · 05-wasm` — `$__print_str` does not guard its own null: an absent `?string` whose shape `optInfoOf` does not recognise is printed by loading a length from address 0 (the WASI iovec) and writing whatever bytes are there — exit 0, no diagnostic. Measured by disabling the `optInfoOf` arm: `@print(s.at(3))` on `"abc"` printed six spaces. The `?T` reader is a hand-maintained list (`optInfoOf`'s arms plus `fn_ret_typerefs`), so every new `?string`-valued lowering must be registered by hand or it answers garbage silently
- [ ] `00 · 03-beam` — beam drops a function whose whole body is `@todo()`: the abort is `{undef, main:notReady/0}`, not the builtin's, so the language cell passes for the wrong reason (found by C-16; `beam_asm.zig` is `.tasks/module-identity`'s until C-01 lands)
- [ ] `00 · 02-erlang` (C-03) — `std-unsupported-on-target` names the module's **first** declaration (`std/erlang.abs`) instead of the function actually called; a wording defect with a cell already in the suite. **Confirmed 2026-09-21 against `2e6bb4ac` with a distinguishing call**: `erlang.element(1, "x")` on commonJS reds `std-unsupported-on-target: std/erlang.abs has no ``@external`` for target 'node'`. `abs` is `erlang.bp`'s first `pub declare fn` and `element` is its fifth — the reader is sent to a function they did not call, and the location is the module rather than the call
- [ ] Decision 113 in landed code — jhonstart 26, 27 and 29 carry a "decision 113's spellings" step (`data-jh-*`, `linkMount` / `formMount`, the router receiving `match`); rakun 23's render leaves `ssr.bp` / `ssr.mjs` once jhonstart front 30 lands; rakun 04 closes with the core on `["erlang"]` and `runtime.mjs` deleted; rakun 24's `actionForm` becomes `actionIdOf`. Decision 114 in landed code: rakun 22's page registry drops `LayoutProps` / `rkAppRegisterPage` over `Element` for an opaque `PageRenderer`; rakun 23's `setPageRender` / `RenderedPage` become `page(pattern, render)` over `ChunkWriter`; rakun 24 reads the wire names from `rakun.actions.field` / `rakun.actions.header`; jhonstart 28 drops `rakun_request_context` and `fillRequest` for the `RequestData` onze hands the render
- [ ] Decision 115 in landed code — the matcher at rakun `modules/rakun/src/file_router.bp:49-479` moves to the bundled library `libs/routing` (`01-std/04-routing-lib`) and rakun 22 imports it (its Step 7); jhonstart 26's router imports `parseTable` / `matchPath` from `routing` and takes no `match`; `file_router.bp:795` reads `rakun.appDir` instead of `onze.appDir`; rakun 24's body limit is `rakun.actions.bodyLimit`; jhonstart 31 gains `redirect(url)` and 30 the late-signal markup (`data-jh-g`, `globals.signal`); onze 49 writes the `rakun.*` keys and 53's pages import `notFound` / `redirect` / `cookies` from jhonstart
- [ ] Decision 116 in landed code — rakun front 63's vocabulary and `n` codec move to `routing`'s `navigation` with `nav:` reasons (no `jhonstart:` spelling left); rakun-web's `middleware.bp:60-110` / `filter.bp:545-565` matchers call `routing`'s `pattern` (front 07); rakun 24 and jhonstart 67 import `libs/actions` and the golden `state` literal leaves both test files; `modules/rakun-validation` (landed, 54/0) moves to `libs/validation` and rakun 14 Step 7 deletes the member, rakun's workspace root becoming `["erlang"]`; the JSON copies (`ssr.bp:641-676`, `report.bp:80`, `rakun-web/src/error.bp:131`, `config.bp:394-433`) go to std `json`; rakun's `request_context.bp:507-610` codec and jhonstart's `decodePairs` / `encodePairs` (`router.bp:123-162`) go to std `encoding`, with jhonstart 26's `a b` cell; emilia's `hashHex` (`emilia.bp:80`) goes to std `contentHash`; onze 69 configures rakun-web 82's static server
- [ ] Decision 117 in landed code — jhonstart 30's render writes to a `Response(status, header, write, close)` and turns `notFound` / `redirect` into 404 / 307 itself, `globals.bp` gains `signal` (`__bp2`), the redirect target is checked against the table and `allowedRedirects`, and `#[layout]` / `#[page]` / `#[template]` refuse a function that is not `#[@use] … -> @Component<Element>`; jhonstart 26 gains `clientApp`; rakun 23's `ChunkWriter` gains `setStatus` / `setHeader` and a page renderer's signal becomes a 500, rakun 63 drops page signals and `boundaryFor`, rakun 24's action `redirect` goes into the envelope's `n`; onze 49 adapts the response with no signal `case` and writes `rakun.actions.bodyLimit` from `OnzeConfig.actionsBodyLimit`; onze 53's pages, layouts and templates are components; `01-std-lib-enablement` gains the JSON writers (front 07 folded in) and `Json` / `json.decode`, which rakun 05's config reader, `actions` and jhonstart's payload reader use; `routing`, `actions` and `validation` carry no sidecar; `rakun-test` is `["erlang"]`
- [ ] `04-jhonstart` 32/67 — decision 78 taken; ready (94 landed)
- [ ] `05-emilia` front 46 — decision 81 taken (`AccentAuto`); ready

## Open
- [ ] `00` C-04, C-07…C-11, C-13…C-18, C-20 (absorbed by C-26), C-21…C-25 — no worktree, no owner
- [ ] `03-rakun` — 51 fronts; **04, 05, 06, 07, 14, 22, 23, 62, 72 and 74 landed** (core 369/0 on commonJS after the routing and validation moves, `rakun-web` 104/0; `rakun-validation` is now the bundled `validation`). Next unblocked: 08, 11, 13, 80
- [ ] `04-jhonstart` — 9 fronts; 94, 26, 27, 28 and 29 landed (125 / 0 on both rows). Track C is now genuinely blocked: 30 wants `async`, 31 wants `content_hash`, 32 wants `escape` — all three `01-std` step 6, all specified, none written
- [ ] `05-emilia` — 22 fronts; 33–41, 44, 45, 54 and 56 landed (569 / 569 on both rows, fifteen example members). `Transition.Transform` is a leaf, so a `Transform` *section* beside it is safe, but a top-level `Transform(…)` **payload** variant is the shape that silently breaks a section of the same name. Level 3 still holds 42, 43, 45, 46, 47, 48 and 59; level 2 holds 55, 57 and 58
- [ ] `06-onze` — 9 fronts, none started; `49-onze-stand-up` after the name takeover, then `68-onze-client-bundle`

## Deferred out of this milestone
- See [`deferred.md`](./deferred.md) — GraalVM/AOT/CRaC/JNDI/Servlet/agents, Vercel hosting, the bundler plugin APIs, the CSS features that read user files, the old `onze` mocking runtime; plus the per-track reference-coverage holes listed in each `unification.md`.
