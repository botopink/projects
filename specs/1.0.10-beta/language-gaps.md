# Language gaps — 1.0.10-beta

The compiler is frozen for every **library** front. Every library front is built from plain botopink
plus `@Decl`, comptime, `@emit` and `#[@External.<Target>]`, and a library front that needs a
compiler change does not get one. What it does instead is write the nearest form that parses today,
mark the line `// LANGUAGE GAP:`, and record the gap here.

The compiler is **not** frozen for [`00-compiler-carry-over`](./00-compiler-carry-over/README.md),
which carries the open compiler fronts. The last column of every table below says which of those
fronts a gap belongs to, or `—` when it belongs to none and stays a library-side workaround. A gap
owned by `00` is **still worked around** by the library front that hits it — `00` runs beside the
libraries, not ahead of them.

The exit gate in [`fronts.md`](./fronts.md) requires every `// LANGUAGE GAP:` marker left in an
example to appear in a spec here — a gap that exists only as a comment in a `.bp` file is a gap
nobody will fix. Rows are added as fronts are written and removed when the gap closes. A row names
the gap, where it bites, what the nearest valid form is today, and what surface would close it.

## Confirmed gaps

| Gap | Bites | Nearest valid form today | Proposed surface | 1.0.10 owner |
|---|---|---|---|---|
| **No bitwise operators — by design.** `&`, `\|`, `^`, `<<`, `>>` are a form the language decided against, refused by name at the operator (`bitwise-operator-absent`; [`15-language-surface`](./00-compiler-carry-over/15-language-surface/README.md) § What holds) | 01 · 03 · 68 | Push the whole operation into an `#[@External.<Target>]` host template, once per target | — (by design) | — |
| **No byte or binary type** | 01 · 13 · 15 · 24 · 25 · 70 · 71 | Every host cell marshals through `string`, so `Socket.recv` answers UTF-8 for a byte stream. Front 25 refuses `multipart/form-data` with 415 rather than read it lossily | A `Bytes` primitive with a declared encoding boundary. Without it no file upload, no file download and no image endpoint is expressible | [`00`](./00-compiler-carry-over/README.md) · 01-checker (a new primitive) — **waits on [decisions-pending lg2-a](./decisions-pending.md#lg2-a--a-byte-type)** |
| **`@Task<T>` lowers eagerly on erlang** — the type carries no concurrency on the server target (`libs/std/src/io/http.bp`'s header) | 02 · 23 · 25 · 28 · 30 · 60 | Design over **unstarted thunks** (`Array<fn() -> @Task<T>>`) and spawn one BEAM process per task, gathering by index — what `std/async` provides | A real scheduler behind `@Task` on BEAM, or an explicit `spawn`/`join` pair in the language | **waits on [decisions-pending lg2-b](./decisions-pending.md#lg2-b--what-taskt-means-on-the-beam)** |
| **A decorator cannot rewrite or wrap the body it annotates** — `@emit` only adds module-level declarations (every decorator in `rakun`'s `decorators.bp` emits `val`s and `fn`s and touches no body) | 06 · 07 · 08 · 10 · 12 · 16 · 83 — **the headline gap for track B** | `#[transactional]`, `#[secured]`, `#[cacheable]` are delivered as **emitted proxy types** (`<Type>Tx`, `<Type>Sec`) injected by name; `'use cache'` on a plain fn becomes the `cacheThrough(policy, keys, load)` combinator | A decorator form that receives and returns the annotated body | **waits on [decisions-pending lg2-c](./decisions-pending.md#lg2-c--a-decorator-that-rewrites-or-wraps-the-body-it-annotates)** |
| **A decorator cannot read the body of the declaration it annotates** — `@Decl` exposes kind/name/fields/methods/params/annotations and no statements | 83 | A saga is a value pairing each step with its compensation, not a decorated fn | Statement access on `@Decl`, or a body-walking comptime API | **waits on [decisions-pending lg2-d](./decisions-pending.md#lg2-d--a-decorator-that-reads-the-body-it-annotates)** |
| **A method-level `@Decl` carries no owner and no parameter list** — `Param` exists only on `Method` entries of a *type* decl (`builtins.d.bp`) | 06 · 07 · 08 · 09 · 10 · 29 | Method-level markers are placement-only; the **type-level decorator does every `@emit`** — the pattern `#[restController]`/`#[getMapping]` uses. Front 29 needs a second decorator on the props record to check serializability | `owner` and `params` on a method-level `@Decl` | **waits on [decisions-pending lg2-e](./decisions-pending.md#lg2-e--a-method-level-decls-owner-and-parameters)** |
| **A decorator argument cannot name a type** — arguments are ordinary values and there is no type-of-type, so `#[conditionalOnMissingBean(MailSender)]` does not compile | 72 · 78 | The type's name as a string (Spring's own `excludeName` spelling) | Type-valued decorator arguments | [`00`](./00-compiler-carry-over/README.md) · 01-checker (types-as-values) — **waits on [decisions-pending lg2-f](./decisions-pending.md#lg2-f--a-decorator-argument-that-names-a-type)** |
| **No `@typeName<T>()`** — a registry key cannot be derived from a type | 06 | The key travels as an unchecked string beside `T` | A comptime type-name intrinsic | [`00`](./00-compiler-carry-over/README.md) · 13-module-identity (the type's atom inside the value is the run-time half) — **waits on [decisions-pending lg2-g](./decisions-pending.md#lg2-g--typenamet)** |
| **No typed raise and no catch by type** — `throw` is legal only where the return carries a `@Result`; `try … catch` works over `@Result` alone | 07 · 31 · 63 | `#[exceptionHandler("string.tag")]` instead of by type; navigation signals are `erlang:throw` on the host side | Typed error values, or a `catch` arm that matches on a tag | **waits on [decisions-pending lg2-h](./decisions-pending.md#lg2-h--raising-and-catching-by-type)** |
| **A decorator argument is a raw lexeme** — an array literal written there, or declared as the parameter's default, reaches the body as its source text (`[1, 2]` has length 6); only `string`, numeric and `bool` arguments are checked | 07 | `#[crossOrigin("origin", "GET,POST")]` — comma-joined strings | Typed decorator arguments | **waits on [decisions-pending lg2-i](./decisions-pending.md#lg2-i--a-decorator-argument-is-a-raw-lexeme)** |
| **A decorator body cannot accumulate comptime state across invocations** | 05 | Front 05's key catalogue is a run-time registry dumped from a headless boot, not a comptime document | Comptime mutable state scoped to a compilation | [`00`](./00-compiler-carry-over/README.md) · 14-comptime-on-beam (the evaluator) — **waits on [decisions-pending lg2-j](./decisions-pending.md#lg2-j--comptime-state-across-decorator-invocations)** |
| **No comptime reflection over the project** — no way to enumerate a package's modules or read its manifest | 81 | The release's application list and SBOM component set are hand-written or discovered by a build-time filesystem walk | A comptime `@project()` intrinsic | **waits on [decisions-pending lg2-k](./decisions-pending.md#lg2-k--comptime-reflection-over-the-project)** |
| **The navigation signals do not return `noreturn`** — a user fn may return `noreturn` and a call to it ends its path, but `noreturn` fits no value position (`throw notFound();` is `expected string, got noreturn`), so jhonstart's `notFound()` / `redirect()` declare `-> string` | 63 | Every call site binds `val _gone = notFound();` to a value never produced | Declare the signals `-> noreturn`, making code after a signal unreachable | — (jhonstart's surface) — **waits on [decisions-pending lg2-l](./decisions-pending.md#lg2-l--whether-noreturn-is-a-bottom-type)** |
| **No module-level annotation** — Next's file-level `'use cache'` has no spelling | 12 | A module-level `val` holding the default policy | An inner attribute, `#![useCache]` | [`00`](./00-compiler-carry-over/README.md) · 15-language-surface — **waits on [decisions-pending lg2-m](./decisions-pending.md#lg2-m--a-module-level-annotation)** |
| **`Children` coerces from array, `Element` and `string` but not from a thunk** | 30 | A Suspense boundary takes its child as a named field, not as a child | Thunk coercion into `Children` | **waits on [decisions-pending lg2-n](./decisions-pending.md#lg2-n--a-thunk-coerced-into-children)** |
| **`await` is refused in a lambda — by design.** The effect is the function's return, and a lambda has none (decision 118) | 28 · 30 | `async { … }` (decision 124) is the Task in the middle of a function; or lift into a named `@Task` fn | — (by design: the lambda has no await channel) | — |
| **A comptime body has no filesystem access** — it sees only its runtime's prelude, so a `wsdl """…"""` template cannot read a schema file | 88 · 93 | `rakun ws generate` emits checked-in `.bp`; scaffolding is a run-time copy | A sandboxed comptime build-input read | [`00`](./00-compiler-carry-over/README.md) · 14-comptime-on-beam — **waits on [decisions-pending lg2-o](./decisions-pending.md#lg2-o--filesystem-access-from-a-comptime-body)** |
| **No cancellation** — a losing racer and an expired timeout keep running | 02 | Document it; let the work complete and discard the result | Cancellation tokens, or linked processes with a kill path | **waits on [decisions-pending lg2-p](./decisions-pending.md#lg2-p--cancellation)** |
| **`@Decl` carries no source location** — a decorator cannot learn which file it annotates | 22 | The app-relative segment is an explicit decorator argument, generated and verified by front 50's CLI | A source-location field on `@Decl`, reusing `@src()`'s `SourceLocation` record | [`00`](./00-compiler-carry-over/README.md) · 01-checker — **waits on [decisions-pending lg2-q](./decisions-pending.md#lg2-q--decls-source-location)** |
| **No assignment to a `self` field — by design.** A record is immutable (1.0.5 decision 37): `self.x = …` is refused, and a changed copy is the update form `Name(..base, x: v)` (`docs.md` § type — records) | 18 · 26 · 27 · 31 · 32 · 62 · 67 · 69 · 70 · 71 · 78 · 83 · 87 | Return a new value with the update form; `withAttribute` + explicit `store.save` for sessions (forgetting the save is uncatchable); front 62's request frame lives in the host process dictionary | — (by design) | — |
| **A bodyless method in a `type` body is only a host-backed method** — `pub declare fn` inside a type body is the `#[@External.*]` method form (one with no binding at all is refused at every call); nothing lets a decorator supply the body | 08 · 09 · 78 | Give the method a body that calls a generated helper | A decorator-supplied body for a declared method, the natural shape for `#[query]`-style decorators | [`00`](./00-compiler-carry-over/README.md) · 15-language-surface — **waits on [decisions-pending lg2-r](./decisions-pending.md#lg2-r--a-body-a-decorator-supplies-for-a-declared-method)** |
| **A section-typed value cannot be constructed standalone** — `val a: Tok.Alpha = .50;` → `this token cannot appear here` — so a payload variant cannot take a section-typed field | 33 | Payload fields are builtin-typed (`i32`, `string`, `Token[]`): `Token.Alpha(percent: 50, inner: xs)` | Dot-shorthand rooted at the parameter's declared section type | [`00`](./00-compiler-carry-over/README.md) · 01-checker |
| **No module-graph reflection** — `@Decl` exposes declarations, not imports | 68 | `importsOf` is a textual scan that fails loudly on an unparsable line | An `imports` field on the module `@Decl` | **waits on [decisions-pending lg2-s](./decisions-pending.md#lg2-s--module-graph-reflection)** |
| **No spelling for a negative numeric enum leaf** — numeric leaves are bare digits, so `-rotate-12`, `-translate-y-2` and every negative utility have no direct token | 35 · 36 · 45 | A `Neg { … }` sub-section convention (front 45 ships it) | A signed numeric leaf, or a unary `-` on enum paths | [`00`](./00-compiler-carry-over/README.md) · 15-language-surface — **waits on [decisions-pending lg2-t](./decisions-pending.md#lg2-t--a-negative-numeric-enum-leaf)** |
| **No expression-position decorator** — a decorator is `@Decl`-first and annotates a declaration, so `#[emilia([.Pad.All.4])] div(…)` cannot exist; compounded by the sibling-fn rule, since a decorator body cannot call `emilia()` | 48 | Plain function calls | Expression-position decorators, or a comptime call form that runs in the eval script | **waits on [decisions-pending lg2-u](./decisions-pending.md#lg2-u--an-expression-position-decorator)** |

## Toolchain gaps — not the language, the compiler-cli

| Gap | Bites | Nearest form today | Fix | 1.0.10 owner |
|---|---|---|---|---|
| **A built erlang program cannot load its `.erl` sidecars** — `__bp_load_siblings/0` is emitted only under the test flag (`codegen/erlang.zig`) | 04 · 81 · 85 — every server front that ships a host module | Green under `botopink test --target erlang`; **`botopink run` cannot serve HTTP on the BEAM**. Front 81's "the tarball starts and serves" acceptance is gated on it | Emit the sibling loader for `build` entry points too | [`00`](./00-compiler-carry-over/README.md) · 13-module-identity (it owns `codegen/erlang.zig`) |
| **A sidecar cannot reach an external OTP application** — the sidecar path loads `.erl` files beside the emitted module, not a dependency tree | 04 (why `gen_tcp` and not cowboy) · 85 (why the default SMTP client is over `io.net` and `gen_smtp` is an adapter that fails loudly) | Dependency-free implementations over OTP's own applications (`gen_tcp`, `ssl`, `crypto`) | A dependency declaration in `botopink.json` that the erlang build resolves into the release's application list | [`00`](./00-compiler-carry-over/README.md) · 10-cli-residuals + bpmp; [`02-packaging/README.md`](./02-packaging/README.md) states the manifest side |
| **`DepSpec` has no subdirectory field** — a dependency is `{git, path, ref}`, and every `rakun-*` module is a directory inside one repository, so an out-of-tree consumer cannot install a starter from git | 73 | A `path` dependency; front 73 cannot ship a git-installable starter this milestone | A `subdir` field on `DepSpec`, resolved by `bpmp` | [`00`](./00-compiler-carry-over/README.md) · 10-cli-residuals (`compiler-cli/src/cli/config.zig`) + bpmp; [`02-packaging/README.md`](./02-packaging/README.md) — **waits on [decisions-pending lg2-v](./decisions-pending.md#lg2-v--a-subdirectory-in-a-git-dependency)** |
| **`try` inside a lambda does not propagate** — the lambda's `try` does not return the `Error` from the enclosing fn, so an assertion written through it can pass vacuously | 08 | Bind the `@Result` and branch on it, or call a named fn | Either propagate out of the lambda (its own `@Result` return) or refuse `try` where it cannot propagate | [`00`](./00-compiler-carry-over/README.md) · 01-checker — **waits on [decisions-pending lg-a](./decisions-pending.md#lg-a--where-a-try-inside-a-lambda-may-appear)**: what a lambda's return is when nothing annotates it |
| **A `var` mutated inside a lambda body (outside a `for`) does not lower on erlang** — `var n = 0; captureSignals({ -> n = n + 1; 1; }, 0)` emits `Reached@1` unbound and `erlc` refuses the module; beam drops the write and answers the old value at exit 0 | 63 (a "never reached" counter) | Record the fact outside the lambda (a property write) | Lower a captured-var write in any closure the way `for` bodies are lowered, or refuse it at check time | [`00`](./00-compiler-carry-over/README.md) · 02-erlang — **waits on [decisions-pending lg-b](./decisions-pending.md#lg-b--what-a-lambdas-write-to-a-captured-var-means-on-the-beam)**: a per-process cell, an ETS cell or a refusal |
| **A decorator body cannot call a host function** — a bodied function of the decorator's own module, or one it imports from the project or a package, travels into the decorator module; a host `declare fn` does not — std's (`json.quote`: "no primitive type … and no decorator host function provides" through the namespace, `call to undefined function quote/1` through a leaf import) or the project's own — on the wat and BEAM runtimes alike | 16 (`#[scheduled]` repeats the cron rules inline) · every decorator | Keep the logic in a module of the project or a package | Lower a std host function's Erlang cell in the decorator module | [`00`](./00-compiler-carry-over/README.md) · 14-comptime-on-beam — **waits on [decisions-pending lg2-w](./decisions-pending.md#lg2-w--a-host-function-called-from-a-decorator-body)** |
