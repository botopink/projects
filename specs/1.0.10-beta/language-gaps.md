# Language gaps — 1.0.10-beta

The compiler is frozen for every **library** front. Every library front is built from plain botopink
plus `@Decl`, comptime, `@emit` and `#[@external]`, and a library front that needs a compiler change
does not get one. What it does instead is write the nearest form that parses today, mark the line
`// LANGUAGE GAP:`, and record the gap here.

What is new in this milestone is that the compiler is **not** frozen for
[`00-compiler-carry-over`](./00-compiler-carry-over/README.md), which carries the open compiler fronts
of 1.0.5-beta (`01-checker`, `13-module-identity`, `15-language-surface`, `16-formatter`,
`17-beam-memory`, …). The last column of every table below says which of those fronts a gap belongs
to, or `—` when it belongs to none and stays a library-side workaround. A gap owned by `00` is
**still worked around** by the library front that hits it — `00` runs beside the libraries, not ahead
of them, except for the two items pulled ahead in [`fronts.md`](./fronts.md) (module identity and
`@src()`).

This file is the feed for the next milestone. The exit gate in [`fronts.md`](./fronts.md) requires
every `// LANGUAGE GAP:` marker left in an example to appear in a spec here — a gap that exists only
as a comment in a `.bp` file is a gap nobody will fix.

Rows are added as fronts are written and removed when the gap closes. A row names the gap, where it
bites, what the nearest valid form is today, and what surface would close it.

## Confirmed gaps

| Gap | Bites | Nearest valid form today | Proposed surface | 1.0.10 owner |
|---|---|---|---|---|
| **No bitwise operators** — `&`, `\|`, `^`, `<<`, `>>` do not exist | 01 · 03 · 68 | Push the whole operation into an `#[@External]` host template, once per target | The five operators on the integer behaviors | [`00`](./00-compiler-carry-over/README.md) · 15-language-surface (grammar) + the four backends |
| **No `toString(radix)`** on the integer behaviors | 01 · 03 | Same — a host template per target | A radix argument, or a `std/fmt` module that does it in botopink | — (std surface; a `01-std` fold-in, not a compiler front) |
| **No byte or binary type** | 01 · 13 · 15 · 24 · 25 · 70 · 71 | Every host cell marshals through `string`, so `net.recv` answers UTF-8 for a byte stream. Front 25 refuses `multipart/form-data` with 415 rather than read it lossily | A `Bytes` primitive with a declared encoding boundary. Without it no file upload, no file download and no image endpoint is expressible | [`00`](./00-compiler-carry-over/README.md) · 01-checker (a new primitive) — unassigned in 1.0.5; the first 1.0.10 compiler decision to take |
| **A std module cannot call another std module** — a cross-module bare import of an `#[@External]` symbol is `undefined` at runtime | 01 | Each module re-declares its own host cell | Fix the lowering so a bare import of an external symbol resolves | [`00`](./00-compiler-carry-over/README.md) · 13-module-identity (`crossModule.zig` is its file) — a lowering, not a grammar. Decision 106 stands on this row: its purity rule (a root std module imports nothing from `io/`) is checked **only inside std** and is vacuous while this row holds; the day it closes, the cross-module import inside std is governed by that rule ([`00 · 23-std-purity`](./00-compiler-carry-over/23-std-purity/README.md) step 4), never left as a limitation |
| **No array destructuring in a binding** — `val [a, b] = xs;` does not parse | 02 | `xs.at(0)`, `xs.at(1)` | Destructuring patterns in `val`/`var` | [`00`](./00-compiler-carry-over/README.md) · 15-language-surface (`parser/patterns.zig` is 01-checker's) |
| **`@Task<T>` lowers eagerly on erlang** — the type carries no concurrency on the server target (`libs/std/src/io/http.bp:16-18`) | 02 · 23 · 25 · 28 · 30 · 60 | Design over **unstarted thunks** (`Array<fn() -> @Task<T>>`) and spawn one BEAM process per task, gathering by index — which is what front 02 now provides | A real scheduler behind `@Task` on BEAM, or an explicit `spawn`/`join` pair in the language | — |
| **A decorator cannot rewrite or wrap the body it annotates** — `@emit` only adds module-level declarations (`rakun/src/decorators.bp:48-240` is the proof: every decorator emits `val`s and `fn`s and touches no body) | 06 · 07 · 08 · 10 · 12 · 16 · 83 — **the headline gap for track B** | `#[transactional]`, `#[secured]`, `#[cacheable]` are delivered as **emitted proxy types** (`<Type>Tx`, `<Type>Sec`) injected by name; `'use cache'` on a plain fn becomes the `cacheThrough(policy, keys, load)` combinator | A decorator form that receives and returns the annotated body | — |
| **A decorator cannot read the body of the declaration it annotates** — `@Decl` exposes kind/name/fields/methods/params/annotations and no statements | 83 | A saga is a value pairing each step with its compensation, not a decorated fn | Statement access on `@Decl`, or a body-walking comptime API | — |
| **A method-level `@Decl` carries no owner and no parameter list** — `Param` exists only on `Method` entries of a *type* decl (`builtins.d.bp:459-477`) | 06 · 07 · 08 · 09 · 10 · 29 | Method-level markers are placement-only; the **type-level decorator does every `@emit`** — the pattern `#[restController]`/`#[getMapping]` already uses. Front 29 needed a second decorator on the props record to check serializability | `owner` and `params` on a method-level `@Decl` | — |
| **A function cannot forward a `@Result` value** — inside a `-> @Result<…>` fn `return r` re-wraps | 04 · 08 · 09 · 79 | The doubled `query`/`tryQuery` surface | A `return` that passes a `@Result` through unchanged — decided (decision 119) | [`00`](./00-compiler-carry-over/README.md) · **24-effects-by-return** |
| **Explicit generic arguments do not parse at a call site** — `ctx.resolve<UserRepository>()` is unwritable | 06 | The type comes from an annotated binding: `val repo: UserRepository = ctx.resolve();` | Turbofish or angle-bracket call arguments | [`00`](./00-compiler-carry-over/README.md) · 15-language-surface (its audit lists the form as one of the 24 that do not parse) |
| **A decorator argument cannot name a type** — arguments are ordinary values and there is no type-of-type, so `#[conditionalOnMissingBean(MailSender)]` does not compile | 72 · 78 | The type's name as a string (Spring's own `excludeName` spelling) | Type-valued decorator arguments | [`00`](./00-compiler-carry-over/README.md) · 01-checker (types-as-values is in its description) |
| **No `@typeName<T>()`** — a registry key cannot be derived from a type | 06 | The key travels as an unchecked string beside `T` | A comptime type-name intrinsic | [`00`](./00-compiler-carry-over/README.md) · 13-module-identity (the type's atom inside the value is the runtime half) |
| **No typed raise and no catch by type** — `throw` is legal only where the return carries a `@Result` and yields a value; `try/catch` works over `@Result` alone | 07 · 31 · 63 | `#[exceptionHandler("string.tag")]` instead of by type; navigation signals are `erlang:throw` on the host side | Typed error values, or a `catch` arm that matches on a tag | — |
| **A decorator argument is a raw lexeme** — an array literal there is unverified | 07 | `#[crossOrigin("origin", "GET,POST")]` — comma-joined strings | Typed decorator arguments | — |
| **A decorator body cannot accumulate comptime state across invocations** | 05 | Front 05's key catalogue is a run-time registry dumped from a headless boot, not a comptime document | Comptime mutable state scoped to a compilation | [`00`](./00-compiler-carry-over/README.md) · 14-comptime-on-beam (the evaluator is its file) |
| **No comptime reflection over the project** — no way to enumerate a package's modules or read its manifest | 81 | The release's application list and SBOM component set are hand-written or discovered by a build-time filesystem walk | A comptime `@project()` intrinsic | — |
| **No bottom/never type** — `notFound()` and `redirect()` cannot return, but must declare a return type | 63 | Every call site binds `val _gone = notFound();` to a value never produced | `never`, making code after a signal a compile error | [`00`](./00-compiler-carry-over/README.md) · 01-checker |
| **No record-update expression** — "override the ancestor field by field" cannot be a mutation | 60 | A constructor over seven `case` results | `SegmentConfig(base, revalidate: 60)` | [`00`](./00-compiler-carry-over/README.md) · 15-language-surface |
| **No module-level annotation** — Next's file-level `'use cache'` has no spelling | 12 | A module-level `val` holding the default policy | An inner attribute, `#![useCache]` | [`00`](./00-compiler-carry-over/README.md) · 15-language-surface |
| **`Children` coerces from array, `Element` and `string` but not from a thunk** (`infer.zig:4228-4239`) | 30 | A Suspense boundary takes its child as a named field, not as a child | Thunk coercion into `Children` | — |
| **`await` is unusable as a lambda's last statement** — nothing can `map` over resolves | 28 · 30 | Lift into a named `@Task` fn | Allow `await` in closure tail position | [`00`](./00-compiler-carry-over/README.md) · 01-checker |
| **`pub val` of a user record type is unexercised** — only primitive `pub val`s exist in the tree (`math.bp:16`, `path.bp:13`) | 32 | Metadata exports are functions, not values | Verify or reject `pub val` of a record at module scope | [`00`](./00-compiler-carry-over/README.md) · 17-beam-memory (module-level values are its declaration) — verify, not implement |
| **A comptime body has no filesystem access** — it sees only a minimal native-JS prelude, so a `wsdl """…"""` template cannot read a schema file | 88 · 93 | `rakun ws generate` emits checked-in `.bp`; scaffolding is a runtime copy | A sandboxed comptime build-input read | [`00`](./00-compiler-carry-over/README.md) · 14-comptime-on-beam (the sandbox is its prelude) |
| **Tuple labels are lost through generic instantiation** | 89 | Positional access after a generic hop | Preserve labels on instantiated tuple types | [`00`](./00-compiler-carry-over/README.md) · 01-checker |
| **No cancellation** — a losing racer and an expired timeout keep running | 02 | Document it; let the work complete and discard the result | Cancellation tokens, or linked processes with a kill path | — |
| **`@Decl` carries no source location** — a decorator cannot learn which file it annotates | 22 | The app-relative segment is an explicit decorator argument, generated and verified by front 50's CLI | A source-location field on `@Decl`. This is the defining premise of file-system routing, so the workaround is a CLI that must stay in step with the tree | [`00`](./00-compiler-carry-over/README.md) · 01-checker, **and** [`01-std/src-builtin.md`](./01-std/src-builtin.md) — `@src()` is the test-side half of the same intrinsic; the `@Decl` field reuses its `SourceLocation` record |
| **No `await` inside a `loop` or a closure** — the effect is the fn's return, not the closure's | 23 · 24 · 25 | Lift the awaited call out of the loop, or move the body into its own `@Task` fn | The loop half: `stream loop { … }` awaits inside its body (decisions [105](./decisions-taken.md#105-three-loop-keywords-and-generator-loop-is-a-generator-scope) and 125); the closure half: `async { … }` is a Task in the middle of any function (decision 124) | [`00`](./00-compiler-carry-over/README.md) · **24-effects-by-return** (both halves) |
| **Declared parameter defaults are never applied** | every front | Pass every argument explicitly — this is why every jhonstart call spells `attrs:`, and why `LayoutProps` is one record rather than three parameters | Apply declared defaults at the call site | [`00`](./00-compiler-carry-over/README.md) · 01-checker ("trailing defaults" is in its front description) |
| **No assignment to a `self` field** | 18 · 26 · 27 · 31 · 32 · 62 · 67 · 69 · 70 · 71 · 78 · 83 · 87 | Return a new value; `withAttribute` + explicit `store.save` for sessions (forgetting the save is uncatchable); front 62's request frame lives in the host process dictionary because of it | Either a mutable field form, or a documented statement that records are immutable by design | — (a language decision, 1.0.5 decision 37 says record fields are immutable; the gap is documentation) |
| **No bodyless method in a `type` body** | 08 · 09 · 78 | Give the method a body that calls a generated helper, or declare it at module level as `pub declare fn` with a host cell | Bodyless method declarations inside a `type`, as the natural shape for `#[query]`-style decorators | [`00`](./00-compiler-carry-over/README.md) · 15-language-surface |
| **A dot-shorthand path followed by a payload call does not propagate the typed-array context inside an array literal** — `[.Color.Hex("#abc")]`, `[.Arb.Value(prop: …)]`, `[.Container.At.Md(inner)]` (`repository/emilia/src/emilia.bp:498-503`). **Re-measured 2026-09-21 against `ead0b645`, and it needs no enum section and no collision — the minimal form is a plain top-level payload variant**: `fn take(ts: Array<Tok>)` called as `take([.Raw("x")])` reds ``unbound variable ''`` — *an empty name* — with the caret on the **open paren**, while `take([Tok.Raw(v: "x")])` in the same literal answers `1`. Whatever the right answer for this form is, a diagnostic that quotes the empty string is not it | 33 · 34 · 40 · 41–48 · 57 · 58 — every consumer of a payload-carrying token | A typed `val` intermediate, or a wrapper fn returning the token | Let the array's element type drive dot-shorthand resolution through the call | — |
| **A payload leaf nested inside an enum section cannot be constructed by any spelling** — re-verified 2026-09-21 against `ead0b645` on a minimal enum: `Tok.Color.Hex("#abc")` → `'Hex' is not declared in any behavior implemented for 'Tok'`; `val h: Tok = .Color.Hex("#abc")` → `unbound variable 'Color'`. **On the real `Token` the second one names a different type** — emilia front 41 measured `'Hex' is not declared in any behavior implemented for 'Ns'`, because the leading-dot resolver walked `.Color` into front 54's `Ns` enum, which happens to have a `Color` member, rather than into the annotated `Token` written one token earlier. The minimal repro cannot show that; both spellings are true and the difference is the whole of `01-checker` step 12. A top-level `Tok.Raw("#abc")` constructs, destructures and runs. **So `Color.Hex(value: string)` in today's `tokens.bp` is dead surface — declared, pattern-matched, unreachable.** | 33 · 40 · 46 · 47 · 57 | Every payload-carrying variant is a **top-level `Token` variant** (`Token.Arb(prop, value)`, `Token.InteractAccent(value)`), never a section leaf | Let the section-path resolver accept a trailing payload call | — |
| **A section-typed value cannot be constructed standalone** — `val a: Tok.Alpha = .50;` → `this token cannot appear here` — so a payload variant cannot take a section-typed field | 33 | Payload fields are builtin-typed (`i32`, `string`, `Token[]`): `Token.Alpha(percent: 50, inner: xs)` | Dot-shorthand rooted at the parameter's declared section type | [`00`](./00-compiler-carry-over/README.md) · 01-checker |
| **No module-graph reflection** — `@Decl` exposes declarations, not imports | 68 | `importsOf` is a textual scan that fails loudly on an unparsable line | An `imports` field on the module `@Decl` | — |
| **`if (a && b)` does not parse in condition position** — a compound boolean must be bound first | 53 · 60 | `val ok = a && b; if (ok) { … }` | Accept a boolean expression directly as the condition | [`00`](./00-compiler-carry-over/README.md) · 01-checker (`prec.equality` in an `if` condition is its named site in `parser/exprs.zig`, fronts.md note 17) |
| **No spelling for a negative numeric enum leaf** — numeric leaves are bare digits, so `-rotate-12`, `-translate-y-2` and every negative utility have no direct token | 35 · 36 · 45 | A `Neg { … }` sub-section convention (front 45 ships it) | A signed numeric leaf, or a unary `-` on enum paths | [`00`](./00-compiler-carry-over/README.md) · 15-language-surface |
| **No expression-position decorator** — a decorator is `@Decl`-first and annotates a declaration, so `#[emilia([.Pad.All.4])] div(…)` cannot exist; compounded by the sibling-fn rule, since a decorator body cannot call `emilia()` | 48 | Plain function calls | Expression-position decorators, or a comptime call form that runs in the eval script | — |
| **`xs[0]` silently drops the index on the BEAM backend** | every server front | `.at(i)`, `.first()`, `.slice(…)` | Fix the beam lowering, or reject the index expression at compile time rather than mis-compiling it | [`00`](./00-compiler-carry-over/README.md) · **15-language-surface** — there is **no index expression** in the grammar (`xs[0]` is a parse error in every position); 1.0.5 decision 63 answers `T` with an absent key a failure, so the row is a grammar + typing + four lowerings. Until it lands, `.at(i)` everywhere; `format --check` (16-formatter) leaks the desugaring |
| **A `behavior` method call lowers to another type's same-named method on erlang** — a method a `behavior` declares without a body, called on a receiver typed by that behavior, is emitted as `<Owner>:<name>(Recv, …)` for whichever type of the program declares `<name>/<arity>`, because `behaviorMethodNode` (`codegen/erlang.zig`) steps aside whenever `method_owners` holds the key. Minimal repro: `pub behavior Source { fn query(self: Self, name: string) -> string; }` · `pub type Table(rows: string) { pub fn query(self: Self, name: string) -> string { return self.rows + name; } }` · `fn read(s: Source) -> string { return s.query("k"); }` emits `read(S) -> zscratch@main@@Table:query(S, <<"k">>).` — measured on compiler `f011850c`; in rakun a handler's `req.query(…)` in any module that also imports rakun-web's `WebRequest` dies with `badarg` | 04 · 07 · every rakun front whose handler module imports `WebRequest` | Keep a `Request` handler out of modules that import a type declaring the same method, or read a path parameter (`modules/rakun-web/test/dispatch_seam_test.bp`) | Decide by the receiver's static type: a receiver typed by a `behavior` dispatches through the value (`(maps:get(M, V))(V, …)`) whatever other owners exist | [`00`](./00-compiler-carry-over/README.md) · 02-erlang |
| **A `return` inside an `if` nested in an `if` block is dropped on erlang** — the inner `if` lowers to `case … of true -> <<"big">>; _ -> ok end` whose value is discarded, and execution falls through to the statements after the outer block. Minimal repro: `fn pick(n: i32) -> string { if (n > 0) { val m: i32 = n * 2; if (m > 2) return "big"; }; return "small"; }` — `pick(5)` prints `small` under `botopink run --target erlang` (compiler `f011850c`). Wrong answers, not a refusal | every rakun front (met by 07's `errorEntry`) | Bind the condition and the value first, and keep every early `return` at the top level of the fn body | Lower an early return at any depth — or refuse it at check time with a located error rather than mis-compiling it | [`00`](./00-compiler-carry-over/README.md) · 02-erlang |
| **A METHOD returning `@Result` is compiled as a plain function on erlang** — a `throw` in it is an uncaught Erlang throw and a returned value is not wrapped in `Ok`; the same body in a module fn is an `Error` / `Ok` value. Minimal repro: `pub type Reader { pub fn read(self: Self, body: string) -> @Result<string, string> { if (body == "") throw "empty"; return body; } }` · `Reader().read("")` dies with `{nocatch, <<"empty">>}` under `botopink run --target erlang` (compiler `f011850c`) | every rakun front whose type methods refuse (met by 07's `#[messageConverter]`) | Keep the refusal in a module fn and `try` it from the method | Lower a method's `throw` like a module fn's | [`00`](./00-compiler-carry-over/README.md) · 02-erlang |
| **`xs.sort()` on a `string[]` type-checks and is not lowered on erlang** — it emits a bare local `sort(Xs)` and `erlc` refuses the module (`function sort/1 undefined`). Minimal repro: `val xs: string[] = ["b", "a", "c"]; @print(xs.sort().join(","));` under `botopink run --target erlang` (compiler `f011850c`) | 11 (the actuator's listings) | Sort in a host cell (`lists:sort/1`) or by hand | Lower `Array.sort` to `lists:sort/1` (or refuse it at check time) | [`00`](./00-compiler-carry-over/README.md) · 02-erlang |
| **A dependency's module body never runs on erlang** — `'_botopink_init'/0` runs for the entry module and the test module only, so a module-level `val` in any other module (a decorator's self-registration: `rkScan`, `rkRegisterQuery`, `rkRegisterHealthIndicator`) never executes when the module is reached as a library. Measured by rakun front 08 (`$HOME/.cache/bp-rakun/front-08/` repro, compiler `f011850c`) | 08 · 11 · every front that self-registers from library code | An explicit registration call the application makes at boot (`registerDbHealth()`, `mountActuator()`) | Run every loaded module's init once, in dependency order, before the entry's `main` | [`00`](./00-compiler-carry-over/README.md) · 02-erlang |
| **A transitive `path`/workspace dependency is not loaded** — with A → B → C, compiling A leaves B's call into C an `unbound variable`; a consumer must list C itself, before B | 08 (`rakun-data` → `rakun-actuator-api`) | List the transitive dependency directly | Resolve the dependency graph transitively | [`00`](./00-compiler-carry-over/README.md) · 10-cli-residuals |
| **`try` inside a lambda does not propagate** — the lambda's `try` does not return the `Error` from the enclosing fn, so an assertion written through it can pass vacuously | 08 | Bind the `@Result` and branch on it, or call a named fn | Either propagate out of the lambda (its own `@Result` return) or refuse `try` where it cannot propagate | [`00`](./00-compiler-carry-over/README.md) · 01-checker |
| **A test file in a `test/` subdirectory fails with `{error,undef}` on erlang** on every call into the project | 08 | Keep test files flat in `test/` | Load the project's modules for a nested test file as for a flat one | [`00`](./00-compiler-carry-over/README.md) · 11-tooling |
| **A record method named `length` hijacks `xs.length` on an array in the same module** — the array read calls the method (`badarg`) | 08 (`Rows.rowCount`) | Name the method something else | Resolve `.length` by the receiver's static type | [`00`](./00-compiler-carry-over/README.md) · 02-erlang |
| **A function-typed field of an IMPORTED record cannot be called directly on erlang** — `out.write(x)` with `out: ChunkWriter` imported from another module lowers to a method call `write/2` (`undefined`); in the declaring module, or through a local (`val w = out.write; w(x)`), it works. Measured on compiler `f011850c` (rakun front 23) | 23 (`ChunkWriter`) · every seam passed as a record of functions | Call through functions of the declaring module (`writeTo`, `setStatusOn`, …) or bind the field to a local first | Lower a call on a function-typed field by the receiver's record type wherever the type is declared | [`00`](./00-compiler-carry-over/README.md) · 02-erlang |
| **An imported fn-type alias resolves its names in the importer's scope** — `PageRenderer = fn(req: Request, …)` imported into a module that does not itself import `Request` makes a lambda `{ req, out -> … }` a type mismatch (`expected behavior Request … got Request`) | 23 (`examples/rakun-ssr`) | Import every type the alias mentions | Resolve an alias's body in its declaring module | [`00`](./00-compiler-carry-over/README.md) · 13-module-identity |
| **A method on a std `Dict` built in another module is `bp_unsupported_method`** — `queryDict(q).at("y")` from a test module raises `{bp_unsupported_method, <<"at">>, 1, {std@collections@@Dict, …}}` on erlang | 23 (`queryDict`) | Read the pairs (`splitQuery`) instead | Dispatch `Dict`'s methods by its type wherever the value was built | [`00`](./00-compiler-carry-over/README.md) · 02-erlang |
| **A method call with fewer arguments than the method declares type-checks** — `cookies().set("s", "1")` against `set(self, name, value, attrs)` compiles and dies at run time on erlang (`undef` / a raise the caller reads as a refusal); declared parameter defaults are not applied either | 25 · 23 (two tests that passed for the wrong reason until the third argument was written) | Pass every argument | Check a method call's arity like a function call's | [`00`](./00-compiler-carry-over/README.md) · 01-checker |
| **A `var` mutated inside a lambda body (outside a `for`) does not lower on erlang** — `var n = 0; captureSignals({ -> n = n + 1; 1; }, 0)` emits `Reached@1` unbound and `erlc` refuses the module | 63 (a "never reached" counter) | Record the fact outside the lambda (a property write) | Lower a captured-var write in any closure the way `for` bodies are lowered, or refuse it at check time | [`00`](./00-compiler-carry-over/README.md) · 02-erlang |
| **A method call on a value whose type is not imported dispatches to ANOTHER imported type's same-named method** — `logger("a").isEnabled(Level.Debug)` with `Logger` not imported lowered to `rakun@request_context@@DraftMode:isEnabled/2` (`undef`) | 17 | Import the receiver's type (and its closure) | Resolve by the receiver's inferred type whether or not it is imported | [`00`](./00-compiler-carry-over/README.md) · 13-module-identity |
| **A `case … Error(e)` pattern (or `val assert Error(e)`) did not match `{error, …}` from a fn using `try`/`throw`** although `r.isError()` was true (rakun front 17, `bootLogging()`; not reduced to a minimal repro) | 17 | Branch on `isError()` and read the message through a helper | Lower the `@Result` patterns against the same shape `isError()` reads | [`00`](./00-compiler-carry-over/README.md) · 02-erlang |
| **A decorator body can call no function** — neither a sibling (`call to undefined function problemOf/1`) nor an import (`json.quote`: "no primitive type … and no decorator host function provides") | 16 (`#[scheduled]` repeats the cron rules inline) · every decorator | Inline the logic in the body; test both copies against one corpus | Let a decorator body call pure functions of its own package | [`00`](./00-compiler-carry-over/README.md) · 14-comptime-on-beam |
| **`from` is reserved as an identifier**; in a dependency member `botopink check` then reports only a misleading "imported symbol is not exported" against the importer | 16 | Rename | A located diagnostic at the declaration | [`00`](./00-compiler-carry-over/README.md) · 15-language-surface |
| **`req.method == HttpMethod.Post` never matches on erlang** — the runtime hands the verb as a binary while `Request.method` is typed `HttpMethod` | 16 · 17 | Dispatch on the path or read the verb through a host cell | Adopt the verb into the enum at the host boundary (front 04's `request/6`), or type the field `string` | 04 (rakun) · 02-erlang |
| **In a type a decorator EMITS, `self.inner.m()` dispatches to the LAST decorated type of the module** — with two `#[methodSecurity]` types in one module, `LedgerSec.status` calls `Runbook:status/1` (`undef`); the same proxy written by hand is correct. Security-relevant: a proxy can call the wrong type's code. Repro: `$HOME/.cache/bp-rakun/front-10/repro_proxy_dispatch/` (compiler `f011850c`) | 10 · 08 (`#[transactional]` emits the same shape) | One such type per module | Resolve an emitted method call by the emitted field's declared type | [`00`](./00-compiler-carry-over/README.md) · 14-comptime-on-beam |
| **A workspace dependency's module cannot be imported as a namespace** — `import {jwt} from "rakun-security"` then `jwt.x()` is `unbound variable`; the same form works for the bundled `routing` and `std` | 10 | Import the leaves: `from "rakun-security/security"` | One resolution for bundled and workspace packages | [`00`](./00-compiler-carry-over/README.md) · 13-module-identity |
| **A value of an implementing type does not convert to its behavior type** — `val l: Lookup = memory();` with `memory()` returning the implementer is a type mismatch; a record literal typed as the behavior compiles | 10 | Hold a record of functions instead of a behavior value | Convert an implementer to its behavior at assignment | [`00`](./00-compiler-carry-over/README.md) · 01-checker |
| **A curried call does not type** — `adder(3)(4)` reports `unbound variable ''` (`tests/language/expected-failures.txt`) | 31 | `catchError(id, fallback, child)` returns the boundary record and the caller renders it | Type a call whose callee is a returned function | [`00`](./00-compiler-carry-over/README.md) · 01-checker |
| **A decorator body cannot call a sibling function** — only the decorator itself reaches the eval script (`rakun/src/decorators.bp:44-46`) | 29 · 30 | Every check is inlined, comment-free, in each decorator body (`client.bp`'s whitelist, `routes.bp`'s segment decoding) | Emit the module's other top-level fns beside the decorator | [`00`](./00-compiler-carry-over/README.md) · 01-checker (comptime) |
| **`botopink check` skips decorator invocation** — every `@emit`ted name reads as unbound there | 29 · 30 | Gate on `botopink test`, never on `check` (`__jhClient_<Name>`, `<page>Params`) | Run decorators under `check`, or report emitted names as known | [`00`](./00-compiler-carry-over/README.md) · 10-cli-residuals |


## Compiler-front gaps — 1.0.5-beta fronts the library tracks stand on

Rows that are not workarounds a library front invented but open compiler fronts the libraries
already pay for. Each is worked in [`00-compiler-carry-over`](./00-compiler-carry-over/README.md);
the *Bites* column is why it is listed here rather than only there.

| Gap | Bites | Nearest valid form today | 1.0.10 owner |
|---|---|---|---|
| **No `@src()` builtin** — a test cannot learn its own file, line and name, so a snapshot helper cannot compute `<dir>/__snapshots__/<suite>/<slug>.snap` (contract 7) | every `-test` submodule · every `test-snap.md` | Pass the path by hand, which is what makes snapshot tests unwritable at scale; there is no honest workaround | [`01-std/src-builtin.md`](./01-std/src-builtin.md) — a compiler builtin returning `SourceLocation(file, line, column, fnName)`, specified by 01-std, its compiler files a named carve-out from `00` (`fronts.md`); **pulled ahead** |
| **A record's identity is not in its value on erlang** — `Person(name: "a", age: 1) == Vec(name: "a", age: 1)` answers `true`; there is one module per file, not per type | every server front that compares records (contract 5b's signals, 62's epochs, `asserts.deepEqual`, `asserts.typeOf`) | Compare a discriminating field; never rely on two different record types being unequal | [`00`](./00-compiler-carry-over/README.md) · **13-module-identity** (module atom `std@math` · one BEAM module per `type`/`behavior` · the atom inside the value); **pulled ahead** — it re-records ≈318 erlang/beam cells and every library re-runs its erlang cells after it |
| **No module-level `var`** — `val hits = 0;` reassigned passes `check` and throws on node, does not compile on erlang | 04 (`runtime.mjs`'s registry: 96 of 231 lines, 13 of 16 `@External.Node` cells are registry maintenance), 05, 12, 16, 62 | A registry in the host process dictionary or an ETS table behind a host cell | [`00`](./00-compiler-carry-over/README.md) · **17-beam-memory** (`#[@BeamMemory.…]` and the `var` declaration; `libs/std/src/beam.bp` is layer 1) |
| **No index expression** — `xs[0]`, `d["k"]`, `s[0]`, `xs[0..2]` are parse errors in every position | every front (the `.at(i)` spelling everywhere; the BEAM row above is the same gap seen from the backend) | `.at(i)`, `.first()`, `.slice(…)`, `dict.lookup` | [`00`](./00-compiler-carry-over/README.md) · **15-language-surface** (grammar) + 01-checker (decision 63's typing) + the four backends |
| **The formatter refuses or rewrites shipped forms** — a trailing lambda's one-line body does not parse after formatting (`botopink format` refuses three committed `examples/jhonstart-app` files); `xs[0]` and `a ?? b` leak their desugarings | every example project (`format --check` is in the exit gate) | Write the multi-line lambda form; avoid the forms the formatter leaks | [`00`](./00-compiler-carry-over/README.md) · **16-formatter** |
| **`if` takes no compound condition** and `_` is not a binder — the checker half of two parser gaps | 53 · 60 · every front binding a boolean first | `val ok = a && b; if (ok) { … }` | [`00`](./00-compiler-carry-over/README.md) · **01-checker** (its named sites in `parser/exprs.zig`) |
| **`botopink run --target erlang` and the output layout** — `D/erl/std@math.erl` stems, `-pa` runner, crash exit `1` | 81 · 88 · every example run on erlang | Run through `botopink test` (fully green) rather than `botopink run` | [`00`](./00-compiler-carry-over/README.md) · **13-module-identity** (`cli/{build,run}.zig`) + 10-cli-residuals |

## Toolchain gaps — not the language, the compiler-cli

| Gap | Bites | Nearest form today | Fix | 1.0.10 owner |
|---|---|---|---|---|
| **A built erlang program cannot load its `.erl` sidecars** — `__bp_load_siblings/0` is emitted only under the test flag (`codegen/erlang.zig:1650-1670`) | 04 · 81 · 85 — every server front that ships a host module | Fully green under `botopink test --target erlang`; **cannot demonstrate `botopink run` serving HTTP on the BEAM**. Front 81's "the tarball starts and serves" acceptance is gated on it | One emitter change in `codegen/erlang.zig`: emit the sibling loader for `build` entry points too. Belongs to whoever owns the erlang entry-point emitter, not to rakun | [`00`](./00-compiler-carry-over/README.md) · 13-module-identity (it owns `codegen/erlang.zig` wholesale for halves 2–3) — the loader emission for `build` entry points |
| **A sidecar cannot reach an external OTP application** — the sidecar path loads `.erl` files beside the emitted module, not a dependency tree | 04 (why `gen_tcp` and not cowboy) · 85 (why the default SMTP client is over `std/net` and `gen_smtp` is an adapter that fails loudly) | Dependency-free implementations over OTP's own applications (`gen_tcp`, `ssl`, `crypto`) | A dependency declaration in `botopink.json` that the erlang build resolves into the release's application list | [`00`](./00-compiler-carry-over/README.md) · 10-cli-residuals + bpmp — a `botopink.json` field the erlang build resolves into the release application list; [`02-packaging/README.md`](./02-packaging/README.md) states the manifest side |
| **`DepSpec` has no subdirectory field** — a dependency is `{git, path, ref}`, and every `rakun-*` module is a directory inside one repository, so an out-of-tree consumer cannot install a starter from git | 73 | A `path` dependency; front 73 cannot ship a git-installable starter this milestone | A `subdir` field on `DepSpec`, resolved by `bpmp` | [`00`](./00-compiler-carry-over/README.md) · 10-cli-residuals (`compiler-cli/src/cli/config.zig`) + bpmp — and [`02-packaging/README.md`](./02-packaging/README.md), whose `modules/<lib>-<domain>/` layout is exactly a subdirectory dependency |
| **Sidecar module atoms collide with emitted basenames** — `shipErlSidecars` skips any atom matching a module this build emitted (`libs.zig:596`), and the failure is silent: the build succeeds and the program dies with `undefined function runtime:scan/1` | every front naming a sidecar | **Every sidecar is `src/sidecars/rakun_<name>.erl`**, never a bare `<name>.erl` that shadows an emitted `rakun/<name>` module | Make the collision a build error | [`00`](./00-compiler-carry-over/README.md) · 10-cli-residuals (`compiler-cli/src/cli/libs.zig:596`) |
| **`dependencies` in object form breaks bpmp** — the compiler (`compiler-cli/src/cli/config.zig` `DepEntry`/`DepSpec`) and the language server (`project_graph.zig` `extractDepNames`) normalise both `["rakun"]` and `{"rakun": {"git"|"path", "branch"}}`, but `bpmp` (`modules/bpmp/src/manifest.zig:85-96`) returns `ManifestInvalid` on anything but a string array, and `docs/botopink-json.md` (absent from this checkout, present in sibling worktrees) documents `[]string` only | 95 · every `modules/*/botopink.json` (rakun's thirteen already use the object form) · every `examples/*/botopink.json` | Object form for the compiler; do not run `bpmp` against a manifest in object form | [`02-packaging/README.md`](./02-packaging/README.md) states the shape; [`00`](./00-compiler-carry-over/README.md) · 10-cli-residuals + bpmp make `manifest.dependencies()` accept both |
| **A `path` dependency is resolved by name, not by path** — `DepSpec.path` is parsed and never read: `libs.zig:loadOne` looks for `<root>/<name>/botopink.json` across the root list (`BOTOPINK_LIB_ROOTS`, then `D/repository/botopink-lang/libs`, `D/repository`, `D/libs`, then `.botopinkbuild/deps/`), so `"rakun-web": {"path": "../rakun-web"}` resolves only if some root has `rakun-web/` as an **immediate child**. `repository/<lib>/modules/<sub>/` is a child of no root | 95 · every submodule import (`from "rakun-web"`), every example depending on a submodule | `BOTOPINK_LIB_ROOTS=repository/rakun/modules:…` when building, set by `scripts/test-libs.sh`; the runner's `--lib-root` for ad-hoc runs | [`02-packaging/README.md`](./02-packaging/README.md) (the env rule and the walk-up extension it asks for); [`00`](./00-compiler-carry-over/README.md) · 10-cli-residuals (`libs.zig`) |
| **`botopink-lib-test` sees only `<root>/*/botopink.json`** — `discovery.zig:discover` iterates the immediate children of each root; a lib "has tests" iff `test/*.bp` exists or a `src/**/*.bp` contains a `test` block; `modules/**` and `examples/**` are invisible, and a dependency's `entry: root.bp` is never followed — only its `files` list is loaded (`LibManifest{src, files}`), so a submodule manifest with `entry` and no `files` gives its consumer zero modules | 95 · every `-test` submodule · every example project · the exit gate's `zig build test-libs` | Add `repository/<lib>/modules` and `repository/<lib>/examples` to `BOTOPINK_LIB_ROOTS` (or `--lib-root`), and list every consumer-visible module in `files` | [`02-packaging/README.md`](./02-packaging/README.md) § Discovery; [`00`](./00-compiler-carry-over/README.md) · 10-cli-residuals (`modules/lib-test-runner/**` "where a fix needs it") |
| **`bpmp install`'s store is invisible to the scan** — `resolveFallbackRoots` adds `.botopinkbuild/deps` as a library root, and `bpmp install` materialises every entry under it with `symLink(…, .{ .is_directory = true })` (`modules/bpmp/src/commands/install.zig:311`), while `manifest.scanRoots` keeps a root child only when `entry.kind == .directory` — so no installed dependency is ever seen. Measured: a package reachable only through a symlink answers `error: dependency 'side' was not found under any library root`, exit 1; the same tree copied as a real directory builds | every consumer outside the meta checkout, i.e. every `bpmp install` | Depend by `path` or `git`, or copy the package as a real directory | `scanRoots` follows a symlink that resolves to a directory, or `bpmp install` stops materialising with symlinks — one of the two, not both | [`00`](./00-compiler-carry-over/README.md) · 10-cli-residuals (`modules/manifest/src/root.zig`) + bpmp |

## Unowned surface — not language gaps, but nobody's front

These came out of writing the fronts and have no owner. Each needs a decision before the wave that
needs it starts.

| Missing thing | First needed by | Note |
|---|---|---|
| **A std JSON walker** | 24 · 25 | `std/json` has no structured value, so a route handler can validate a body and return raw text and nothing more. Candidate for 1.0.10-beta, or a fold-in to a std front |
| **Module-level `pub val` of a user-defined type** — fronts 32 (`metadata`) and 70 (`§18`'s route surface) both export one, and it is unverified in the tree | 32 · 70 | Both use the same answer: a zero-argument `pub fn` | Verify or reject at the language level |
