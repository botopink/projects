# Snapshot review: comptime `infer_decls.zig` (42) and `variants.zig` (39)

Repo root: `/home/ericfillipe/develop/botopink-lang/repository/botopink-lang`
Short names used below:
- `SN/` = `modules/compiler-core/snapshots/comptime/node/`
- `SE/` = `modules/compiler-core/snapshots/comptime/node/errors/`
- `ID` = `modules/compiler-core/src/comptime/tests/infer_decls.zig`
- `VA` = `modules/compiler-core/src/comptime/tests/variants.zig`
- `INF` = `modules/compiler-core/src/comptime/infer.zig`

> **Status (1.0.1-beta close):** the harness defects this report leans on (H1-H10) are fixed: the RUN LOG is decided by the process exit status, an `erlc` warning no longer blanks a log, a program that does not compile fails its snapshot test (or records a `COMPILE DIAGNOSTIC`), every backend is compared in one round, and the 0-byte snapshots are gone. The checker rows below predate the comptime-folding wave and are otherwise untouched - re-derive each one at HEAD before acting on it. Residuals are tracked in [`1.0.2-beta/09-review-tooling/README.md`](../../1.0.2-beta/09-review-tooling/README.md). The tables below are the audit record and are kept verbatim.

## Re-check summary (2026-09-15, against HEAD)

- Every non-`ok` row (59) and every cross-cutting finding (X1-X10) was re-checked against the current source and snapshots. Every claim marked "(CLI)" was re-run with `zig-out/bin/botopink check`, **now rebuilt from HEAD** (mtime 2026-09-15 19:48), in scratch projects under `scratchpad/rev-ct-errors-decls/p/`. `zig build` was not run.
- All test `file:line` references still match (the test files are unchanged), and every quoted snapshot line is still accurate, with one exception: the `COMPTIME VALUES` section now prints `ct_N: <declaration> → <literal>` instead of `ct_N = <literal>` (see the `expressions_of_multiple_types` row). Copies are still byte-identical across `comptime/{node,erlang,beam,wasm}` and `…/errors`.
- Line drift: `snapshot.zig` +2 (failed-outcome arms are now 642-644, serializer lines +1); `eval.zig` grew a `numberValue` helper, so the folder lines moved to 102-113 / 202-207. `INF` line numbers held.
- No verdict changed. 6 rows and 6 cross-cutting notes were corrected (line numbers, one quote format, and three behaviour nuances). Nothing was withdrawn.

| re-check class | rows | cross-cutting (X) |
|---|---|---|
| confirmed | 53 | 4 |
| corrected | 6 | 6 |
| withdrawn | 0 | 0 |
| uncertain | 0 | 0 |

Corrections that matter (beyond line numbers):
- `expressions_of_multiple_types`: the `COMPTIME VALUES` quote is now `ct_0: val pi = comptime 3.14 * 2.0 → 0`. The folded values are unchanged (`0`, `101`, `0`).
- `complex_nested_patterns`: the suggested fix is insufficient. Naming the payloads makes the **enum declaration** parse, but the arms `Single(Ok(v))` and `Multiple([Ok(v), ..])` each still fail to parse on their own.
- X6 (spread positional hack): the probe result depends on the record's arity. With the 2-field `Person` used by the error tests, `Person(..alice, age: 25)` gives the same spread artifact (`expected string, found Person`); with the 3-field `Person` of `simple_field_update` it gives `'Person' expects 3 argument(s), got 2`.
- `multi_module_extension_activated_via_star_import`: `methodNotActive` is defined (`error.zig:217`) and rendered (`error.zig:305`, `helpers.zig:280`) but never constructed by the checker. Side finding: in a CLI project the `PatoNada*` import is rejected up front (`imported symbol is not exported by the named module`), so the CLI cannot mirror the harness here; the no-`*` variant checks OK, and so does a call to a method that exists nowhere.

## Header / counts

| verdict | infer_decls (42) | variants (39) | total (81) |
|---|---|---|---|
| ok | 15 | 7 | **22** |
| wrong-output | 6 | 7 | **13** |
| wrong-test | 5 | 10 | **15** |
| weak | 16 | 14 | **30** |
| uncertain | 0 | 1 | **1** |
| orphan | 0 | 0 | **0** |
| duplicate | 0 | 0 | **0** (near-duplicates are noted inside rows) |

(Verdict counts are unchanged by the re-check.)

Mechanical checks (scripts in `.../scratchpad/run-comptime-decls/`, re-check in `.../scratchpad/rev-ct-errors-decls/`):
- Slugs are computed with the `slugFromSrc` rule (text after the first `": "`, non-alphanumeric runs become `_`). No two snapshot-writing tests anywhere under `src/` share a slug. Re-check: every `slug → file:line` pair in the tables below still resolves.
- **69 tests write snapshots**: 61 `assertComptimeAst*` and 8 `assertTypeErrorSnap`. All 69 snapshot files exist. **12 tests have no snapshot**: they use `assertInfersOk`, or call `comptimeMod.compile` directly (`ID:270`).
- The `SOURCE CODE` sections of all 69 snapshots match the test bodies exactly (checked by script).
- Copies are byte-identical (`filecmp`, shallow=False; re-checked with `cmp`). Every AST snapshot matches its `comptime/{erlang,beam,wasm}` copy, and every error snapshot matches its `comptime/erlang/errors` copy.
- No orphans: every `*.snap.md` in `comptime/{node,node/errors,erlang,erlang/errors,beam,wasm}` maps to some test slug. There are no `.new` or other stray files.

How behaviour was confirmed: by reading the source, plus probes with `zig-out/bin/botopink check`. In the first pass that binary was stale (2026-09-14 11:23); **in the re-check it is rebuilt from HEAD**, and every probe reproduced its earlier result unless a row says otherwise. Claims resting mostly on the CLI stay marked "(CLI)".

---

## Cross-cutting findings (root causes behind many rows)

| # | Finding | Evidence | re-check |
|---|---|---|---|
| X1 | **Success snapshots silently record failed compiles.** `buildSnapshot` writes nothing for failed outcomes, so a test whose source fails to parse or type-check still "passes" and its snapshot holds only `SOURCE CODE`. 10 tests in this batch are in that state (see the rows marked *source-only*). | `modules/compiler-core/src/comptime/snapshot.zig:642-644`: `.validationError => {},` / `.typeError => {},` / `.parseError => {},`. Neither `helpers.assertComptimeAst` (`helpers.zig:83-124`) nor `snapshot.assertComptimeAstWithPath` (`snapshot.zig:679-687`) inspects the outcome. | corrected (line numbers; the 10 source-only files re-verified by grep) |
| X2 | **`fn_def` JSON is not inferred data.** Param and return types are read back from the syntax (`typeNameFromTypeRef`). Any typeRef that is not `.named` prints as `"?"`, and `body` holds raw source lines. Fn-centric tests therefore never show inferred types. | `snapshot.zig:236-239`: `.named => ... else => "?",`; `snapshot.zig:418,429`: `.type = typeNameFromTypeRef(p.typeRef)` / `typeNameFromTypeRef(rt) else "void"` | corrected (line numbers) |
| X3 | **Return statements are never unified with the declared return type.** Because of this, fn/lambda tests with a declared `-> T` do not check what the body returns. | `INF:2868-2871`: `// Infer body (for type checking; we ignore the result for now).` / `_ = try inferExpr(env, stmt.expr);`. `INF:5561`: a return is typed `void`, with no unify. (CLI) `fn g() -> string { return 1; }` → `Checked`. | confirmed (CLI re-run) |
| X4 | **A `case` expression's type is always a fresh type variable**, and the arms are never unified with each other. | `INF:7468`: `return TypedExpr{ .collection = .{ .loc = loc, .type_ = try env.freshVar(), .kind = .{ .case = ...`. (CLI) `val a: bool = case 42 { 0 -> "a"; _ -> "b"; };` → `Checked` | confirmed (CLI re-run) |
| X5 | **A method with no return annotation gets a fresh-var return type, and unknown methods fall through to a permissive path.** As a result, `x.m()` type-checks even when no method `m` exists. | `INF:1002-1006`: `Only methods with an explicit return-type annotation get a stored signature ... leave such calls to the fresh-var fallback`. `INF:6490`: `unknown methods fall through to the permissive` path. (CLI) `record Pato { id: i32 } val donald = Pato(1); val splash = donald.swim();` → `Checked`. | confirmed (CLI re-run) |
| X6 | **Record-update spread (`..x`) is a positional hack.** With one spread and fewer than two other args, the spread unifies positionally against field 0. Otherwise the remaining args unify positionally and **labels are ignored**. | `INF:7201`: `// Keep the historical spread behavior (and snapshots) for narrow update/error cases.`; `INF:7220-7230` (line 7228) unifies `f.params[paramIndex]` without looking at the label. (CLI, 2-field `Person`) `Person(..alice, name: "Bob", nickname: 25)` → `Checked`; `Person(..alice, age: 25)` → `type mismatch: expected string, got Person at main:6:20`. (CLI, 3-field `Person`) `Person(..alice, age: 25)` → `'Person' expects 3 argument(s), got 2`. | corrected (the arity-error probe needs the 3-field record; with 2 fields the positional artifact fires instead) |
| X7 | **The comptime folder only folds integers in binary ops.** Every `binaryOp` is folded with `evalConstInt`, so float and string operands become `0`. (A bare float literal does fold, via `numberValue`, `eval.zig:196-200` — it is the binary-op path that is integer-only.) | `modules/compiler-core/src/comptime/eval.zig:102-113`: `.binaryOp => |b| { const lhs = evalConstInt(b.lhs.*); ... .add => lhs + rhs,`; `eval.zig:202-207`: `.numberLit => |n| std.fmt.parseInt(i64, n, 10) catch 0, else => 0` | corrected (line numbers + the bare-float nuance) |
| X8 | **`"id"` on `record_def` / `enum_def` looks like a placeholder: it is `0` in every snapshot.** Two distinct records in one module both get `0` (outside this batch: `SN/disjoint_records_merge_correctly.snap.md` has `"name": "User", "id": 0` and `"name": "Timestamps", "id": 0`). The serializer defaults to 0 when the lookup misses (`snapshot.zig:443` for records, `454` for enums: `.id = resolvedTypeId orelse 0`). Root cause not isolated. | every `record_def`/`enum_def` in this batch shows `"id": 0` | corrected (line numbers) |
| X9 | **Interfaces, `implement` blocks, record/enum methods, enum variants and doc comments never reach the JSON.** | `snapshot.zig:459-466`: `interface_def` has only name/generic. `record_def` holds only `fields` (`436-446`). `implement` produces no binding. | corrected (line numbers) |
| X10 | **Record method bodies are best-effort and skipped on inference gaps.** | `INF:2911` (doc comment of `inferTypeMethods`): `This is BEST-EFFORT: a method whose body trips an inference gap is skipped`. (CLI) a method body `return self.nope;` → `Checked`; so does `fn quack(self: Self) -> i32 { return "x"; }`. | confirmed (CLI re-run, plus a second probe) |

---

## Non-`ok` findings

"CLI" means `botopink check` rebuilt from HEAD.

| slug | test file:line | verdict | re-check | evidence (quoted lines + path) | expected vs actual | suggested fix |
|---|---|---|---|---|---|---|
| expressions_of_multiple_types | VA:21 | wrong-output | corrected (quote format) | `SN/expressions_of_multiple_types.snap.md` now renders the listing with the declaration text: `ct_0: val pi = comptime 3.14 * 2.0 → 0` / `ct_1: val maxVal = comptime 100 + 1 → 101` / `ct_2: val banner = comptime "Hello, " + "World" → 0` (`eval.zig:60-74`), and TRANSFORM `val pi = 0;` / `val banner = 0;`, while the JSON says `"indent": "pi", "return_type": "f64"` and `"indent": "banner", "return_type": "string"` | Expected `→ 6.28` and `→ "Hello, World"`, with the transform keeping those values. Actual: both are folded to integer `0`, which changes program semantics (X7). | Make `eval.zig` fold float and string binary ops (or reject unsupported folds with an error), then regenerate. |
| case_or_patterns_with_block_arm_body | VA:136 | wrong-output | confirmed | `SN/case_or_patterns_with_block_arm_body.snap.md`: second arm `"ast": "block", "body": [ {"return_type": "string"}, {"return_type": "void"} ], "return_type": "void"` and case `"return_type": "?"` | Expected the block arm (`break value;` with `value = "odd"`) to be `string` and the case to be `string`. Actual: arm `void`, case `?` (X4). | Unify arm types into the case type, and type a `break v` block as `v`. |
| case_union_return_type_from_mismatched_arms | VA:148 | wrong-output | corrected (line numbers) | `SN/case_union_return_type_from_mismatched_arms.snap.md`: arms `"return_type": "string"` and `"return_type": "i32"`, case `"return_type": "?"` | The test name promises a union: expected `string \| i32` (`typeNameOf` supports `.union_`, `snapshot.zig:525-535`), or a type error. Actual: `?`. | Implement union or join for case arms, or turn this into an error test. |
| case_nested_case_in_block_arm | VA:157 | wrong-output | confirmed | `SN/case_nested_case_in_block_arm.snap.md`: block body `"return_type": "?"`, arm `"return_type": "?"`, case `"return_type": "?"` | Expected `i32` everywhere (all leaves are `54`/`1`). Actual: three unresolved vars. | Same fix as X4. |
| variant_mismatch (error) | VA:243 | wrong-output | confirmed | `SE/variant_mismatch.snap.md`: `error: type mismatch` / `6 │ val dog = Subject.Animal(..alice);` / caret at `:6:28` / `expected: string` / `found:    Subject` | Expected an error saying a `Person` variant cannot be spread into `Animal` (variant mismatch). Actual: a spread artifact that unifies `..alice` with `species: string` (X6, historical narrow path `INF:7202-7212`). | Implement real record-update semantics, including variant checking. |
| non_existent_field (error) | VA:254 | wrong-output | confirmed | `SE/non_existent_field.snap.md`: `error: type mismatch` / caret under `alice` at `:6:20` / `expected: string` / `found:    Person` | Expected `unknown field` `'Person' has no field 'nickname'` at `nickname`. Actual: the unrelated positional mismatch (X6). | Same fix as X6. |
| field_type_mismatch (error) | VA:265 | wrong-output | confirmed | `SE/field_type_mismatch.snap.md`: `6 │ val bob = Person(..alice, age: "thirty");` / caret at `:6:20` (under `alice`) / `expected: string` / `found:    Person` | Expected `expected: i32, found: string` at `"thirty"` (col 32). Actual: the same unrelated spread error, byte-identical to non_existent_field apart from the source. | Same fix as X6. |
| local_extension_method_resolves_without_activation | ID:232 | wrong-output | confirmed (CLI) | `SN/local_extension_method_resolves_without_activation.snap.md`: `"indent": "splash", "return_type": "?"` with `"expr": {"ast": "call", "params": [], "return_type": "?"}` | Expected `void` (no `->` on `fn swim(self: Self);`) or `i32` (body `return self.id;`), never an unresolved var. Actual: `?` (X5). It also cannot show that resolution happened: (CLI) `donald.swim()` with no implement at all also checks. | Infer or store a return type for unannotated methods, make unknown methods an error, and add an annotated `val splash: T`. |
| qualified_extension_call_needs_no_activation | ID:366 | wrong-output | confirmed | `SN/qualified_extension_call_needs_no_activation.snap.md`: `"indent": "splash", "return_type": "?"`, call `"params": [ {"value": "Pato"} ], "return_type": "?"` | Same as above: the `PatoNada.swim(donald)` result is unresolved. | Same as above. |
| multi_module_local_extension_resolves_on_an_imported_record | ID:382 | wrong-output | confirmed | `SN/multi_module_local_extension_resolves_on_an_imported_record.snap.md` main.json: `"indent": "splash", "return_type": "?"` | Same `?`. The test would also pass with the `implement` removed (X5). | Same as above. |
| multi_module_extension_activated_via_star_import | ID:405 | wrong-output | corrected (methodNotActive sites; CLI nuance) | `SN/multi_module_extension_activated_via_star_import.snap.md` main.json: `"indent": "splash", "return_type": "?"`; the use entry lists only `"indent": "Pato"` (no `PatoNada`). `methodNotActive` is never raised: it is defined at `error.zig:217`, formatted at `error.zig:305` and rendered at `helpers.zig:280-281`, with no construction site in `infer.zig`. | Same `?`. The `PatoNada*` activation leaves no trace in the snapshot and is not proven. (CLI) in a project layout the star import is rejected before inference (`imported symbol is not exported by the named module` for a `pub val … = implement …`), while the no-`*` variant checks — and so does `donald.fly()`, a method that exists nowhere (X5). | Same as above, plus a companion error test without `*` (and a project-level test for the export rule). |
| inherent_record_method_is_always_available | ID:429 | wrong-output | confirmed (CLI) | `SN/inherent_record_method_is_always_available.snap.md`: `"indent": "noise", "return_type": "?"` | Expected `void`/`i32`. Actual: `?`. (CLI) `val a: string = donald.quack(); val b: i32 = donald.quack();` → `Checked`. | Same as above. |
| optional_t_is_rejected_the_optional_type_is_t (error) | ID:567 | wrong-output | corrected (column) | `SE/optional_t_is_rejected_the_optional_type_is_t.snap.md`: source `fn takeOptional(x: @Optional<i32>) -> i32 {`, error `` error: `@Option<T>` is not a type — the optional type is written `?T` ``; no `┌─` location line. (CLI) same message, `in main`. | Expected the message to name `@Optional<T>`, with a location at **1:20** (the `@` of `@Optional`; the first pass said 1:19). Actual: a hard-coded `@Option<T>` and no loc (`INF:4698-4703`: the `"Option"` or `"Optional"` branch uses one fixed message without `.withLoc`). | Interpolate `b.name` into the message and attach the loc. |
| record_constructor | ID:33 | wrong-test | confirmed (CLI) | `SN/record_constructor.snap.md` is *source-only*: it ends right after `@print(p);` / ```` ``` ````, with no `TYPED AST JSON` | Top-level `@print(p);` is a parse error (CLI: `parser: UnexpectedToken at token[28] = builtinIdent '@print'` → `error: parse error in main`; without the `@print` line → `Checked`). The test passes via X1. | Remove the top-level `@print` (or wrap it in `fn main`), regenerate, and make the harness fail on non-ok outcomes. |
| pub_fn_basic_greet_returns_string | ID:54 | wrong-test | confirmed (CLI) | `SN/pub_fn_basic_greet_returns_string.snap.md` *source-only* (last source line `@print(msg);`) | Same parse error from a top-level `@print` (CLI confirmed; without it → `Checked`). Nothing about `greet` is checked. | Same as above. |
| pub_fn_with_local_val_binding_in_body | ID:64 | wrong-test | confirmed (CLI) | `SN/pub_fn_with_local_val_binding_in_body.snap.md` *source-only* (last line `@print(result);`) | Same (CLI: with the top-level `@print` → `error: parse error in main`; without it → `Checked`). | Same as above. |
| pub_fn_using_enum_case_in_body | ID:85 | wrong-test | confirmed (CLI) | `SN/pub_fn_using_enum_case_in_body.snap.md` *source-only* (last line `@print(n);`) | Same (CLI confirmed both ways). | Same as above. |
| record_with_fields_and_tostring_method | ID:169 | wrong-test | confirmed (CLI) | `SN/record_with_fields_and_tostring_method.snap.md`: `"lat": "number", "lon": "number"` | `number` is not a defined type. (CLI) `val x: number = 1;` → `type mismatch: expected number, got i32`, and `val R = record { lat: bogusType };` → `Checked`, so unknown field type names are silently accepted. It is also a near-duplicate of `record_with_method` (ID:41, `f64` fields plus a `val g = GPSCoordinates(...)`). | Use `f64`/`i32`, or merge into `record_with_method`. Add an error test for unknown field types. |
| shared_field_without_pattern_matching (error) | VA:186 | wrong-test | confirmed (CLI) | `SE/shared_field_without_pattern_matching.snap.md`: `6 │     r.kind` / caret `:6:7` / `'Result' has no field 'kind'` | `kind` is not a field of **any** variant (`Ok(value: i32)`, `Error(message: string)`), so the test never exercises a shared field. (CLI) a real shared field `Circle(name: string, …)`/`Square(name: string, …)` with `s.name` → `unknown field 'name' on type 'Shape'` too. | Access a truly shared field (decide the semantics), plus a non-shared one like `r.value`. |
| variant_does_not_escape_clause_scope (error) | VA:198 | wrong-test | confirmed | `SE/variant_does_not_escape_clause_scope.snap.md`: `10 │     return r.kind;` / `'Result' has no field 'kind'` | Same flaw: the error has nothing to do with clause scope, because `kind` exists nowhere. Cosmetic: the gutter is misaligned for 2-digit lines (`  │` vs `10 │`, fixed 2-space gutter at `helpers.zig:213,221`). | Access a variant-specific field (`r.value`) after the `case`. |
| non_empty_list_pattern | VA:276 | wrong-test | confirmed (CLI) | `SN/non_empty_list_pattern.snap.md` *source-only*: `val first_or_default = fn(list: i32[], default: i32) -> i32 {` | `default` is a keyword (`lexer.zig:667`: `"default"` → `.default`). (CLI) renaming it to `d` → `Checked`. | Rename the parameter and regenerate. |
| assign_pattern_in_enum | VA:287 | wrong-test | confirmed (CLI) | `SN/assign_pattern_in_enum.snap.md` *source-only*: `Ok(v) as result -> "Got: " + v;` | `as` patterns do not parse (CLI: parse error; without `as` → `Checked`). No docs or `.bp` code use `) as x ->`. | Implement `as` patterns, or convert to a parse-error test. |
| type_unification_does_not_allow_different_variants_to_be_treated_as_safe | VA:302 | wrong-test | confirmed (CLI) | `SN/type_unification_does_not_allow_different_variants_to_be_treated_as_safe.snap.md` *source-only*: `Ok(..) as b -> Wibble(..b, value: 1);` | The name (a Gleam port) implies an error is expected, but it is asserted as success. `Wibble`/`Wobble` are undefined, and `Ok(..)` plus `as` are parse errors (CLI: even `Ok(..) -> "a"` alone → parse error). | Rewrite in supported syntax as `assertTypeErrorSnap`. |
| assign_pattern_in_record | VA:317 | wrong-test | confirmed (CLI) | `SN/assign_pattern_in_record.snap.md` *source-only*: `Person(name, age) as person -> name + " is " + age;` | Same `as` parse error (CLI: without `as` → `Checked`). | Same as `assign_pattern_in_enum`. |
| complex_nested_patterns | VA:331 | wrong-test | corrected (fix is insufficient) | `SN/complex_nested_patterns.snap.md` *source-only*: `Single(Result<i32, string>),` / `Multiple(Result<i32, string>[]),` | Unnamed variant payloads do not parse (CLI: the enum pair alone → parse error; naming them `Single(r: …)` / `Multiple(rs: …)` makes the declarations check). But the `case` arms also fail: `Single(Ok(v)) -> v;` and `Multiple([Ok(v), ..]) -> v;` each still give a parse error with named payloads (nested patterns under an identifier-first payload; compare the parser note at exhaustiveness.zig:180-184). | Name the payloads **and** rewrite the arms to supported (flat) pattern syntax, then regenerate — or file the nested-pattern parse gap. |
| in_if_branch_infers_void | VA:406 | wrong-test | confirmed (CLI) | `SN/in_if_branch_infers_void.snap.md` *source-only*: `if x > 0 {` | `if` needs parentheses (CLI: `if (x > 0) {` → `Checked`; ID:534 uses `if (n < 0)`). | Add the parentheses and regenerate. |
| two_segment_path_hover_inner_resolves_to_ctor_call | VA:463 | wrong-test | confirmed (CLI) | Name says `.Hover.Inner`; `SN/two_segment_path_hover_inner_resolves_to_ctor_call.snap.md` source: `return .Text.Bold;` | The source never uses `.Hover.Inner`. (CLI) a real `return .Hover.Inner;` (`Hover(inner: i32)` has no sub-variant `Inner`) → `Checked`, which is a likely hidden resolver bug (compare ES4 erroring on `.Color.Bogus.500`). The JSON carries only `"source": "return .Text.Bold;"`, not the "lowered call shape" the comment at VA:459-461 claims. | Rename the test to `.Text.Bold`, and add a `.Hover.Inner` test (probably an error). |
| payload_sibling_color_hex_string_untouched | VA:491 | wrong-test | confirmed (CLI) | `SN/payload_sibling_color_hex_string_untouched.snap.md` source: `fn red() -> Token { return .Color.Red.500; }` | Name and comment claim `.Color.Hex("…")` type-checks, but the source never calls it. (CLI) `fn hex() -> Token { return .Color.Hex("ff0000"); }` → `unbound variable 'Color' at main:7:28`. Otherwise it is a subset of `three_segment_path_color_red_500_with_numeric_leaf`. | Actually use `.Color.Hex("ff0000")`, and fix or pin the resulting error. |
| record_with_method | ID:41 | weak | confirmed | `SN/record_with_method.snap.md`: `record_def` has only `"fields": {"lat": "f64", "lon": "f64"}`; `toString` is absent | The method signature and body are not in the snapshot (X9), and method bodies are best-effort (X10). | Serialize methods with inferred signatures. |
| interface_with_field_and_abstract_method | ID:150 | weak | confirmed | `SN/interface_with_field_and_abstract_method.snap.md`: only `{"ast": "interface_def", "name": "Drawable"}` | `val color: string` and `fn draw` are not captured, so the snapshot would not change if member inference regressed. | Serialize interface fields and methods. |
| interface_with_multiple_abstract_methods | ID:159 | weak | confirmed | `SN/interface_with_multiple_abstract_methods.snap.md`: only `{"ast": "interface_def", "name": "Canvas"}` | Same as above. | Same as above. |
| implement_single_interface_for_record | ID:181 | weak | confirmed | `SN/implement_single_interface_for_record.snap.md`: JSON = `Drawable` interface_def + `Circle` record_def only | Nothing about the implement is captured. Near-duplicate of `implement_block_is_invisible_to_the_binding_list` (ID:135) minus `val c`. | Merge into ID:135, or serialize implements. |
| implement_two_interfaces_with_qualified_methods | ID:195 | weak | confirmed | `SN/implement_two_interfaces_with_qualified_methods.snap.md`: two `interface_def` + `SmartCamera` record_def only | `UsbCharger.Connect` / `SolarCharger.Connect` are not reflected, and no qualified call is exercised. | Add `SmartCamera(...)` calls to both qualified methods, with annotated results. |
| doc_comment_on_function | ID:215 | weak | confirmed | `SN/doc_comment_on_function.snap.md`: the `fn_def` for `add` has no doc field (`params`, `return_type`, `body` only) | The JSON is identical to the same program without `//// Adds two numbers`. | Serialize doc comments, or drop the test. |
| doc_comment_on_record | ID:225 | weak | confirmed | `SN/doc_comment_on_record.snap.md`: `record_def` Point without doc | Same as above. | Same as above. |
| net_new_implement_an_interface_for_a_primitive_and_dispatch | ID:252 | weak | confirmed (CLI) | `assertInfersOk`; `val six = n.double();` (no annotation) | Would pass without the `implement` (X5; CLI `val six: i32 = n.double();` with no impl → `Checked`). | Annotate `val six: i32` and add a negative test. |
| net_new_inherent_method_wins_over_a_same_name_implemented_one | ID:328 | weak | confirmed | inherent `-> string` and interface `-> string`; `val l: string = t.label();` | Both candidates return `string`, so the test cannot tell which one wins. | Make the return types differ (inherent `i32`, iface `string`) and annotate. |
| net_new_chained_extension_calls_resolve_each_link | ID:351 | weak | confirmed (CLI) | `val r: i32 = n.inc().dec();` | (CLI) `val r: string = n.inc().dec();` with the impl, and `val r: i32 = n.inc().dec();` (and the `string` variant) without any impl → all `Checked` (X5). | Fix the permissive fallback, then add a negative case. |
| var_binding_mutable_local_inside_fn | ID:442 | weak | confirmed | `SN/var_binding_mutable_local_inside_fn.snap.md`: body `"source": "var n = 0;"`, `"source": "return n;"` | The inferred type of `n` never appears (X2), and returns are unchecked (X3). | Snapshot the typed locals. |
| var_binding_mutable_string | ID:452 | weak | confirmed | `SN/var_binding_mutable_string.snap.md`: `"source": "var msg = \"hello\";"` | Same as above. | Same as above. |
| star_fn_async_returns_future_is_valid | ID:469 | weak | confirmed | `SN/star_fn_async_returns_future_is_valid.snap.md`: `"return_type": "?"` for `-> @Future<i32>` | `?` comes from `typeNameFromTypeRef` on a non-`.named` typeRef (X2), so the snapshot neither shows `@Future<i32>` nor proves inference. | Render the inferred fn type. |
| star_fn_generator_returns_iterator_is_valid | ID:478 | weak | confirmed | `SN/star_fn_generator_returns_iterator_is_valid.snap.md`: `"return_type": "?"` for `-> @Iterator<i32>` | Same as above. | Same as above. |
| t_expect_proven_in_bounds_unwrap_returns_inner_type | ID:546 | weak | confirmed (CLI) | `val s = firstChar("abc").expect("");` (no annotation) | The name promises "returns inner type", but that is never asserted. (CLI) `val s: bool = …expect("")` → `type mismatch: expected bool, got string at main:3:36`, so an annotation would pin it. | Write `val s: string = ...`. |
| option_t_is_rejected_the_optional_type_is_t (error) | ID:556 | weak | confirmed | `SE/option_t_is_rejected_the_optional_type_is_t.snap.md`: `` error: `@Option<T>` is not a type ... `` / `hint: ...` with no `┌─ :line:col` block | The message is right but the location is missing, so a regression that reports the error from a different site is not caught. | Attach the loc of the `@Option<i32>` typeRef. |
| case_list_patterns_empty_single_spread | VA:117 | weak | confirmed | `SN/case_list_patterns_empty_single_spread.snap.md`: only `fn_def` with body sources `"val items = [\"a\", \"b\", \"c\"];"`, `"return case items {"` | The pattern bindings `x`, `first`, `rest` and the arm types are never shown (X2, X3, X4). | Use a top-level `val r = case ...` or typed-local serialization. |
| delegate_declaration | VA:130 | weak | confirmed (CLI) | `SN/delegate_declaration.snap.md`: `"declarations": []` | The declaration is absent from the JSON, so nothing is verified. (CLI) `declare fn Callback(msg: string) -> void; val c = Callback;` → `unbound variable 'Callback' at main:2:9`. | Serialize `declare fn`, or clarify what "delegate" means and assert it. |
| field_access_after_pattern_matching | VA:171 | weak | confirmed (CLI) | `SN/field_access_after_pattern_matching.snap.md`: `"body": [ {"source": "case r {"} ]` | The type of `v` and the arms are never captured, and returns are unchecked. (CLI) the same fn with `-> string`, or with arm `Error(m) -> m` under `-> i32` → `Checked`. | Fix X3/X4 and serialize arm types. |
| multiple_variants_with_different_fields | VA:214 | weak | confirmed (CLI) | `SN/multiple_variants_with_different_fields.snap.md`: body `"source": "case s {"` only | Same (CLI: with `-> string` → `Checked`). | Same as above. |
| simple_field_update | VA:231 | weak | confirmed | `SN/simple_field_update.snap.md`: `"params": [ {"name": "..", "value": "Person"}, {"name": "name", "value": "string"}, {"name": "age", "value": "i32"} ]` (record `Person { name, age, city }`) | `bob: Person` is right, but it passes only because `name`, `age` happen to line up positionally with fields 0 and 1 (X6). A spread with one label reds with an arity error, and a bogus label is accepted. | Implement label-based update, then add a one-field update case. |
| access_variant_specific_field_after_matching | VA:351 | weak | confirmed (CLI) | `SN/access_variant_specific_field_after_matching.snap.md`: body `"source": "case s {"` | Same as above (CLI: declared `-> string` → `Checked`). The case ends with `};`, so the fn tail is a statement. | Same as above. |
| pattern_matching_on_generic_enum | VA:366 | weak | confirmed | `SN/pattern_matching_on_generic_enum.snap.md`: `"name": "opt", "type": "?"`, `"name": "f", "type": "?"`, `"return_type": "?"` | Expected `Option<i32>`, `fn(i32) -> i32`, `Option<i32>`. Actual `?` from the serializer (X2), so generic instantiation is invisible. | Render resolved param and return types. |
| single_string_argument_infers_void | VA:381 | weak | corrected (line number) | `SN/single_string_argument_infers_void.snap.md`: `"return_type": "void"` (the default for no `->`), body source only | The `@print` result type never appears. `void` is the serializer default (`snapshot.zig:429`). | Use `val r = @print("hello")` at fn level with typed-local output. |
| multiple_arguments_infers_void | VA:389 | weak | confirmed | `SN/multiple_arguments_infers_void.snap.md`: same shape (`"source": "@print(\"x =\", 42, true);"`) | Same as above. | Same as above. |
| expression_argument_infers_void | VA:397 | weak | confirmed | `SN/expression_argument_infers_void.snap.md`: same shape | Same as above. | Same as above. |
| string_interpolation_argument | VA:418 | weak | confirmed | `SN/string_interpolation_argument.snap.md`: body `"@print(\"Hello, \" + name + \"!\");"` | Same as above. Also, the name says "interpolation" but the source is concatenation. | Same fix, plus a rename. |
| single_section_sibling_payload_variant_type_checks | VA:433 | weak | confirmed | `SN/single_section_sibling_payload_variant_type_checks.snap.md`: only `{"ast": "enum_def", "name": "Token", "id": 0}` | Sections and variants are not captured. Only an error-vs-ok regression would show. | Serialize variants and sections (or the synthesized inner enums). |
| nested_sections_with_numeric_leaves_type_check | VA:445 | weak | confirmed | `SN/nested_sections_with_numeric_leaves_type_check.snap.md`: only the `Token` enum_def | Same as above. | Same as above. |
| three_segment_path_color_red_500_with_numeric_leaf | VA:477 | weak | confirmed | `SN/three_segment_path_color_red_500_with_numeric_leaf.snap.md`: `"return_type": "Token"`, body `"return .Color.Red.500;"` | `Token` is copied from the typeRef (X2), and the return is not unified (X3). A resolver that yields the wrong type would not change the snapshot. | Use `val t: Token = .Color.Red.500;` at top level (binding type is inferred). |
| path_access_with_bad_tail_raises_focused_error (error) | VA:507 | uncertain | confirmed (CLI) | `SE/path_access_with_bad_tail_raises_focused_error.snap.md`: `┌─ :7:25` / `7 │     return .Color.Bogus.500;` with the caret under `500`. (CLI) same message and position: `… at main:7:25`. | The message is right. The loc points at the last segment (col 25) rather than the offending `Bogus` (col 19) or the chain head (col 12). Whether that is intended is unclear. | Consider pointing at `Bogus`. |

---

## `ok` slugs (22)

infer_decls.zig:
- `enum_constructors` (ID:21). Note: `c3` overlaps `dotident_resolved_from_type_annotation`; the id is always 0 (X8).
- `pub_fn_with_comptime_params` (ID:76)
- `val_with_explicit_type_annotation` (ID:109)
- `val_dependency_chain` (ID:117)
- `dotident_resolved_from_type_annotation` (ID:125)
- `implement_block_is_invisible_to_the_binding_list` (ID:135)
- `pub_val_infers_same_as_private_val` (ID:462)
- `test_body_typechecks` (ID:487, no snapshot)
- `anonymous_test_body_typechecks` (ID:499, no snapshot)
- `external_fn_no_body_typechecks` (ID:507, no snapshot)
- `std_package_import_binds_namespace` (ID:519, no snapshot)
- `builtin_result_namespace_qualified_calls_typecheck` (ID:530, no snapshot)
- `forward_reference_top_level_a_calls_b_declared_after` (ID:578, no snapshot; unbound-name check is meaningful)
- `mutual_recursion_rendertostring_and_renderchildren_call_each_other` (ID:585, no snapshot)
- `net_new_two_imported_libs_activating_the_same_method_are_ambiguous` (ID:270, no snapshot; asserts the specific error kind)

variants.zig:
- `single_module_basic_val_bindings` (VA:29)
- `import_single_val_from_dependency_module` (VA:38)
- `import_multiple_vals_from_dependency_module` (VA:50)
- `import_fn_from_dependency_module` (VA:64). Note: the use-declaration renders the fn `double` as `"return_type": "i32"` because `typeNameOf` maps `.func` to its return type (`snapshot.zig:508`), which is misleading but consistent.
- `three_level_chain_a_imports_b_b_imports_c` (VA:78)
- `unused_dependency_does_not_pollute_main_bindings` (VA:94)
- `import_record_constructor_from_dependency` (VA:105). Note: `models.bp` declares a non-`pub` `record Point` and the import still works. This is by design per `modules/compiler-core/src/comptime.zig:843-844`: "Non-pub types still export their constructor (above) for value use".
