# Snapshot review — codegen `values.zig` (32) · `dispatch.zig` (11) · `externals.zig` (9)

Repo: `/home/ericfillipe/develop/botopink-lang/repository/botopink-lang` (HEAD `beb19e9`).
Snapshot root below is abbreviated `S/` = `modules/compiler-core/snapshots/codegen/`
(`S/commonJS/`, `S/erlang/`, `S/beam/`, `S/wasm/`).

Method: read every snapshot of the 51 snapshot-producing tests (204 files) in full; extracted
every generated module into
`<scratch>/run-values-dispatch-externals/work/`
and ran it: `node`, `erlc` + `erl -s main _botopink_main`, `erlc +from_asm` + `erl`, `wasmtime run`.
Full raw run log: `.../run-values-dispatch-externals/results.txt`. Beam argument values were
confirmed with `dbg` traces. No repo file was touched, no `zig build` was run.

The 52nd test (`external ---- net-new: no target for the active backend errors`) produces no
snapshot by design (`expectError(error.MissingExternalTarget, …)`): **ok**.

## Counts (per slug×backend cell, 51 slugs × 4 = 204 cells)

| verdict | cells |
|---|---|
| ok | 62 |
| wrong-output | 60 |
| weak | 26 |
| skip-undocumented | 16 (4 slugs) |
| duplicate | 16 (4 slugs) |
| known | 11 |
| wrong-test | 11 |
| uncertain | 2 |
| orphan | 0 (no unmatched snapshot names in the tree) |

Plus 5 harness-level findings (H1–H5) that affect the reliability of RUN LOGs in this batch.

Note (once): `runtime.executeWat` is a stub (`runtime.zig:393-401`) so every wasm RUN LOG is
empty — **known**. Where the WAT is additionally invalid, or where wasmtime prints something
different from the other backends, the wasm cell is graded `wrong-output` (latent, masked by the
stub).

---

## Harness-level findings

| id | verdict | evidence | expected vs actual | suggested fix |
|---|---|---|---|---|
| H1 | wrong-output (harness) | `src/codegen/runtime.zig:364-365`: `const assemble_result = try runWithTimeout(allocator, io, &.{ "erlc", "+from_asm", "-o", tmp_dir, asm_filename }, RUNTIME_TIMEOUT_NS);` / `if (assemble_result.len == 0) return allocator.dupe(u8, "");` (same inverted test for aux at `:374-375`). `erlc +from_asm -o dir main.S` on success is silent (verified: `rc=0 out=0 err=0`). | A successful assembly must continue to `erl`; actual: success returns `""`, so on a cold cache **every** beam RUN LOG is empty. The committed beam RUN LOGs are served from `modules/compiler-core/.botopinkbuild/runtime-cache`, entries dated **Jun 27 2026**, i.e. before commit `80e2766` (Jul 1) that introduced the check. Verified cache hits by recomputing `cacheKey`: `fn_max_via_if_comparison` beam → `OK:7`, `dispatch_inherent_record_method_call` beam → `OK:#{n => 5}`, `external_call_emits_module_symbol` beam → `OK:ok`. | Invert to "return empty on non-zero exit" (check the process term, not output length). Then clear the cache and regenerate. |
| H2 | wrong-output (harness) | `runtime.zig:305-306`: `const compile_out = try runWithTimeout(… "erlc", "-o", tmp_dir, erl_filename …);` / `if (compile_out.len > 0) return allocator.dupe(u8, "");` combined with `runWithTimeout` appending stderr on success (`:61-67`). erlc prints warnings on stderr. | Any erlc **warning** blanks the RUN LOG. This is the `executeErlang` leak mechanism. In this batch it makes `external_a3_result_template_owned_declare_fn` erlang (`Warning: variable 'R' shadowed in 'fun'`) produce an empty RUN LOG on a cold cache, while the snapshot says `42` (cache hit `OK:42`, dated Jun 27). | Gate on erlc exit status only. |
| H3 | weak (harness) | `runtime.zig:12-16` doc says "return `result.stdout` as-is. stderr is dropped", but `runWithTimeout` (`:61-67`) concatenates stderr when non-empty. | Doc and code disagree. | Return stdout only, as documented. |
| H4 | known | `runtime.zig:393-401` `executeWat` returns `""`. | wasm RUN LOG always empty. | — |
| H5 | skip-undocumented (tree-wide context) | 116 zero-byte `.snap.md` files under `S/` (committed as the empty blob `e69de29`). 4 slugs of this batch are among them (see below). `utils/snap.zig:61-119` `compareOrCreate` only *creates* on `FileNotFound`; an existing empty file is compared, mismatches, writes `.new` and returns `error.SnapshotMismatch`. | Those tests are always red and have no recorded baseline. | Regenerate (delete the empty files so they get created) or mark the tests skipped with a documented reason. |

---

## Non-`ok` findings

| slug | backend(s) | verdict | evidence (quoted lines + path) | expected vs actual | suggested fix |
|---|---|---|---|---|---|
| val_number_literal, val_string_literal, val_null_literal, val_optional_annotation_with_null, val_pub_val_declaration | beam | known | e.g. `S/beam/val_number_literal.snap.md`: `{move, {integer, 42}, {x, 0}}.` / `{deallocate, 0}.` / `return.` (no `allocate`) | `deallocate` without `allocate` (known). No RUN LOG impact (no `main`). | — |
| val_binary_expression | all 4 | skip-undocumented | `S/{commonJS,erlang,beam,wasm}/val_binary_expression.snap.md` are **0 bytes** (`git ls-files -s` → `e69de29…`). Test `values.zig:29-34` (`val sum = 1 + 2; @print(sum);`) has no skip or comment. | Expected a snapshot with `3` in RUN LOG; actual: empty file → test is always red (H5). All 4 zero-byte slugs share the shape "top-level `val` + top-level `@print`" (related to the known erlang top-level-`val` issue). | Regenerate baselines or skip with a documented reason. |
| fn_private_function_with_return | all 4 | skip-undocumented | 0-byte snapshots in all 4 dirs; test `values.zig:36-44` (`val result = double(5); @print(result);`). | Expected RUN LOG `10`; no baseline. | same |
| fn_pub_exported_function | all 4 | skip-undocumented | 0-byte snapshots in all 4 dirs; test `values.zig:61-69`. | Expected RUN LOG `7`; no baseline. | same |
| fn_with_local_binding | all 4 | skip-undocumented | 0-byte snapshots in all 4 dirs; test `values.zig:71-80`. | Expected RUN LOG `20`; no baseline. | same |
| fn_max_via_if_comparison | node | wrong-output | `S/commonJS/fn_max_via_if_comparison.snap.md` TYPESCRIPT TYPEDEF: `export declare function max(a: , b: ): i32;` | Expected valid TS (`max(a: number, b: number): number`). Actual: parameter types are empty (syntax error), and `i32` is not a TS type. The same broken `(x: )` pattern appears in every `pub` fn typedef in this batch (external_call_emits_module_symbol `str_length(s: ): i32`, external_global_math `floor(n: ): f64`, external_import_binds_symbol `abs(n: ): i32`, external_a2_chained `b64encode(s: ): string`, external_a3 `parseInt(s: ): …`). | Emit parameter types in typedef output; map botopink numeric types to `number`. |
| fn_max_via_if_comparison | wasm | known | RUN LOG empty; wasmtime on the snapshot WAT prints `7`. | — | — |
| call_qualified_module_call_resolves_arity | node, erlang, beam | weak | `S/commonJS/…resolves_arity.snap.md`: `return List.map(this.items, f);` · `S/erlang/…`: `list:map(maps:get(items, Self), F).` · `S/beam/…`: `{call_ext_last, 2, {extfunc, list, map, 2}, 0}.` | No `main`, nothing runs. The target module `list` / global `List` do not exist on any host, so an arity bug would never show in a RUN LOG. Only the textual arity `2` is pinned. Also inconsistent naming between the two Erlang-family backends: erlang emits `run/2`, beam emits `'Pipeline_run'/2`. | Add a `main` that calls into a real (test-local) module, or pin arity with a needle assertion. |
| call_qualified_module_call_resolves_arity | wasm | wrong-output | `S/wasm/…resolves_arity.snap.md`: `local.get $f` / `call $map` (no `$map` func in module) | wasmtime: `Error: unknown func: failed to find name '$map'`. Invalid WAT. | Emit an import or a diagnostic for unresolved module calls. |
| call_qualified_module_call_with_trailing_lambda_arity | node, erlang, beam | weak | node `return List.map(this.items, (x) => {`; erlang `list:map(maps:get(items, Self), fun(X) ->`; beam `{make_fun3, {f, 6}, 0, 0, {x, 0}, {list, []}}.` … `{call_ext_last, 2, {extfunc, list, map, 2}, 0}.` | Arity 2 is correct textually; same weakness as above (nothing runs). | as above |
| call_qualified_module_call_with_trailing_lambda_arity | wasm | wrong-output | `S/wasm/…trailing_lambda_arity.snap.md`: `local.get $self` / `i32.load ;; .items` / `call $map` | This is exactly what the test name checks, and wasm gets it wrong: the trailing lambda is **dropped** (call with 1 arg). `$map` is also undefined (wasmtime: `unknown func … '$map'`). | Lower the trailing lambda as an arg (funcref/table index) or refuse to emit. |
| operators_comparison | wasm | wrong-output (latent) | `S/wasm/operators_comparison.snap.md`: `call $isPositive` / `call $__print_i32` | wasmtime prints `1\n0\n`; node/erlang/beam print `true\nfalse\n` → backends disagree. The shared prelude `$__print_i32` is also wrong for negative numbers with ≥2 digits: it does `local.get $buf; local.get $len; i32.add; local.get $buf; local.get $len; call $__memmove` (shift by `len`, not by 1). Verified with wasmtime: `-12` → `-21`, `-123` → `-231` (`-5` → `-5`, correct only by accident). | Print bools via a `$__print_bool` helper; fix memmove to `dst = buf+1`. |
| operators_equality_maps_to | node | weak | test name `"operators ---- equality maps to =="` vs `S/commonJS/operators_equality_maps_to.snap.md`: `return (n === 0);` | The name says `==`, the code emits `===` (erlang `=:=`). The slug drops the operator, so the name doesn't describe what is pinned. | Rename, e.g. "equality maps to strict equality". |
| operators_equality_maps_to | beam | uncertain | `S/beam/operators_equality_maps_to.snap.md`: `{test, is_eq, {f, 10}, [{x, 0}, {integer, 0}]}.` vs erlang `(N =:= 0).` | beam `is_eq` is `==` (arithmetic); erlang uses `=:=` (exact; beam equivalent `is_eq_exact`). Same result for i32 operands, but different for `1 == 1.0`. Whether this is a bug depends on the typing guarantees. | Use `is_eq_exact` for parity with erlang. |
| operators_equality_maps_to | wasm | wrong-output (latent) | `call $isZero` / `call $__print_i32` | wasmtime `1\n0\n` vs others `true\nfalse\n`. | bool printing |
| operators_logical_and | beam | wrong-output | `S/beam/operators_logical_and.snap.md` main: `{move, {atom, true}, {x, 0}}.` / `{move, {x, 0}, {x, 0}}.` / `{move, {atom, false}, {x, 0}}.` / `{move, {x, 0}, {x, 1}}.` / `{move, {x, 0}, {x, 0}}.` / `{move, {x, 1}, {x, 1}}.` / `{call, 2, {f, 3}}.` | Source `both(true, false)`. The 2nd arg is loaded into x0 **before** x0 is saved, so arg 1 is clobbered. dbg trace: `call main:both(false,false)`. RUN LOG `false` is right **by coincidence**. Root cause: call-arg materialisation lowers each arg into x0 and only then moves it to its scratch slot, but the first arg is never stashed. | Stash each lowered arg into a scratch x-register (`cur_arity+i`) before lowering the next one. Add a test whose result depends on arg order, e.g. `both(true, true)` / `sub(5, 3)`. |
| operators_logical_and | erlang | wrong-output (latent) | `S/erlang/operators_logical_and.snap.md`: `(A and B).` | `&&` is short-circuit on node (`&&`), beam (test-then-branch) and wasm-by-value. Erlang `and` evaluates both operands, so `a && sideEffect()` diverges. Not observable in this test. | Lower `&&`/`\|\|` to `andalso`/`orelse`. |
| operators_logical_and | wasm | wrong-output (latent) | `i32.and` / `call $__print_i32` | wasmtime `0` vs others `false`. | bool printing |
| operators_logical_or | beam | wrong-output | `S/beam/operators_logical_or.snap.md` main: `{move, {atom, false}, {x, 0}}.` / `{move, {x, 0}, {x, 0}}.` / `{move, {atom, true}, {x, 0}}.` / `{move, {x, 0}, {x, 1}}.` | Source `either(false, true)`; dbg trace: `call main:either(true,true)`. RUN LOG `true` correct by coincidence. Same root cause as logical_and. | as above |
| operators_logical_or | erlang | wrong-output (latent) | `(A or B).` | Not short-circuit (see above). | `orelse` |
| operators_logical_or | wasm | wrong-output (latent) | `i32.or` | wasmtime `1` vs `true`. | bool printing |
| operators_logical_not | wasm | wrong-output (latent) | `i32.eqz` / `call $__print_i32` | wasmtime `0` vs `false`. | bool printing |
| operators_chained_logical_and | erlang | wrong-output (latent) | `((A and B) and C).` | Same non-short-circuit lowering. | `andalso` |
| operators_chained_logical_and | node, beam, wasm | weak | no `main`/`@print` in source (`values.zig:163-169`) | Nothing executes, so a wrong chain would not change any RUN LOG. | Add `main` printing e.g. `allThree(true, true, false)`. |
| val_null_literal | beam | wrong-output | `S/beam/val_null_literal.snap.md`: `{move, {atom, nil}, {x, 0}}.` (from `beam_asm.zig:1724-1726` `.null_ => … {atom, nil}`) vs `S/erlang/val_null_literal.snap.md`: `undefined.` | Beam's own option helpers test absence with `{atom, undefined}` (`beam_asm.zig:3283`, `:3300`, `:4010`: `{test, is_eq, {f, …}, [{x, 0}, {atom, undefined}]}`). So a `null` stored in a `?T` is treated as **present** on beam (e.g. `unwrapOr` would return `nil`). Erlang uses `undefined` consistently. | Lower `null` to `{atom, undefined}` on beam. |
| val_optional_annotation_with_null | beam | wrong-output | `S/beam/val_optional_annotation_with_null.snap.md`: `{move, {atom, nil}, {x, 0}}.` | Same as above; the test is specifically about `?string = null`. | same |
| call_trailing_lambda_block | all 4 | wrong-test | source `values.zig:183-194`: `fn run() {` … `run { x ->` | `run` declares **no** parameter but is called with a trailing lambda; the compiler accepts the arity mismatch. Backends disagree: node `run((x) => {…})`; erlang `run(fun(X) -> …)` fails erlc: `function run/1 undefined, did you mean run/0?`; beam builds the fun and then `{call, 0, {f, 3}}` (fun dropped); wasm `call $run` (lambda dropped). The `@todo()` body means nothing observable ever runs. Minor divergence: beam lowers `@todo()` to `{move, {atom, undef}, {x, 0}}` + `erlang:error/1`, erlang to `erlang:error({todo, <<"not implemented">>})`. | Declare `fn run(f: fn(x: i32) -> string)`, give it a real body and `@print` the result. The type checker should reject the arity mismatch. |
| call_trailing_lambda_with_multiple_params | all 4 | wrong-test | source `values.zig:196-207`: `fn calc(factor: i32) -> i32 {` … `val r = calc(2) { a, b ->` | 1-param fn called with 2 args. erlang: `function calc/2 undefined, did you mean calc/1?`. beam: `{make_fun3, {f, 11}, 0, 0, {x, 0}, {list, []}}.` then `{move, {integer, 2}, {x, 0}}.` overwrites the fun, then `{call, 1, {f, 3}}.` wasm: lambda dropped. Nothing is printed. | Add the fn-typed parameter and print `r`. |
| val_pub_val_declaration | node | wrong-output | `S/commonJS/val_pub_val_declaration.snap.md`: `const VERSION = 1;` / `const HOST = "localhost";` (no `exports.`), while the typedef says `export declare const VERSION: i32;` | `pub val` is not exported (compare `exports.max = max;` for `pub fn`). Consequence visible in `S/commonJS/import_multi_module_pub_val_import.snap.md`: `config.js` has no exports, but `main.js` does `const { PORT, HOST } = require("./config.js");` → both `undefined`. | Emit `exports.<NAME> = <NAME>;` for `pub val`. |
| val_pub_val_declaration | erlang | wrong-output | `S/erlang/val_pub_val_declaration.snap.md`: `-module(main).` / `'VERSION'() ->` (no `-export`) | erlc: `Warning: function 'VERSION'/0 is unused`. `pub val` accessors are not exported, so a remote read is impossible. | Export `pub val` accessor functions. |
| val_pub_val_declaration | beam | known | `{exports, []}.` + `deallocate` without `allocate` | Known (imported pub val as atom / deallocate). Also `'VERSION'/0` not exported. | — |
| val_pub_val_declaration | wasm | wrong-output | `(global $VERSION (export "VERSION") i32 (i32.const 1))` vs `(global $HOST (mut i32) (i32.const 256))` | Inconsistent: one `pub val` exported, the other not (string pub val not exported and `mut`). | Export both. |
| comment_single_line_before_fn | wasm | wrong-output | `S/wasm/comment_single_line_before_fn.snap.md`: `(func $main` / `i32.const 0` / `)` | The void func leaves a value on the stack. wasmtime: `type mismatch: values remaining on stack at end of block`. Invalid module. | `drop` the unit expression statement, or emit nothing for `null;`. |
| comment_inside_function_body | node | wrong-output (cosmetic) | `S/commonJS/comment_inside_function_body.snap.md`: `// Initialize value;` / `// Return null;` | Comment statements get a statement terminator appended. | Don't append `;` to comment statements. |
| comment_inside_function_body | wasm | wrong-output | `(local $x i32)` / `drop` / `i32.const 1` / `local.set $x` / `drop` / `i32.const 0` | Each comment is lowered to a `drop` on an empty stack, plus a trailing value in a void func. wasmtime: `type mismatch: expected a type but nothing on stack`. | Skip comment statements in WAT body lowering. |
| doc_comment_multiline_before_struct | node | weak | `/** User account structure */` / (blank) / `/** Holds name and email */` / (blank) / `class Account {` | A 2-line doc comment becomes two separate JSDoc blocks, and only the last one attaches. Name says "struct", source is `record`. | Merge consecutive `///` lines into one JSDoc block. |
| assign_update_var_with_pluseq | node, erlang, beam | weak | source `var count = 0; count += 1; @print(count);`, RUN LOG `1` | `0 + 1` is indistinguishable from `count = 1` (a `+=` lowered as `=` would still print `1`). | Start from a non-zero value, e.g. `var count = 5; count += 2;` → `7`. |
| assign_update_var_with_pluseq | wasm | known | RUN LOG empty (wasmtime prints `1`). | — | — |
| field_assign_self_field_update | node | wrong-output | `S/commonJS/field_assign_self_field_update.snap.md`: `static inc() {` / `this.count += 1;` | A method with no `self` parameter is emitted as `static` (`commonJS.zig:1447-1453`), but the body uses `self` → `this` is the class, not an instance. | Reject `self` in a method without a `self` param, or treat it as an instance method consistently. |
| field_assign_self_field_update | erlang | wrong-output | `S/erlang/field_assign_self_field_update.snap.md`: `inc() ->` / `%% field assignment is not directly supported in Erlang.` | The function body is only a comment → erlc: `main.erl:5:7: syntax error before:`. Uncompilable module pinned as a baseline (`erlang.zig:2447`). | Emit a functional update (`Self#{count => maps:get(count, Self) + 1}`) or a hard codegen error. |
| field_assign_self_field_update | beam | wrong-output | `S/beam/field_assign_self_field_update.snap.md`: `{move, {integer, 1}, {x, 0}}.` / `{move, {x, 0}, {x, 0}}.` / `{move, {atom, self}, {x, 0}}.` / `{put_map_exact, {f, 0}, {x, 0}, {x, 0}, 1, {list, [{atom, count}, {x, 0}]}}.` | `self` is lowered to the **atom** `self`. The increment operand is overwritten before use, and the map update targets an atom (badmap at runtime). No read of the old `count`. | Bind self to a register; compute `count+1` into a scratch register. |
| field_assign_self_field_update | wasm | wrong-test | `(func $Counter_inc (param $self i32)` … `i32.store ;; .count +=` | wasm alone synthesises an implicit `$self` (correct store). The source uses an implicit-self method (`fn inc()` without `self: Self`), unlike the other tests (`fn run(self: Self, …)`). Backends interpret it 3 different ways. | Rewrite the source as `fn inc(self: Self)` (and decide whether implicit self is legal). |
| self_field_access_in_method | node | wrong-output | `static sum() {` / `return (this.x + this.y);` | Same static/`this` mismatch. | as above |
| self_field_access_in_method | erlang | wrong-output | `sum() ->` / `(maps:get(x, Self) + maps:get(y, Self)).` | erlc: `variable 'Self' is unbound` (twice). Uncompilable. | Add the `Self` param. |
| self_field_access_in_method | beam | wrong-output | `{move, {atom, self}, {x, 0}}.` / … `{get_map_elements, {f, 4}, {x, 0}, {list, [{atom, x}, {x, 0}]}}.` / `{label, 4}.` / `{move, {atom, self}, {x, 0}}.` / … `{get_map_elements, {f, 5}, {x, 0}, {list, [{atom, y}, {x, 0}]}}.` / `{label, 5}.` / `{gc_bif, '+', {f, 0}, 1, [{x, 0}, {x, 0}], {x, 0}}.` | Two bugs: (1) `self` is the atom `self`; (2) the value of `x` is overwritten by the `y` load, so the result would be `y + y`, not `x + y` (register clobber in binary-op lowering). | Stash the left operand in a scratch/y register before lowering the right one. |
| self_field_access_in_method | wasm | wrong-test | `(func $Point_sum (param $self i32) (result i32)` … `i32.load ;; .x` … `i32.load offset=4 ;; .y` | Correct under implicit-self; same ill-formed source as above. | `fn sum(self: Self)` |
| block_block_builtin | node, erlang | weak | source has no `@print` (`values.zig:297-309`); RUN LOG empty | The `@block` value (`"Baixo"`) is never observed. | `@print(status)`. |
| block_block_builtin | beam | wrong-output (latent) | `S/beam/block_block_builtin.snap.md`: `{move, {literal, <<"Alto">>}, {x, 0}}.` / `{deallocate, 2}.` / `return.` / `{label, 8}.` / `{move, {literal, <<"Baixo">>}, {x, 0}}.` / `{deallocate, 2}.` / `return.` / `{move, {x, 0}, {y, 2}}.` / `{move, {y, 2}, {x, 0}}.` | `return` inside `@block` is lowered as a **function** return, not a block-value exit. Equivalent here only because `status` is returned immediately. Dead code after it writes `{y, 2}`, outside `{allocate, 2, 0}`. | Lower block `return` to a jump to a join label that stores into `status`'s register. |
| block_block_builtin | wasm | wrong-output | `local.set $calculo` / `global.get $calculo` (no `(local $calculo …)` declared); `i32.const 264` / `return` / `local.set $status` | Undeclared local, read via `global.get`, and block `return` exits `main`. wasmtime: `unknown local: failed to find name '$calculo'`. | Hoist block locals; use `block`/`br` for the block value. |
| string_interpolation_lowers_to_concat | erlang | known (executeErlang leak) | `S/erlang/string_interpolation_lowers_to_concat.snap.md`: `io:format("~p~n", [((<<"hi ">> + Name) + <<"!">>)]).`; erlc output: `main.erl:6:36: Warning: evaluation of operator '+'/2 will fail with a 'badarith' exception` | Expected `<<"hi world!">>` (erlang `~p` formatting). Actual: the erlc warning is non-empty stderr → H2 returns `""`. Even without H2, runtime crashes `{badarith,[{erlang,'+',[<<"hi ">>,<<"world">>],…}`. | Lower template concat to `<<A/binary, B/binary>>` / `iolist_to_binary`. |
| string_interpolation_lowers_to_concat | beam | wrong-output | `S/beam/string_interpolation_lowers_to_concat.snap.md`: `{move, {literal, <<"hi ">>}, {x, 0}}.` / `{gc_bif, '+', {f, 0}, 1, [{x, 0}, {y, 0}], {x, 0}}.` / `{move, {literal, <<"!">>}, {x, 0}}.` / `{gc_bif, '+', {f, 0}, 1, [{x, 0}, {x, 0}], {x, 0}}.` | RUN LOG empty despite `@print`. Running it: `{badarith,[{erlang,'+',[<<"hi ">>,<<"world">>]…`. Two root causes: (1) same `+`-on-binaries as the known erlang issue (`beam_asm.zig:1716-1717`: "Desugared to a `+` chain by the transform pass"); (2) register clobber: the `<<"!">>` literal overwrites the first concat result, so even a working `+` would compute `"!" + "!"`. | Binary construction (`bs_create_bin`) + stash left operand. |
| string_interpolation_lowers_to_concat | wasm | wrong-output (latent) | `i32.const 268` / `local.get $name` / `i32.add` / `i32.const 276` / `i32.add` / `call $__print_i32` | Adds string **pointers** and prints an integer. wasmtime prints `800` (256+268+276) instead of `hi world!`. | Real string concat helper + string print. |
| net_new_interpolation_with_two_holes_lowers_on_every_backend | node | weak | no `main`; `return ((("" + a) + "-") + b);` | Test comment claims interpolation "lowers consistently on every backend", but nothing executes. | Add `main` printing `label("x", "y")`. |
| net_new_interpolation_with_two_holes_lowers_on_every_backend | erlang | known | `(((<<"">> + A) + <<"-">>) + B).` | Known `+` on binaries; would crash with badarith. It contradicts the test comment ("lowers consistently"). | as above |
| net_new_interpolation_with_two_holes_lowers_on_every_backend | beam | wrong-output | `S/beam/net_new_…two_holes….snap.md`: `{allocate, 0, 2}.` / `{move, {literal, <<"">>}, {x, 0}}.` / `{move, {x, 0}, {x, 2}}.` / `{gc_bif, '+', {f, 0}, 3, [{x, 2}, {x, 0}], {x, 0}}.` | Parameter `a` (x0) is overwritten by the `<<"">>` literal before use, so `a` is lost entirely. Plus `+` on binaries. | Don't lower literals into x0 while params still live there. |
| net_new_interpolation_with_two_holes_lowers_on_every_backend | wasm | wrong-output | `i32.const 256` / `local.get $a` / `i32.add` / `i32.const 260` / `i32.add` / `local.get $b` / `i32.add` | Pointer arithmetic instead of concatenation. | as above |
| net_new_record_equality_vs_array_equality_across_backends | all 4 | weak | No `main`; node `return (a === b);` / erlang `(A =:= B).` / beam `{test, is_eq, {f, 6}, [{y, 0}, {y, 1}]}.` / wasm `i32.eq` on heap pointers | The test pins a real cross-backend **semantic divergence** (record `==` is `false` on node/wasm, `true` on erlang/beam) as intended, but proves nothing by execution. Its comment is also inaccurate: it says erlang/beam both emit `=:=`, but beam emits `is_eq` (`==`), not `is_eq_exact`. | Print both results; fix comment or use `is_eq_exact`; decide whether `==` semantics should be unified. |
| implement_attaches_methods_to_prototype | node | wrong-test | name "attaches methods to prototype" vs `S/commonJS/implement_attaches_methods_to_prototype.snap.md`: `const PersonPrintable = {` / `print(self) {` / `return self.name;` | Nothing is attached to `Person.prototype`: an `implement` is a standalone object literal. The name describes a lowering that no longer exists. | Rename, e.g. "implement lowers to a standalone method table". |
| implement_attaches_methods_to_prototype | erlang, beam, wasm | weak | erlang `print(Self) ->` (unexported, erlc `Warning: function print/1 is unused`); beam `{exports, [{'Person_print', 1}]}.`; wasm `(func $Person_print` | No call site, nothing runs. Also erlang names the fn `print/1` while beam uses `'Person_print'/1` (and exports it). | Add a `main` calling `PersonPrintable.print(Person("x"))`. |
| dispatch_inherent_record_method_call | beam | wrong-output | `S/beam/dispatch_inherent_record_method_call.snap.md`: `%% unresolved method call: atual/1` then `{move, {x, 0}, {x, 1}}.` … `{call_ext, 2, {extfunc, io, format, 2}}.`; RUN LOG `#{n => 5}` | Expected `5`. The RUN LOG pins the receiver map. Root cause: the fallback path `beam_asm.zig:2327-2331` looks up `fnLabelsFor(cc.callee = "atual", 1)`, but the method is emitted as `'Contador_atual'/1` (`{function, 'Contador_atual', 1, 3}.`). The call is dropped and the receiver is printed. The `implement` path (`'Pato_swim'`) resolves; the inherent path doesn't. | Resolve inherent record methods through the mangled `'<Record>_<method>'` name; add a regression that fails on `#{…}`. |
| dispatch_inherent_record_method_call | wasm | wrong-output | `local.get $__mem0` / `local.set $c` / `call $atual` | `$atual` is undefined (func is `$Contador_atual`) and the receiver `$c` is never pushed. wasmtime: `unknown func: failed to find name '$atual'`. | Same mangling + push receiver. |
| dispatch_string_contains_lowers_to_native_includes | wasm | wrong-output | `i32.const 256` / `local.set $hw` / `i32.const 272` / `call $contains` | Undefined `$contains` and receiver not pushed (wasmtime `unknown func … '$contains'`). The test comment's claim that a record `contains` keeps its own dispatch is not exercised by any snapshot (minor weakness). | Prim-method lowering for wasm strings. |
| dispatch_string_startswith_lowers_to_native_startswith | wasm | wrong-output | `i32.const 272` / `call $startsWith` | Undefined `$startsWith`, receiver not pushed. | same |
| dispatch_auto_applied_extension_method_call, dispatch_qualified_extension_method_call | wasm | known | RUN LOG empty; wasmtime prints `2` / `3` (correct). | — | — |
| dispatch_multi_module_implement_on_an_imported_record | erlang | wrong-output | `S/erlang/dispatch_multi_module_implement_on_an_imported_record.snap.md`: `Donald = 'Pato'(2),`; erlc: `main.erl:14:14: function 'Pato'/1 undefined` | Expected RUN LOG `2`; actual empty. The imported record constructor is lowered as a call to a nonexistent local fn instead of a map literal `#{id => 2}` (as the single-module variant does). | Build imported records as maps (erlang records are maps). |
| dispatch_multi_module_implement_on_an_imported_record | beam | wrong-output | `S/beam/…imported_record.snap.md`: `{move, {integer, 2}, {x, 0}}.` / `%% unresolved local call: Pato/1` / … `{call, 1, {f, 3}}.`; `'Pato_swim'`: `{test, is_map, {f, 10}, [{x, 0}]}.` / `{get_map_elements, {f, 10}, …}` / `{label, 10}.` / `{deallocate, 0}.` / `return.` | The constructor is dropped: `donald` is the integer `2`. `Pato_swim(2)` fails `is_map`, and the fail label falls through returning the argument. dbg: `call main:'Pato_swim'(2)` → `returned … -> 2`. RUN LOG `2` correct **by coincidence** (id == constructor arg). The `is_map` fail label silently returning the input is itself a bug. | Construct imported records; fail labels must raise (`badmap`) not fall through. Use an id different from the printed value, or print `donald.swim() + 1`. |
| dispatch_multi_module_implement_on_an_imported_record | wasm | wrong-output | `;; cross-module import not linked (wasm single-module): Pato from pond` / `i32.const 0 ;; field access .id (unknown receiver type)` / `call $Pato` | `$Pato` undefined (wasmtime `unknown func … '$Pato'`); `swim` returns constant 0. | — |
| dispatch_multi_module_extension_activated_via_star_import | erlang | known (executeErlang leak) | `S/erlang/…star_import.snap.md` main.erl: `Donald = 'Pato'(2),` / `io:format("~p~n", [swim(Donald)]).`; erlc: `main.erl:7:14: function 'Pato'/1 undefined` and `main.erl:8:24: function swim/1 undefined` (pond.erl: `Warning: function swim/1 is unused`) | Expected `2`; empty because main.erl doesn't compile. Besides the constructor, the extension call is unqualified `swim(Donald)`: it should be `pond:swim(Donald)`, and pond must `-export([swim/1])`. | Qualify imported extension calls; export `pub val` implement methods. |
| dispatch_multi_module_extension_activated_via_star_import | beam | wrong-output | `S/beam/…star_import.snap.md`: `{move, {integer, 2}, {x, 0}}.` / `%% unresolved local call: Pato/1` / … `%% unresolved method call: swim/1` / `{move, {x, 0}, {x, 1}}.` … `io, format` | **Both** the constructor and the method call are dropped; the raw integer `2` is printed. RUN LOG `2` correct by coincidence. The test comment's claim that "`donald.swim()` lowers to the imported symbol" is false on beam: no `call_ext pond:'Pato_swim'/1`. | Resolve star-imported extension to `{call_ext, 1, {extfunc, pond, 'Pato_swim', 1}}`. |
| dispatch_multi_module_extension_activated_via_star_import | wasm | wrong-output | `call $Pato` / `local.set $donald` / `call $swim` | Undefined funcs, receiver not pushed. | — |
| delegate_emits_comment / delegate_declaration | all 4 | duplicate | `dispatch.zig:159-163` and `:174-178` have the identical source `declare fn Callback(msg: string) -> void;`; all 4 snapshot pairs are byte-identical (`cmp` equal). Additionally beam (`{labels, 2}.` only) and wasm emit **no** comment, contradicting "emits comment". | One test is redundant. | Delete `delegate ---- declaration` (or give it different content). |
| interface_emits_comment | beam, wasm | weak | `S/beam/interface_emits_comment.snap.md`: `{module, main}.` … `{labels, 2}.` (no comment); wasm module has no comment | Name "emits comment" holds only on node/erlang. | Scope the name or emit the comment. |
| external_call_emits_module_symbol | node | wrong-output | typedef `export declare function str_length(s: ): i32;`; RUN LOG empty although `@print` | Broken typedef. RUN LOG empty because `require("./gleam_stdlib.mjs")` throws `Cannot find module './gleam_stdlib.mjs'`, so the node call is never verified. | Point the fixture at a resolvable host symbol (e.g. a global) or ship the aux module. |
| external_call_emits_module_symbol | beam | wrong-output | `S/beam/external_call_emits_module_symbol.snap.md`: `{function, str_length, 1, 3}.` … `{move, {atom, ok}, {x, 0}}.` / `{deallocate, 0}.` / `return.`; main: `{call, 1, {f, 3}}.`; RUN LOG `ok` | Expected `5` (erlang backend prints `5`). The external `declare fn` is emitted as a local stub that returns `ok`, and the call site calls that stub instead of `{call_ext, 1, {extfunc, string, length, 1}}`. `beam_asm.zig` consults `externalFor` only for interface methods (`:844`, `:887`), never for top-level `declare fn`. | Handle `FnDecl.externalFor("erlang")` in beam: `call_ext` for the module/symbol form, template rendering for the `$0` form. |
| external_call_emits_module_symbol | wasm | wrong-output | `(func $str_length (export "str_length") (param $s i32) (result i32)` / `)` | Empty body for a func with a result. wasmtime: `type mismatch: expected i32 but nothing on stack`. Invalid module. | Import it (`(import "env" …)`) or refuse. |
| external_global_math | node | wrong-output | typedef `export declare function floor(n: ): f64;` | Broken typedef. RUN LOG `1` is fine. | typedef fix |
| external_global_math | erlang | ok (divergence noted) | RUN LOG `1.0` vs node `1` | Host float formatting differs (`~p` of `1.0` vs `console.log(1)`); whether `@print` of `f64` must be uniform is unspecified. | Decide `@print` float formatting. |
| external_global_math | beam | wrong-output | `{function, floor, 1, 3}.` … `{move, {atom, ok}, {x, 0}}.`; RUN LOG `ok` | Expected `1.0`; stub instead of `math:floor/1`. | as above |
| external_global_math | wasm | wrong-output | `(func $floor (export "floor") (param $n f64) (result f64)` / `)`; `f32.const 1.7` / `call $floor` / `call $__print_i32` | Empty body; `f32` literal passed to an `f64` param; `f64` result fed to `$__print_i32`. wasmtime: `expected f64 but nothing on stack`. | — |
| external_import_binds_symbol | node | wrong-output | typedef `export declare function abs(n: ): i32;`; RUN LOG empty (`Cannot find module './stdlib.mjs'`) | Broken typedef; the call is never executed. | as for external_call |
| external_import_binds_symbol | beam | wrong-output | `{function, abs, 1, 3}.` … `{move, {atom, ok}, {x, 0}}.`; RUN LOG `ok` | Expected `5` (erlang prints `5`). | as above |
| external_import_binds_symbol | wasm | wrong-output | `(func $abs (export "abs") (param $n i32) (result i32)` / `)` | Invalid (`expected i32 but nothing on stack`). | — |
| external_target_template_equivalent_to_external_target_template | all 4 | duplicate (+ wrong-test) | `externals.zig:51-61` source is byte-identical to `externals.zig:9-19`; all 4 snapshots `cmp`-identical to `external_call_emits_module_symbol`. The name promises a *template* form "equivalent to @external(target, template)", but the source uses the 2-arg module/symbol form and no template. | Redundant test; does not test what its name says. | Replace with a real template-vs-module/symbol equivalence check, or delete. |
| external_target_mixed_with_external_in_one_decl | all 4 | duplicate (+ wrong-test) | `externals.zig:63-75` differs from `:21-33` only by a space (`@External.Erlang( "math", "floor")`); snapshots differ only in that SOURCE line. The name and comment say "a single decl may use the new form for one target and the legacy form for another", but both annotations use the new `@External.<Target>` form. | Redundant; mixed form is not exercised (the legacy form may no longer parse — then the test should be removed). | Delete or rewrite. |
| external_a2_chained_host_call_renders_verbatim | node | wrong-output | typedef `export declare function b64encode(s: ): string;` | Broken typedef; JS body `console.log(Buffer.from("hi", 'utf8').toString('base64'));` and RUN LOG `aGk=` are correct. | typedef fix |
| external_a2_chained_host_call_renders_verbatim | erlang | wrong-output (divergence) | `%% external fn b64encode (no erlang target)` / `io:format("~p~n", [base64:encode(<<"hi">>)]).`; RUN LOG `<<"aGk=">>` | Node prints `aGk=`, erlang prints `<<"aGk=">>`: a backend disagreement on `@print` of a string (`~p`). The comment "(no erlang target)" is false: an erlang template is present and used. | Print binaries with `~s`; fix the comment. |
| external_a2_chained_host_call_renders_verbatim | beam | wrong-output | `{function, b64encode, 1, 3}.` … `{move, {atom, ok}, {x, 0}}.`; RUN LOG `ok` | Template `base64:encode($0)` ignored; stub returns `ok`. | beam external templates |
| external_a2_chained_host_call_renders_verbatim | wasm | wrong-output | `(func $b64encode (export "b64encode") (param $s i32) (result i32)` / `)` | Invalid. | — |
| external_a2_method_on_global_template_keeps_receiver_bound | erlang | wrong-output | `S/erlang/…receiver_bound.snap.md`: `io:format("~p~n", [iolist_to_binary(io_lib:format(\"~p\", [42]))]).`; erlc: `main.erl:7:57: unterminated string starting with "~p\", [42]))]).\n\n"` + `function '_botopink_main'/0 undefined` | Expected `<<"42">>`; RUN LOG empty despite `@print`. The botopink string escape `\"` in the annotation is copied **verbatim** into Erlang source instead of being unescaped to `"`. | Unescape annotation string args before template substitution (or use `"""…"""` in the fixture and add an escape test). |
| external_a2_method_on_global_template_keeps_receiver_bound | beam | wrong-output | `{function, stringify, 1, 3}.` … `{move, {atom, ok}, {x, 0}}.`; RUN LOG `ok` | Expected `<<"42">>`. Stub. The fn is also absent from `{exports, …}` while its siblings in other tests are exported; irrelevant here. | beam external templates |
| external_a2_method_on_global_template_keeps_receiver_bound | wasm | wrong-output | `(func $stringify (param $value i32) (result i32)` / `)` | Invalid. | — |
| external_a3_result_template_owned_declare_fn | node | wrong-output | typedef `export declare function parseInt(s: ): { tag: "Ok"; result: i32 } \| { tag: "Error"; error: string };` vs JS runtime shape `{ ok: __n } : { error: "not a number" }` and unwrap `"error" in _r ? ((-1)) : _r.ok` | The typedef describes a `{tag, result}` shape that the generated JS never produces; the param type is empty. The typedef also names `parseInt`, which shadows the JS global. | Align typedef with `{ ok } \| { error }`. |
| external_a3_result_template_owned_declare_fn | erlang | uncertain | `io:format("~p~n", [(fun(R) -> case R of {ok, V} -> V; _ -> ((-1)) end end)(R)]).`; erlc: `main.erl:8:29: Warning: variable 'R' shadowed in 'fun'` | Runtime output `42` is correct (verified), but per H2 the warning makes a cold-cache run produce an empty RUN LOG. The `42` comes from a stale cache entry (`OK:42`, Jun 27). The shadowing `fun(R)` is also a codegen smell (hygienic name needed). | Fresh temp name in the unwrapOr fun; fix H2. |
| external_a3_result_template_owned_declare_fn | beam | known | `{function, parseInt, 1, 3}.` … `{move, {atom, ok}, {x, 0}}.`; main: `{test, is_tagged_tuple, {f, 10}, [{x, 0}, 2, {atom, ok}]}.` → `{label, 10}.` `{move, {integer, -1}, {x, 0}}.`; RUN LOG `-1` | Known wrong RUN LOG. **Root cause confirmed:** the `#[@External.Erlang("""(fun(__S) -> …)($0)""")]` template is never rendered on beam. `parseInt` is a stub returning the atom `ok`, which fails `is_tagged_tuple {ok,_}`, so `unwrapOr(-1)` yields `-1`. Same root cause as every beam `ok` RUN LOG above. | beam external templates |
| external_a3_result_template_owned_declare_fn | wasm | wrong-output | `(local $r i32)` … `local.set $r` / `(local $_res0 i32)` (declaration after instructions); `(func $parseInt (export "parseInt") (param $s i32) (result i32)` / `)` | wasmtime: `unknown operator or unexpected token` at `(local $_res0 i32)`. Invalid WAT (locals must precede instructions; empty result body). | Hoist synthesized locals. |

---

## `ok` slugs

Fully ok on all 4 backends: `doc_comment_before_fn`, `module_comment_at_top_of_file`.

Also ok: the non-snapshot test `external ---- net-new: no target for the active backend errors`.

Ok on the listed backends (other cells graded in the table above):
- `val_number_literal`, `val_string_literal` — node, erlang, wasm
- `fn_max_via_if_comparison` — erlang, beam
- `operators_comparison`, `operators_logical_not` — node, erlang, beam
- `operators_equality_maps_to` — erlang
- `operators_logical_and`, `operators_logical_or` — node
- `val_null_literal`, `val_optional_annotation_with_null` — node, erlang, wasm
- `comment_single_line_before_fn` — node, erlang, beam
- `comment_inside_function_body` — erlang, beam
- `doc_comment_multiline_before_struct` — erlang, beam, wasm
- `string_interpolation_lowers_to_concat` — node
- `interface_emits_comment` — node, erlang
- `dispatch_inherent_record_method_call` — node, erlang
- `dispatch_string_contains_lowers_to_native_includes`, `dispatch_string_startswith_lowers_to_native_startswith` — node, erlang, beam
- `dispatch_auto_applied_extension_method_call`, `dispatch_qualified_extension_method_call` — node, erlang, beam
- `dispatch_multi_module_implement_on_an_imported_record`, `dispatch_multi_module_extension_activated_via_star_import` — node
- `external_call_emits_module_symbol`, `external_global_math`, `external_import_binds_symbol` — erlang
- `external_a2_method_on_global_template_keeps_receiver_bound` — node
