# Snapshot review — codegen `comptime.zig`, `std_package.zig`, `runtime_scratch.zig`, `comptime_module.zig`, `dts_skips_templates.zig` (+ cross-cutting)

Repo: `/home/ericfillipe/develop/botopink-lang/repository/botopink-lang` (HEAD `beb19e9`, clean tree).
Snapshot root below: `modules/compiler-core/snapshots/codegen/` (abbrev. `S/`).
Execution evidence was produced only in the scratch dir `run-codegen-comptime/` (node 25.8, OTP `/usr/bin/erl`, wasmtime 45). Scripts: `run_all.py` (extract every code section from every snapshot in this batch and run it: node / erlc+erl / erlc +from_asm+erl / wasmtime compile+run), `cachekey.py` (recompute `runtime.zig` cache keys), `orphans.py` (slug rule + orphan scan). Full run log: `run-codegen-comptime/run_all.log`.

## Verdict counts (36 tests: 26 comptime.zig, 2 std_package.zig, 3 runtime_scratch.zig, 3 comptime_module.zig, 2 dts_skips_templates.zig)

| verdict | count |
|---|---|
| ok | 6 |
| wrong-output | 14 |
| wrong-test | 5 |
| weak | 5 |
| duplicate | 1 |
| known | 5 |
| skip-undocumented | 0 |
| orphan | 0 |
| uncertain | 0 |

(Each slug gets one primary verdict = most serious; secondary issues are listed in the evidence column.)

---

## 0. Cross-cutting harness defects that explain most of the batch (read first)

These are root causes; the per-slug table references them as H1..H7.

**H1 — parse/type errors produce an EMPTY snapshot that passes vacuously.**
`codegenEmit` silently drops modules with `.parseError`/`.typeError`:
`src/codegen/commonJS.zig:52-53` (`.parseError => continue, .typeError => continue`), same at `erlang.zig:315-316`, `beam_asm.zig:403-404`, `wat.zig:96-97`. With zero outputs `buildSnapshotMulti` returns `""`; `utils/snap.zig:81` trims both sides and `"" == ""` passes. A 0-byte `.snap.md` therefore means "this source does not compile" and the test asserts nothing. There are **29 slugs × 4 backends = 116 empty snapshot files** in `S/` (list in §3); 6 are in this batch. Fix: `assertJs` must fail when `outputs.items.len == 0` or when any module had parse/type error; delete the empty files and regenerate.

**H2 — `assertJs`/`assertJsSingle` ignore `comptime_err`.** A validation error is emitted as `js = ""`, `comptime_script = null`, no run (`commonJS.zig:54-63`), and the snapshot records an empty code block with no RUN LOG (see `comptime_block_with_break`). Fix: in `helpers.assertJs` fail if `o.result.comptime_err != null`.

**H3 — `executeBeamAsm` has an inverted success check** (`src/codegen/runtime.zig:365`, same at `:375`, introduced by `80e27667`, 2026-07-01):
```zig
const assemble_result = try runWithTimeout(allocator, io, &.{ "erlc", "+from_asm", "-o", tmp_dir, asm_filename }, RUNTIME_TIMEOUT_NS);
if (assemble_result.len == 0) return allocator.dupe(u8, "");
```
`runWithTimeout` returns `""` on non-zero exit AND a silent successful `erlc +from_asm` also prints nothing (verified in scratch: rc=0, empty stdout/stderr). So on a cache miss every BEAM RUN LOG is `""`. Every non-empty BEAM RUN LOG in this batch (`page` ×4, `8081`, `-1\n<<"less">>`) is a **runtime-cache hit from 2026-06-27** — verified by recomputing the SHA-256 key of `runtime.zig:cacheKey` for each snapshot's code and finding `modules/compiler-core/.botopinkbuild/runtime-cache/<key>` with mtime `2026-06-27 01:12–01:14` (e.g. `template_end_to_end_holed_html_via_parts_runs` beam → `OK:page\n`). On a clean cache (CI / fresh clone) these tests would mismatch. Fix: `if (assemble_result.len != 0)` is also wrong (warnings) — check the exit status, not output length.

**H4 — `executeErlang` returns `""` whenever erlc emits any warning** (`runtime.zig:306`, `:314`): `runWithTimeout` merges stderr into the returned buffer, and erlc warnings go to stderr, so `if (compile_out.len > 0) return ""` drops the run for any module with an unused variable/shadowing warning. `builtin_result_namespace_qualified_call_lowers_inline` (warning `variable 'R' shadowed in 'fun'`) and `std_package_order_enum_module_with_type_export` (warnings on `Lt ->`) keep their erlang RUN LOG only through June-27 cache hits (`OK:42\n`, `OK:-1\n<<"less">>\n`). Fix: check exit status only.

**H5 — `executeWat` is a stub** (`runtime.zig:393-401`, `caa7377d`): returns `""` unconditionally → every wasm RUN LOG empty (known). Note wasmtime would *reject* most wasm modules of this batch (see table), so re-enabling execution will surface them.

**H6 — RUN LOG contains stderr on success** (`runWithTimeout` comment says "stdout-only", code appends stderr when non-empty), contrary to the contract at `runtime.zig:8-24`.

**H7 — the runtime cache key excludes the harness/toolchain**, so H3/H4 regressions are masked locally by stale entries (470 entries, oldest 2026-06-27).

**Generic backend bugs seen repeatedly (outside this batch too):**
- **B1 (beam)**: in any function with arity ≥ 1, the first local binding is materialized through `{x,0}` before the parameter is saved, clobbering the parameter. 54 function bodies across `S/beam` start with `{move, {integer|literal|atom,...}, {x, 0}}` right after `allocate` (scan in scratch). Verified at runtime: `'scale_$0'(100)` returns `4` (expected 200), `'scale_$1'(100)` returns `9` (expected 300); loop-unrolling `'execute_$0'(10)` returns `0` (expected 20).
- **B2 (wasm)**: specialized functions are injected with `.returnType = null` (`src/comptime/transform.zig:272`), so `wat.zig:875` omits `(result i32)` while the body still pushes a value + `return` → wasmtime: `type mismatch: expected i32 but nothing on stack`. Affects every `*_$N` function in the batch.
- **B3 (erlang)**: module-level runtime vals are bound as locals in `'_botopink_main'/0` (`src/codegen/erlang.zig:755-766`) but referenced as free variables from other functions → `variable 'X' is unbound` compile error (root cause of the known `Cfg`/`Page`/`COMMANDS` issues, also `Base`).
- **B4 (erlang)**: string `+` is emitted as arithmetic `+` on binaries (badarith) — also present in non-template fixtures such as `S/erlang/builtin_print_with_variable.snap.md` line 16 `io:format("~p~n", [(<<"Hello, ">> + Name)])`.
- **B5 (beam)**: references to module-level vals are lowered to atoms (`{move, {atom, page}, {x, 0}}`, `{atom, cfg}`, `{atom, base}`, `{atom, 'COMMANDS'}`).
- **B6 (wasm)**: module-level non-literal vals are not emitted (`global.get $page` / `$base` → `unknown global`), and `(local ...)` declarations are emitted mid-body (invalid WAT).

---

## 1. Findings table (all non-`ok`)

| slug | backend(s) | verdict | evidence | expected vs actual | suggested fix |
|---|---|---|---|---|---|
| comptime_folding_integer_addition_folds_to_literal | all 4 | wrong-test | All 4 files are 0 bytes (`S/{commonJS,erlang,beam,wasm}/comptime_folding_integer_addition_folds_to_literal.snap.md`, blob `e69de29`, empty since `0c30a38`). Source (`comptime.zig:19-20`) has a top-level statement `@print(v1);`; `@print` lexes as `builtinIdent` and `parser.zig:333-347` returns `UnexpectedToken` for it → parseError → H1. | Expected (after wrapping in `fn main`): `const v1 = 2;`, `ct_0 = 2`, RUN LOG `2`. Actual: nothing pinned; test passes vacuously. | Move `@print` into `fn main() {}`; delete the empty files; fix H1. |
| comptime_folding_block_with_break_value_inlines_result | all 4 | wrong-test | 0-byte snapshots; source `comptime.zig:26-29` ends with top-level `@print(t);` → parseError (H1). | Expected `const t = 24;`, `ct_0 = 24`, RUN LOG `24`. Actual: empty. | Same as above. |
| comptime_folding_float_multiplication_folds_to_literal | all 4 | wrong-test | 0-byte snapshots; top-level `@print(pi2);` (`comptime.zig:38`). | Expected `ct_0 = 6.28`, RUN LOG `6.28` (3.14*2.0 = 6.28 exactly in IEEE double). Actual: empty. | Same. |
| comptime_folding_multiplication_binds_tighter_than_addition | all 4 | wrong-test | 0-byte snapshots; top-level `@print(n);` (`comptime.zig:47`). | Expected `ct_0 = 14`, RUN LOG `14`. Actual: empty. | Same. |
| comptime_val_comptime_val_folds_arithmetic_to_literal | all 4 | wrong-test | 0-byte snapshots; top-level `@print(result);` (`comptime.zig:69`). | Expected `const result = 30;`, `ct_0 = 30`, RUN LOG `30`. Actual: empty. | Same. |
| comptime_validation_runtime_identifier_inside_comptime_raises_error | errors/all 4 | weak | Error text identical on 4 backends (md5 `5acd9491…`), location correct: `2 │     break greeting;` / `^^^^^^^^` at `:2:11` (col 11 = `g`). But (a) `greeting` is never declared — the test title says "runtime identifier" and the message says `'greeting' is a runtime identifier`, yet there is no runtime val at all (an unbound name hits the same branch `comptime/error.zig` `.identifier => … return ComptimeError{ .ident = name …}`); (b) the full source does not parse (top-level `@print(msg);`), so `codegen.generate` yields no outputs and the error is recovered by `helpers.extractComptimeValidationError` re-parsing a truncated prefix; (c) header `┌─ :2:11` has an empty file name. | Expected: a fixture with `val greeting = "hi";` before the comptime block, inside a parseable program, rendered with `main.bp:2:11`. Actual: validates an undeclared identifier in a truncated source. | Add `val greeting = "hi";`, wrap `@print` in `fn main`, pass a path so the location reads `main.bp:2:11`. |
| comptime_specialization_distinct_string_args_generate_specialized_functions | erlang, beam, wasm | wrong-output (also duplicate) | erlang: `((Prefix + <<": ">>) + Name).` → running it: `{error,badarith,[{erlang,'+',[<<"INFO">>,<<": ">>]…` (B4). beam `'build_$0'`: `{move, {literal, <<"INFO">>}, {x, 0}}.` overwrites param `Name` (B1), then `{gc_bif, '+', …}` → badarith at runtime (verified). wasm: `(func $build_$0 (param $name i32)` has no `(result i32)` (B2) and concatenates by `i32.add` of pointers; wasmtime: `type mismatch: expected i32 but nothing on stack`. RUN LOG empty everywhere only because `main` never prints. All 4 outputs are byte-identical to `comptime_specialization_same_string_arg_reuses_specialized_function` (diff "Files are identical" for beam/wasm; node/erlang differ only in the SOURCE line `prefix comptime:` vs `comptime prefix:`). | Expected `r1 = "INFO: Sistema iniciado"`, `r2 = "WARN: Memória alta"` (JS gives exactly that when called). Actual: crash (erlang/beam), invalid module (wasm). | Fix B1/B2/B4; add `@print(r1); @print(r2); @print(r3);` so RUN LOG pins behavior; rename test to cover the postfix `name comptime:` syntax or make the call sites differ from the "reuse" test. |
| comptime_specialization_same_string_arg_reuses_specialized_function | all 4 | duplicate | Same call sites and same generated code as the test above (only param-modifier position differs). Inherits the same erlang/beam/wasm wrong-output. | Expected: a distinct fixture (e.g. only repeated `"INFO"` args, asserting a single `build_$0`). Actual: duplicate. | Merge or differentiate. |
| comptime_specialization_distinct_integer_args_generate_specialized_functions | beam, wasm (+weak) | wrong-output | beam `'multiply_$0'`: `{move, {integer, 2}, {x, 0}}. {move, {x, 0}, {y, 0}}. {gc_bif, '*', {f, 0}, 1, [{x, 0}, {y, 0}], {x, 0}}.` → returns 2*2; same shape in `'scale_$0'` verified to return `4` for input 100 (B1). wasm: `(func $multiply_$0 (param $x i32)` no result → wasmtime `type mismatch` (B2). No `main`, so `calculate` never runs and RUN LOG can never show it (erlang `-module(main).` without exports; erl run: `main:_botopink_main/0 is not exported`). | Expected `double=42, triple=63, doubleAgain=20` (JS verified). Actual beam `4, 9, 4`; wasm invalid. | Fix B1/B2; rename `calculate`→`main` and print the three values. |
| comptime_specialization_comptime_val_used_as_specialization_argument | erlang, beam, wasm (+wrong-test) | wrong-output | erlang: `Doubled = 'scale_$0'(Base),` with `Base = 15` bound only in `'_botopink_main'` → erlc `variable 'Base' is unbound` (B3). beam: `{move, {atom, base}, {x, 0}}.` (B5) and param clobber in `'scale_$0'` (B1). wasm: `global.get $base` → wasmtime `unknown global: failed to find name $base` (B6) + missing result (B2). Test-name mismatch: `base` is passed in the *runtime* position (`scale(2, base)`), the comptime args are literals `2`/`3`, so "comptime val used as specialization argument" is not exercised. `ct_0 = 15` is correct (10+5). | Expected `30, 45, 200` (JS verified). Actual: erlang compile error, beam `4, 9, 4`, wasm invalid. | Fix B1/B3/B5/B6; use `scale(base, 2)` to actually specialize on the comptime val; add prints. |
| comptime_specialization_constrained_type_meta_kind_specializes_per_value | wasm | wrong-output | `(func $coerce_$0 (param $x i32)\n    local.get $x\n    return\n  )` — no `(result i32)`; wasmtime: `Invalid input WebAssembly code at offset 88: type mismatch` (B2). JS/erlang/beam correct (`coerce_$0(10)=10`, `coerce_$1(42)=42` verified on beam). No prints (weak). | Expected valid module. Actual invalid. | Fix B2 (`transform.zig:272` should carry `fn_decl.returnType`). |
| comptime_specialization_simple_function_body_without_loop | wasm | wrong-output | `(func $execute_$0 (param $input i32)` … `i32.add\n    return` without result (B2) → wasmtime type mismatch. Other backends correct. No prints (weak). | Valid module expected. | Fix B2. |
| comptime_loop_unrolling_single_if_condition_resolved_per_element | beam, wasm (+erlang minor) | wrong-output | beam `'execute_$0'`: `{move, {integer, 0}, {x, 0}}.` (`var output = 0`) overwrites `Input`, then `{gc_bif, '*', … [{x, 0}, {integer, 2}] …}` → 0; verified `execute_$0(10)=0`, `execute_$1(42)=0` (B1). wasm: missing result (B2) → invalid. erlang: `COMMANDS = ["calc", "noop", "help"],` emits charlists (the ct value text pasted verbatim) while every other string in the backend is a binary (compare partial test `[<<"calc">>, …]`); harmless here only because unused. `ct_0 = ["calc", "noop", "help"]` correct. | Expected `20, 84` (JS & erlang semantics). Actual beam `0, 0`; wasm invalid. | Fix B1/B2; render comptime string arrays as binaries in erlang; add prints. |
| comptime_loop_unrolling_nested_if_else_chain_fully_folded | beam, wasm | wrong-output | beam `'execute_$1'`: `{move, {integer, 0}, {x, 0}}. {move, {x, 0}, {y, 0}}. {move, {x, 0}, {y, 0}}. {move, {y, 0}, {x, 0}}.` → returns 0 (verified `0 0`). wasm missing result (B2). JS/erlang correct (`output = input`). | Expected `20, 42`. Actual beam `0, 0`; wasm invalid. | Fix B1/B2; add prints. |
| comptime_loop_unrolling_case_expression_folded_inside_unrolled_loop | beam, wasm | wrong-output | Generated code identical to the nested-if test on all backends (same beam bodies as above → `0, 0`; wasm missing result). | Expected `20, 42`. | Fix B1/B2; add prints. |
| comptime_partial_runtime_array_loop_preserved_comptime_param_specialized | erlang (known), beam, wasm | wrong-output | erlang (known): `end, Output, COMMANDS),` with `COMMANDS` bound only in `'_botopink_main'` → erlc `variable 'COMMANDS' is unbound` (B3, root `erlang.zig:755-766`). beam: `{move, {atom, 'COMMANDS'}, {x, 0}}` (B5), `lists:foreach` instead of a fold, closure body `%% assign to unknown variable: output` (unsupported, `beam_asm.zig:1546`), `{move, {atom, slug}, {x, 0}}`; running it: `{function_clause,[{lists,foreach_1,[#Fun<…>,'COMMANDS']…` crash. wasm: `i32.const 0 ;; loop over non-range\n    drop` — loop silently dropped (`wat.zig:2542`), plus missing result. | Expected `20, 84` (JS verified). Actual erlang compile error, beam crash, wasm loop removed + invalid. | Fix B3/B5; beam: lower mutated-var loops as `lists:foldl` like erlang; wasm: implement array loops or fail loudly. |
| comptime_block_with_break | all 4 | wrong-output | All 4 snapshots show an empty code block and no RUN LOG/typedef, e.g. node: ```` ```javascript\n``` ````. Cause: `comptime/error.zig` `validateComptimeExpr` has no arm for the local `val x = 10;` statement (falls to `else => return ComptimeError{…}`), so a validation error is produced; `commonJS.zig:54-63` emits `js = ""` and `assertJsSingle` never checks `comptime_err` (H2). | Expected `const result = 20;` with `ct_0 = 20` (and equivalents). Actual: a silent validation error snapshotted as success. | Allow local bindings inside comptime blocks in the validator (the evaluator already folds); make `assertJs` fail on `comptime_err`. |
| template_end_to_end_bounded_html_expansion | erlang, wasm, beam | known | erlang: `main() ->\n    io:format("~p~n", [Page]).` + `Page = ((<<"\n<p>">> + Name) + <<"</p>\n">>),` inside `'_botopink_main'` → erlc `variable 'Page' is unbound` (B3) and B4. beam: `{move, {atom, page}, {x, 0}}.` → RUN LOG `page` (B5, same root as known holed_html) and that RUN LOG is only a cache hit (H3). wasm: `global.get $page` → `unknown global` (B6) + `__print_i32` on a string; RUN LOG empty (H5). JS correct (`\n<p>world</p>\n`; `"""` preserves newlines by lexer design, `lexer.zig:373`). | Expected RUN LOG `<p>world</p>` block on every backend. | Fix B3/B4/B5/B6/H3/H5. |
| template_end_to_end_generic_expr_via_code_builtin | wasm | known | Code is correct on all 4 backends; wasm module is valid and `wasmtime run --invoke _botopink_main` prints `8081`, but RUN LOG is empty because of H5. beam RUN LOG `8081` exists only via cache hit dated 2026-06-27 (H3). | Expected wasm RUN LOG `8081`. | Restore executeWat; fix H3. |
| template_end_to_end_holed_html_via_parts_runs | erlang, beam, wasm | known | Confirmed root causes: erlang `Page = (((<<"">> + <<"<p>">>) + Name) + <<"</p>">>),` in `'_botopink_main'` vs `io:format("~p~n", [Page])` in `main()` (B3+B4; erlc: `variable 'Page' is unbound`); beam `{move, {atom, page}, {x, 0}}.` → `page` (B5; RUN LOG from June-27 cache, H3); wasm `global.get $page` undefined (B6). JS `((("" + "<p>") + name) + "</p>")` → `<p>world</p>` correct. | — | as above |
| template_end_to_end_line_string_template_with_hole | erlang, beam, wasm | known | Same three root causes (`Page = ((<<"<div>\n  <p>">> + Name) + <<"</p>\n</div>">>),`; beam `{atom, page}` → `page` cache-only; wasm `global.get $page`). JS `<div>\n  <p>world</p>\n</div>` correct. | — | as above |
| template_end_to_end_cross_module_html_mirrors_the_canonical_example | node typedef (new) + erlang/beam/wasm (known) | wrong-output | node `main.d.ts`: `import { html } from "view";` while `view.d.ts` is empty (template fns are dropped) → dangling import in the generated typedef. erlang/beam/wasm: same known root causes (`Page` unbound; `{atom, page}` → `page` cache-only; `global.get $page`). JS RUN LOG `<div>\n  <p>world</p>\n  <Page1/>\n</div>` correct. | Expected main.d.ts without the import of a template-only symbol. | Drop imports of template fns from `.d.ts` (the `dts_skips_templates` tests should cover this). |
| template_end_to_end_lookup_ref_splices_a_caller_scope_reference | all 4 | wrong-output | All 4 files are 0 bytes, truncated in commit `e699be9` ("add comptime type-manipulation functions mergeRecords/partial/omit/pick", −47/−38/−58/−192 lines). That commit added an inference-time builtin intercept on the bare callee name `pick` (`comptime/infer.zig:4051`, dispatch `:4066`, called unconditionally at `:6899` before user bindings), which shadows the test's user fn `pick` (`resolvePick` requires 2 args: "pick expects a type and field names") → typeError → H1 empty output. The same commit emptied the two `wat.zig` tests that also define `fn pick` (`optional_fn_return_null_path`, `optional_fn_return_present_path_with_optional_chaining`). Previous node snapshot (`git show e699be9^:…`) had `const s = greeting;` and RUN LOG `ola mundo`. | Expected JS `const s = greeting;` RUN LOG `ola mundo` (erlang/beam had their own known val issues). Actual: regression hidden by an emptied snapshot. | Only intercept `pick/omit/partial/mergeRecords` when no user binding of that name exists (or namespace them); restore the snapshots. |
| template_end_to_end_yaml_model_computes_a_typed_record | erlang, wasm (known), beam | known | erlang: `io:format("~p~n", [(maps:get(port, Cfg) + 1)]).` with `Cfg = #{port => 8004, debug => true},` in `'_botopink_main'` → erlc `variable 'Cfg' is unbound` (B3). beam: `{move, {atom, cfg}, {x, 0}}.` → runtime `{badarith,[{erlang,'+',[cfg,1]…` (B5; RUN LOG empty). wasm: `i32.const 0 ;; field access .port (unknown receiver type)` → wasmtime prints `1` (not 8005). Fold is correct: `8000 + "yaml".length = 8004`, JS RUN LOG `8005`. | Expected `8005` everywhere. | Fix B3/B5; wasm record field access. |
| builtin_result_namespace_qualified_call_lowers_inline | beam, wasm (+erlang H4) | wrong-output | beam `.S` is rejected by erlc: `function parse/1+9: Internal consistency check failed … Instruction: {test_heap,3,3} Error: {{x,1},not_live}` and same for `main/0+11` (`{test_heap,{alloc,[…{funs,1}]},3}`); snapshot RUN LOG empty hides it. wasm: `(local $_res0 i32)` inside `(then …)` → wasmtime `unknown operator or unexpected token --> main.wat:12:6`. erlang output correct (`42`) but erlc warns `variable 'R' shadowed in 'fun'`, so the RUN LOG survives only via the June-27 cache (H4). JS correct (`42`). | Expected RUN LOG `42` on beam/wasm. Actual: assembly rejected / invalid WAT. | beam: correct live counts for `test_heap`; wasm: hoist locals; erlang: avoid shadowing `R` in the inline fun. |
| std_package_order_enum_module_with_type_export | erlang, beam, node typedef, wasm | wrong-output | erlang `describe/1`: `S = case O of\n        Lt ->\n            <<"less">>;\n        Gt ->` — imported enum variants lowered as *variables* (erlc: `variable 'Lt' is unused`, `this clause cannot match`) → RUN LOG `-1\n<<"less">>`. beam `describe/1` and `order:toInt/1`/`reverse/1`: arms are emitted with no test/`select_val` at all (`{move, {literal, <<"less">>}, {x, 0}}. {jump, {f, 10}}.` then unreachable arms) → always first arm; RUN LOG `-1\n<<"less">>` (and cache-only per H3). node JS correct: RUN LOG `-1\ngreater`. node typedef `std/order.d.ts`: `export declare function toInt(o: ): i32;` / `reverse(o: ): Order;` — empty param type and non-TS type `i32` (invalid TypeScript); also `Order` is declared in `.d.ts` but not exported from `order.js`. wasm: `(local $__case_0 i32)` mid-body → invalid, case collapsed to first arm, `call $lt` referenced in main.wat without import. Also erlang/beam print `<<"less">>` (`~p`) vs JS `less` — cross-backend @print format divergence. | `describe(reverse(lt()))` = `describe(Gt)` = `"greater"`. Actual erlang/beam `"less"`. | Atomize imported enum variant patterns (erlang), emit `select_val` (beam), fix TS param type rendering, hoist wasm locals. |
| executeJavaScript cleans up scratch dir on success (runtime_scratch.zig:25) | — | weak | Calls `runtime.executeJavaScript(…, "console.log(42);", &.{}, io)` with empty `aux`; that path (`runtime.zig:193-199`) runs `node -e` and never creates a scratch dir (and may short-circuit on a cache hit), so `before == after` holds trivially. | Should exercise the aux path (`aux.len > 0`) that actually calls `makeScratchDir` + `deleteTree`. | Pass a non-empty `aux`. |
| executeJavaScript leaks under .botopinkbuild/tmp/, never as a root sibling (runtime_scratch.zig:39) | — | weak | Same: empty `aux` → no scratch dir created, so asserting no `.tmp-exec-*` sibling is vacuous. Title says "leaks under" but nothing is created. | Use the aux path with a throwing script. | Pass non-empty `aux`; rename. |
| .d.ts: free fn returning @Expr<T> is skipped (dts_skips_templates.zig:36) | node typedef | weak | Only negative checks (`Expr<`/`ExprCustom<` absent); does not assert that `plain` IS emitted, nor that no import of a template symbol leaks (the cross-module snapshot shows `import { html } from "view";` dangling). | Positive assertion for `export declare function plain(x: …)` expected. | Add positive needles + an importer module case. |
| .d.ts: interface method returning @Expr<T> is skipped (dts_skips_templates.zig:45) | node typedef | weak | Same: no assertion that `Tpl` / `name(): string` survive; a typedef that drops the whole interface (or `typedef == null`, `orelse ""`) also passes. | — | Add positive needles. |

---

## 2. Known items — root causes confirmed

- **wasm RUN LOG always empty**: `runtime.zig:393-401` `executeWat` discards its input and returns `""` (commit `caa7377d`). Note: for this batch only `template_end_to_end_generic_expr_via_code_builtin` (prints `8081`), `template_end_to_end_yaml_model_computes_a_typed_record` (valid but prints `1` instead of `8005`), `comptime_basic_comptime_val_and_plain_function_coexist` and `comptime_val_runtime_val_with_string_literal` produce wasmtime-valid modules; all others fail validation (B2/B6/mid-body locals).
- **erlang `template_end_to_end_*`**: `erlang.zig:755-766` binds runtime module-level vals as locals of `'_botopink_main'/0`, then `main/0` reads them → erlc "variable 'Page'/'Cfg' is unbound" → compile fails → `executeErlang` returns `""`. Independently the concat is `(<<…>> + Name)` (B4) which would badarith, and `~p` would print `<<"…">>` not raw text.
- **erlang `comptime_partial_runtime_array_loop…`**: same B3 (`COMMANDS` in `'execute_$0'/1`).
- **beam `template_end_to_end_holed_html_via_parts_runs` prints `page`**: `{move, {atom, page}, {x, 0}}` (B5); beam never materializes module-level vals. Additionally the `page` RUN LOG (and the identical ones in bounded/line_string/cross_module) is reproducible only from runtime-cache entries dated 2026-06-27 because of H3.
- **wasm `template_end_to_end_*` empty**: H5 + `global.get $page` on a never-declared global (B6) + `__print_i32` used for strings.

---

## 3. Orphans / missing snapshots (all codegen)

Method (`run-codegen-comptime/orphans.py`): for every `test "…" {` in `modules/compiler-core/src/codegen/tests/*.zig`, take the body up to the next test, detect `h.assertJs|assertJsSingle|assertJsError|assertJsTestMode(std.testing.allocator, @src()` (grep confirmed no other `@src()`/`SourceLocation` users and no direct `assertCodegen` calls outside helpers). Slug = `helpers.slugFromSrc`: strip `test.`, take text after the first `": "` (whole name if none), ASCII-alnum lowercased, runs of other chars → single `_`, trailing `_` dropped. Expected files: assertJs/assertJsSingle → node/erlang/beam/wasm; assertJsTestMode → node + erlang only (`configs[0..2]`); assertJsError → `errors/<4 backends>`.

Results:
- 296 `test` blocks; 280 produce snapshots (16 are needle/unit tests). No duplicate slugs, no test with two snapshot asserts, no `SkipZigTest`.
- `commonJS` 279 files = 279 expected; `erlang` 279 = 279; `beam` 278 = 278; `wasm` 278 = 278; `errors/*/*` 1 each = 1. No non-`.snap.md` files (no stray `.new`).
- **Orphans: 0. Missing: 0.**
- The 3 tests `runtime_scratch.zig`, 3 `comptime_module.zig`, 2 `dts_skips_templates.zig` produce no snapshots by design.

**But 29 slugs have 0-byte snapshots on all 4 backends (116 files) — vacuous passes via H1:**
`builtin_print_in_if_branch`, `builtin_print_in_loop`, `comptime_folding_block_with_break_value_inlines_result`, `comptime_folding_float_multiplication_folds_to_literal`, `comptime_folding_integer_addition_folds_to_literal`, `comptime_folding_multiplication_binds_tighter_than_addition`, `comptime_val_comptime_val_folds_arithmetic_to_literal`, `fn_private_function_with_return`, `fn_pub_exported_function`, `fn_with_local_binding`, `interface_literal_basic`, `interface_literal_with_fields`, `lambda_standalone_with_params`, `loop_even_numbers_with_break`, `loop_filter_with_conditional_break`, `loop_map_with_break_add_tax`, `loop_map_with_break_simple`, `narrow_and_condition_field_access`, `narrow_assert_pattern_with_print`, `narrow_case_option_some_none`, `narrow_early_return_with_print`, `narrow_type_guard_if_codegen`, `optional_fn_return_null_path`, `optional_fn_return_present_path_with_optional_chaining`, `string_slice_without_end_arg_slices_to_source_length`, `template_end_to_end_lookup_ref_splices_a_caller_scope_reference`, `throw_inside_case_arm`, `throw_inside_loop_body`, `val_binary_expression`.
(Spot checks: `values.zig:36` `fn private function with return` and `builtins.zig:265` `@print in loop` sources contain top-level statements / non-parseable forms; three `pick` fixtures were emptied by `e699be9`.) Commit `80e27667` message claims "Delete 110 orphaned empty snapshot files" — these 116 are not orphans; they are live tests whose output is empty.

---

## 4. `test_runner` asymmetry

- Produced by `src/codegen/tests/builtins.zig:291` `test "codegen: test runner"` → slug `test_runner` (text after `": "`). It calls `h.assertJsTestMode`, which iterates `configs[0..2]` (commonJS + erlang only, `helpers.zig:257`). The missing beam/wasm files are therefore **intended, not orphans**.
- Weaknesses:
  - erlang `S/erlang/test_runner.snap.md` RUN LOG is empty because the test-mode module has no `'_botopink_main'` (`-export([main/1]).`), so `executeErlang` early-returns at `runtime.zig` (`indexOf(erl_code, "_botopink_main") == null`). The erlang runner body (`'__bp_run_tests'/1`) is never executed by the snapshot.
  - node RUN LOG pins timing: `  duration 0ms` twice — fails if a test body takes ≥ 0.5 ms on a cold node; only stable thanks to the output cache (H7).
  - node RUN LOG embeds nested ```` ``` ```` fences and `----- RUN LOG -----` inside the outer ```` ```logs ```` block, which breaks Markdown fencing of the snapshot (cosmetic, but confuses section parsers).
  - beam/wasm test mode is untested by design (no documented reason in the test).

---

## 5. `ok` slugs / tests

- `comptime_val_runtime_val_with_string_literal` — correct on all 4 (wasm `(data (i32.const 256) "\0d\00\00\00Hello, World!")`, heap 276 = align4(256+4+13)); RUN LOG empty is correct (no main). Note: lives in the "comptime val" group but has no comptime.
- `comptime_basic_comptime_val_and_plain_function_coexist` — `ct_0 = 3` correct; JS/erlang/beam/wasm valid (wasmtime ok, beam assembles), RUN LOG empty is correct (no print).
- `makeScratchDir lands under .botopinkbuild/tmp/<hex>/` (runtime_scratch.zig:9).
- `comptime module: host enum member lowers to an atom, method call to a host fn` (comptime_module.zig:27) — static review only (not executed; zig build forbidden); needles consistent with `erlang.zig` comptime helper forms.
- `comptime module: \`+\` and \`.len\` dispatch at runtime, rebinding versions the variable` (comptime_module.zig:52) — static review only; `'__bp_add'`/`'__bp_len'`/`'__bp_json'`/`'__bp_text'` clauses exist in `erlang.zig:380-455` in the asserted shape.
- `comptime module: forEach with a mutated var fuses into a fold` (comptime_module.zig:72) — static review only.

## 6. COMPTIME VALUES audit (every `ct_N` in the batch)

| slug | ct line | hand-computed | result |
|---|---|---|---|
| comptime_basic_comptime_val_and_plain_function_coexist | `ct_0 = 3` | 1 + 2 = 3 | correct |
| comptime_specialization_comptime_val_used_as_specialization_argument | `ct_0 = 15` | 10 + 5 = 15 | correct |
| comptime_loop_unrolling_* (3 tests) | `ct_0 = ["calc", "noop", "help"]` | array literal | correct |
| comptime_folding_* (4), comptime_val_comptime_val_folds_arithmetic_to_literal | none (empty file) | 2, 24, 6.28, 14, 30 | missing (H1) |
| comptime_block_with_break | none | 20 | missing (validation error, H2) |
| template_end_to_end_yaml_model_computes_a_typed_record | (no section; folded into code) `port: 8004` | 8000 + len("yaml") = 8004 | correct |
