# Snapshot review: codegen `builtins.zig` (31 tests) + `aggregates.zig` (22 tests)

Repo root: `/home/ericfillipe/develop/botopink-lang/repository/botopink-lang`

Path legend used below:
- `S/node/<slug>` = `modules/compiler-core/snapshots/codegen/node/commonJS/<slug>.snap.md`
- `S/erl/<slug>` = `modules/compiler-core/snapshots/codegen/erlang/erlang/<slug>.snap.md`
- `S/beam/<slug>` = `modules/compiler-core/snapshots/codegen/beam/beam/<slug>.snap.md`
- `S/wasm/<slug>` = `modules/compiler-core/snapshots/codegen/wasm/wasm/<slug>.snap.md`

Scratch verification dir: `<scratch>/run-builtins-aggregates/`
(`erl/<slug>/erlc_out.txt`, `beam/<slug>/asm_out.txt`, `wasm/<slug>/compile_out.txt`, `beamrun/run.py`, `beamrun/run2.py`, `jsrun/checks.js`, `cachekey.py`).
Tools: OTP 29 `erlc`/`erl`, `node`, `wasmtime 45.0.0`. Nothing in the repo was modified, and `zig build` was not run.

## Counts

53 tests: 52 snapshot slugs + 1 `assertJsContains` test (`codegen: test runner excluded from normal build`, no snapshot). `test_runner` uses test mode, so it only has node and erlang snapshots. That is expected. No slug collisions and no orphan snapshots were found for these prefixes. I checked this against all `test "..."` names under `src/`.

Primary verdict per test (the worst verdict wins):

| verdict | tests |
|---|---|
| ok | 5 |
| wrong-output | 39 |
| wrong-test | 4 |
| weak | 5 |
| known (primary) | 0 (known issues also show up as secondary items in 16 tests, see table) |
| uncertain (primary) | 0 (3 secondary uncertain items) |
| skip-undocumented / orphan / duplicate | 0 |

## Harness-level findings (affect several slugs in this batch)

| id | verdict | evidence | expected vs actual | suggested fix |
|---|---|---|---|---|
| H1 | wrong-output (harness) | `src/codegen/runtime.zig:364-365`: `const assemble_result = try runWithTimeout(... "erlc", "+from_asm" ...);` `if (assemble_result.len == 0) return allocator.dupe(u8, "");` | A successful `erlc +from_asm` prints nothing. I checked this: all 44 assemblies that succeeded here had empty stdout and stderr. So the check is inverted, and a cold-cache run returns `""` for **every** BEAM RUN LOG. The non-empty BEAM RUN LOGs in this batch (`builtin_print_single_argument`, `builtin_print_multiple_arguments`, `builtin_print_expression`, `builtin_print_return_value_void`, `record_implement_fields_round_trip_at_runtime`) only reproduce because matching entries exist in `modules/compiler-core/.botopinkbuild/runtime-cache/`. I recomputed the SHA256 keys in `cachekey.py` and all 5 hit, e.g. `beam builtin_print_multiple_arguments CACHE HIT: 'OK:<<"Hello">>\n'`. If the cache is cleared, those snapshots change. | Invert to `if (assemble_result.len > 0) return ""`, the same as `executeErlang`. Then regenerate without the cache. |
| H2 | weak (harness) | `runtime.zig:306` `if (compile_out.len > 0) return allocator.dupe(u8, "");` plus `runWithTimeout`, which appends stderr on exit 0 | Any erlc **warning** (these go to stderr) blanks the erlang RUN LOG. `S/erl/builtin_print_return_value_void` shows `<<"started">>`/`<<"done">>`, but erlc prints `main.erl:9:5: Warning: variable 'X' is unused` for that file, so a cold-cache run gives an empty RUN LOG. The current log comes only from cache entry `erl builtin_print_return_value_void CACHE HIT: 'OK:<<"started">>\n<<"done">>\n'`. This is the same "executeErlang leak" family. | Only treat a non-zero erlc exit as failure (`+nowarn` or check the exit code), or stop emitting unused bindings (`_X`). |
| H3 | known | `runtime.zig:393-401` `executeWat` is a stub | Every wasm RUN LOG is empty (noted once here and not repeated per test). Running the WAT myself with wasmtime found real output bugs, listed below. | - |
| H4 | weak | `runtime.zig:275` `if (std.mem.indexOf(u8, erl_code, "_botopink_main") == null) return ""` | In test mode the erlang module only exports `main/1`, so `S/erl/test_runner` has an empty RUN LOG. Running it by hand (`erl -eval 'main:main([])'`) prints the same envelope as node (`2 passed, 0 failed`). | Also run `main/1` in test mode, or emit `_botopink_main` in test mode. |
| H5 | weak | Every `S/node/*` in this batch has `----- TYPESCRIPT TYPEDEF -- main.d.ts` containing only blank lines, even for records with classes (e.g. `S/node/record_two_fields`) | A `.d.ts` for `class Point { x; y }` would be expected, but it is blank. The typedef section pins nothing. | Check the typedef emitter, or drop the empty section. |

## Findings table (all non-`ok` items)

| slug | backend(s) | verdict | evidence (quoted lines + path) | expected vs actual | suggested fix |
|---|---|---|---|---|---|
| assert_simple_assertion, assert_with_arithmetic_comparison, assert_with_message, assert_array_equality | beam | wrong-output | `S/beam/assert_with_message` (the same body appears in all 4): `{allocate, 0, 0}.` `{move, {atom, undefined}, {x, 0}}.` `{move, {atom, ok}, {x, 0}}.` `{deallocate, 0}.` | The assertion should be evaluated and raise on false. Actually the body is dropped entirely: after exporting it, `f()` for `assert false, "error message"` returned `ok` (`beamrun/run.py`: `m_as1:f() => ok`). | Lower `assert` in beam_asm, e.g. `is_eq_exact` + `erlang:error`. |
| same 4 | wasm | wrong-output | `S/wasm/assert_simple_assertion`: `(func $f` `i32.const 0` `)` | The module should be valid. wasmtime rejects it: `type mismatch: values remaining on stack at end of block` (`wasm/assert_*/compile_out.txt`, all 4). The assert is dropped and the func has no result. | Lower assert to `if (i32.eqz cond) unreachable`, and never leave a value in a void func. |
| same 4 | node | uncertain | `S/node/assert_with_message`: `console.assert(false, "error message");` | In normal builds `console.assert` does not throw. It prints `Assertion failed: error message` to stderr and execution continues (checked in `jsrun/checks.js`). Erlang raises (`true = (false)` → badmatch), and test mode uses a throwing `__bp_assert`. It is unclear whether non-fatal asserts in a normal JS build are intended. | Decide on the semantics. If asserts should be fatal, lower to `if (!c) throw ...`. |
| assert_with_message | erlang | wrong-output | `S/erl/assert_with_message`: `true = (false).` | The message `"error message"` should reach the error, but it is dropped. erlc also warns `main.erl:4:5: Warning: no clause will ever match`. | Emit `case C of true -> ok; _ -> erlang:error({assert, Msg}) end`, as test mode already does. |
| assert_array_equality | node vs erlang | weak | `S/node/assert_array_equality`: `console.assert(([] === []));` and `S/erl/assert_array_equality`: `true = (([] =:= [])).` | JS evaluates `[] === []` to `false` (checked), so the assertion always fails. Erlang evaluates it to `true`. This is documented as "backend-defined" in `src/codegen/tests/values.zig:332-336`, but nothing runs, so the test pins nothing about equality. | Add a `main` that prints the result, or note in the test that it is ref-eq. |
| assert_pattern_with_catch_throw, _catch_default_value, _list_pattern, _string_literal, _number_literal, _enum_variant, _empty_list, _multiple_element_list, _list_and_rest (9) | all | weak (source) | e.g. `S/node/assert_pattern_with_catch_throw` SOURCE: `val assert Person(name, age) = r catch throw Error("is not person");`. `r`, `items`, `greeting`, `answer`, `result`, `list`, `numbers`, `Person` and `Error` are never declared, and there is no `main`. | These sources could not typecheck or run. Only the code shape is pinned, and no RUN LOG can catch a regression. | Declare the scrutinee and record, add `main` with `@print` of the bound names. |
| same 9 | erlang | wrong-output | `S/erl/assert_pattern_with_catch_throw`: `case R of {tag, Person, Name, Age} -> R; _ -> erlang:throw('Error'(<<"is not person">>)) end.` | The module should compile. Actual erlc output (`erl/*/erlc_out.txt`), exit 1 for all 9: `main.erl:4:10: variable 'R' is unbound`, `main.erl:4:64: function 'Error'/1 undefined` (`'Person'/2 undefined` in `_catch_default_value`). Two codegen bugs are independent of the undeclared names: (a) `Person`/`Ok` are emitted as **variables** inside the pattern, so any `{tag,_,_,_}` matches, and records are maps everywhere else (`S/erl/record_implement_fields_round_trip_at_runtime`: `#{tag => <<"x">>, n => 5}`), so this tuple pattern can never match a real record; (b) `throw Error(...)` calls a nonexistent local `'Error'/1`. `Name`/`Age`/`First`… are also bound only inside the case clause and never exported to the enclosing scope. | Match records as maps (`#{name := Name, age := Age}`), lower `Error(msg)` to a real constructor, and bind pattern variables in the enclosing scope. |
| same 9 | beam | wrong-output | `S/beam/assert_pattern_with_list_and_rest` (same body in all 9): `{move, {atom, undefined}, {x, 0}}.` `{move, {atom, ok}, {x, 0}}.` | Pattern test, bindings and throw/default should all appear. Everything is dropped. | Implement `val assert` in beam_asm. |
| same 9 | wasm | wrong-output | `S/wasm/assert_pattern_with_catch_throw`: `(func $f` `i32.const 0` `)` | wasmtime: `values remaining on stack at end of block` (all 9). | As for assert. |
| same 9 | node | wrong-output | `S/node/assert_pattern_with_list_pattern`: `(() => { const _match = items; if ((Array.isArray(_match) && _match.length >= 1)) { return _match; } else { throw Error("not a list"); } })();` | `val assert [first, ..] = items` should bind `first`, but the IIFE result is discarded and no binding is made. In `jsrun/checks.js`, reading `first` afterwards gives `ReferenceError: first is not defined`. | Emit `const [first] = (IIFE)` or destructure. |
| assert_pattern_with_multiple_element_list | node | wrong-output | `S/node/assert_pattern_with_multiple_element_list`: `if ((Array.isArray(_match) && _match.length >= 3))` | Pattern `[1, 2, 3]` should require exactly 3 elements with values 1,2,3. `[9,9,9,9]` is accepted (checked). Erlang correctly emits `case Numbers of [1, 2, 3]`. | Emit an exact length check plus element equality. |
| assert_pattern_with_empty_list | node | wrong-output | `S/node/assert_pattern_with_empty_list`: `if ((Array.isArray(_match)))` | Pattern `[]` should require length 0. `[1,2]` is accepted (checked). | Add `&& _match.length === 0`. |
| assert_pattern_with_list_and_rest | node | wrong-output | `S/node/assert_pattern_with_list_and_rest`: `_match.length >= 2` ... `return _match;` | The `>=2` check is right, but `first`/`second`/`rest` are never bound. | Destructure. |
| assert_pattern_with_catch_default_value | node | wrong-output | `S/node/assert_pattern_with_catch_default_value`: `else { return Person("bob", 12); }` | The record ctor needs `new`, as in `S/node/record_implement_fields_round_trip_at_runtime` (`return new E("x", 5);`). With a class `Person` it throws `TypeError: Class constructor Person cannot be invoked without 'new'` (checked). | Use the record-ctor lowering in the catch arm. |
| assert_pattern_with_enum_variant | node | uncertain | `S/node/assert_pattern_with_enum_variant`: `if ((_match instanceof Ok))` | `Ok` is the `@Result` variant, and elsewhere results are `{ok: …}`/`{error: …}` objects (`S/node/stdlib_result_isok_and_iserror_predicates`: `"error" in _r`). So `instanceof Ok` would throw or never match. The source never declares `Ok`, so it may be a user enum. | Clarify in the test source which `Ok` is meant. |
| builtin_todo_with_message | beam | wrong-output | `S/beam/builtin_todo_with_message`: `{move, {atom, undef}, {x, 0}}.` `{call_ext, 1, {extfunc, erlang, error, 1}}.` | Expected `erlang:error({todo, <<"implement this function">>})`, which is what `S/erl` emits. Actual raises `error:undef` with the message lost. `undef` is also the reason Erlang uses for undefined functions, so it is misleading. Source: `beam_asm.zig:3155` `if (... "todo")) "undef" else "panic"`. | Build a `{todo, Msg}` tuple. |
| builtin_panic_with_message | beam | wrong-output | `S/beam/builtin_panic_with_message`: `{move, {atom, panic}, {x, 0}}.` | Expected `error({panic, <<"something went wrong">>})`. Actual `error:panic` (checked: `m_b2:fail() => CRASH error:panic`), with the message lost. | Same. |
| builtin_todo_with_message, builtin_panic_with_message | all | weak | no `main`, so no RUN LOG | A regression in the error payload would go unnoticed. | Add a `main` that catches and prints. |
| builtin_print_multiple_arguments | erlang | wrong-output + known (leak) | `S/erl/builtin_print_multiple_arguments`: `io:format("~p~n", [<<"Hello">>, 42, true]).`, RUN LOG empty | erlc (`erl/builtin_print_multiple_arguments/erlc_out.txt`): `main.erl:5:23: Warning: the format string requires an argument list with 1 argument, but the argument list contains 3 arguments`. The warning goes to stderr, so `runtime.zig:306` blanks the RUN LOG (the known leak). Independent of the leak, the code crashes at runtime: `main:main() => CRASH error:badarg`. The expected output is all 3 values. Template: `erlang.zig:1330` `"io:format(\"~p~n\", [$args])"`. | Build a format string with one `~p` per arg (e.g. `"~p ~p ~p~n"`), or print each arg. |
| builtin_print_multiple_arguments | beam | wrong-output | `S/beam/builtin_print_multiple_arguments`: only `{move, {literal, <<"Hello">>}, {x, 0}}.` then `io:format/2`. RUN LOG: `<<"Hello">>` | Expected `42` and `true` to be printed too. Actual drops them: `beam_asm.zig:3161-3164` lowers only `cc.args[0]`. The RUN LOG also exists only through the cache (H1). | Lower all args. |
| builtin_print_multiple_arguments | wasm | wrong-output | `S/wasm/builtin_print_multiple_arguments`: only `(data (i32.const 256) "\05\00\00\00Hello")`, with no 42/true | `wasmtime run` prints only `Hello`. `wat.zig:1750-1771` handles only `cc.args[0]`. | Lower all args. |
| builtin_print_with_variable | erlang | wrong-output + known (leak) | `S/erl/builtin_print_with_variable`: `io:format("~p~n", [(<<"Hello, ">> + Name)]).`, RUN LOG empty | erlc: `main.erl:6:39: Warning: evaluation of operator '+'/2 will fail with a 'badarith' exception`. The warning on stderr blanks the RUN LOG (leak), and the code also crashes (`CRASH error:badarith`). String `+` must lower to binary concat `<<A/binary, B/binary>>`. Expected `<<"Hello, world">>`. | Type-directed string concat in erlang.zig. |
| builtin_print_with_variable | beam | wrong-output | `S/beam/builtin_print_with_variable`: `{gc_bif, '+', {f, 0}, 1, [{x, 0}, {y, 0}], {x, 0}}.` | Expected binary concat. Actual `CRASH error:badarith` (`beamrun/run.py`). | Same. |
| builtin_print_with_variable | wasm | wrong-output | `S/wasm/builtin_print_with_variable`: `i32.const 268` `local.get $name` `i32.add` `call $__print_i32` | Expected `Hello, world`. Actual `wasmtime run` prints `524` (pointer 268 + pointer 256). | Concat strings, and print them with fd_write rather than `__print_i32`. |
| builtin_print_return_value_void | wasm | wrong-output | `S/wasm/builtin_print_return_value_void`: `i32.const 256` `call $log` `drop` and `(func $log (param $msg i32)` `local.get $msg` `call $__print_i32` | wasmtime: `type mismatch: expected a type but nothing on stack` (drop after a void call). `@print(msg)` of a string parameter would also print the pointer as an integer instead of `started`. | Don't `drop` void calls, and pick the print lowering by type. |
| builtin_print_return_value_void | erlang | weak | RUN LOG `<<"started">>`/`<<"done">>` | Correct for the program, but the cache masks it: erlc warns `variable 'X' is unused` (H2). | See H2. |
| stdlib_result_map_transforms_ok_propagates_error_intact | beam | wrong-output | `S/beam/stdlib_result_map_transforms_ok_propagates_error_intact`: `{get_tuple_element, {x, 0}, 1, {x, 2}}.` `{test_heap, {alloc, [{words, 0}, {floats, 0}, {funs, 1}]}, 3}.` | Live=3 but `x1` was never set. `erlc +from_asm` fails: `Internal consistency check failed ... Instruction: {test_heap,{alloc,[...]},3} Error: {{x,1},not_live}` (`beam/.../asm_out.txt`, exit 1). | Compute live correctly, or move V into x1 before `test_heap`. |
| stdlib_result_flatmap_chains_and_flattens | beam | wrong-output | `S/beam/stdlib_result_flatmap_chains_and_flattens`: same `{test_heap, {alloc, ...}, 3}.` after `get_tuple_element ... {x, 2}` | Same validator error, exit 1. | Same. |
| stdlib_chain_map_flatmap_unwrapor_types_correctly | beam | wrong-output | `S/beam/stdlib_chain_map_flatmap_unwrapor_types_correctly` line: `{test_heap, {alloc, [{words, 0}, {floats, 0}, {funs, 1}]}, 3}.` | Same validator error `{{x,1},not_live}`, exit 1. | Same. |
| stdlib_result_map…, stdlib_result_flatmap…, stdlib_chain… | wasm | wrong-output | `S/wasm/stdlib_result_map_transforms_ok_propagates_error_intact`: `(else` … `(local $n i32)` … `(local $_res1 i32)` inside the function body | Locals must be declared at the top of the function. wasmtime: `unknown operator or unexpected token --> main.wat:24:6 \| (local $n i32)` (chain: line 29, flatMap: line 27). | Hoist lambda-param and temp locals to the function header. |
| stdlib_option_map_flatmap_and_unwrapor_mirror_result | erlang | wrong-output | `S/erl/stdlib_option_map_flatmap_and_unwrapor_mirror_result`: `(<<"Hello ">> + N)` | Binary concat expected, but `+` on binaries → badarith. It compiles (unused-var warnings only), but the code is wrong. | String concat lowering. |
| stdlib_option_map_flatmap_and_unwrapor_mirror_result | beam | wrong-output | `S/beam/stdlib_option_map_flatmap_and_unwrapor_mirror_result`: `{move, {x, 0}, {x, 3}}.` `{test_heap, {alloc, [...]}, 1}.` … `{move, {x, 3}, {x, 0}}.` and in `'-greet/1-fun-0-'`: `{move, {literal, <<"Hello ">>}, {x, 0}}.` `{move, {x, 0}, {x, 1}}.` `{gc_bif, '+', {f, 0}, 2, [{x, 1}, {x, 0}], {x, 0}}.` | With `greet` exported, the validator fails: `Instruction: {move,{x,3},{x,0}} Error: {uninitialized_reg,{x,3}}`. The value lives in x3 but `test_heap` live=1 kills it. It only assembled in the committed form because `greet` is unexported and dead-code-eliminated. The lambda overwrites its parameter `n` (x0) with `"Hello "` before reading it, so it computes `"Hello " + "Hello "`, and uses `'+'` on binaries. | Fix live counting and the argument clobber, and add binary concat. |
| stdlib_option_map_flatmap_and_unwrapor_mirror_result | wasm | wrong-output | `S/wasm/stdlib_option_map_flatmap_and_unwrapor_mirror_result`: `(then` `(local $n i32)` and `i32.const 256` `local.get $n` `i32.add` | wasmtime: `unknown operator or unexpected token --> main.wat:22:6`. String concat is also lowered as pointer addition. | Hoist locals and add a concat helper. |
| stdlib_result_* (4), stdlib_option_* (2), stdlib_chain (1) | all | weak | e.g. SOURCE `fn parseAge(s: string) -> @Result<i32, string> { @todo(); }` and `val r = parseAge("42").map({ n -> n + 1 });`, with every RUN LOG empty | The names promise behaviour ("transforms Ok, propagates Error intact", "returns data on Ok, default on Error"), but every producer is `@todo()` and nothing is printed. The main crashes immediately on all backends (checked: wasm `parseAge` trap, beam `error:undef`), so neither the Ok nor the Error branch is ever exercised. | Give producers real bodies returning Ok and Error, and `@print` the results. |
| stdlib_result_isok_and_iserror_predicates | erlang | weak | `S/erl/stdlib_result_isok_and_iserror_predicates`: `Ok = (fun(R) -> case R of {ok, _} -> true; _ -> false end end)(R),` | erlc: `main.erl:9:15: Warning: variable 'R' shadowed in 'fun'` (twice). Harmless now, but it would blank the RUN LOG as soon as a `@print` is added (H2). | Use a fresh lambda var name (e.g. `R__`). |
| record_method_with_todo_placeholder | erlang | wrong-output (minor) | `S/erl/record_method_with_todo_placeholder`: `erlang:error({todo, "not implemented"}).` vs `S/erl/stdlib_result_map_transforms_ok_propagates_error_intact`: `erlang:error({todo, <<"not implemented">>}).` | The same builtin gives a charlist in one place and a binary in another. Source: `erlang.zig:1319` argc=0 template `"erlang:error({todo, \"not implemented\"})"`. | Use `<<"not implemented">>` in the argc=0 templates for `todo` and `panic`. |
| record_method_with_todo_placeholder | beam | wrong-output (minor) | `S/beam/record_method_with_todo_placeholder`: `{move, {atom, undef}, {x, 0}}.` `{call_ext_only, 1, {extfunc, erlang, error, 1}}.` | `{todo, <<"not implemented">>}` expected. Actual `error:undef` (checked). | As for builtin_todo. |
| test_runner | node | weak | `S/node/test_runner` RUN LOG: `  duration 0ms` | Wall-clock timing sits in the snapshot. A slow host could print `1ms`, and the cache hides this (`js test_runner CACHE HIT`). | Normalise the duration in snapshot mode. |
| test_runner | erlang | weak | `S/erl/test_runner` RUN LOG empty | See H4. The generated code is correct and prints the same envelope when run by hand. | See H4. |
| test_runner_excluded_from_normal_build | node only | weak | `builtins.zig:322-323`: `assertJsContains(... &.{"function add"})` / `assertJsNotContains(... "__bp_test", "__bp_assert", "__bp_run_tests")` | Only commonJS is checked. Erlang/beam exclusion of `'__bp_test_0'`/`'__bp_run_tests'` is untested. | Loop over the erlang config as well. |
| builtin_print_in_if_branch | all 4 | wrong-test | All 4 snapshot files are **0 bytes** (committed empty in HEAD, `git cat-file -s` = 0). SOURCE in test: `if x > 0 {` | Parser `src/parser/exprs.zig:139-145` requires `if (cond)`: `_ = try this.consume(.leftParenthesis);`. The source does not parse, so `codegen.generate` cannot produce a snapshot, and an empty file can never equal `buildSnapshot` output (which always starts with `----- SOURCE CODE`). The test cannot pass. I confirmed with `zig-out/bin/botopink check` (built 2026-09-14, possibly stale): `error: parse error in main`, while `if (x > 0) {` checks fine. | Change to `if (x > 0) { ... }`, give it a `main` that calls `check`, and regenerate. |
| builtin_print_in_loop | all 4 | wrong-test | All 4 snapshot files are 0 bytes. SOURCE: `loop {` … `val i = i - 1;` | `parseLoopExpr` (`src/parser/exprs.zig:1604-1618`) requires `loop (iter) {`: `_ = try this.consume(.leftParenthesis);`. The binary also says `parse error in main`. Even if it parsed, `val i = i - 1;` inside the body shadows rather than mutates, so the loop would never terminate. | Rewrite with `loop (0..n) { i -> @print(i); }` or `var`, and regenerate. |
| interface_literal_basic, interface_literal_with_fields | all 4 | wrong-test (cause uncertain) | All 8 snapshot files are 0 bytes, committed empty by `8d88372 test: add interface literal tests ...`. SOURCE: `val DeclKind = record { Record: "Record", Fn: "Fn" };` `val decl = @Decl(kind: DeclKind.Record, ...)` | An empty snapshot always mismatches, so the test is red or never produced output. The interface-literal parser exists (`src/parser/exprs.zig:892-936`), but the stale binary reports `parse error in main` (it predates that commit). I could not tell whether `record { Record: "Record" }` as a value parses on HEAD without `zig build`. | Regenerate. If `record {Key: "value"}` is not valid syntax, use an enum or plain strings. |
| record_methods_using_self_fields_in_arithmetic | beam | wrong-output | `S/beam/record_methods_using_self_fields_in_arithmetic`: `{get_map_elements, {f, 6}, {x, 0}, {list, [{atom, x}, {x, 0}]}}.` `{label, 6}.` `{move, {x, 0}, {x, 1}}.` `{test, is_map, {f, 7}, [{x, 0}]}.` | Self (x0) is overwritten by the first field read, so later `is_map` tests fail and jump. `'Vec2_lengthSq'(#{x=>3.0,y=>4.0})` should be `25.0` but returns `90.0` (checked). The erlang backend gives `25.0`. | Load fields into scratch registers and keep self in x0/y. |
| record_methods_using_self_fields_in_arithmetic | wasm | wrong-output | `S/wasm/record_methods_using_self_fields_in_arithmetic`: `(func $Vec2_lengthSq (param $self i32) (result i32)` `i32.load ;; .x` `i32.mul` | Fields are `f64` in the source, but the code is lowered as i32 load/mul/result, so fractional values are wrong. | Use `f64.load`/`f64.mul` and an `(result f64)` type. |
| record_method_with_throw | erlang | wrong-output | `S/erl/record_method_with_throw`: `erlang:throw('Error'(<<"invalid invoice">>)).` | erlc exit 1: `main.erl:9:18: function 'Error'/1 undefined`. | Lower `new Error(msg)` to a real term, e.g. `{error, Msg}`. |
| record_method_with_throw | beam | wrong-output | `S/beam/record_method_with_throw`: `%% unresolved local call: Error/1` and `{call_ext_only, 1, {extfunc, erlang, throw, 1}}.` and `Invoice_total` with the same x0 clobber `{get_map_elements, {f, 6}, {x, 0}, {list, [{atom, subtotal}, {x, 0}]}}.` | The `%%` marker is an unsupported-lowering comment, and `throw` gets a bare binary (`CRASH throw:<<"invalid invoice">>`). `'Invoice_total'(#{subtotal=>100.0, taxRate=>0.5})` should be `150.0` but returns `1.01e4` (checked). | Fix the Error ctor and the self clobber. |
| record_method_with_throw | wasm | wrong-output | `S/wasm/record_method_with_throw`: `call $Error` | wasmtime: `unknown func: failed to find name $Error`. Fields are also f64 lowered as i32. | Emit an Error ctor or `unreachable`, and use f64. |
| record_shorthand_declaration_without_val_name | beam | wrong-output | `S/beam/record_shorthand_declaration_without_val_name`: `{get_map_elements, {f, 4}, {x, 0}, {list, [{atom, x}, {x, 0}]}}.` … `{move, {x, 0}, {x, 2}}.` `{test, is_map, {f, 6}, [{x, 0}]}.` | `'Vec2_dot'(#{x=>1.0,y=>2.0}, #{x=>3.0,y=>5.0})` should be `13.0` but returns `18.0` (checked). `self.y` is never read, because self was clobbered. | Same as record_methods. |
| record_shorthand_declaration_without_val_name | wasm | wrong-output | `S/wasm/record_shorthand_declaration_without_val_name`: `(func $Vec2_dot (param $self i32) (param $other i32) (result i32)` `i32.load ;; .x` | f64 fields lowered as i32. | f64 lowering. |
| array_string_array_literal, array_val_with_array_type_annotation, array_prepend_with_empty_array, _single_element_array, _multiple_elements_array, array_prepend_with_identifier | beam | known | e.g. `S/beam/array_string_array_literal`: `{move, nil, {x, 0}}.` … `{deallocate, 0}.` with no `{allocate…}` | When exported, the validator fails: `Instruction: {deallocate,0} Error: {allocated,none}`. With `deallocate` removed, the values are correct (`[<<"hello">>,<<"world">>]`, `[1,2,3]`, `[1,2,3,4]`). | Known. |
| array_prepend_with_identifier | erlang | wrong-output (+ known) | `S/erl/array_prepend_with_identifier`: `list() ->` `[1, 2, rest].` | Expected `[1, 2] ++ rest()` = `[1,2,3,4]`. Actual gives `[1,2,rest]`: the top-level val is referenced as an atom (the known "top-level val read" issue), **and** the spread is lost. Literal spreads work (`[1, 2] ++ [3, 4]` in `S/erl/array_prepend_with_multiple_elements_array`). | Lower `..ident` to `++ ident()`. |
| array_prepend_with_identifier | beam | wrong-output | `S/beam/array_prepend_with_identifier`: `list/0` builds only `{integer, 2}` and `{integer, 1}` on `nil` | Expected `[1,2,3,4]`, actual `[1,2]` (checked after removing deallocate). `..rest` is silently dropped. | Call `rest/0` and append. |
| array_* (6), tuple_string_pair_literal, tuple_val_with_tuple_type_annotation, tuple_mixed_types, tuple_literal_pair, tuple_nested_tuples, call_children_coercion_list_single_text | wasm | wrong-output | e.g. `S/wasm/array_string_array_literal`: `(global $xs (mut i32) (i32.const 0))`; `S/wasm/call_children_coercion_list_single_text`: `(global $many (mut i32) (i32.const 0))` | Top-level `val` initializers (arrays, tuples, calls) are dropped and every global is a 0 placeholder. No data segment, no allocation, no start/init function. The WAT validates but has no semantics. | Emit a module init function that allocates and stores the value. |
| tuple_string_pair_literal, tuple_val_with_tuple_type_annotation, tuple_mixed_types, tuple_literal_pair | beam | wrong-output (+ known deallocate) | `S/beam/tuple_string_pair_literal`: `{move, {literal, <<"56454">>}, {x, 0}}.` `{move, {x, 0}, {x, 0}}.` `{move, {literal, <<"85484">>}, {x, 0}}.` `{move, {x, 0}, {x, 1}}.` `{put_tuple2, {x, 0}, {list, [{x, 0}, {x, 1}]}}.` | The first element is overwritten by the second. `t()` should be `{<<"56454">>,<<"85484">>}` but returns `{<<"85484">>,<<"85484">>}`. Mixed gives `{<<"5452">>,<<"5452">>}` and pair gives `{<<"hello">>,<<"hello">>}` (all checked after removing deallocate). | Move element i into x_i (the `{move,{x,0},{x,0}}` should target x_i). |
| tuple_nested_tuples | beam | wrong-output (+ known) | `S/beam/tuple_nested_tuples` (same clobber pattern, 3 `put_tuple2`) | Expected `{{1,2},{3,4}}`, actual `{{4,4},{4,4}}` (checked). | Same. |
| record_fn_typed_field_hook_shape_record | erlang | wrong-output | `S/erl/record_fn_typed_field_hook_shape_record`: `set(S, maps:get(value, S)),` | erlc exit 1: `main.erl:11:5: function set/2 undefined`. Expected `(maps:get(set, S))(maps:get(value, S))`. | Call fn-typed fields as closures. |
| record_fn_typed_field_hook_shape_record | beam | wrong-output | `S/beam/record_fn_typed_field_hook_shape_record`: `%% unresolved method call: set/2` and in `make/0`: `{move, {x, 0}, {x, 1}}.` `{test_heap, {alloc, [{words, 0}, {floats, 0}, {funs, 1}]}, 0}.` … `{put_map_assoc, ... {atom, value}, {x, 1} ...}` | The `s.set(...)` call is silently dropped: `apply` with a printing `set` fun never prints. When exported, the validator fails in `make/0`: `Error: {{x,1},not_live}`. | Implement closure-field calls and fix live counting. |
| record_fn_typed_field_hook_shape_record | wasm | wrong-output | `S/wasm/record_fn_typed_field_hook_shape_record`: `i32.const 0 ;; lambda` and `call $set` | Lambda placeholder, and wasmtime fails with `unknown func: failed to find name $set`. | Use a funcref table + `call_indirect`. |
| call_children_coercion_list_single_text | beam | wrong-output | `S/beam/call_children_coercion_list_single_text`: `{move, {x, 0}, {x, 1}}.` `{call, 0, {f, 3}}.` `{put_list, {x, 0}, {x, 1}, {x, 0}}.` (no `allocate`) | x1 does not survive a `call`, and there is no stack frame. When exported, the validator fails: `many/0+8 ... Instruction: {call,0,{f,2}} Error: {allocated,none}` (also `one/0`, `txt/0`). Related to the known missing-allocate family, but the lost x1 across the call is separate. | Allocate a frame and keep partial lists in y registers. |
| call_children_coercion_list_single_text | all | weak | SOURCE `fn box(children: Children) -> string { return "x"; }` | `box` ignores its argument and nothing is printed, so all code is identical regardless of coercion. The snapshot cannot detect a coercion regression (the test is really a type-check test). | Print or inspect `children`, or move to an infer test. |

## `@print` formatting consistency across backends

Lowering: node `console.log($args)` (`commonJS.zig:970`), erlang `io:format("~p~n", [$args])` (`erlang.zig:1330`), beam `io:format("~p~n", [Arg0])` with only the first argument (`beam_asm.zig:3161-3170`), and wasm `fd_write` for string literals, otherwise `$__print_i32` of the first argument only (`wat.zig:1750-1771`).

What the snapshots in this batch show (plus my own wasmtime runs):

| value | node | erlang | beam | wasm (wasmtime, RUN LOG stubbed) |
|---|---|---|---|---|
| string literal `"Hello, World!"` | `Hello, World!` | `<<"Hello, World!">>` | `<<"Hello, World!">>` | `Hello, World!` |
| int expr `x * 2` | `20` | `20` | `20` | `20` |
| field `mk().n` | `5` | `5` | `5` | `5` |
| multiple args `"Hello", 42, true` | `Hello 42 true` | badarg crash (format mismatch), empty log | `<<"Hello">>` (args dropped) | `Hello` (args dropped) |
| string concat `"Hello, " + name` | `Hello, world` | badarith crash | badarith crash | `524` (pointer sum) |
| string param `msg` | `started` | `<<"started">>` | `<<"started">>` | would print pointer ints (module invalid anyway) |

Assessment:
- Erlang and BEAM agree with each other (`~p`, so binaries show as `<<"...">>`). Node prints raw strings. This split is consistent across this batch and comes straight from the templates. Nothing documents cross-backend textual equality as a goal, so I treat it as **uncertain whether intended**. The RUN LOGs are not comparable across backends for strings, records (`#{n => 5,tag => <<"x">>}` vs JS objects), bools or floats (`~p` of `3.0` vs JS `3`).
- Real bugs, independent of that design choice: multi-arg `@print` (erlang format/arity mismatch; beam and wasm drop args 1..n); string values in wasm printed as i32 pointers; string `+` lowered as arithmetic in erlang, beam and wasm.
- Suggested: add a shared runtime `print` helper per target that formats strings raw (`io:put_chars`/`~s` for binaries), joins args with spaces, and matches node. Or state explicitly that RUN LOGs are backend-native.

## `ok` slugs

- `builtin_print_single_argument` (node/erlang/beam RUN LOGs correct for their formats; wasm known; beam log cache-backed per H1)
- `builtin_print_expression` (all 4 correct; wasmtime prints `20`)
- `record_implement_fields_round_trip_at_runtime` (node/erlang/beam `5`; wasmtime prints `5`; beam log cache-backed per H1)
- `record_two_fields` (JS class; erlang/beam/wasm emit no runtime artefact, consistent with map records)
- `tuple_access_elements` (`t[0]` / `element(1, T)` / beam `element/2`, which returns `7` for `{7,<<"s">>}` / wasm `i32.load`)

## Notes on method

- Erlang: every `ERLANG` block was compiled with `erlc` (`erl/<slug>/erlc_out.txt`).
- BEAM: every `.S` was assembled with `erlc +from_asm`, then re-assembled with all functions exported so the validator checks them. The committed modules mostly export nothing, so unused local functions are dropped before validation, which hides errors. Functions were run with `erl -eval` (`beamrun/run.py`, `run2.py`).
- WASM: every WAT was checked with `wasmtime compile`, and modules with `_start` were run with `wasmtime run`.
- JS semantics were checked in `jsrun/checks.js`.
- Runtime cache hits were reproduced by recomputing `runtime.zig:cacheKey` in Python (`cachekey.py`).
- `zig-out/bin/botopink` (built 2026-09-14 11:23) was only used to probe parseability of the 4 empty-snapshot sources. The parser-source citations are the authoritative evidence for the `if`/`loop` cases.
