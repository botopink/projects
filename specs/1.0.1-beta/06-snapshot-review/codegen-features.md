# Snapshot review: `modules/compiler-core/src/codegen/tests/features.zig`

- **Batch:** codegen features (lambda/enum/destructure/star/import/range/pipeline/hooks/prim methods/enum sections)
- **Tests reviewed:** 66 (62 `assertJs`/`assertJsSingle` snapshot tests, 2 `assertJsContains`, 2 `assertConsumerJs` needle-only tests with no snapshot by design)
- **Snapshots reviewed:** 248 files (62 tests x 4 backends), all read in full. No orphans: no snapshot in the 4 dirs with a prefix from this file lacks a test. No slug collides with another codegen test file. The slugs that collide with format/parser/comptime tests are written under different snapshot roots.
- **Path legend:** `node/<slug>` = `modules/compiler-core/snapshots/codegen/node/commonJS/<slug>.snap.md`. `erlang/`, `beam/` and `wasm/` map to `.../erlang/erlang/`, `.../beam/beam/` and `.../wasm/wasm/` the same way.
- **How it was verified:** the generated code was copied into `scratchpad/run-features/`. The JS was run with `node`, the Erlang with `erlc` + `erl -s main _botopink_main`, the BEAM with `erlc +from_asm` + `erl`, and the WAT was validated with `wasmtime compile`. For hidden paths, non-exported functions were called directly on patched copies (exports only). Nothing was run in the repo.
- **Known and not re-flagged per file:** the wasm RUN LOG is always empty (`executeWat` in `src/codegen/runtime.zig:393` is a stub). "Invalid WAT" below means `wasmtime compile` rejects the module, which is independent of that stub.

## Verdict counts

Per test x backend cell (252 cells: 62x4 plus 4 commonJS-only needle tests):

| verdict | cells |
|---|---|
| ok | 130 |
| wrong-output | 108 |
| wrong-test | 4 (all 4 cells of `lambda_standalone_with_params`) |
| uncertain | 4 |
| known | 3 |
| weak | 3 |

Test-level annotations, on top of the cell verdicts:

| annotation | tests |
|---|---|
| weak | 34 |
| wrong-test | 4 |
| duplicate | 2 |
| skip-undocumented | 0 |
| orphan | 0 |

Fully clean tests (all cells ok, no test-level issue): 7. They are listed at the end.

---

## Systemic root causes (referenced from the per-slug table as S#)

| id | backend | verdict | evidence | expected vs actual | suggested fix |
|---|---|---|---|---|---|
| S1 | beam | wrong-output | `beam/destructure_tuple_val_binding`: `{move, {integer, 12}, {x, 0}}.` `{move, {x, 0}, {x, 0}}.` `{move, {literal, <<"hello">>}, {x, 0}}.` `{move, {x, 0}, {x, 1}}.` `{put_tuple2, {x, 0}, {list, [{x, 0}, {x, 1}]}}.` RUN LOG `<<"hello">>` | In 0-arity fns (and args whose earlier siblings are literals), the "scratch" register is `x0` itself, so element/arg 0 is overwritten by element/arg 1. `#(12,"hello")` builds `{<<"hello">>,<<"hello">>}`, and `f(1,"one")` passes `("one","one")`. | `src/codegen/beam_asm.zig` `lowerTupleLit` (l.3418-3432, `scratch_base = self.cur_arity`) and `materializeCallArgs` (l.3347) need `@max(self.cur_arity, 1)`-style flooring, as `lowerArrayLit` already does at l.3406. |
| S2 | beam | wrong-output | `beam/bool_instance_default_fn_methods`: `%% unresolved method call: negate/1` then the register is printed. Also `%% unresolved local call: double/1` (`beam/import_multi_module_pub_fn_import`) and `%% prim method not lowered on beam (complex arg): split/2` (`beam/string_methods_map_to_native_js_names`) | A comment is emitted, codegen continues, and RUN LOGs pin whatever sits in `x0` (e.g. `true false true false` instead of `false true false true`). | `beam_asm.zig` l.2329 / l.2395 / l.2704: materialize primitive-interface `default fn` bodies (as `'Array_range'` already is) and dispatch to them, or hard-fail instead of emitting a comment. |
| S3 | erlang | wrong-output | `erlang/array_slice_2_arg_lowers_byte_identically_across_backends`: `io:format("~p~n", [slice(Xs, 1, 4)]).` erlc: `function slice/3 undefined`. Same for `all/2`, `nor/2`, `nand/2`, `exclusiveOr/2`, `clamp/3`, `isEven/1`. | `default fn` instance methods of primitive interfaces lower to bare local calls with no definition, so the module does not compile and the RUN LOG is empty. Only associated fns (`array_range`, `array_repeat`, `pair_of`, ...) get injected. | `src/codegen/erlang.zig` instance-method lowering: inject the default-fn body (e.g. `array_slice/3`) and rewrite the call, or route through the `@External.Erlang` template of `arraySlice1` / `stringSlice1`. |
| S4 | commonJS (std) | wrong-output | `node/option_method_on_tuple_element` l.66: `Array.prototype.slice = function(start, end) {` / `if (end) { return require("./gleam_stdlib.mjs").slice(this, start, end); } ...`. `node/string_methods_map_to_native_js_names` l.41: `String.prototype.slice = ... require("./gleam_stdlib.mjs").string_slice(...)`. Node: `Error: Cannot find module './gleam_stdlib.mjs'` | Any program using `.slice` (directly, or via the `zip` template, which calls `.slice`) crashes, so the RUN LOG is empty. The override also replaces native `slice` globally, and `if (end)` misbehaves for `end == 0`. | `libs/std/src/primitives.bp` l.165-171 and l.572-578 (`default fn slice`), plus l.234-236 and l.827-833 (`@External.Node("./gleam_stdlib.mjs", "slice"/"string_slice")`). commonJS should also never patch a native `Array.prototype` / `String.prototype` method (`src/codegen/commonJS.zig` prototype patcher). |
| S5 | erlang, beam (std) | wrong-output | `erlang/endswith_...`: `string:suffix(S, <<"bar">>)`. `beam/endswith_...`: `{call_ext, 2, {extfunc, string, suffix, 2}}.` Runtime: `{undef,[{string,suffix,[<<"foobar">>,<<"bar">>],[]}` (OTP 28; `erlang:function_exported(string,suffix,2)` = false) | `string:suffix/2` does not exist in OTP, so both backends crash and the RUN LOG is empty (expected `true`). | `libs/std/src/primitives.bp` l.143-144: use e.g. `(string:find($self, $0, trailing) =:= $0)` for Erlang and a matching BEAM body. |
| S6 | erlang | wrong-output | `erlang/enum_payload_variants_with_method_using_variantfields_case`: `{tag, Circle, Radius} ->`, `{tag, Square, Side} ->`, while ctors elsewhere are `{'Circle', 5}` | The pattern matches the atom `tag` and binds `Circle` as a variable. Reproduced: `area({'Square',2.0}) -> 0.0` (expected 4.0), `check({'Just',<<"x">>})` gives `case_clause`. erlc warns "this clause cannot match". | `src/codegen/erlang.zig` `patternNode` `.variant` (l.3091-3094): emit `{'Name', Fields...}` (`Ast.Expr.a(v.name)`), not `Ast.Expr.a("tag"), Ast.Expr.r(v.name)`. |
| S7 | beam | wrong-output | `beam/enum_unit_variants_with_method_using_ident_case`: `{move, {x, 0}, {y, 0}}.` `{move, {literal, <<"GET">>}, {x, 0}}.` `{jump, {f, 4}}.` (no test before the arm) | Nullary-variant ident patterns are treated as catch-all bindings. Reproduced: `'HttpMethod_name'('Post')` and `('Delete')` both return `<<"GET">>`, and `'Maybe_check'({'Just',..})` returns `<<"nothing">>`. | `beam_asm.zig` case-arm lowering for `.ident` patterns naming an enum variant: emit `{test, is_eq_exact, {f,Next}, [{x,0},{atom,'Get'}]}` (erlang.zig already distinguishes via `enum_variants`). |
| S8 | wasm | wrong-output | `wasm/array_join_...`: `i32.const 256` `call $join` (receiver dropped, `$join` undefined). `wasm/interface_associated_fn_namespace`: `call $of`. `wasm/lambda_with_type_annotation`: `i32.const 0 ;; lambda` ... `call $func`. wasmtime: `unknown func: failed to find name '$join'` | Primitive method calls, interface and enum methods (`src/codegen/wat.zig` l.191 `.@"enum", .interface, ... => {}`) and lambdas emit calls to functions that are never defined, so the module is invalid. | `src/codegen/wat.zig`: either lower or emit these helpers, or refuse the program instead of emitting invalid WAT. |
| S9 | wasm | wrong-output | `wasm/star_fn_generator_with_yield`: `(func $counter (result i32)` `i32.const 1` `i32.const 2` `i32.const 3`. `wasm/option_method_on_tuple_element` l.62: `(local $_res0 i32)` in mid-body. `wasm/destructure_tuple_parameter_in_fn`: `(func $process (param $ i32) (result i32)` `global.get $x` | wasmtime: "values remaining on stack at end of block", "unknown operator or unexpected token" at `(local`, and "empty identifier". | `wat.zig`: fold yields to one result (or reject), hoist `$_resN` locals to the function header (l.1809), and name destructured params and bind their fields. |
| S10 | commonJS (TS typedef) | wrong-output | `node/star_fn_pub_typedefs`: `export declare function loadOne(x: ): Promise<i32>;`. `node/reserved_word_identifiers`: `export declare function delete(with: , class: ): string;`. `node/import_cross_module_record_construct_and_assoc_fn` http.d.ts: `ok(body: ): Response;` and main.d.ts has `import { Response, App } from "http";` twice | Param types are empty (invalid TS), botopink `i32` is not mapped to `number`, a static assoc fn is declared as an instance method, imports are duplicated, and reserved words are not renamed. | `src/codegen/typescript.zig` `emitParam` (l.326-330 uses `p.typeName`, which is empty; the type now lives in the TypeRef), `emitTypeRef .named` (l.394) needs a primitive map, and `emitUse` is invoked twice. |
| S11 | beam | wrong-output | `beam/star_fn_generator_with_yield`: `{move, {integer, 1}, {x, 0}}.` `{deallocate, 0}.` `return.` then dead `{move, {integer, 2}, ...}` | `yield` lowers to `return` (`beam_asm.zig` l.1463-1468). Reproduced: `counter()` gives `1`, while erlang gives `[1, 2, 3]`. | Collect yields into a list, as `erlang.zig` "eager lowering" does. |
| S12 | commonJS | wrong-output | `node/destructure_record_val_binding_with_spread`: `const { x, ... } = p;`. `node/destructure_record_parameter_in_fn`: `function greet({ name, ... } = ) {` | SyntaxError (reproduced with node). | `src/codegen/commonJS.zig` l.1987 and l.2257: drop `, ...` (or emit `...__rest`). `emitParam` must not write ` } = ` without a default. |
| S13 | erlang, beam, wasm | wrong-output | `erlang/destructure_tuple_val_binding`: `io:format("~p~n", [A, B]).` Runtime `{error,badarg,...}`. beam: `{put_list, {x, 1}, nil, {x, 1}}` holds only the first arg. wasm: only `local.get $a` `call $__print_i32` | Multi-arg `@print` is broken: erlang crashes, beam and wasm silently drop args 2..n. JS prints `12 hello`. | `@print` lowering in erlang.zig and beam_asm.zig: build a `~p ~p~n` format with N args (or join), and do the same in wat.zig. |
| S14 | beam | wrong-output | `beam/codegen_use_memo_infers_dependency_array` fun: `{move, {atom, count}, {x, 0}}.`. `beam/iterator_fromlist_yields_array_items` fun: `{move, {atom, out}, {x, 0}}.`. `beam/destructure_record_parameter_in_fn`: `{move, {atom, name}, {x, 0}}.`. `beam/destructure_tuple_parameter_in_fn`: `{move, {atom, x}, {x, 0}}.` | Free variables captured by closures and names bound by destructured params lower to atoms (`make_fun3 ... {list, []}` has no env). Reproduced: `process({1,2})` returns `x`, and the RUN LOG prints `name`. | `beam_asm.zig` identifier lowering: resolve closure captures (pass env to `make_fun3`) and bind destructured params (`get_map_elements` / `get_tuple_element` on the param register). |
| S15 | commonJS, beam vs erlang | uncertain | `node/codegen_inline_implement_context_base_erased_at_runtime`: `function render() {` `new Element();` `}`. erlang: `render() ->` `#{}.`. beam: `{move, {literal, #{}}, {x, 0}}.` `{move, {atom, ok}, {x, 0}}.` | For a trailing expression statement in a fn with a declared return type (`fn render() -> Element { Element(); }`), JS returns `undefined`, erlang returns the value and beam returns `ok`. At most one is right. | Decide the language rule (implicit tail value or not), then align `commonJS.zig`, `beam_asm.zig` `emitBody` (l.1396-1407), `erlang.zig`, or fix the fixtures to use `return`. |
| S16 | erlang, beam vs commonJS | uncertain | `node/array_join_...` RUN LOG `10, 20, 30` vs `erlang/...`/`beam/...` RUN LOG `<<"10, 20, 30">>`. `node/optional_chaining...` `ana` vs `<<"ana">>` | `@print` of strings uses `~p` on Erlang targets, so the tests titled "lowers byte-identically across backends" can never have identical RUN LOGs. Possibly intended, but it defeats cross-backend comparison. | Use `~ts` for binaries in the `@print` template, or rename the tests. |
| S17 | all | wrong-test | `node/lambda_standalone_with_params.snap.md` (and erlang/beam/wasm) are 0 bytes; `git ls-files -s` blob `e69de29` (empty blob) | An empty baseline can never equal the generated text (`src/utils/snap.zig` `compareOrCreate` compares trimmed `""` with non-empty output), so the test fails or asserts nothing. Repo-wide, 116 `*.snap.md` under `snapshots/` are 0 bytes. | Regenerate (delete the empty files and rerun), and add a guard in `snap.zig` that treats an empty baseline as missing or as an error. |

---

## All non-ok findings (per slug)

| slug | backend(s) | verdict | evidence (quoted) | expected vs actual | suggested fix |
|---|---|---|---|---|---|
| star_fn_async_function_with_await | beam | wrong-output | `beam/...`: `%% unsupported expr in tail position: jump` then `{move, {atom, undefined}, {x, 0}}.` `{gc_bif, '+', {f, 0}, 1, [{y, 0}, {y, 0}], {x, 0}}.` | `loadTwice(3)` should be 6. Reproduced: `badarith` on `undefined + undefined` (the `await fetch(x)` call is never emitted). | `beam_asm.zig` l.1852: lower `val a = await call(...)` like a plain call (eager future). |
| star_fn_async_function_with_await | test | weak | No `main`/`@print`, so the RUN LOG is empty on every backend. | Behaviour is never executed. | Add `fn main(){ @print(loadTwice(3)) }` (JS would need an await-aware print). |
| star_fn_generator_with_yield | beam | wrong-output | S11: `{move, {integer, 1}, {x, 0}}.` `{deallocate, 0}.` `return.` + dead code | `[1,2,3]` (erlang) vs `1`. | S11 |
| star_fn_generator_with_yield | wasm | wrong-output | S9: `(func $counter (result i32)` `i32.const 1` `i32.const 2` `i32.const 3` `)` | Invalid WAT (wasmtime: "values remaining on stack"). | S9 |
| star_fn_generator_with_yield | erlang, beam, wasm, test | weak | `%% #[@future] / #[@asyncGenerator] — eager lowering` is emitted above an `#[@iterator]` fn (also in 5 other slugs). No consumer or RUN LOG. | Misleading comment, nothing executed. | `erlang.zig` l.1846, `beam_asm.zig` l.1094, `wat.zig` l.859: emit the actual effect name. |
| star_fn_async_generator | beam, wasm | wrong-output | beam: `{move, {integer, 1}, {x, 0}}.` `return.` then dead `{move, {integer, 2}, ...}`. wasm: `i32.const 1` `i32.const 2` with `(result i32)` | erlang `[1, 2]` vs beam `1`. wasm is invalid (wasmtime). | S11, S9 |
| star_fn_async_generator | test | weak | No execution. | | |
| star_fn_pub_typedefs | node (d.ts) | wrong-output | `export declare function loadOne(x: ): Promise<i32>;` `export declare function count(): IterableIterator<i32>;` | Expected `loadOne(x: number): Promise<number>`. The actual output is invalid TypeScript. This test exists specifically to pin typedefs. | S10 |
| star_fn_pub_typedefs | beam | wrong-output | `{function, count, 0, 5}` ... `{move, {integer, 1}, {x, 0}}.` | erlang `count() -> [1].` vs beam `1`. | S11 |
| effect_annotation_future_iterator_asyncgenerator_result | beam | wrong-output | `counter`: `{move, {integer, 1}, {x, 0}}.` `return.` + dead `{move, {integer, 2}...` | erlang `[1, 2]` vs `1`. | S11 |
| effect_annotation_future_iterator_asyncgenerator_result | wasm | wrong-output | `(if (result i32)` `(then` `(local $_res0 i32)` ...; `$counter`: `i32.const 1` `i32.const 2` | Invalid WAT (wasmtime: "unknown operator or unexpected token" at `(local`). | S9 (`wat.zig` l.1809) |
| effect_annotation_future_iterator_asyncgenerator_result | test | weak | No main or RUN LOG; `parse` and `counter` are never called. | | |
| effect_annotation_generator_lowers_to_function | beam, wasm | wrong-output | beam `range`: `{allocate, 0, 2}.` `{deallocate, 0}.` `return.` then dead `{move, {x, 1}, {x, 0}}.`. wasm: `local.get $a` `local.get $b` with `(result i32)` | Reproduced: beam `range(1,2)` gives `1` vs erlang `[A, B]`. wasm is invalid. | S11, S9 |
| effect_annotation_generator_lowers_to_function | test | weak | No execution. | | |
| enum_payload_variants_with_method_using_variantfields_case | erlang | wrong-output | `{tag, Circle, Radius} ->` / `{tag, Square, Side} ->` | Reproduced: `area({'Square',2.0})` gives `0.0` (expected 4.0). | S6 |
| enum_payload_variants_with_method_using_variantfields_case | wasm | wrong-output | Whole module is `(memory ...) (global $__heap_ptr ...)`; `fn area` is absent. | The method is silently dropped (beam emits `'Shape_area'`). | S8 (`wat.zig` l.191) |
| enum_payload_variants_with_method_using_variantfields_case | test | weak | `area` is never called; empty RUN LOG hides S6. | | Add `main` calling `Shape.area(Shape.Square(2.0))`. |
| enum_unit_variants_with_method_using_ident_case | beam | wrong-output | `{move, {x, 0}, {y, 0}}.` `{move, {literal, <<"GET">>}, {x, 0}}.` `{jump, {f, 4}}.` (no guard test) | Reproduced: `name('Post')` and `name('Delete')` both give `<<"GET">>`. | S7 |
| enum_unit_variants_with_method_using_ident_case | wasm | wrong-output | Empty module; `fn name` dropped. | | S8 |
| enum_unit_variants_with_method_using_ident_case | test | weak | Never executed. | | |
| enum_mixed_unit_and_payload_with_method_using_mixed_case | erlang | wrong-output | `{tag, Just, Value} ->` | Reproduced: `check({'Just',<<"x">>})` gives `case_clause`. | S6 |
| enum_mixed_unit_and_payload_with_method_using_mixed_case | beam | wrong-output | `{move, {literal, <<"nothing">>}, {x, 0}}.` `{jump, {f, 4}}.` before any test; `{label, 5}.` `{label, 4}.` | Reproduced: `'Maybe_check'({'Just',<<"x">>})` gives `<<"nothing">>`. | S7 |
| enum_mixed_unit_and_payload_with_method_using_mixed_case | wasm | wrong-output | Empty module (method dropped). | | S8 |
| enum_mixed_unit_and_payload_with_method_using_mixed_case | test | weak | Never executed. | | |
| enum_method_using_qualified_enum_member | wasm | wrong-output | Empty module (method dropped). | | S8 |
| enum_method_using_qualified_enum_member | test | weak | Source `fn isDefault(s: Self) -> string {` `val current = Status.Active;` `return current;` returns a `Status` from a `-> string` fn, and the name suggests bool. | The fixture is type-inconsistent; nothing is executed. | Fix the fixture (`-> Self`) and call it. |
| import_named_imports | test | weak | Source `import { foo, bar };` (no `from`). node: `const { foo, bar } = require("./module");`. erlang only `%% import foo, bar`; beam and wasm emit nothing. | `./module` is a phantom path (node: `Cannot find module './module'`). The snapshot pins a meaningless default. | Either reject a from-less import or document the `./module` default. |
| codegen_use_object_destructure_state_to_usestate | erlang | wrong-output | `{Count, SetCount} = state(0),` | An object/record destructure is lowered as a tuple match, while every record in erlang is a map (`#{...}`), and `state(0)` returns `0`, so it would `badmatch`. | `erlang.zig` destructure lowering for `.names` should use `#{count := Count, ...}`. |
| codegen_use_object_destructure_state_to_usestate | beam | wrong-output | `state`: `{move, {atom, ok}, {x, 0}}.`. `Counter`: `{get_map_elements, {f, 6}, {x, 0}, {list, [{atom, count}, {x, 0}]}}.` `{label, 6}.` ... second `get_map_elements` reads from `{x, 0}`, which now holds `count` | `erlc +from_asm`: `Internal consistency check failed ... {get_map_elements,{f,5},{x,0},...}`, so the module does not even load. `state/1` returns `ok` instead of `initial`. | `beam_asm.zig`: `is_map` test before `get_map_elements`, keep the source map in a scratch register, and see S15 for the tail value. |
| codegen_use_object_destructure_state_to_usestate | test | weak | No execution; `useState` is an undefined global in JS. | | |
| codegen_use_tuple_destructure_state_to_usestate | node, beam | uncertain | node: `function state(initial) {` `initial;` `}`. beam `state`: `{move, {atom, ok}, {x, 0}}.` vs erlang `state(Initial) ->` `Initial.` | Three different return values for the same fn. | S15 |
| codegen_use_tuple_destructure_state_to_usestate | test | weak | No execution. | | |
| codegen_use_memo_infers_dependency_array | erlang | wrong-output | `Doubled = memo(fun() ->` ... `end),` while only `memo() ->` `0.` is defined | erlc: `function memo/1 undefined, did you mean memo/0?` (the module does not compile). | `erlang.zig` `use <hook> { lambda }` lowering: define the arity or drop the lambda consistently. |
| codegen_use_memo_infers_dependency_array | beam | wrong-output | Fun body `{move, {atom, count}, {x, 0}}.` `{gc_bif, '*', {f, 0}, 1, [{x, 0}, {integer, 2}], {x, 0}}.`; `{make_fun3, {f, 11}, 0, 0, {x, 0}, {list, []}}.` then `{call, 0, {f, 5}}` (fun discarded) | `count` becomes an atom, so `count * 2` would be `badarith`, and the memo lambda is never passed. | S14 |
| codegen_use_memo_infers_dependency_array | test | weak | No execution (JS `useMemo` is undefined). | | |
| codegen_use_effect_void_hook_empty_deps | erlang | wrong-output | `effect(fun() ->` `cleanup()` `end),` with only `effect() ->` defined | erlc: `function effect/1 undefined`. | Same as memo. |
| codegen_use_effect_void_hook_empty_deps | beam | wrong-output | `cleanup`/`effect`: `{move, {integer, 0}, {x, 0}}.` `{move, {atom, ok}, {x, 0}}.`; `Widget`: `{make_fun3, ...}` then `{call, 0, {f, 5}}` | Fns return `ok` (erlang returns `0`), and the effect lambda is dropped. | S15; pass or drop the fun consistently. |
| codegen_use_effect_void_hook_empty_deps | wasm | wrong-output | `(func $cleanup` `i32.const 0` `)` (no result) | wasmtime: "values remaining on stack at end of block". | `wat.zig`: `drop` the expression-statement value in void fns. |
| codegen_use_effect_void_hook_empty_deps | test | weak | No execution. | | |
| codegen_inline_implement_context_base_erased_at_runtime | node, beam | uncertain | node `function render() {` `new Element();` `}`. beam `{move, {atom, ok}, {x, 0}}.` vs erlang `#{}.` | Tail-value divergence. | S15 |
| codegen_inline_implement_context_base_erased_at_runtime | test | weak | No execution. | | |
| destructure_record_val_binding | erlang | wrong-output | `{X, Y} = P,` with `P = #{x => 3, y => 4}` and `io:format("~p~n", [X, Y]),` | Expected `3 4` (JS RUN LOG). Actual: runtime `{badmatch,#{x=>3,y=>4}}`, empty RUN LOG. | Map pattern for record destructure; S13. |
| destructure_record_val_binding | beam | wrong-output | `{get_map_elements, {f, 10}, {x, 1}, {list, [{atom, x}, {x, 0}]}}.` (no `is_map`). Print: `{put_list, {x, 1}, nil, {x, 1}}` (only `x`) | `erlc +from_asm`: `Internal consistency check failed ... {bad_type,{needed,{t_map,any,any}},{actual,any}}`, so there is no RUN LOG. The print would also drop `y`. | `beam_asm.zig`: emit `{test, is_map, ...}` first (done elsewhere, e.g. optional chaining); S13. |
| destructure_record_val_binding | wasm | wrong-output | `$main`: `local.get $__mem0` `call $describe` `)` (result left on stack). Print: only `local.get $x` `call $__print_i32` | Invalid WAT ("values remaining on stack"); drops `y`. | `wat.zig`: `drop` unused call results; S13. |
| destructure_record_val_binding_with_spread | node | wrong-output | `const { x, ... } = p;` | Node: `SyntaxError: Unexpected token '}'`. | S12 |
| destructure_record_val_binding_with_spread | erlang | wrong-output | `{X, _} = P,` | A 2-tuple pattern for a 3-field map record would `badmatch`. | Map pattern `#{x := X}`. |
| destructure_record_val_binding_with_spread | test | weak | `describe` is never called; the JS SyntaxError is invisible (RUN LOG empty either way). | | Add `main`. |
| destructure_record_parameter_in_fn | node | wrong-output | `function greet({ name, ... } = ) {` | Expected RUN LOG `Ana`. Actual: SyntaxError, empty RUN LOG. | S12 |
| destructure_record_parameter_in_fn | erlang | wrong-output | `greet({Name, _}) ->` called with `greet(#{name => <<"Ana">>, age => 30})` | Runtime `function_clause`, empty RUN LOG. | `greet(#{name := Name})`. |
| destructure_record_parameter_in_fn | beam | wrong-output | `{move, {atom, name}, {x, 0}}.` (print) and `{move, {atom, name}, {x, 0}}.` (return); RUN LOG `name` | The pinned RUN LOG `name` is wrong (expected `<<"Ana">>`). | S14 |
| destructure_record_parameter_in_fn | wasm | wrong-output | `(func $greet (param $ i32) (result i32)` `global.get $name` | wasmtime: "empty identifier". | S9 |
| destructure_tuple_val_binding | erlang | wrong-output | `io:format("~p~n", [A, B]).` | Runtime `badarg`, empty RUN LOG (expected `12` / `<<"hello">>`). | S13 |
| destructure_tuple_val_binding | beam | wrong-output | `{move, {integer, 12}, {x, 0}}.` `{move, {x, 0}, {x, 0}}.` `{move, {literal, <<"hello">>}, {x, 0}}.`; RUN LOG `<<"hello">>` | `a` should be 12. The tuple is built as `{<<"hello">>,<<"hello">>}`, `b` is not printed, and the RUN LOG pins a wrong value. | S1, S13 |
| destructure_tuple_val_binding | wasm | wrong-output | Only `local.get $a` `call $__print_i32` | `b` is dropped. | S13 |
| destructure_tuple_parameter_in_fn | beam | wrong-output | `{move, {atom, x}, {x, 0}}.` | Reproduced: `process({1,2})` gives `x` (expected `1`). | S14 |
| destructure_tuple_parameter_in_fn | wasm | wrong-output | `(func $process (param $ i32) (result i32)` `global.get $x` | Invalid WAT. | S9 |
| destructure_tuple_parameter_in_fn | test | weak | Never called. | | |
| destructure_tuple_var_binding | beam | wrong-output | `{move, {integer, 10}, {x, 0}}.` `{move, {x, 0}, {x, 0}}.` `{move, {integer, 20}, {x, 0}}.` ... `{put_tuple2, {x, 0}, {list, [{x, 0}, {x, 1}]}}.` | Builds `{20,20}` instead of `{10,20}`; invisible because nothing is printed. | S1 |
| destructure_tuple_var_binding | test | weak | No `@print`; the `var` mutability aspect is not exercised. | | |
| destructure_tuple_with_long_names | beam | wrong-output | `get_coordinates`: `{move, {float, 0.0}, {x, 0}}.` `{move, {x, 0}, {x, 0}}.` `{move, {float, 0.0}, {x, 0}}.` | Same clobber as S1, masked because both elements are `0.0`. | S1 |
| destructure_tuple_with_long_names | wasm | wrong-output | `f32.const 0.0` `i32.store` | wasmtime: "type mismatch: expected i32, found f32". | `wat.zig`: use `f32.store` (and f32 locals) for float tuple elements. |
| destructure_tuple_with_long_names | test | weak | Identical literals mask S1; no execution. | | Use `#(1.5, 2.5)` and print. |
| destructure_tuple_with_try_catch | test | weak | No main, so the catch path is never executed on any backend. | | |
| import_multi_module_pub_fn_import | node (d.ts) | wrong-output | math.d.ts `export declare function double(x: ): i32;` | Invalid TS. | S10 |
| import_multi_module_pub_fn_import | erlang | wrong-output | main.erl `result() ->` `double(21).` | erlc: `function double/1 undefined`. It should be `math:double(21)`. | `erlang.zig` call lowering: consult `CrossModule.ownerModuleAtom` (`crossModule.zig` l.59) for imported fns. |
| import_multi_module_pub_fn_import | beam | wrong-output | main.S `{move, {integer, 21}, {x, 0}}.` `%% unresolved local call: double/1` `{deallocate, 0}.` `return.` | No call is emitted (and `deallocate` without `allocate` is the known issue). | `beam_asm.zig` l.2395: `call_ext math:double/1`. |
| import_multi_module_pub_fn_import | test | weak | No `@print`, so nothing is executed and the erlang/beam link failures are not visible in the RUN LOG. | | |
| import_multi_module_pub_val_import | node | wrong-output | config.js `const PORT = 8080;` `const HOST = "localhost";` (no `exports.PORT`/`exports.HOST`); main.js `const { PORT, HOST } = require("./config.js");` | Reproduced: `require("./config.js")` gives `{}`, so `PORT`/`HOST` are `undefined`. | `commonJS.zig`: emit `exports.X = X` for `pub val` (it already does for `pub fn`). |
| import_multi_module_pub_val_import | node (d.ts) | wrong-output | main.d.ts `import { PORT, HOST } from "config";` appears twice | Duplicate import. | S10 |
| import_multi_module_pub_val_import | erlang | wrong-output | config.erl has no `-export` for `'PORT'/0`, `'HOST'/0`; main.erl `addr() ->` `HOST.` `port() ->` `PORT.` | erlc: `variable 'HOST' is unbound`; it should be `config:'HOST'()`. | `erlang.zig`: export `pub val` accessors and lower imported vals as remote calls. |
| import_multi_module_pub_val_import | beam | known | main.S `{move, {atom, 'HOST'}, {x, 0}}.`; config.S `{exports, []}.` | Known "imported pub val lowered to atom". Note: `pub val` is also not exported. | |
| import_multi_module_pub_val_import | test | weak | No execution. | | |
| import_cross_module_record_construct_and_assoc_fn | node (d.ts) | wrong-output | http.d.ts `ok(body: ): Response;` (static declared as instance, empty param type); main.d.ts duplicate `import { Response, App } from "http";` | JS RUN LOG `hi`/`8080` is correct; the typedefs are wrong. | S10 |
| import_cross_module_record_construct_and_assoc_fn | erlang | wrong-output | http.erl `-module(http).` `-export([ok/1]).`; main.erl `R = response:ok(<<"hi">>),` `A = 'App'(8080, <<"/">>),` | erlc: `function 'App'/2 undefined`, and module `response` does not exist. Expected RUN LOG `<<"hi">>` / `8080`. The test comment claims "erlang/beam remote-call the owner atom + build the owner's map", which the snapshot contradicts. | `erlang.zig` + `crossModule.zig`: owner module is `http` (not the lowercased record name); build `#{port => 8080, path => <<"/">>}` for the imported record ctor. |
| import_cross_module_record_construct_and_assoc_fn | beam | wrong-output | main.S `{call_ext, 1, {extfunc, response, ok, 1}}.` (http.S exports `{'Response_ok', 1}`); `{move, {integer, 8080}, {x, 0}}.` `{move, {literal, <<"/">>}, {x, 0}}.` `%% unresolved local call: App/2` | Runtime `undef response:ok/1`, empty RUN LOG. Also S1 clobber of `8080`. | `beam_asm.zig` cross-module call path: use owner module and mangled name `'Response_ok'`; S1; S2. |
| import_cross_module_record_construct_and_assoc_fn | wasm | weak | `;; cross-module import not linked (wasm single-module)` then `call $ok` / `call $App` | The single-module limitation is documented in the test comment, but the emitted module is invalid (wasmtime "unknown func `$ok`"). | Emit a stub or refuse. |
| pipeline_simple_chain | erlang | wrong-output | `Result = Inc(Double(1)),` | erlc: `variable 'Inc' is unbound`, `variable 'Double' is unbound`. Expected RUN LOG `3` (JS and beam print `3`); erlang is empty. | `erlang.zig` `pipelineNode` (l.2791): lower a bare fn ident on the RHS as a local fn name (`inc(double(1))`), not a variable. |
| pipeline_with_labeled_args | erlang | wrong-output | Same `Result = Inc(Double(1)),` | Same as above. | Same. |
| pipeline_with_labeled_args | test | wrong-test + duplicate | features.zig l.504-515: source is byte-identical to `pipeline simple chain` (l.491-502); no labeled args. All 4 snapshots are identical apart from the slug. | The name promises labeled args, which the source does not use. | Use e.g. `1 \|> add(to: 2)` or delete the test. |
| range_iterate_over_range | beam | wrong-output | `{move, {integer, 0}, {x, 0}}.` `{move, {x, 0}, {x, 1}}.` `{gc_bif, '-', {f, 0}, 2, [{x, 0}, {integer, 1}], {x, 0}}.` | `N` in `x0` is clobbered before computing `N-1`. Reproduced: `sumTo(5)` gives `[]` (erlang gives `[0,1,2,3,4]`). | `beam_asm.zig` range lowering (~l.3795-3814): materialize the start into a scratch register, not `x0`, while the end still reads `x0`. |
| range_iterate_over_range | wasm | wrong-output | Loop body `local.get $i` (value discarded), then after the loop `i32.const 0` `return` | Always returns `0` instead of a collection. | `wat.zig`: loop-yield collection unsupported; reject. |
| range_iterate_over_range | test | weak | Source `fn sumTo(n: i32) -> i32 { return loop (0..n) { i -> yield i; }; }` returns a list from a `-> i32` fn named `sumTo`, and is never called. | Name and type are misleading. | Rename, fix the return type, add main/print. |
| range_open_ended_range | node | wrong-output | `for (const i of (() => { throw new Error("open-ended range unsupported on commonJS"); })()) {` and `(() => { if ((i > 100)) { return return; } })();` | Node: `SyntaxError: Unexpected token 'return'`, and a runtime throw by design. The snapshot pins invalid JS. | `commonJS.zig` l.3108: generator-based infinite range; `break` inside a lambda body must not emit `return return`. |
| range_open_ended_range | erlang | wrong-output | `true ->` `;` and `lists:seq(X, infinity)` | erlc: `syntax error before: ';'`; `lists:seq/2` with `infinity` is invalid at runtime. | `erlang.zig` l.2669-2677 + break lowering. |
| range_open_ended_range | beam | wrong-output | `{move, {atom, infinity}, {x, 2}}.` `{call_ext, 2, {extfunc, lists, seq, 2}}.`; fun: `{test, is_lt, ...}` `{deallocate, 0}.` `return.` | `lists:seq(X, infinity)` crashes, and `break` only returns from the fun (does not stop `foreach`). | `beam_asm.zig` l.3795-3814. |
| range_open_ended_range | wasm | wrong-output | `(if (result i32)` `(then` `)` `(else` `i32.const 0` `)` `)` | wasmtime: "expected i32 but nothing on stack"; `break` never branches to `$__break`. | `wat.zig` break inside `if`: `br $__break`. |
| range_open_ended_range | test | weak | `countUp` is never called; every backend is broken, but the snapshot is accepted. | | |
| negation_in_expression | beam | wrong-output | `{move, {x, 0}, {x, 2}}.` `{gc_bif, '-', {f, 0}, 2, [{integer, 0}, {x, 1}], {x, 0}}.` `{gc_bif, '+', {f, 0}, 3, [{x, 2}, {x, 0}], {x, 0}}.` | `erlc +from_asm`: `{{x,2},not_live}` (the first `gc_bif` declares Live=2 while `x2` holds `x`), so the module is rejected. Expected RUN LOG `7` (JS and erlang print `7`); beam is empty. | `beam_asm.zig` binop lowering (~l.1967-2010): the live count for the rhs `gc_bif` must include the saved-lhs scratch register (3). |
| enum_method_with_case_on_self | node | wrong-output | `name: function() {` `(() => {` `const _s = this;` ... `})();` `},` | No `return`, so the result is `undefined`, and `this` is the frozen `Color` object, never a variant string. | `commonJS.zig`: return the case value; enum methods without a self param should not use `this`. |
| enum_method_with_case_on_self | erlang | wrong-output | `name() ->` `case Self of` | erlc: `variable 'Self' is unbound`. | `erlang.zig`: implicit `self` param for enum methods using `self`. |
| enum_method_with_case_on_self | beam | wrong-output | `{move, {atom, self}, {x, 0}}.` `{move, {literal, <<"red">>}, {x, 0}}.` `{jump, {f, 4}}.` ... `{label, 4}.` `{move, {atom, ok}, {x, 0}}.` | Reproduced: `'Color_name'()` gives `ok` (`self` is an atom, the arms are unguarded, and the result is overwritten). | S7, S14, S15 |
| enum_method_with_case_on_self | wasm | wrong-output | Empty module (method dropped). | | S8 |
| enum_method_with_case_on_self | test | weak | Source method `fn name() -> string` has no `self` param yet uses `case (self)`, and has no `return`. Never called. | The fixture itself is ill-formed. | Declare `self: Self`, add `return`, call it. |
| lambda_with_parameter | test | wrong-test | features.zig l.578-584: `fn apply(f: syntax fn(x: i32) -> i32) -> i32 { return f(10); }` has no lambda literal. | The name says "lambda with parameter", but only a fn-typed param is exercised. | Add `apply({ x -> x + 1 })`. |
| lambda_with_parameter | wasm | wrong-output | `(func $apply (param $f i32) (result i32)` `i32.const 10` `call $f` | wasmtime: `unknown func $f`. | S8 (`call_indirect`). |
| lambda_multi_statement_body | test | wrong-test + duplicate | features.zig l.586-592: same shape as `lambda_with_parameter` (renamed `process`, `f(5)`); all 4 snapshots identical modulo names; no multi-statement lambda body anywhere. | Name does not match source. | Give it a real `{ x -> val y = x * 2; return y + 1; }` body. |
| lambda_multi_statement_body | wasm | wrong-output | `call $f` | Invalid WAT. | S8 |
| lambda_standalone_with_params | node, erlang, beam, wasm | wrong-test | All four `lambda_standalone_with_params.snap.md` are 0 bytes (committed empty blob `e69de29`). | Nothing is pinned; the test cannot pass against an empty baseline. The source also uses top-level statements (`val result = add(10, 20);` `@print(result);`), whose support is unverified (not runnable here without `zig build`). | S17: regenerate; if top-level `@print` is unsupported, turn it into an error test. |
| lambda_with_type_annotation | wasm | wrong-output | `i32.const 0 ;; lambda` `local.set $func` `i32.const 256` `call $func` | wasmtime: `unknown func $func`. | S8 |
| lambda_with_type_annotation | test | weak | `main` returns `func("hello")` but nothing is printed, so every RUN LOG is empty. `lambda_simple_standalone` produces byte-identical generated code, so the annotation's effect is not observable. | | `@print(func("hello"))`. |
| lambda_multi_param_with_type_annotation | wasm | wrong-output | `i32.const 0 ;; lambda` ... `call $add` | Invalid WAT. | S8 |
| lambda_multi_param_with_type_annotation | test | weak | No `@print`. | | |
| lambda_simple_standalone | wasm | wrong-output | `call $func` | Invalid WAT. | S8 |
| lambda_simple_standalone | test | weak | No `@print`. | | |
| lambda_full_type_annotation_infers_params | node | weak | Needle only: `"add(2, 3)"` | The JS call text `add(2, 3)` does not depend on the inferred param types. If a type error does not abort codegen, the needle would still match, so it does not prove the comment's claim (uncertain whether a type error aborts `codegen.generate` here). | Assert on inferred types (comptime test) or on RUN LOG `5`. |
| lambda_string_typed_annotation_infers_params | node | weak | Needle only: `"join("` | Trivially present; does not check string typing or concatenation. | Assert RUN LOG `xy`. |
| optional_chaining_member_access_short_circuits_null | wasm | wrong-output | `local.get $__mem1` `i32.load ;; ?.name` `call $__print_i32` | Prints the string pointer as an integer (strings unsupported in `@print`). | `wat.zig` `@print` for string type. |
| optional_chaining_member_access_short_circuits_null | test | weak | Source `val u: ?User = User(name: "ana");`: only the non-null path is exercised. | The name claims "short-circuits null", but the null branch is never executed or checked. | Add `val n: ?User = null; @print(n?.name);`. |
| reserved_word_identifiers | node (d.ts) | wrong-output | `export declare function delete(with: , class: ): string;` | `delete`/`with`/`class` are not renamed in the d.ts, and the params have no types (invalid TS, contrary to the test's intent). | S10 (apply `jsIdent` in typescript.zig). |
| reserved_word_identifiers | erlang | wrong-output | `Static = (With + Class),` | Runtime `badarith` `'+'(<<"a">>,<<"b">>)`, empty RUN LOG (expected `<<"ab">>`). Same class as the known template-concat issue, but here it is a plain string `+`. | `erlang.zig` binop on string types: `<<A/binary, B/binary>>`. |
| reserved_word_identifiers | beam | wrong-output | `{gc_bif, '+', {f, 0}, 2, [{x, 0}, {x, 1}], {x, 0}}.`; main `{move, {literal, <<"a">>}, {x, 0}}.` `{move, {x, 0}, {x, 0}}.` `{move, {literal, <<"b">>}, {x, 0}}.` | Reproduced: `badarith` on `<<"b">> + <<"b">>` (string `+` plus S1 arg clobber). Empty RUN LOG. | S1; binary construction for string `+`. |
| reserved_word_identifiers | wasm | wrong-output | `local.get $with` `local.get $class` `i32.add` | Adds two string pointers. | `wat.zig`: lower string `+` to a concat helper. |
| iterator_fromlist_yields_array_items | erlang | wrong-output | `toList(Iter) ->` `Out = [],` `lists:foreach(fun(Item) ->` `(Out ++ [Item])` `end, Iter),` `Out.`; RUN LOG `<<>>` | The pinned RUN LOG `<<>>` is wrong (expected `<<"1,2,3">>`; JS prints `1,2,3`). `var` mutation inside a loop lambda is lost. | `erlang.zig` loop-with-mutated-var lowering: `lists:foldl` threading `Out`. |
| iterator_fromlist_yields_array_items | beam | wrong-output | `fromList`: `{call_ext, 2, {extfunc, lists, map, 2}}.` `{move, {atom, ok}, {x, 0}}.`; `toList`: `{move, nil, {x, 0}}.` (clobbers `Iter`); fun `{move, {atom, out}, {x, 0}}.`; RUN LOG `<<>>` | The pinned RUN LOG is wrong (`fromList` returns `ok`, `iter` is clobbered, `out` becomes an atom). | S14, S15; foldl threading as in erlang. |
| iterator_fromlist_yields_array_items | wasm | wrong-output | `(func $fromList ...` `i32.const 0 ;; loop over non-range` `)`; main `i32.const 256` `call $join` | Invalid WAT (`$join` undefined). | S8 |
| option_method_on_tuple_element | node | wrong-output | l.66 `Array.prototype.slice = ... require("./gleam_stdlib.mjs").slice(this, start, end)`; main uses `xs.slice(1, xs.length)` | Expected RUN LOG `1` / `true`. Actual: `Cannot find module './gleam_stdlib.mjs'`, empty RUN LOG. The test comment also says `.unwrapOr` lowers to `__bp_option_unwrapOr`, but the snapshot has an inline `((_o) => _o != null ? _o : ((-1)))(head)` (stale comment). | S4 |
| option_method_on_tuple_element | erlang | wrong-output | `Rest = slice(Xs, 1, length(Xs)),` | erlc: `function slice/3 undefined`, empty RUN LOG. | S3 |
| option_method_on_tuple_element | beam | wrong-output | `{test, is_eq, {f, 24}, [{x, 0}, {atom, nil}]}.`; `%% unresolved method call: slice/3`; `{get_map_elements, {f, 21}, {x, 0}, {list, [{atom, length}, {x, 0}]}}.`; RUN LOG `1` / `false` | The pinned `false` is wrong (expected `true`). `null` lowers to atom `nil` while `.at` miss returns `undefined`; `xs.length` is read as a map key; `slice` is unresolved. | `beam_asm.zig` l.4059 `.null_ => {atom, nil}` should be `undefined` (erlang uses `undefined`); `length` via `erlang:length/1`; S2. |
| option_method_on_tuple_element | wasm | wrong-output | l.62 `(local $_res0 i32)` mid-body; `i32.const 0` `call $at` | Invalid WAT. | S9, S8 |
| array_slice_2_arg_lowers_byte_identically_across_backends | node | wrong-output | Prelude `Array.prototype.slice = function(start, end) {` ... `require("./gleam_stdlib.mjs").slice(this, start, end)` | Expected `[ 2, 3, 4 ]`. Actual: crash, empty RUN LOG. The test comment ("commonJS keeps `xs.slice(start, end)`") is contradicted. | S4 |
| array_slice_2_arg_lowers_byte_identically_across_backends | erlang | wrong-output | `io:format("~p~n", [slice(Xs, 1, 4)]).` | erlc `slice/3 undefined`, empty RUN LOG. The comment claims "erlang lowers to `lists:sublist`". | S3 |
| array_slice_2_arg_lowers_byte_identically_across_backends | wasm | wrong-output | `local.get $xs` `i32.const 1` `i32.const 4` `call $__str_slice` `call $__print_i32` | Array slice routed to the string-slice helper, and the pointer is printed. | `wat.zig` array slice. |
| array_join_lowers_byte_identically_across_backends | wasm | wrong-output | `i32.const 256` `call $join` (receiver `[10,20,30]` never built) | Invalid WAT. | S8 |
| array_join_lowers_byte_identically_across_backends | erlang, beam vs node | uncertain | node `10, 20, 30` vs erlang/beam `<<"10, 20, 30">>` | Not byte-identical as the name claims. | S16 |
| array_indexof_lowers_byte_identically_across_backends | wasm | wrong-output | `i32.const 3` `call $indexOf` (receiver dropped) | Invalid WAT. | S8 |
| array_at_lowers_byte_identically_across_backends | wasm | wrong-output | `i32.const 0` `call $at` | Invalid WAT. | S8 |
| interface_associated_fn_namespace | beam | wrong-output | main `{move, {integer, 1}, {x, 0}}.` `{move, {literal, <<"one">>}, {x, 0}}.` `{move, {x, 0}, {x, 1}}.`; RUN LOG `<<"one">>` | The pinned RUN LOG is wrong: expected `1` (JS and erlang print `1`). `Pairish.of(1,"one")` is called as `("one","one")`. | S1 (`materializeCallArgs`, `scratch_base = cur_arity = 0`). |
| interface_associated_fn_namespace | wasm | wrong-output | `call $of` / `call $first` (interface not emitted) | Invalid WAT. | S8 |
| stdlib_associated_fn_namespace_injected | beam | known | RUN LOG `<<"one">>` / `42` / `10` | Known (listed). Root cause is visible: S1 for `Pair.of`, S14 for closures (`%% unresolved local call: f/1`, `{move, {atom, x}, {x, 0}}.`). | |
| stdlib_associated_fn_namespace_injected | wasm | wrong-output | `call $of`, `i32.const 0 ;; lambda` `call $compose` | Invalid WAT. | S8 |
| array_instance_default_fn_methods | erlang | wrong-output | `io:format("~p~n", [all(Xs, fun(X) ->` | erlc `function all/2 undefined`, empty RUN LOG (expected `<<"0,1,2,3">>` / `6` / `false` / `true`). | S3 |
| array_instance_default_fn_methods | beam | wrong-output | `%% unresolved method call: fold/3`, `%% unresolved method call: all/2` | `erlc +from_asm`: `Internal consistency check failed ... {move,{x,1},{x,1}} {uninitialized_reg,{x,1}}`, so no RUN LOG. | S2 |
| array_instance_default_fn_methods | wasm | wrong-output | `call $join`, `call $fold`, `call $isEmpty`, `call $all` | Invalid WAT. | S8 |
| array_instance_default_fn_methods | node | ok (latent) | The prelude also installs the gleam-backed `Array.prototype.slice` override (l.60), which is harmless here only because `slice` is not called. | | S4 |
| array_zip_via_external_node_template | node | wrong-output | `Array.prototype.zip = function(other) { return this.map(...).slice(0, Math.min(this.length, (other).length)); };` plus the gleam-backed `Array.prototype.slice` at l.58 | This is the test's main subject (the JS zip template), yet the RUN LOG is empty. Reproduced: `Cannot find module './gleam_stdlib.mjs'` from `Array.zip`. Expected `[ [ 1, 'a' ], [ 2, 'b' ], [ 3, 'c' ] ]`. | S4 |
| array_zip_via_external_node_template | beam | known | RUN LOG `[<<"a">>,<<"b">>,<<"c">>]` | Known (listed); root is S2 `%% unresolved method call: zip/2`. | |
| array_zip_via_external_node_template | wasm | wrong-output | `local.get $ys` `call $zip` | Invalid WAT. | S8 |
| bool_instance_default_fn_methods | erlang | wrong-output | `nor(false, false)`, `nand(true, true)`, `exclusiveOr(true, false)` | erlc: undefined functions, empty RUN LOG (expected `false true false true`). | S3 |
| bool_instance_default_fn_methods | beam | wrong-output | `%% unresolved method call: negate/1` ... RUN LOG `true` `false` `true` `false` | The pinned RUN LOG is the exact inverse of the correct `false true false true` (JS). | S2 |
| bool_instance_default_fn_methods | wasm | wrong-output | `call $negate` with no args pushed | Invalid WAT. | S8 |
| numeric_instance_methods_external_default_fn | erlang | wrong-output | `clamp(N, 0, 5)`, `isEven(X)` | erlc: undefined functions, empty RUN LOG (expected `5 -5 10 0 false`). | S3 |
| numeric_instance_methods_external_default_fn | beam | wrong-output | `%% unresolved method call: abs/1` ... `clamp/3` ... `isEven/1`; RUN LOG `-5` `3` `10` `5` `7` | The pinned RUN LOG is wrong (expected `5` `-5` `10` `0` `false`). | S2 |
| numeric_instance_methods_external_default_fn | wasm | wrong-output | `call $abs` with the receiver `$n` never loaded | Invalid WAT. | S8 |
| string_methods_map_to_native_js_names | node | wrong-output | l.41 `String.prototype.slice = ... require("./gleam_stdlib.mjs").string_slice(self, start, end)` | Reproduced: prints 3 lines, then crashes on `s.slice(0, 5)`, so the RUN LOG is empty (expected `HELLO,WORLD` / `hello,world` / `Hello\|World` / `Hello`). The comment says same-named `slice` "use the engine directly", which the snapshot contradicts. Also `String.prototype.lines` uses `/\\r?\\n/` (double-escaped regex, matches a literal backslash). | S4; `primitives.bp` `lines` template escaping. |
| string_methods_map_to_native_js_names | erlang | wrong-output | `io:format("~p~n", [slice(S, 0, 5)]).` | erlc `slice/3 undefined`, empty RUN LOG. | S3 |
| string_methods_map_to_native_js_names | beam | wrong-output | `%% prim method not lowered on beam (complex arg): split/2` then `lists:map` over the binary; `%% unresolved method call: slice/3` | Reproduced: `{case_clause,<<"Hello,World">>}` in `lists:map`, empty RUN LOG. | S2 (`beam_asm.zig` l.2704) |
| string_methods_map_to_native_js_names | wasm | wrong-output | `call $toUpper` (receiver not pushed), `call $join` | Invalid WAT. | S8 |
| endswith_lowers_via_external_beam_single_line_body | erlang | wrong-output | `io:format("~p~n", [string:suffix(S, <<"bar">>)]).` | Runtime `undef string:suffix/2`, empty RUN LOG (expected `true`). | S5 |
| endswith_lowers_via_external_beam_single_line_body | beam | wrong-output | `{call_ext, 2, {extfunc, string, suffix, 2}}.` | Same `undef`. The test comment calls this an "e2e gate", but the empty RUN LOG hides the crash. | S5 |
| endswith_lowers_via_external_beam_single_line_body | wasm | wrong-output | `i32.const 268` `call $endsWith` | Invalid WAT. | S8 |
| enum_sections_section_emits_as_synthesised_inner_enum_decl | test | weak | The header comment (features.zig l.938-946) says F4 "pins that an enum carrying sections emits byte-identically against the manually-written enum-of-enum form", but the test snapshots only the sectioned form (no comparison with a hand-written form). No values are constructed or printed. | Byte-identity is not actually checked. | Add a `generateJs` equality check against the hand-written enum-of-enum source. |
| enum_sections_nested_sections_with_numeric_leaves_emit_decls | test | weak | Same as above. The comment in the sibling test says the leaf is `__Token__Color__Red._500`, but the snapshots show `__500` (stale comment). | | |

---

## ok slugs (all 4 backends ok, no test-level issue)

- enum_unit_variants
- enum_payload_variant
- enum_shorthand_declaration_without_val_name
- negation_simple_unary_minus
- enum_sections_path_access_lowers_to_qualified_ctor_calls
- import_disk_lib_namespace_member_binds_emits_the_namespace_object (commonJS needle test, no snapshot by design)
- import_disk_lib_namespace_merges_across_the_lib_s_modules (commonJS needle test, no snapshot by design)

Per-backend ok cells worth noting, where the RUN LOG was verified by hand and by execution:

- node RUN LOGs correct:
  - destructure_record_val_binding (`3 4`)
  - destructure_tuple_val_binding (`12 hello`)
  - import_cross_module_record_construct_and_assoc_fn (`hi`/`8080`)
  - pipeline_* (`3`)
  - negation_* (`-42`, `7`)
  - optional_chaining (`ana`)
  - reserved_word_identifiers (`ab`)
  - iterator_fromlist (`1,2,3`)
  - array_join / indexOf / at
  - interface_associated (`1`)
  - stdlib_associated (`1 42 22`)
  - array_instance_default_fn_methods
  - bool
  - numeric (`5 -5 10 0 false`)
  - endsWith (`true`)
- erlang RUN LOGs correct: negation_*, optional_chaining, array_join / indexOf / at, interface_associated, stdlib_associated, array_zip.
- beam RUN LOGs correct: pipeline_*, negation_simple_unary_minus, optional_chaining, array_slice_2_arg (`[2,3,4]`), array_join / indexOf / at.
