# Snapshot review: comptime errors, exhaustiveness, effects

Repo: `/home/ericfillipe/develop/botopink-lang/repository/botopink-lang` (all paths below are relative to `modules/compiler-core/`).

Batch: `src/comptime/tests/infer_errors.zig` (61), `exhaustiveness.zig` (13), `effect_result.zig` (7), `effect_future.zig` (7), `effect_generator.zig` (4). Total 92 tests.

> **Status (1.0.1-beta close):** the harness defects this report leans on (H1-H10) are fixed: the RUN LOG is decided by the process exit status, an `erlc` warning no longer blanks a log, a program that does not compile fails its snapshot test (or records a `COMPILE DIAGNOSTIC`), every backend is compared in one round, and the 0-byte snapshots are gone. The checker rows below predate the comptime-folding wave and are otherwise untouched - re-derive each one at HEAD before acting on it. Residuals are tracked in [`1.0.2-beta/06-snapshot-review.md`](../../1.0.2-beta/06-snapshot-review.md). The tables below are the audit record and are kept verbatim.

## Re-check summary (2026-09-15, against HEAD)
- Every non-`ok` row (48) and every cross-cutting note (6) was re-checked against the current source and snapshots. Every probe claim was re-run with `zig-out/bin/botopink check`, **now rebuilt from HEAD** (mtime 2026-09-15 19:48), in scratch projects under `scratchpad/rev-ct-errors-decls/p/`. `zig build` was not run.
- All test `file:line` references still match (the test files are unchanged). Every quoted snapshot line is still accurate. The `node` copies are still byte-identical to the `erlang` copies (106/106 in `errors/`) and to the `erlang`/`beam`/`wasm` AST copies.
- Every probe gave the same result as before. The one change is in the parse-failure output: the CLI now prints `comptime: parse failed for module 'main'` plus the source before the final `error: parse error in main`.
- Line drift: `snapshot.zig` moved +2 (failed-outcome arms are now 642-644). `diagnostics.zig` R10/R12/R15 comments are one line off from what was cited. Most `infer.zig` numbers held, but a few cited the wrong line in the first pass (the body-ignore loop, the return-arm range, the `.add`/`.sub…` `unify` calls, one external-arity line).

| re-check class | rows | cross-cutting notes |
|---|---|---|
| confirmed | 39 | 3 |
| corrected | 9 | 3 |
| withdrawn | 0 | 0 |
| uncertain | 0 | 0 |

Corrections that matter (beyond line numbers):
- `duplicate_method_across_interfaces_without_qualification`: the expected caret is 9:8, not 10:8.
- `type_mismatch_mul_with_non_numeric`: the claim that `"a" * "b"` "would presumably pass" is now probed, and it does check OK.
- `implement_declares_method_not_in_interface`: the sources differ by more than separators (different names, and a `record Pato { … }` statement form). The duplicate-rule verdict stands.

## Method
- Slug rule (`tests/helpers.zig:63-76`): take the test name after the **first** `": "`, lowercase it, keep ASCII alphanumerics, and collapse every other run of characters into one `_`. I recomputed the slug for every test with a script (`scratchpad/run-comptime-errors/slugs.py`; re-check: `scratchpad/rev-ct-errors-decls/slugs.json`).
- `assertTypeErrorSnap` writes `comptime/{node,erlang}/errors/<slug>`. `assertComptimeAstSingle` writes `comptime/{node,erlang,wasm,beam}/<slug>`. `assertInfersOk` writes no snapshot.
- Every error snapshot (72) and AST snapshot (4) exists. Each `node` copy is byte-identical to its `erlang` copy (error snapshots) or to its `erlang`/`beam`/`wasm` copies (AST snapshots). Checked with `filecmp`, then again with `cmp` at HEAD.
- Orphans: none. All 106 files in `snapshots/comptime/node/errors/` and every `snapshots/comptime/node/*.snap.md` map to a test name somewhere in `src/`.
- I read all 76 snapshots in full and cross-checked them against `comptime/infer.zig`, `comptime/diagnostics.zig`, `lexer.zig` and `parser*.zig`.
- Corroborating probes: first pass used a stale prebuilt binary (2026-09-14 11:23). **Re-check:** every probe below was re-run with the binary rebuilt from HEAD, and all results are unchanged. Every probe claim is also backed by source code.

## Counts per verdict
| verdict | count |
|---|---|
| ok | 44 |
| wrong-output | 24 |
| weak | 9 |
| wrong-test | 9 |
| duplicate | 6 |
| orphan | 0 |
| uncertain | 0 |
| **total** | **92** |

(The verdict counts are unchanged by the re-check. No row was withdrawn or reclassified.)

## Cross-cutting findings (root causes behind several rows)
1. **The AST harness does not fail on a parse or type error.** *(re-check: corrected, line numbers)* `snapshot.zig:642-644` renders `.validationError`, `.typeError` and `.parseError` as `{}`, so only the SOURCE section is written. `helpers.zig:assertComptimeAst` (`helpers.zig:83-124`) and `snapshot.assertComptimeAstWithPath` (`snapshot.zig:679-687`) never check the outcome. A broken "success" test therefore snapshots cleanly. `nested_pattern_matching` is exactly this case. Out of batch, `snapshots/comptime/node/non_empty_list_pattern.snap.md` also has no `TYPED AST JSON` section and also uses `default`.
2. **Return values are never unified with the declared return type.** *(re-check: corrected, line numbers)* `infer.zig:2868-2871`:
   ```
   // Infer body (for type checking; we ignore the result for now).
   for (f.body) |stmt| {
       _ = try inferExpr(env, stmt.expr);
   }
   ```
   The `.@"return"` jump arm (`infer.zig:5459-5562`) never unifies `valPtr` with the fn return type; it types the jump as `void` (`infer.zig:5561`). A probe at HEAD confirms that `fn f() -> string { return 1; }` checks OK. Every `assertInfersOk` "happy path" that relies on `return v` to pin a type verifies nothing.
3. **Logical operators unify operand-first.** *(re-check: confirmed)* `infer.zig:5372-5373` and `5411` call `unifyAt(env, operandTy, bool, loc)`, but `unifyAt` is documented as "always called target-first (`unifyAt(param, arg)`)" (`infer.zig:3768-3769`). As a result, "expected" and "found" are swapped in the rendered error. The CLI shows the same: `val bad = 1 && true;` gives `type mismatch: expected i32, got bool at main:1:13`.
4. **Effect/external diagnostics point at the body, not the declaration.** *(re-check: confirmed)* `infer.zig:2789` (`const fnLoc: ?ast.Loc = if (f.body.len > 0) f.body[0].expr.getLoc() else null`) and `infer.zig:2073` (same line) do this. Wrapper errors land on the first body statement, and `declare fn` errors have no location at all.
5. **Many `TypeError` constructors carry no location.** *(re-check: corrected, line numbers)* Examples: `unknownInterface` (`infer.zig:330`), `unknownMethod` (`336`, `355`), `ambiguousMethod` (`359`), `missingMethod` (`385`); `extendRequiresInterface`, `redundantActivation`, `notAnExtension` (`infer.zig:761`, `781`, `783`); pub-default (`infer.zig:190-196`); RG3 (`infer.zig:4638-4642`). `.add` and `.sub/.mul/.div/.mod` call plain `unify` without `At` (`infer.zig:5384` and `5392`). These snapshots have no `┌─` box, so a regression that moves the error would not be visible. The CLI agrees: these errors print `… in main` with no `at main:L:C`.
6. **Renderer cosmetics (`helpers.zig:213,221`).** *(re-check: confirmed)* The gutter `"  │"` has a fixed width, so it misaligns for line numbers of 10 or more (`10 │ …` over `  │`). It also joins constraint lists with `", "` (`helpers.zig:370-373`, join at 371), which renders `type string, int, bool` instead of the source `string | int | bool`.

## Non-ok findings

"CLI" means `botopink check` rebuilt from HEAD.

| slug | test file:line | verdict | re-check | evidence (quoted lines + path) | expected vs actual | suggested fix |
|---|---|---|---|---|---|---|
| `nested_pattern_matching` | exhaustiveness.zig:79 | **wrong-test (high)** | confirmed (CLI) | `snapshots/comptime/node/nested_pattern_matching.snap.md` holds only the SOURCE section. It has no `----- TYPED AST JSON`. Source: `val unwrap_or = fn(r: Result<i32, string>, default: i32) -> i32 {`. `lexer.zig:667` `if (std.mem.eql(u8, text, "default")) return .default;`, and `parser.zig:1040-1042` `consumeParamName` accepts only `.identifier`. CLI: the original source gives `comptime: parse failed for module 'main'` … `error: parse error in main`; renaming to `fallback` gives `Checked`. | Expected a successful typed AST showing that `Ok(v)`/`Err(_)` is exhaustive. Actual: a parse failure was snapshotted as a pass, so no exhaustiveness is exercised. | Rename the param (e.g. `fallback`), regenerate, and make `assertComptimeAst` fail when any output outcome is not `.ok`. |
| `star_fn_returning_a_non_async_type` | infer_errors.zig:293 | **wrong-output (high)** | confirmed (CLI) | `snapshots/comptime/node/errors/star_fn_returning_a_non_async_type.snap.md`: `error: effect-wrapper-mismatch: \`#[@future]\` requires a \`-> @Future<…>\` return type` with `┌─ :3:5` / `3 │     return "x";`. `diagnostics.zig:30-34`: `/// R3 — annotation effect kind disagrees with the return wrapper kind.` and `/// R4 — annotation present, return wrapper missing (\`#[@result] fn f() -> i32\`).` `infer.zig:2796-2799` picks R3 whenever `retDeref.* == .named`. Every primitive is `.named`, and so are the wrappers (`Future` is `.named`, `infer.zig:5555`). CLI: `#[@result] fn bad() -> i32 { return 1; }` gives `effect-wrapper-mismatch … at main:3:5`. | Expected `effect-missing-wrapper` (R4) located at the `-> string` return type (line 2). Actual: the R3 code, pointing at the body statement `return "x"`. R4 is unreachable for every primitive return type. | Choose R4 when the return type is not one of the effect wrappers. Anchor the location on the fn decl or return type. Rename the stale "star fn" wording. |
| `type_mismatch_non_bool_lhs_with` | infer_errors.zig:107 | wrong-output (med) | confirmed (CLI) | `.../errors/type_mismatch_non_bool_lhs_with.snap.md`: `val bad = 1 && true;` … `expected: i32` / `found:    bool`. Root cause 3. CLI: `expected i32, got bool at main:1:13`. | Expected `expected: bool, found: i32`. Actual: swapped. The caret sits on `&&` (col 13), which is acceptable. | Swap the args: `unifyAt(env, bool, operandTy, loc)` at `infer.zig:5372-5373,5411`. |
| `type_mismatch_non_bool_rhs_with` | infer_errors.zig:113 | wrong-output (med) | confirmed | `.../errors/type_mismatch_non_bool_rhs_with.snap.md`: `val bad = true \|\| 0;` … `expected: i32` / `found:    bool` | Expected `expected: bool, found: i32`. Actual: swapped. | Same fix as above. The slugs also lose the operator (`…_lhs_with`, `…_rhs_with`, `…_non_bool_with`); consider spelling it out, e.g. `with and`. |
| `type_mismatch_non_bool_with` | infer_errors.zig:119 | wrong-output (med) | confirmed (CLI) | `.../errors/type_mismatch_non_bool_with.snap.md`: `val bad = !42;` … `expected: i32` / `found:    bool`. CLI: `expected i32, got bool at main:1:11`. | Expected `expected: bool, found: i32`. Actual: swapped. | Same fix as above. |
| `std_package_unknown_module` | infer_errors.zig:407 | wrong-output (med) | confirmed (CLI) | `.../errors/std_package_unknown_module.snap.md`: `hint: Available std modules: bool. (…)`. The message comes from hard-coded `infer.zig:72-75` (hint string at 74). The very next test imports `order` from `"std"` successfully (`std_package_member_missing`). CLI: `import {order} from "std";` checks. There is no location (CLI: `… in main`). | Expected a hint that lists the actual `env.stdModules` keys (or no list), with a caret on `linked_list`. Actual: a stale single-module list and no location. | Build the hint from `env.stdModules` and add `.withLoc(import loc)`. |
| `unreachable_arm_after_wildcard` | exhaustiveness.zig:127 | wrong-output (med) | confirmed | `.../errors/unreachable_arm_after_wildcard.snap.md`: `┌─ :10:17` / `10 │         Blue -> "blue";` / caret under `"blue"`; `this arm is already covered by an earlier arm ('Color')`. `infer.zig:5046` `.withLoc(arm.body.getLoc())`. | Expected the caret on the pattern `Blue` (10:9), with the parenthetical naming the covering arm (`_`) or labelled as a type. Actual: the caret is on the arm body, and `('Color')` reads as if an arm named Color covered it. The gutter is misaligned. | Use the pattern loc. Reword the renderer (`helpers.zig:410`) to e.g. `… covered by an earlier arm (type 'Color')`. |
| `duplicate_variant_arm` | exhaustiveness.zig:217 | wrong-output (med) | confirmed (CLI) | `.../errors/duplicate_variant_arm.snap.md`: `┌─ :10:16` / `10 │         Red -> "again";` / caret under `"again"`; `variant 'Red' is already covered by an earlier arm ('Color')`. `infer.zig:5061`. CLI: `… at main:10:16`. | Expected the caret on the second `Red` pattern (10:9). Actual: the caret is on the body. | Same fix as above. |
| `normal_fn_returning_future_must_be_star_fn` | infer_errors.zig:302 | wrong-output (low) | confirmed (CLI) | `.../errors/normal_fn_returning_future_must_be_star_fn.snap.md`: `┌─ :2:5` / `2 │     return 0;` for a problem in `fn bad() -> @Future<i32>` (line 1). Root cause 4. CLI: `… at main:2:5`. | Expected the location on the signature (line 1). Actual: first body statement. The name uses the obsolete "star fn" terminology (effects are `#[@future]` now). | Anchor on the decl. Rename to "plain fn returning @Future needs an effect annotation". |
| `effect_annotation_does_not_match_the_return_wrapper` | infer_errors.zig:349 | wrong-output (low) | confirmed | `.../errors/effect_annotation_does_not_match_the_return_wrapper.snap.md`: `┌─ :3:5` / `3 │     return 0;`. The mismatch is `#[@future]` against `-> @Result<i32, string>` on lines 1-2. | The R3 code is right, but the location should be the annotation or return type, not `return 0`. | Anchor on the decl or return type. |
| `two_pub_default_fn_in_one_package` | infer_errors.zig:21 | wrong-output (low) | confirmed | `.../errors/two_pub_default_fn_in_one_package.snap.md`: `error: a package declares at most one …` with no `┌─` box (`infer.zig:190-196`, no `withLoc`). | Expected a caret on the second `pub default fn two`. Actual: no location. | Add `.withLoc` for the second default decl. |
| `two_pub_default_mod_in_one_package` | infer_errors.zig:28 | wrong-output (low) | confirmed | `.../errors/two_pub_default_mod_in_one_package.snap.md`: same message, no location. | Expected a caret on `pub default mod beta;`. | Same as above. |
| `type_arg_violates_constraint` | infer_errors.zig:35 | wrong-output (low) | confirmed | `.../errors/type_arg_violates_constraint.snap.md`: `'v' has type 'f64', which does not satisfy 'type string, int, bool'`. Source: `comptime v: type string \| int \| bool`. `helpers.zig:371` joins with `", "`. | The error, caret (`3.14`, 4:18) and type are correct. The constraint is printed in a syntax that does not exist. | Join with `" \| "`. |
| `implement_missing_a_required_interface_method` | infer_errors.zig:44 | wrong-output (low) | confirmed (CLI) | `.../errors/implement_missing_a_required_interface_method.snap.md`: `error: missing interface method` / `'Circle' does not implement 'erase' required by interface 'Drawable'`, no location (`infer.zig:385`). CLI: `… in main` (no loc). | The error is correct. Expected a caret on `implement Drawable for Circle` (line 6). | Add a loc to `TypeError.missingMethod`. |
| `implement_method_not_declared_in_the_interface` | infer_errors.zig:59 | wrong-output (low) | confirmed | `.../errors/implement_method_not_declared_in_the_interface.snap.md`: `'explode' is not declared in any interface implemented for 'Circle'`, no location (`infer.zig:336/355`). | Expected a caret on `fn explode` (9:8). | Add a loc. |
| `implement_qualified_prefix_is_not_a_declared_interface` | infer_errors.zig:76 | wrong-output (low) | confirmed | `.../errors/implement_qualified_prefix_is_not_a_declared_interface.snap.md`: `'Renderable' is not an interface implemented here (method 'draw')`, no location (`infer.zig:330`). | Expected a caret on `Renderable.draw` (6:8). | Add a loc. |
| `duplicate_method_across_interfaces_without_qualification` | infer_errors.zig:90 | wrong-output (low) | corrected (expected position) | `.../errors/duplicate_method_across_interfaces_without_qualification.snap.md`: `'connect' is declared by both 'UsbCharger' and 'SolarCharger' — qualify it`, no location (`infer.zig:359`). | Expected a caret on `fn connect` at **9:8** (the first pass said 10:8; line 9 is `    fn connect(self: Self) {`). | Add a loc. |
| `type_mismatch_i32_bool` | infer_errors.zig:125 | wrong-output (low) | corrected (line number; CLI) | `.../errors/type_mismatch_i32_bool.snap.md`: `error: type mismatch` / `expected: i32` / `found:    bool`, no `┌─` box. `infer.zig:5384` (`.add` arm; first pass said 5391) `try unify(env, lhsTy, rhsTy);`. CLI: `type mismatch: expected i32, got bool in main` (no loc). | The direction is acceptable (lhs is the reference). Expected a caret on `+` or `true`. | Use `unifyAt(..., loc)` in the `.add` (`5384`) and `.sub/.mul/.div/.mod` (`5392`) arms. |
| `type_mismatch_mul_with_non_numeric` | infer_errors.zig:131 | wrong-output (low) | corrected (line number; probe added) | `.../errors/type_mismatch_mul_with_non_numeric.snap.md`: `expected: f64` / `found:    string`, no location (`infer.zig:5392`; first pass said 5398). CLI: `val bad = "a" * "b";` gives `Checked`. | Expected a caret on `*` or `"oops"`. The message also never says "non-numeric": it reports an f64-vs-string unify, and `"a" * "b"` **does** pass (confirmed by probe, no numeric constraint exists). | Use `unifyAt`. Add a numeric-operand check. |
| `extend_without_an_interface_requires_implement` | infer_errors.zig:207 | wrong-output (low) | confirmed | `.../errors/extend_without_an_interface_requires_implement.snap.md`: `error: extend requires an interface`, no location (`infer.zig:761`). | Expected a caret on `extend Pato` (2:15). | Add a loc. |
| `redundant_local_activation_star_is_for_imports` | infer_errors.zig:218 | wrong-output (low) | confirmed | `.../errors/redundant_local_activation_star_is_for_imports.snap.md`: `` `PatoNada*` is redundant… ``, no location (`infer.zig:781`). | Expected a caret on `PatoNada*` (10:1). | Add a loc. |
| `activation_of_non_extension_symbol` | infer_errors.zig:257 | wrong-output (low) | confirmed | `.../errors/activation_of_non_extension_symbol.snap.md`: `'Pato' does not name an implement/extend symbol`, no location (`infer.zig:783`). | Expected a caret on `Pato*` (2:1). | Add a loc. |
| `rg3_future_rejects_t_is_required` | infer_errors.zig:442 | wrong-output (low) | confirmed (CLI) | `.../errors/rg3_future_rejects_t_is_required.snap.md`: `error: generic-required-arg-missing: a required generic argument is missing`, no location, and the message never names `@Future` or `T` (`infer.zig:4638-4642`). CLI: same message, `in main`. | Expected a caret on `@Future<>` and the wrapper and parameter named. | Add a loc and name the wrapper/parameter in the message. |
| `rg3_iterator_rejects_t_is_required` | infer_errors.zig:449 | wrong-output (low) | confirmed | `.../errors/rg3_iterator_rejects_t_is_required.snap.md`: the same generic message, no location. The three RG3 snapshots have identical ERROR sections, so they cannot tell which wrapper fired. | Same as above. | Same as above. |
| `rg3_result_i32_rejects_e_is_required_no_default` | infer_errors.zig:456 | wrong-output (low) | confirmed | `.../errors/rg3_result_i32_rejects_e_is_required_no_default.snap.md`: the same generic message, no location. | Same as above. | Same as above. |
| `external_builtin_typechecks_args` | infer_errors.zig:391 | wrong-test | corrected (line number; CLI) | `.../errors/external_builtin_typechecks_args.snap.md`: source `#[@External.Python("string", "length")]`; error `` `@external` target must be a Target member: node, typescript, erlang, beam or wasm `` / `hint: Example: #[@External.Erlang( "string", "length")]`. There is no location, because `declare fn` has no body and `infer.zig:2073` reads `fnLoc` from the body. CLI: same message, `in main`. | The name promises argument type-checking. The test actually exercises target-member validation. The hint has a stray space after `(`. | Rename to "external ---- unknown target is rejected". Fix the hint text (`infer.zig:2107`, also `2086` and `2099`; first pass said 2098). Take the loc from the annotation. |
| `external_wrong_arity` | infer_errors.zig:398 | wrong-test | corrected (line number; CLI) | `.../errors/external_wrong_arity.snap.md`: source `#[@External.Erlang(..)]`; error `` `@external` module and symbol must be string literals `` / `hint: Example: #[@External.Node("./gleam_stdlib.mjs", "string_length")]` (from `infer.zig:2113`). The test comment says `// 1 arg is below the 2..3 range (target alone).` but `infer.zig:2098-2100` accepts 1..2 args (plus an optional trailing `true`/`false` inline flag, `2091-2097`) and has a dedicated message: `` `@external` expects 1 or 2 arguments ``. CLI: `#[@External.Erlang("a", "b", "c")]` gives exactly that arity message. | Expected an arity error. Actual: the string-literal check fires, so the arity rule is never tested. The comment is stale, and the hint names `Node` although the source uses `Erlang`. | Use `#[@External.Erlang("a", "b", "c")]` to hit the arity branch (verified). Update the comment. |
| `missing_string_patterns_with_wildcard` | exhaustiveness.zig:69 | wrong-test | confirmed | `.../errors/missing_string_patterns_with_wildcard.snap.md`: source has only `"hello" -> "greeting";`; error `` `string` has no wildcard `_` arm; it cannot be matched exhaustively ``. | The name says "(with wildcard)" but the source has no wildcard. The output is correct for a missing wildcard. | Rename to "missing string patterns (no wildcard)". |
| `t_inside_future_reds_future_return_must_be_bare_t` | effect_future.zig:27 | wrong-test (low) | confirmed | Test name `"§1F RF1 — return Future.resolved(value: t) inside …"` contains `": "`. `helpers.zig:70-72` slices after the first `": "`, so the slug becomes `t_inside_future_…` and loses `rf1_return_future_resolved`. Output is correct and byte-identical to `rf1_return_future_resolved_inside_future_reds_future_return_must_be_bare_t.snap.md`. | Expected slug `1f_rf1_return_future_resolved_value_t_…`. Actual: a truncated slug that collides easily. | Drop the colon from the name (e.g. `(value = t)`). |
| `e_inside_future_reds_future_throw_must_be_bare_e` | effect_future.zig:38 | wrong-test (low) | confirmed | Name `"… Future.rejected(error: e) …"`, same truncation. The snapshot is byte-identical to `rf2_throw_future_rejected_…`. | Same as above. | Same as above. |
| `e_inside_future_reds_future_return_type_mismatch` | effect_future.zig:49 | wrong-test (low) | confirmed | Name `"… Future.rejected(error: e) …"`, truncated slug. Output (`future-return-type-mismatch`, 3:5) is correct. | Same as above. | Same as above. |
| `t_inside_future_reds_future_throw_type_mismatch` | effect_future.zig:62 | wrong-test (low) | confirmed | Name `"… Future.resolved(value: t) …"`, truncated slug. Output (`future-throw-type-mismatch`) is correct. | Same as above. | Same as above. |
| `1_r10_yield_nonsense_inside_generator_reds_yield_label_unbound` | effect_generator.zig:24 | wrong-test (low) | corrected (line numbers) | The name says "R10" and the header (`effect_generator.zig:6-7`) says `RI4 … (alias of R10)`. `diagnostics.zig:51` (first pass said 50) `/// R10 — alias of R6 (\`throw\` inside \`#[@context]\` / \`#[@generator]\` body).` and `diagnostics.zig:78-79` (first pass said 79-80) `/// R15 — \`yield :label <expr>\` where the label is not bound.` The snapshot hint mentions iterator syntax for a generator: `hint: Label a generator fn (\`#[@iterator] fn … -> @Iterator<T> :name\`)`. | The error itself is correct (3:5, `yield-label-unbound`). The rule ID is wrong, and the hint names the wrong effect. | Rename to R15/RI4. Make the hint effect-aware (`infer.zig:5756`). |
| `import_of_val_unbound_variable` | infer_errors.zig:200 | weak | confirmed (CLI) | `.../errors/import_of_val_unbound_variable.snap.md`: `import {SECRET};` / `val x = SECRET;` produces `'SECRET' is not in scope`. CLI: `val x = SECRET;` without the import gives the same `unbound variable 'SECRET'` (at 1:9; with the import, at 2:9, i.e. the same `SECRET` token). | The import line has no effect in this single-module harness, so the "import of a val" rule (if there is one) is untested. In effect this duplicates `unbound_variable_undefined_identifier`. | Use the multi-module harness with a module exporting `val SECRET` and assert the intended diagnostic, or delete the test. |
| `await_outside_a_star_fn` | infer_errors.zig:310 | weak (low) | confirmed (CLI) | Source `val x = await ready();`. `ready` is never defined. The error is `effect-await-without-future` at 2:13 (correct). CLI: with `#[@future] fn ready() -> @Future<i32> { return 1; }` defined, the same R7 error still fires, so the suggested fix is viable. | If `await` outside a future were wrongly accepted, the test would still red (with `'ready' is not in scope`). Only a snapshot diff would notice. The name uses the obsolete "star fn" terminology. | Define `#[@future] fn ready() -> @Future<i32> { return 1; }`. Rename. |
| `loop_await_on_a_non_async_iterable` | infer_errors.zig:338 | weak (low) | confirmed (CLI) | Source `loop await (5) { x -> ping(x); }`. `ping` is undefined. Error `` `loop await` expects an `@AsyncIterator<T, E>` value `` with caret on `loop` (3:5). CLI: with `fn ping(x: i32) -> i32` defined, the same error still fires, on `loop`. | The same latent second error. The caret would be better on `(5)`. | Define `ping` or use a builtin. Use the subject loc. |
| `net_new_nested_record_destructuring_binds_inner_fields` (no snapshot) | exhaustiveness.zig:185 | weak | confirmed (CLI) | `assertInfersOk` only. Body: `Line(_, Point(x, y)) -> x + y;` in `-> i32`. Root cause 2. CLI: with `-> string` it checks OK, and `val s: string = case l { Line(_, Point(x, y)) -> x + y; };` also checks OK. | The name claims the inner fields are bound (as i32), but nothing pins their types. | Add a use that needs `x: i32`, e.g. pass `x` to `fn needI32(v: i32)`, plus a negative twin. |
| `net_new_case_as_val_vs_trailing_expr_type_check_identically` (no snapshot) | exhaustiveness.zig:199 | weak | confirmed (CLI) | `assertInfersOk` only. Nothing compares the two forms, and returns are unchecked (root cause 2). CLI: `val r: i32 = case n { 0 -> "zero"; _ -> "other"; };` checks OK. | The claim "type-check identically (both yield string)" is not verified. | Snapshot with `assertComptimeAstSingle` (shows the `case` return_type), or annotate `val r: string` and add a negative twin. |
| `1_r12_return_result_error_inside_result_reds_result_return_type_mismatch` | effect_result.zig:34 | weak | corrected (line numbers; CLI) | The snapshot shows `result-return-type-mismatch` at 3:5 for `return Result.Error("boom");`. `diagnostics.zig:57-58` (first pass said 56-57) defines R12 as manual construction, and `diagnostics.zig:63-64` documents `/// \`return e;\` where \`e: E\` inside \`#[@result]\``. `infer_errors.zig:481` also uses "R12" for the manual-construction rule. CLI: `#[@result] fn fetch() -> @Result<i32, string> { return "boom"; }` checks OK. | The "R12" label conflicts with diagnostics.zig and another test. The documented rule (bare `return e` with `e: E`) is not enforced. Only the syntactic `Result.Error(...)` shape triggers the code. | Rename the ID. Add a `return "boom";` error test once the E-vs-R check exists (it currently would not red). |
| `1_happy_path_try_unwraps_result_d_e_to_d` (no snapshot) | effect_result.zig:81 | weak | confirmed (CLI) | `val v = try parse(n) catch 0; return v;` in `-> i32`. Root cause 2. CLI: changing the return type to `-> string` still checks OK. `val v: string = try parse(n) catch 0;` does red (`expected string, got i32 at main:8:21`). | "unwraps to D" is not asserted. | Write `val v: i32 = try parse(n) catch 0;`, or add a negative twin with `val v: string`. |
| `1_happy_path_nested_result_call_propagates_error_via_try` (no snapshot) | effect_result.zig:96 | weak | confirmed (CLI) | `val v = try parse(n); return v + v;`. `+` does not pin i32, and the return is unchecked. CLI: `val v: string = try parse(n);` reds (`expected string, got i32`). | Neither propagation nor the unwrapped type is verified. | Annotate `val v: i32`. Optionally snapshot the transform. |
| `1_happy_path_return_r_inside_generator_sets_the_r_channel` (no snapshot) | effect_generator.zig:56 | weak | corrected (line numbers; CLI) | `yield 1; return "done";` in `@Generator<i32, string>`. CLI: `return 42;` in the same fn checks OK. The `.@"return"` arm (`infer.zig:5459-5562`; first pass said 5458-5555) has no generator R unify. | The name claims `return` sets R. The checker does not check R at all, so the test would pass with any value. | Implement the R unify and add a negative test (`return 42` must red). |
| `implement_declares_method_not_in_interface` | infer_errors.zig:264 | duplicate | corrected (wording) | `.../errors/implement_declares_method_not_in_interface.snap.md`: `error: unknown method` / `'fly' is not declared in any interface implemented for 'Pato'`. Same rule and code path (`infer.zig:355`) as `implement_method_not_declared_in_the_interface` (infer_errors.zig:59): `'explode' is not declared in any interface implemented for 'Circle'`. The sources differ in more than the `;` vs `,` member separators and `}` vs `};`. They also use different names (`Swimmer`/`Pato`/`fly` vs `Drawable`/`Circle`/`explode`) and a `record Pato { … }` statement instead of `val Circle = record { … }`. Neither snapshot has a location. | Same check, twice. | Merge, or rename to state the syntactic variant it covers. |
| `r11_return_result_ok_inside_result_reds_return_must_be_bare_r` | infer_errors.zig:463 | duplicate | confirmed | The ERROR section is identical to `1_r11_return_result_ok_inside_result_reds_return_must_be_bare_r.snap.md` (effect_result.zig:23). The sources differ in `fn parse(n: i32)` / `Result.Ok(result: n * 2)` vs `fn fetch()` / `Result.Ok(42)`. | Same rule and message. | Keep one (the dedicated `effect_result.zig`), or make the difference explicit in the name. |
| `r11_mirror_throw_result_error_inside_result_reds_throw_must_be_bare_e` | infer_errors.zig:472 | duplicate | confirmed | The ERROR section is byte-identical to `1_r11_mirror_throw_result_error_inside_result_reds_throw_must_be_bare_e.snap.md` (diff of the ERROR sections is empty). Sources: `throw Result.Error(error: "boom")` vs `throw Result.Error("boom")`. | Same as above. | Same as above. |
| `rf1_return_future_resolved_inside_future_reds_future_return_must_be_bare_t` | infer_errors.zig:491 | duplicate | confirmed | `cmp` shows it is byte-identical to `t_inside_future_reds_future_return_must_be_bare_t.snap.md` (effect_future.zig:27). Same source, same error. | Exact duplicate. | Delete one. |
| `rf2_throw_future_rejected_inside_future_reds_future_throw_must_be_bare_e` | infer_errors.zig:500 | duplicate | confirmed | Byte-identical (`cmp`) to `e_inside_future_reds_future_throw_must_be_bare_e.snap.md` (effect_future.zig:38). | Exact duplicate. | Delete one. |
| `rf5_let_binding_future_resolved_inside_future_reds_future_manual_construction_forbidden` | infer_errors.zig:605 | duplicate | confirmed | Byte-identical (`cmp`) to `1f_rf5_let_binding_future_resolved_inside_future_reds_future_manual_construction_forbidden.snap.md` (effect_future.zig:75). | Exact duplicate. | Delete one. |

## ok slugs (44)

### infer_errors.zig (28)
- `type_mismatch_function_argument_wrong_type`: caret 4:18 on `"hello"`, expected i32 / found string.
- `type_mismatch_val_annotation_mismatch`: caret 1:17 on `42`, expected string / found i32.
- `arity_mismatch_too_many_arguments`, `arity_mismatch_too_few_arguments`, `arity_mismatch_zero_param_function_called_with_argument`: caret on the callee, counts correct (1/2, 2/1, 0/1).
- `unbound_variable_undefined_identifier`, `unbound_variable_undefined_function_call`
- `not_a_record_destructure_val_binding_on_primitive`: minor, the caret is on `val` (2:5) rather than the `{ result }` pattern.
- `extension_method_ambiguous_two_local_impls`: caret 19:16 on `swim`. Minor: gutter misaligned (`19 │` over `  │`).
- `try_on_non_result_type`: caret 5:13 on `try`, found 'i32'.
- `await_on_a_non_future_value`: no hint; fine.
- `yield_targets_an_unknown_label`: minor, the caret is on `yield` rather than `:nope`.
- `future_body_using_yield`
- `assert_requires_bool`: caret 2:12 on `42`.
- `test_body_type_error`: caret 5:17 on `"x"`.
- `std_package_member_missing`: caret 4:19 on `collapse`.
- `builtin_result_namespace_unknown_function`: caret 7:20; the hint matches `infer.zig:6167-6175` (map, then, unwrap, isOk, isError; hint string at 6174).
- `still_reports_unbound_a_genuinely_undefined_name_still_errors`: caret 1:24 on `nonexistent`.
- `r12_let_binding_result_ok_inside_result_reds_result_manual_construction_forbidden`: R12 matches `diagnostics.zig:57`; caret 3:20 on `Ok`. Minor: the message says `@Result::Ok` / `@Result::Err`, while the surface syntax is `Result.Ok` / `Result.Error`.
- `ri1_return_expr_inside_iterator_reds_iterator_return_forbidden`
- `ri1_return_expr_inside_asyncgenerator_reds_iterator_return_forbidden`: minor, the hint cites `@Iterator<T, E, C>` for an `@AsyncIterator` fn.
- `ri5_break_unknown_reds_break_label_unbound`
- `ri3_break_expr_with_c_void_reds_iterator_break_without_completion_type`
- `rc5_getcontex_outside_context_fn_reds_context_getcontex_outside_context_fn`
- `rc4_getcontex_value_reds_context_getcontex_expects_type`
- `rc6_use_of_non_context_fn_reds_use_of_non_context_fn`
- `rc3_getcontex_t_outside_enclosing_anchor_tree_reds_context_getcontex_anchor_violation`
- `ri2_break_wrongtype_reds_iterator_break_type_mismatch`: minor, the message does not state the expected (i32) and found (string) types.

### exhaustiveness.zig (7)
- `missing_enum_patterns`: caret 7:5 on `case`, missing Green, Blue.
- `all_enum_patterns_covered`: the typed AST is present, so there is no false non-exhaustive error. Note: the JSON `return_type` is copied from the annotation (`snapshot.zig:429`) and the body is just `"case c {"`, so arm-type regressions would not show.
- `wildcard_covers_remaining_patterns`: same note.
- `or_pattern_covers_multiple_variants`: same note.
- `guarded_arm_does_not_cover_its_variant`: reports `missing variant(s): Blue`, which is correct.
- `net_new_wildcard_arm_makes_a_partial_case_exhaustive` (no snapshot)
- `net_new_case_over_int_and_string_literal_scrutinees` (no snapshot)

### effect_result.zig (4)
- `1_r11_return_result_ok_inside_result_reds_return_must_be_bare_r`
- `1_r11_mirror_throw_result_error_inside_result_reds_throw_must_be_bare_e`
- `1_throw_mismatch_throw_result_ok_inside_result_reds_result_throw_type_mismatch`: note, `diagnostics.zig:66-67` documents this code for bare `throw r;` with `r: R`. The probe `throw 42;` gives a generic `type mismatch: expected string, got i32` instead (re-checked at HEAD: `at main:3:5`), so only the syntactic `Result.Ok` shape uses the code.
- `1_happy_path_bare_return_throw_inside_result_type_checks` (no snapshot)

### effect_future.zig (3)
- `1f_rf5_let_binding_future_resolved_inside_future_reds_future_manual_construction_forbidden`
- `1f_happy_path_bare_return_t_throw_e_inside_future_type_checks` (no snapshot): note, the E channel is not checked. The probe `throw 42;` in `@Future<User, string>` checks OK (re-checked at HEAD).
- `1f_happy_path_await_unwraps_future_t_e_to_t` (no snapshot): `u.name` does pin `u: User`; the probe `u.nope` reds (`unknown field 'nope' on type 'User'`, re-checked at HEAD).

### effect_generator.zig (2)
- `1_happy_path_yield_expr_inside_generator_type_checks` (no snapshot): yield T is checked (probe `yield "x"` reds). Out of batch: that probe message reads `expected string, got i32`, swapped, because `infer.zig:5775` uses value-first `unifyAt` (re-checked at HEAD).
- `1_happy_path_labelled_yield_outer_matches_enclosing_fn_label` (no snapshot)
