/// CLI entry point for the botopink compiler.
///
/// Currently a minimal stub — the main compiler logic lives in the library
/// module (root.zig) and is intended to be wired up here once the CLI
/// interface is fully designed.
pub fn main() !void {}
