----- SOURCE CODE -- main.bp
```botopink
fn extract() {
    val #(a, b) = #(12, "hello");
    @print(a, b);
}
```

----- JAVASCRIPT -- main.js
```javascript
function extract() {
    const [ a, b ] = [12, "hello"];
    console.log(a, b);
}
```

----- TYPESCRIPT TYPEDEF -- main.d.ts
```typescript

```

----- RUN LOG -----
```logs
```
