----- SOURCE CODE -- main.bp
```botopink
val Element = struct implement @Context<Element, Element> { }
fn render() -> Element {
    Element();
}
```

----- ERLANG -- main.erl
```erlang
-module(main).

%% struct Element: 

render() ->
    #{}.
```

----- RUN LOG -----
```logs
```
