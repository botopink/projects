----- SOURCE CODE -- main.bp
```botopink
val Drawable = interface {
    val color: string,
    fn draw(self: Self);
}
```

----- ERLANG -- main.erl
```erlang
-module(main).

%% interface Drawable
```

----- RUN LOG -----
```logs
```
