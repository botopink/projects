----- SOURCE
```botopink
import {order} from "std";
val n = order.toInt(order.lt());
              ↑
```

----- HOVER at (line 1, char 14)
kind: markdown

```botopink
pub fn toInt(o: Order) -> i32
```

*from `std/order`*
