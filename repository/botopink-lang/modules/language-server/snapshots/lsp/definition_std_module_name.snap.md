----- SOURCE
```botopink
import {order} from "std";
        ↑
val n = order.toInt(order.lt());
```

----- DEFINITION at (line 0, char 8)
uri: std/order
range: (0,0) → (0,0)
  import {order} from "std";
  
