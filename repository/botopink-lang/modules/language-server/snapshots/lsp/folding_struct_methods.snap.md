----- SOURCE
```botopink
struct Counter {
    _count: i32 = 0,
    fn increment(self: Self) {
        self._count += 1;
    }
}
```

----- FOLDING RANGES
  line 0–5  kind: region
  line 2–4  kind: region
