# client/src

> Path: `libs/client/src/`
> Parent: [`../AGENTS.md`](../AGENTS.md)
> Docs: [`../docs.md`](../docs.md)

Source for the `client` library's `.bp` declarations. Currently a placeholder —
`client.d.bp` carries a header comment only and declares no symbols.

## Tree

```text
src/
├── AGENTS.md          ← you are here
└── client.d.bp        ← placeholder declaration file (header only)
```

## Conventions

- Declarations stay declarative (signatures only, no method bodies).
- When adding a real `.bp` file, list it in `../botopink.json`'s `files` array.
