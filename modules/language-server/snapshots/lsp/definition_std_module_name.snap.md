----- SOURCE
```botopink
import {list} from "std";
        ↑
val xs = list.map([1], fn(item: i32) -> i32 { return item; });
```

----- DEFINITION at (line 0, char 8)
uri: std/list
range: (0,0) → (0,0)
  import {list} from "std";
  
