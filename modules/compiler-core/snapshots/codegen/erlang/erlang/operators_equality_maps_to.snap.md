----- SOURCE CODE -- main.bp
```botopink
fn isZero(n: i32) -> bool {
    return n == 0;
}
```

----- ERLANG -- main.erl
```erlang
-module(main).

isZero(N) ->
    (N =:= 0).
```

----- RUN LOG -----
```logs
```
