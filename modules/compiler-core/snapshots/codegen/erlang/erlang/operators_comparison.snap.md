----- SOURCE CODE -- main.bp
```botopink
fn isPositive(n: i32) -> bool {
    return n > 0;
}
```

----- ERLANG -- main.erl
```erlang
-module(main).

isPositive(N) ->
    (N > 0).
```

----- RUN LOG -----
```logs
```
