----- SOURCE CODE -- main.bp
```botopink
val sum = 1 + 2;
```

----- ERLANG -- main.erl
```erlang
-module(main).

sum() ->
    (1 + 2).
```

----- RUN LOG -----
```logs
// Erlang execution not yet implemented```
