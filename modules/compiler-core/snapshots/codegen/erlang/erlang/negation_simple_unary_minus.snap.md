----- SOURCE CODE -- main.bp
```botopink
fn negate(x: i32) -> i32 {
    return -x;
}
```

----- ERLANG -- main.erl
```erlang
-module(main).

negate(X) ->
    (-X).
```

----- RUN LOG -----
```logs
```
