----- SOURCE CODE -- pond.bp
```botopink
pub val Swimmer = interface {
    fn swim(self: Self);
}
pub record Pato { id: i32 }
```

----- ERLANG -- pond.erl
```erlang
-module(pond).

%% interface Swimmer

%% record Pato: id
```

----- RUN LOG -----
```logs
```
