----- SOURCE CODE -- main.bp
```botopink
val greeting = "hello";
```

----- ERLANG -- main.erl
```erlang
-module(main).

greeting() ->
    <<"hello">>.
```

----- RUN LOG -----
```logs
Execution error: error.FileNotFound```
