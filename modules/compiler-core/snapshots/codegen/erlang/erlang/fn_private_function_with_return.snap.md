----- SOURCE CODE -- main.bp
```botopink
fn double(x: i32) -> i32 {
    return x * 2;
}
```

----- ERLANG -- main.erl
```erlang
-module(main).

double(X) ->
    (X * 2).
```

----- RUN LOG -----
```logs
// Erlang execution not yet implemented```
