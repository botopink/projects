----- SOURCE CODE -- main.bp
```botopink
val greeting = "Hello, World!";
```

----- ERLANG -- main.erl
```erlang
-module(main).

greeting() ->
    <<"Hello, World!">>.
```

----- RUN LOG -----
```logs
```
