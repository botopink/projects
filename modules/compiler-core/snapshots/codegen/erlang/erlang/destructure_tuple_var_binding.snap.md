----- SOURCE CODE -- main.bp
```botopink
fn main() {
    var #(x, y) = #(10, 20);
}
```

----- ERLANG -- main.erl
```erlang
-module(main).

main() ->
    {X, Y} = {10, 20}.
```

----- RUN LOG -----
```logs
// Erlang execution not yet implemented```
