----- SOURCE CODE -- main.bp
```botopink
val add = { x, y ->
    x + y;
};
val result = add(10, 20);
```

----- JAVASCRIPT -- main.js
```javascript
const add = (x, y) => {
    (x + y);
};

const result = add(10, 20);
```

----- TYPESCRIPT TYPEDEF -- main.d.ts
```typescript



```

----- RUN LOG -----
```logs
```
